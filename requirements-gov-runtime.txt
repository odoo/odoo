# AGI Gov runtime/document processing extras.
# Keep this file focused on features needed by the GOV stack itself,
# without pulling heavyweight ML frameworks into the default container.

# LaTeX validation in gov_processos/models/gov_latex_service.py
pylatexenc>=2.10

# OCR/document helpers used by GOV ingestion flows.
pytesseract>=0.3.10
pdf2image>=1.17.0

# Kodoo Forge engine service.
fastapi>=0.115,<1.0
uvicorn>=0.30,<1.0
SQLAlchemy>=2.0,<3.0
asyncpg>=0.29,<1.0
httpx>=0.27,<1.0
pydantic>=2.8,<3.0
