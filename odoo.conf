[options]
; This is the Odoo config file

; Addons paths (core + custom)
addons_path = /usr/lib/python3/dist-packages/odoo/addons,/var/lib/odoo/addons/16.0,/mnt/extra-addons

; Admin password for database manager
admin_passwd = admin

; Database connection settings
db_host = postgres.railway.internal
db_port = 5432
db_user = odoo_user
db_password = shuwafF2016
db_name = hospital_management

; Logging
log_level = info

; Server port (change to 80 for Railway public access)
xmlrpc_port = 80

; Optional: longpolling port (if needed)
; longpolling_port = 8072

; Enable this if using a proxy (like Railway's default routing)
proxy_mode = True

; Optional: log file location
; logfile = /var/log/odoo/odoo.log
