[options]
; Odoo Configuration for Railway Deployment

addons_path = /opt/odoo/addons
admin_passwd = admin

db_host = ${DB_HOST}
db_port = ${DB_PORT}
db_user = ${DB_USER}
db_password = ${DB_PASSWORD}
db_name = ${DB_NAME}
logfile = False
log_level = info
xmlrpc_port = 8069
