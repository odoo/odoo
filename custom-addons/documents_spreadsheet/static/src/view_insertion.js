/** @odoo-module **/

import { registry } from "@web/core/registry";

registry
    .category("spreadsheet_view_insertion_groups")
    .add("documents.group_documents_user", "documents.group_documents_user");
