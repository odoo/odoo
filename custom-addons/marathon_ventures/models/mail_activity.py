# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MailActivity(models.Model):
    _inherit = 'mail.activity'

    # === SF Metadata ===
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    calldisposition = fields.Char(string='CallDisposition')  # SF: CallDisposition | SF std field with no <type> in metadata
    calldurationinseconds = fields.Char(string='CallDurationInSeconds')  # SF: CallDurationInSeconds | SF std field with no <type> in metadata
    callobject = fields.Char(string='CallObject')  # SF: CallObject | SF std field with no <type> in metadata
    calltype = fields.Char(string='CallType')  # SF: CallType | SF std field with no <type> in metadata
    completeddatetime = fields.Char(string='CompletedDateTime')  # SF: CompletedDateTime | SF std field with no <type> in metadata
    email = fields.Char(string='Email')  # SF: Email | SF std field with no <type> in metadata
    isrecurrence = fields.Char(string='IsRecurrence')  # SF: IsRecurrence | SF std field with no <type> in metadata
    isreminderset = fields.Char(string='IsReminderSet')  # SF: IsReminderSet | SF std field with no <type> in metadata
    isvisibleinselfservice = fields.Char(string='IsVisibleInSelfService')  # SF: IsVisibleInSelfService | SF std field with no <type> in metadata
    phone = fields.Char(string='Phone')  # SF: Phone | SF std field with no <type> in metadata
    priority = fields.Selection(string='Priority', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Priority
    recurrenceinterval = fields.Char(string='RecurrenceInterval')  # SF: RecurrenceInterval | SF std field with no <type> in metadata
    recurrenceregeneratedtype = fields.Char(string='RecurrenceRegeneratedType')  # SF: RecurrenceRegeneratedType | SF std field with no <type> in metadata
    status = fields.Selection(string='Status', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Status
    tasksubtype = fields.Char(string='TaskSubtype')  # SF: TaskSubtype | SF std field with no <type> in metadata
    type = fields.Selection(string='Type', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Type
    whatid = fields.Many2one(string='WhatId', comodel_name='res.partner', ondelete='set null')  # SF: WhatId | TODO: undeclared SF lookup target — verify
    whoid = fields.Many2one(string='WhoId', comodel_name='res.partner', ondelete='set null')  # SF: WhoId | TODO: undeclared SF lookup target — verify

    # === Computed / Roll-Up ===

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_assigned_user_priviledges(self):
        # SF Validation Rule: Assigned_User_Priviledges
        # SF condition (fires when TRUE):
        #   AND($RecordType.Name='Agency Discrepancy', ISCHANGED(OwnerId), NOT(OR(LastModifiedBy.Id = "005C0000003V2Q6", LastModifiedBy.Id = "005C000000B9cPX", LastModifiedBy.Id = "005C000000AQxdy", LastModifiedBy.Id = "005C0000007ulcB" )))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Only the Manager of Operations or a member of the Finance department can change the value of Assigned User. Please ask them for assistance if this field needs to be changed."))

    @api.constrains('sf_external_id')
    def _check_avails_required(self):
        # SF Validation Rule: Avails_Required
        # SF condition (fires when TRUE):
        #   AND(CONTAINS(Subject, "Capping"), Avails__c = 0,  ISPICKVAL( Priority,"High" ),ISPICKVAL(Status,"Completed"))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please refer to your dashboard and enter the total number of avails."))

    @api.constrains('sf_external_id')
    def _check_postlog_checklist_required(self):
        # SF Validation Rule: Postlog_Checklist_Required
        # SF condition (fires when TRUE):
        #   AND(CONTAINS(Subject, "*] Postlog"), OR(ISBLANK(Log_Total_Dollars__c),ISBLANK( Sum_of_Equiv_30__c ),ISBLANK(Log_Record_Count__c)),ISPICKVAL( Priority,"High"),OR(ISPICKVAL(Status,"Completed"), Close_Task__c = TRUE))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please refer to your dashboard and complete the Logs Checklist for both Pre and Postlogs."))

    @api.constrains('sf_external_id')
    def _check_prelog_checklist_required(self):
        # SF Validation Rule: Prelog_Checklist_Required
        # SF condition (fires when TRUE):
        #   AND(CONTAINS(Subject, "*] Prelog"), OR(ISBLANK(Log_Total_Dollars__c),ISBLANK( Sum_of_Equiv_30__c ),ISBLANK(Log_Record_Count__c),ISBLANK(Overruns__c),ISBLANK(PIs__c),ISBLANK(Clearance_Priority_Deals__c),ISBLANK( Clearance_Test_Schedules__c ),ISPICKVAL( Any_Ghosted_Units_Clearing__c,""),ISPICKVAL( Canceled_schedules_still_clearing__c ,"")),  ISPICKVAL( Priority,"High" ),OR(ISPICKVAL(Status,"Completed"), Close_Task__c = TRUE))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please refer to your dashboard and complete the Prelog Checklist."))

    @api.constrains('sf_external_id')
    def _check_statusauthorization(self):
        # SF Validation Rule: StatusAuthorization
        # SF condition (fires when TRUE):
        #   AND($RecordType.Name='Agency Discrepancy',NOT(OR($User.Id="005C0000003V2Q6",$User.Id="005C000000B9cPX",$User.Id="005C000000AQxdy")),ISPICKVAL(Status, "Completed"))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Only members of Finance may mark an Agency Discrepancy task as completed. If further assistance is needed, please contact your System Admin."))
