# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvCheck(models.Model):
    _name = 'mv.check'
    _description = 'Check'
    # === Identification ===
    name = fields.Char(string='Check Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    checkdate = fields.Date(string='Check Date')  # SF: CheckDate__c
    check_number = fields.Char(string='Check Number', size=25, required=True)  # SF: Check_Number__c
    program = fields.Many2one(string='Program', comodel_name='mv.programs', ondelete='set null')  # SF: Program__c
    total_dollars = fields.Char(string='Total Dollars', compute='_compute_total_dollars', store=True)  # SF: Total_Dollars__c | TODO unknown SF type: Summary

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_total_dollars(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.total_dollars = False
