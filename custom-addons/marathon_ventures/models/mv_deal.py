# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvDeal(models.Model):
    _name = 'mv.deal'
    _description = 'Deal'
    # === Identification ===
    name = fields.Char(string='Deal Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.deal.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    access_code = fields.Char(string='Access Code', size=25)  # SF: Access_Code__c
    account_advertiser_brand = fields.Char(string='Account+Advertiser+Brand', compute='_compute_account_advertiser_brand', store=True)  # SF: Account_Advertiser_Brand__c
    account_advertiser_program = fields.Char(string='Account+Advertiser+Program', compute='_compute_account_advertiser_program', store=True)  # SF: Account_Advertiser_Program__c
    account_advertiser = fields.Char(string='Account+Advertiser', compute='_compute_account_advertiser', store=True)  # SF: Account_Advertiser__c
    account_brand_program = fields.Char(string='Account+Brand+Program', compute='_compute_account_brand_program', store=True, help='Calculated Field. Test Field 11.21.  AE Dash.  Dashboard limited to two criteria in table display.  Concatenating Account+Brand so that they count once and we can use AE (Deal Owner) as well.')  # SF: Account_Brand_Program__c
    account_brand = fields.Char(string='Account+Brand', compute='_compute_account_brand', store=True)  # SF: Account_Brand__c
    actual_rating_year = fields.Integer(string='Actual Rating Year', compute='_compute_actual_rating_year', store=True)  # SF: Actual_Rating_Year__c
    advertiser_id = fields.Char(string='Advertiser ID', compute='_compute_advertiser_id', store=True)  # SF: Advertiser_ID__c
    advertiser = fields.Char(string='Advertiser', compute='_compute_advertiser', store=True)  # SF: Advertiser__c
    agency_deal_number = fields.Char(string='Agency Deal Number', size=20, index=True)  # SF: Agency_Deal_Number__c
    brand_id = fields.Char(string='Brand ID', compute='_compute_brand_id', store=True)  # SF: Brand_ID__c
    brands = fields.Many2one(string='Brands', comodel_name='mv.brands', ondelete='set null', help='Please use the look-up function, and double check the Advertiser for the  brand selected.')  # SF: Brands__c
    budget_30 = fields.Monetary(string='Budget :30', currency_field='currency_id', help='12.28.12  Budget :30 Rate.  Entered on "Availability" Deals only.')  # SF: Budget_30__c
    bundle_action = fields.Selection(string='Bundle Action', selection=[('new_buy', 'NEW BUY'), ('add_to_schedule', 'ADD TO SCHEDULE'), ('canceled', 'CANCELED'), ('frequency_revision', 'FREQUENCY REVISION'), ('traffic_revision_only', 'TRAFFIC REVISION ONLY'), ('cancel_before_start', 'CANCEL BEFORE START'), ('frequency_adjustment_cancel', 'FREQUENCY ADJUSTMENT/CANCEL'), ('frequency_revision_traffic_change', 'FREQUENCY REVISION/TRAFFIC CHANGE')])  # SF: Bundle_Action__c | SF Unrestricted picklist
    bundle_start_week = fields.Date(string='Bundle Start Week')  # SF: Bundle_Start_Week__c
    bundle_version = fields.Integer(string='Bundle Version')  # SF: Bundle_Version__c
    cable_synd_pp_del = fields.Char(string='Cable/Synd/PP', compute='_compute_cable_synd_pp_del', store=True)  # SF: Cable_Synd_PP_del__c
    campaign = fields.Char(string='Campaign', size=52)  # SF: Campaign__c
    x_class = fields.Selection(string='Class', selection=[('none', 'Standard'), ('premium', 'Premium'), ('sponsorship', 'Sponsorship'), ('secured', 'Secured'), ('secured_health_care', 'Secured - Health Care'), ('sports_specific_buy', 'Sports Specific Buy')])  # SF: Class__c
    clientcode = fields.Char(string='ClientCode', compute='_compute_clientcode', store=True)  # SF: ClientCode__c
    client_account = fields.Many2one(string='Client Account', comodel_name='res.partner', ondelete='set null')  # SF: Client_Account__c
    client_code = fields.Char(string='Client Code', size=25)  # SF: Client_Code__c
    client_dr_gm_hybrid = fields.Selection(string='Client DR/GM/Hybrid', selection=[('dr', 'DR'), ('gm', 'GM'), ('hybrid', 'Hybrid'), ('end', 'END'), ('val', '--')])  # SF: Client_DR_GM_Hybrid__c
    commercial_type = fields.Char(string='Commercial Type', compute='_compute_commercial_type', store=True, help='HYBRID or DR, based on Advertiser.')  # SF: Commercial_Type__c
    conga_invoice_wapa = fields.Char(string='Conga Invoice WAPA', compute='_compute_conga_invoice_wapa', store=True)  # SF: Conga_Invoice_WAPA__c
    contactaccount = fields.Char(string='ContactAccount', compute='_compute_contactaccount', store=True)  # SF: ContactAccount__c
    contact_email = fields.Char(string='Contact Email', compute='_compute_contact_email', store=True)  # SF: Contact_Email__c
    contact = fields.Many2one(string='Contact', comodel_name='res.partner', ondelete='set null', help='Please use the lookup function and double check the Contact\'s Agency!')  # SF: Contact__c
    count_of_schedules = fields.Char(string='Count of Schedules', compute='_compute_count_of_schedules', store=True)  # SF: Count_of_Schedules__c | TODO unknown SF type: Summary
    count_of_weeks = fields.Char(string='Count of Weeks', compute='_compute_count_of_weeks', store=True)  # SF: Count_of_Weeks__c | TODO unknown SF type: Summary
    deal_count = fields.Integer(string='Deal Count', compute='_compute_deal_count', store=True, help='Defined Field value of "1". To Count number of Deals in a Data Set/SF Report')  # SF: Deal_Count__c
    demographics = fields.Selection(string='Ratings Demos', selection=[('f18_24', 'F18-24'), ('f18_25', 'F18-25'), ('f18_34', 'F18-34'), ('f18_49', 'F18-49'), ('f18_99', 'F18-99'), ('f25_34', 'F25-34'), ('f25_49', 'F25-49'), ('f25_54', 'F25-54'), ('f25_64', 'F25-64'), ('f25_99', 'F25-99'), ('f2_99', 'F2-99'), ('f35_49', 'F35-49'), ('f35_54', 'F35-54'), ('f35_64', 'F35-64'), ('f35_99', 'F35-99'), ('f45_54', 'F45-54'), ('f50_64', 'F50-64'), ('f50_99', 'F50-99'), ('f55_64', 'F55-64'), ('f55_99', 'F55-99'), ('f65_99', 'F65-99'), ('hh', 'HH'), ('m18_24', 'M18-24'), ('m18_34', 'M18-34'), ('m18_49', 'M18-49'), ('m18_99', 'M18-99'), ('m25_34', 'M25-34'), ('m25_49', 'M25-49'), ('m25_54', 'M25-54'), ('m25_64', 'M25-64'), ('m25_99', 'M25-99'), ('m2_99', 'M2-99'), ('m35_49', 'M35-49'), ('m35_54', 'M35-54'), ('m35_64', 'M35-64'), ('m35_99', 'M35-99'), ('m50_64', 'M50-64'), ('m50_99', 'M50-99'), ('m55_64', 'M55-64'), ('m55_99', 'M55-99'), ('m65_99', 'M65-99'), ('p12_17', 'P12-17'), ('p18_24', 'P18-24'), ('p18_34', 'P18-34'), ('p18_49', 'P18-49'), ('p18_99', 'P18-99'), ('p2_11', 'P2-11'), ('p2_17', 'P2-17'), ('p25_34', 'P25-34'), ('p25_49', 'P25-49'), ('p25_54', 'P25-54'), ('p25_64', 'P25-64'), ('p25_99', 'P25-99'), ('p2_99', 'P2-99'), ('p35_49', 'P35-49'), ('p35_54', 'P35-54'), ('p35_64', 'P35-64'), ('p35_99', 'P35-99'), ('p45_54', 'P45-54'), ('p50_64', 'P50-64'), ('p50_99', 'P50-99'), ('p55_64', 'P55-64'), ('p55_99', 'P55-99'), ('p6_11', 'P6-11'), ('p65_99', 'P65-99'), ('w18_34', 'W18-34'), ('w18_49', 'W18-49'), ('w18_99', 'W18-99'), ('w25_54', 'W25-54'), ('w2_99', 'W2-99'), ('w35_99', 'W35-99'), ('w50_99', 'W50-99'), ('f55', 'F55+'), ('fs25_54', 'Fs25-54'), ('fs35_99', 'Fs35-99'), ('hhld', 'HHLD'), ('m55', 'M55+'), ('p2', 'P2+'), ('p35', 'P35+'), ('p55', 'P55+'), ('p65', 'P65+')])  # SF: Demographics__c
    digital_id = fields.Char(string='Digital ID', size=25)  # SF: Digital_ID__c
    disfox_conga_invoice_url = fields.Char(string='DisFox Conga Invoice URL', compute='_compute_disfox_conga_invoice_url', store=True)  # SF: DisFox_Conga_Invoice_URL__c
    e_i_friendly = fields.Boolean(string='E/I Friendly')  # SF: E_I_Friendly__c
    entity_name = fields.Char(string='Entity Name', compute='_compute_entity_name', store=True)  # SF: Entity_Name__c
    estimate = fields.Char(string='Estimate', size=25)  # SF: Estimate__c
    ff_invoice_month = fields.Char(string='FF Invoice Month', compute='_compute_ff_invoice_month', store=True)  # SF: FF_Invoice_Month__c | TODO unknown SF type: Summary
    ff_long_form_invoice_month = fields.Char(string='FF Long Form Invoice Month', compute='_compute_ff_long_form_invoice_month', store=True)  # SF: FF_Long_Form_Invoice_Month__c | TODO unknown SF type: Summary
    hiatus_dates = fields.Char(string='Hiatus Dates', size=255)  # SF: Hiatus_Dates__c
    log_exp_date = fields.Date(string='LOG Exp Date')  # SF: LOG_Exp_Date__c
    ltc_date = fields.Date(string='LTC Date')  # SF: LTC_Date__c
    length = fields.Selection(string='Length', selection=[('v_30', '30'), ('v_40', '40'), ('v_60', '60'), ('v_120', '120'), ('v_300', '300'), ('v_15', '15'), ('v_1710', '1710'), ('v_90', '90'), ('v_180', '180'), ('v_240', '240'), ('v_45', '45'), ('v_75', '75'), ('v_05', '05'), ('v_10', '10'), ('v_150', '150'), ('v_20', '20'), ('v_105', '105'), ('v_5', '5'), ('v_35', '35'), ('v_1714', '1714'), ('v_25', '25'), ('v_7', '7'), ('v_3510', '3510'), ('v_1650', '1650')])  # SF: Length__c | SF Unrestricted picklist
    max_day = fields.Char(string='Max/Day', size=18)  # SF: Max_Day__c
    max_sep = fields.Integer(string='Max Sep', help='Use Brand Separation. If not on paperwork, enter 30 min.')  # SF: Max_Sep__c
    merit_street_media_2025 = fields.Char(string='Merit Street Media 2025', compute='_compute_merit_street_media_2025', store=True)  # SF: Merit_Street_Media_2025__c
    min_sep = fields.Selection(string='Min Sep', selection=[('v_15', '15'), ('v_20', '20'), ('v_5', '5'), ('v_25', '25'), ('v_30', '30'), ('v_35', '35'), ('v_60', '60'), ('v_10', '10'), ('v_45', '45')], help='Uses Brand Separation. If not on paperwork, enter 15 min.')  # SF: Min_Sep__c
    network_deal_number = fields.Char(string='Network Deal Number', size=30, index=True, help='Deal number in Network\'s paper work, in Network\'s records.  Equals 1 if the PI campaign is no longer airing on the network.')  # SF: Network_Deal_Number__c
    pi = fields.Boolean(string='PI')  # SF: PI__c
    priority = fields.Boolean(string='Priority')  # SF: Priority__c
    product_code = fields.Char(string='Product Code', size=25)  # SF: Product_Code__c
    program_account_advertiser_brand = fields.Char(string='Program+Account+Advertiser+Brand', compute='_compute_program_account_advertiser_brand', store=True)  # SF: Program_Account_Advertiser_Brand__c
    program_team_del_del = fields.Char(string='Program Team', compute='_compute_program_team_del_del', store=True)  # SF: Program_Team_del_del__c
    program = fields.Many2one(string='Program', comodel_name='mv.programs', ondelete='restrict')  # SF: Program__c
    quarter = fields.Selection(string='Quarter', selection=[('q1', 'Q1'), ('q2', 'Q2'), ('q3', 'Q3'), ('q4', 'Q4')])  # SF: Quarter__c
    rate = fields.Monetary(string='Rate', currency_field='currency_id')  # SF: Rate__c
    ratings_quarter = fields.Char(string='Ratings Quarter', compute='_compute_ratings_quarter', store=True)  # SF: Ratings_Quarter__c | TODO unknown SF type: Summary
    ratings_year = fields.Integer(string='Ratings Year', compute='_compute_ratings_year', store=True)  # SF: Ratings_Year__c
    raycom_comments = fields.Char(string='Bundle Comments', size=255)  # SF: Raycom_Comments__c
    related_advertiser_log_exp_date = fields.Date(string='[DEP]Advertiser LOG Exp. Date', compute='_compute_related_advertiser_log_exp_date', store=True)  # SF: Related_Advertiser_LOG_Exp_Date__c
    restricted_programming_dna = fields.Many2many(string='Restricted Programming DNA', comodel_name='mv.restricted_programming_dna.tag', relation='mv_deal_restricted_programming_dna_rel')  # SF: Restricted_Programming_DNA__c
    revised = fields.Boolean(string='Revised')  # SF: Revised__c
    sf_conga_invoice_formula = fields.Char(string='SF Conga Invoice Formula', compute='_compute_sf_conga_invoice_formula', store=True)  # SF: SF_Conga_Invoice_Formula__c
    sf_conga_invoice_new_synd = fields.Char(string='SF Conga Invoice New Synd', compute='_compute_sf_conga_invoice_new_synd', store=True)  # SF: SF_Conga_Invoice_New_Synd__c
    sf_conga_invoice_nosey = fields.Char(string='SF Conga Invoice Nosey', compute='_compute_sf_conga_invoice_nosey', store=True)  # SF: SF_Conga_Invoice_Nosey__c
    sf_deal_id = fields.Char(string='SF Deal ID', compute='_compute_sf_deal_id', store=True, help='Used for sending emails to Salesforce so that they can be filed directly to the Deal')  # SF: SF_Deal_ID__c
    sales_plan = fields.Many2one(string='Sales Plan', comodel_name='mv.sales_plan', ondelete='set null')  # SF: Sales_Plan__c
    status = fields.Selection(string='Status', selection=[('sold', 'Sold'), ('canceled', 'Canceled'), ('budget', 'Budget'), ('historical', 'Historical')])  # SF: Status__c | SF Unrestricted picklist
    sum_of_total_dollars = fields.Char(string='Sum of Total Dollars', compute='_compute_sum_of_total_dollars', store=True, help='Sum of Total Dollars for the schedules for this deal.')  # SF: Sum_of_Total_Dollars__c | TODO unknown SF type: Summary
    sum_of_units_available = fields.Char(string='Sum of Units Available', compute='_compute_sum_of_units_available', store=True)  # SF: Sum_of_Units_Available__c | TODO unknown SF type: Summary
    test_pp = fields.Boolean(string='Test PP', help='Paid Programming ONLY.  Check to indicate a test deal for month.')  # SF: Test_PP__c
    tier = fields.Selection(string='Tier', selection=[('v_1', '1'), ('v_2', '2'), ('v_3', '3'), ('v_4', '4')])  # SF: Tier__c
    vendor_account = fields.Char(string='Vendor Account', compute='_compute_vendor_account', store=True)  # SF: Vendor_Account__c
    vendor_commission = fields.Float(string='Vendor Commission', digits=(5, 2), compute='_compute_vendor_commission', store=True)  # SF: Vendor_Commission__c
    wapa_cinco_unwired_pi_url = fields.Char(string='WAPA Cinco Unwired PI URL', compute='_compute_wapa_cinco_unwired_pi_url', store=True)  # SF: WAPA_Cinco_Unwired_PI_URL__c
    wapa_cinco_unwired_url = fields.Char(string='WAPA Cinco Unwired URL', compute='_compute_wapa_cinco_unwired_url', store=True)  # SF: WAPA_Cinco_Unwired_URL__c
    week_min_field = fields.Char(string='Week MIN Field', compute='_compute_week_min_field', store=True)  # SF: Week_MIN_Field__c | TODO unknown SF type: Summary
    week_max = fields.Char(string='Week Max', compute='_compute_week_max', store=True)  # SF: Week_Max__c | TODO unknown SF type: Summary
    week_pending = fields.Date(string='Week Pending', compute='_compute_week_pending', store=True)  # SF: Week_Pending__c
    year = fields.Integer(string='Year')  # SF: Year__c

    # === Computed / Roll-Up ===

    @api.depends('contactaccount', 'advertiser', 'brands.name')
    def _compute_account_advertiser_brand(self):
        # SF formula (verbatim, may need translation):
        #   ContactAccount__c &"-"& Advertiser__c &"-"& Brands__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account_advertiser_brand = '%s-%s-%s' % (
                rec.contactaccount or '',
                rec.advertiser or '',
                rec.brands.name or '',
            )

    @api.depends('contactaccount', 'advertiser', 'program.name')
    def _compute_account_advertiser_program(self):
        # SF formula (verbatim, may need translation):
        #   ContactAccount__c & " - " &  Advertiser__c & " - " & Program__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account_advertiser_program = '%s - %s - %s' % (
                rec.contactaccount or '',
                rec.advertiser or '',
                rec.program.name or '',
            )

    @api.depends('contactaccount', 'advertiser')
    def _compute_account_advertiser(self):
        # SF formula (verbatim, may need translation):
        #   ContactAccount__c &"-"& Advertiser__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account_advertiser = '%s-%s' % (
                rec.contactaccount or '',
                rec.advertiser or '',
            )

    @api.depends('contactaccount', 'brands.name', 'program.name')
    def _compute_account_brand_program(self):
        # SF formula (verbatim, may need translation):
        #   ContactAccount__c & " - " &  Brands__r.Name & " - " &  Program__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account_brand_program = '%s - %s - %s' % (
                rec.contactaccount or '',
                rec.brands.name or '',
                rec.program.name or '',
            )

    @api.depends('contactaccount', 'brands.name')
    def _compute_account_brand(self):
        # SF formula (verbatim, may need translation):
        #   ContactAccount__c & " - " &  Brands__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account_brand = '%s - %s' % (
                rec.contactaccount or '',
                rec.brands.name or '',
            )

    @api.depends()
    def _compute_actual_rating_year(self):
        # SF formula (verbatim, may need translation):
        #   YEAR(Week_Max__c + 7)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.actual_rating_year = False

    @api.depends()
    def _compute_advertiser_id(self):
        # SF formula (verbatim, may need translation):
        #   Brands__r.Advertiser__r.Id
        for rec in self:
            # TODO: translate SF formula to Python
            rec.advertiser_id = False

    @api.depends()
    def _compute_advertiser(self):
        # SF formula (verbatim, may need translation):
        #   Brands__r.Advertiser__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.advertiser = False

    @api.depends()
    def _compute_brand_id(self):
        # SF formula (verbatim, may need translation):
        #   Brands__r.Id
        for rec in self:
            # TODO: translate SF formula to Python
            rec.brand_id = False

    @api.depends()
    def _compute_cable_synd_pp_del(self):
        # SF formula (verbatim, may need translation):
        #   Text(Program__r.Cable_Synd__c)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.cable_synd_pp_del = False

    @api.depends()
    def _compute_clientcode(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.ClientCode__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.clientcode = False

    @api.depends('brands')
    def _compute_commercial_type(self):
        # SF formula (verbatim, may need translation):
        #   TEXT(Brands__r.Advertiser__r.Commercial__c)
        for rec in self:
            advertiser = rec.brands.advertiser
            value = advertiser.commercial if advertiser else False
            if not value:
                rec.commercial_type = ''
                continue
            # TEXT() on a picklist returns the API/stored value; map to the label
            # only if you need the display text instead.
            rec.commercial_type = dict(
                advertiser._fields['commercial']._description_selection(self.env)
            ).get(value, value)

    @api.depends()
    def _compute_conga_invoice_wapa(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id
        #   + "&TemplateId=a0B3b00000iYRCs" +
        #   "&Reportid=00O3b0000075PXY?pv0=" + Program__r.Name + "~pv1=" + Name + ",00ONw000003IA41" + ",00ONw000002dbzR?pv0=" + Name +
        #   "&AttachmentParentId=" + Id +
        #   "&DefaultPDF=1" +
        #   "&OFN=Invoice:+" + Brands__r.Name + "+-+" + Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.conga_invoice_wapa = False

    @api.depends('contact', 'contact.parent_id', 'contact.parent_id.name',
                 'contact.name')
    def _compute_contactaccount(self):
        # SF formula: Contact__r.Account.Name
        # Odoo maps: the Contact picker (`contact` -> res.partner) is
        # the person; the "Account" is that person's parent partner
        # (their company). Fall back to the contact's own name if the
        # contact isn't linked to a parent (i.e., it's the account
        # record itself).
        for rec in self:
            if rec.contact and rec.contact.parent_id:
                rec.contactaccount = rec.contact.parent_id.name or False
            elif rec.contact:
                rec.contactaccount = rec.contact.name or False
            else:
                rec.contactaccount = False

    @api.depends()
    def _compute_contact_email(self):
        # SF formula (verbatim, may need translation):
        #   Contact__r.Email
        for rec in self:
            # TODO: translate SF formula to Python
            rec.contact_email = False

    @api.depends()
    def _compute_count_of_schedules(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.count_of_schedules = False

    @api.depends()
    def _compute_count_of_weeks(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.count_of_weeks = False

    @api.depends()
    def _compute_deal_count(self):
        # SF formula (verbatim, may need translation):
        #   1
        for rec in self:
            # TODO: translate SF formula to Python
            rec.deal_count = False

    @api.depends()
    def _compute_disfox_conga_invoice_url(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id
        #   + "&TemplateId=a0BNw000007ONp3" +
        #   "&Reportid=00ONw000004qm6H?pv0=" + Program__r.Name + "~pv1=" + Name + ",00ONw000004qmEL" + ",00ONw000004qmCj?pv0=" + Id +
        #   "&AttachmentParentId=" + Id +
        #   "&DefaultPDF=1" +
        #   "&OFN=Invoice:+" + Brands__r.Name + "+-+" + Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.disfox_conga_invoice_url = False

    @api.depends()
    def _compute_entity_name(self):
        # SF formula (verbatim, may need translation):
        #   IF( ISPICKVAL( Program__r.Deal_Entity__c , "Marathon Ventures LLC") , "Marathon Ventures LLC", "")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.entity_name = False

    @api.depends()
    def _compute_ff_invoice_month(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.ff_invoice_month = False

    @api.depends()
    def _compute_ff_long_form_invoice_month(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.ff_long_form_invoice_month = False

    @api.depends()
    def _compute_merit_street_media_2025(self):
        # SF formula (verbatim, may need translation):
        #   "&EmailSubject=" & Name & " - " & Advertiser__c & " - " & Brands__r.Name & " - " & Campaign__c &
        #   "&EmailBody=Test" &
        #   "&ReportId= 00OC0000004r48D?pv0=" & Id & ", 00OC0000005T6wJ?pv0=" & Id &
        #   "&QueryID=[Orderline]a1nC0000000RYt0" &
        #   "&TemplateID=a0BC000000OCDl8" &
        #   "&AttachmentParentId=" & Id &
        #   "&OFN=" & "Merit Street Media" & " Name " & TEXT(MONTH(TODAY())) & "_" & TEXT(DAY(TODAY())) & "_" & TEXT(YEAR(TODAY())) &
        #   "&ExcelAutoFitColumns=1" &
        #   "&SC0=1" &
        #   "&SC1=Attachments" &
        #   "&mftsvalue0=NOW" &
        #   "&QMode=SendEmail" &
        #   "&EmailAdditionalTo=jdile@meritstreetmedia.com,gdolce@edgets.com,elang@edgets.com" &
        #   "&EmailCC=kaitlynweeks@mvmediasales.com,ayrianarodriguez@mvmediasales.com" &
        #   "&CongaEmailTemplateID=a4BNw0000002pJN" &
        #   "&id=" & Id
        for rec in self:
            # TODO: translate SF formula to Python
            rec.merit_street_media_2025 = False

    @api.depends()
    def _compute_program_account_advertiser_brand(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.Name &"-"& ContactAccount__c &"-"& Advertiser__c &"-"& Brands__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.program_account_advertiser_brand = False

    @api.depends()
    def _compute_program_team_del_del(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.Team__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.program_team_del_del = False

    @api.depends()
    def _compute_ratings_quarter(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.ratings_quarter = False

    @api.depends()
    def _compute_ratings_year(self):
        # SF formula (verbatim, may need translation):
        #   YEAR( Week_Max__c )
        for rec in self:
            # TODO: translate SF formula to Python
            rec.ratings_year = False

    @api.depends()
    def _compute_related_advertiser_log_exp_date(self):
        # SF formula (verbatim, may need translation):
        #   Brands__r.Advertiser__r.ADV_LOG_with_Expiration_Date__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.related_advertiser_log_exp_date = False

    @api.depends()
    def _compute_sf_conga_invoice_formula(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id
        #   + "&TemplateId=a0B3b00000iYRCs" +
        #   "&Reportid=00O3b0000075PXY?pv0=" + Program__r.Name + "~pv1=" + Name + ",00ONw000003IA41" +
        #   "&AttachmentParentId=" + Id +
        #   "&DefaultPDF=1" +
        #   "&OFN=Invoice:+" + Brands__r.Name + "+-+" + Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sf_conga_invoice_formula = False

    @api.depends()
    def _compute_sf_conga_invoice_new_synd(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id
        #   + "&TemplateId=a0BNw000006o9xV" +
        #   "&Reportid=00ONw000004QnQz?pv0=" + Program__r.Name + "~pv1=" + Name + ",00ONw000003IA41" + ",00ONw000002Sv73?pv0=" + Id +
        #   "&AttachmentParentId=" + Id +
        #   "&DefaultPDF=1" +
        #   "&OFN=Invoice:+" + Brands__r.Name + "+-+" + Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sf_conga_invoice_new_synd = False

    @api.depends()
    def _compute_sf_conga_invoice_nosey(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id
        #   + "&TemplateId=a0BNw000002NmcH" +
        #   "&Reportid=00ONw000001dtgn?pv0=" + Program__r.Name + "~pv1=" + Name + ",00ONw000003IA41" + ",00ONw000002Sv73?pv0=" + Id +
        #   "&AttachmentParentId=" + Id +
        #   "&DefaultPDF=1" +
        #   "&OFN=Invoice:+" + Brands__r.Name + "+-+" + Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sf_conga_invoice_nosey = False

    @api.depends()
    def _compute_sf_deal_id(self):
        # SF formula (verbatim, may need translation):
        #   "(ref:" & Id & ")"
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sf_deal_id = False

    @api.depends()
    def _compute_sum_of_total_dollars(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sum_of_total_dollars = False

    @api.depends()
    def _compute_sum_of_units_available(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sum_of_units_available = False

    @api.depends()
    def _compute_vendor_account(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.Vendor_Account__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.vendor_account = False

    @api.depends()
    def _compute_vendor_commission(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.Vendor_Account__r.Network_Commission__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.vendor_commission = False

    @api.depends()
    def _compute_wapa_cinco_unwired_pi_url(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id +
        #   "&Reportid=00OC0000005zJdn?pv0=" + Id + ",00OC0000005zJao?pv0=" + Id + ",00OC0000005z9yW?pv0=" + Id + ",00OC0000004qoaV?pv0=" + Id + ",00OC0000004qos0?pv0=" + Id + ",00ONw000002K7oz,00OC0000006yzm8?pv0=" + Id +
        #   "&TemplateID=a0BNw00000319p7" +
        #   "&OFN="+Program__r.Name+"+MVMS PI+"+Name+"+"+Advertiser__c+"+"+Account_Brand__c+"+"+Estimate__c+"+"+ Text(Length__c)+"+" + Text(TODAY()) +
        #   "&EmailFromId=0D280000000TNHc" +
        #   "&EmailToId=003Nw00000Gv6PH" +
        #   "&EmailCC=victorturitzin@mvmediasales.com" +
        #   "&QueryID=[ALL]a1nC0000003PhyB?pv1=a06Nw00000EG3Rh" +
        #   "&AttachmentParentId=" + Id +
        #   "&EmailTemplateId=00XNw000001IT4b" +
        #   "&UF0=1" +
        #   "&MFTS0=Revised__c" +
        #   "&MFTSValue0=False" +
        #   "&QMode=SendEmail"
        for rec in self:
            # TODO: translate SF formula to Python
            rec.wapa_cinco_unwired_pi_url = False

    @api.depends()
    def _compute_wapa_cinco_unwired_url(self):
        # SF formula (verbatim, may need translation):
        #   "&id=" + Id +
        #   "&Reportid=00OC0000005zJdn?pv0=" + Id + ",00OC0000005zJao?pv0=" + Id + ",00OC0000005z9yW?pv0=" + Id + ",00OC0000004qoaV?pv0=" + Id + ",00OC0000004qos0?pv0=" + Id + ",00ONw000002K7oz,00OC0000006yzm8?pv0=" + Id +
        #   "&TemplateID=a0BNw00000319p7" +
        #   "&OFN="+Program__r.Name+"+MVMS+"+Name+"+"+Advertiser__c+"+"+Account_Brand__c+"+"+Estimate__c+"+"+ Text(Length__c)+"+" + Text(TODAY()) +
        #   "&EmailFromId=0D280000000TNHc" +
        #   "&EmailToId=003Nw00000Gv6PH" +
        #   "&EmailCC=victorturitzin@mvmediasales.com"  +
        #   "&QueryID=[ALL]a1nC0000003PhyB?pv1=a06Nw00000EG3Rh" +
        #   "&AttachmentParentId=" + Id +
        #   "&CETID=a4BNw0000002MCXMA2" +
        #   "&UF0=1" +
        #   "&MFTS0=Revised__c" +
        #   "&MFTSValue0=False" +
        #   "&QMode=SendEmail"
        for rec in self:
            # TODO: translate SF formula to Python
            rec.wapa_cinco_unwired_url = False

    @api.depends()
    def _compute_week_min_field(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.week_min_field = False

    @api.depends()
    def _compute_week_max(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.week_max = False

    @api.depends()
    def _compute_week_pending(self):
        # SF formula (verbatim, may need translation):
        #   Program__r.Week_Pending__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.week_pending = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_cannot_input_bundle_in_spot(self):
        # SF Validation Rule: Cannot_Input_Bundle_In_Spot
        # SF condition (fires when TRUE):
        #   IF(Program__r.Name = "TVG", NOT(AND(ISBLANK(Raycom_Comments__c), ISBLANK(Max_Day__c))), FALSE )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Cannot add to the Bundle Information fields when uploading information on individual units."))

    @api.constrains('sf_external_id')
    def _check_client_dr_gm_hybrid_required_for_synd(self):
        # SF Validation Rule: Client_DR_GM_Hybrid_Required_for_Synd
        # SF condition (fires when TRUE):
        #   (ISPICKVAL(Program__r.Network_Owner__c, 'NBCU')|| ISPICKVAL(Program__r.Network_Owner__c, 'CBS/FOX') ||  ISPICKVAL(Program__r.Network_Owner__c, 'ABC/Disney')) && ISPICKVAL(Program__r.Network_Owner__c, '')
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("For this Deal the field 'Client DR/GM/Hybrid' is required"))

    @api.constrains('sf_external_id')
    def _check_cornerstone_800_number_character_limit(self):
        # SF Validation Rule: Cornerstone_800_Number_Character_Limit
        # SF condition (fires when TRUE):
        #   AND(ContactAccount__c ="Cornerstone Media Group",LEN(Agency_Deal_Number__c)>7,PI__c =True, ISNEW()=True )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Can only enter a 7 digit number for Cornerstone PI Deals.  For example: type 7775555 instead of 8007775555.  This is necessary for PI Payout uploads."))

    @api.constrains('sf_external_id')
    def _check_deal_brand_needs_advertiser(self):
        # SF Validation Rule: Deal_Brand_Needs_Advertiser
        # SF condition (fires when TRUE):
        #   ISBLANK( Advertiser__c )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("This brand does not have an advertiser entered.  Please go to the brand page to enter."))

    @api.constrains('sf_external_id')
    def _check_emt_agency_code_required(self):
        # SF Validation Rule: EMT_Agency_Code_Required
        # SF condition (fires when TRUE):
        #   AND(
        #   Not( ISPICKVAL( Status__c ,"Historical")),
        #   Contact__r.Account.EMT_Agency_Codes_Required__c =TRUE,
        #   ISNEW() ,
        #   ISBLANK(Access_Code__c),
        #   ISBLANK(Client_Code__c),
        #   ISBLANK(Estimate__c),
        #   ISBLANK(Product_Code__c ),
        #   ISPICKVAL( Program__r.Cable_Synd__c,"Cable"))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Make sure to populate all agency code fields: Client Code, Product Code, Access Code and Estimate"))

    @api.constrains('sf_external_id')
    def _check_needs_contact(self):
        # SF Validation Rule: Needs_Contact
        # SF condition (fires when TRUE):
        #   and(ISBLANK(Contact__c),  Brands__r.Name  <> "Availability")
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please enter a Contact."))

    @api.constrains('sf_external_id')
    def _check_needs_unit_length(self):
        # SF Validation Rule: Needs_Unit_Length
        # SF condition (fires when TRUE):
        #   ISPICKVAL(Length__c, "")
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Missing Unit Length"))

    @api.constrains('sf_external_id')
    def _check_pi_deals_agency_deal_number_required(self):
        # SF Validation Rule: PI_Deals_Agency_Deal_Number_Required
        # SF condition (fires when TRUE):
        #   AND( PI__c =true,  ISBLANK( Agency_Deal_Number__c ))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please fill in the 800 Number in the Agency Deal Number Field."))

    @api.constrains('sf_external_id')
    def _check_paid_programming_length_1710(self):
        # SF Validation Rule: Paid_Programming_Length_1710
        # SF condition (fires when TRUE):
        #   AND(ISPICKVAL(Program__r.Cable_Synd__c , "PP"), not(
        #   OR(
        #   ispickval(Length__c,"1710"),
        #   ispickval(Length__c,"1650"),
        #   ispickval(Length__c,"3510")
        #   )
        #   ))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("The program selected is Paid Programming and the length must equal 1710, 1650 or 3510."))

    @api.constrains('sf_external_id')
    def _check_sales_plan_required(self):
        # SF Validation Rule: Sales_Plan_Required
        # SF condition (fires when TRUE):
        #   AND(ISPICKVAL(Client_DR_GM_Hybrid__c, "GM"), ISBLANK(Sales_Plan__c))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please select a Sales Plan for GM."))

    @api.constrains('sf_external_id')
    def _check_status_not_equal_to_historical(self):
        # SF Validation Rule: Status_Not_Equal_to_Historical
        # SF condition (fires when TRUE):
        #   OR(AND(
        #   ISNEW(),
        #   NOT(OR(CONTAINS(Contact__r.FirstName,"Historical"),
        #     CONTAINS(Contact__r.LastName,"Historical"))),
        #     ISPICKVAL( Status__c , "Historical")
        #   ),
        #   AND(
        #   ISNEW(),OR(CONTAINS(Contact__r.FirstName,"Historical"),
        #     CONTAINS(Contact__r.LastName,"Historical")),
        #     NOT(ISPICKVAL( Status__c , "Historical"))
        #   )
        #   )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Historical Deals must be booked with an historical contact."))

    @api.constrains('sf_external_id')
    def _check_submit_for_new_advertiser_approval(self):
        # SF Validation Rule: Submit_for_New_Advertiser_Approval
        # SF condition (fires when TRUE):
        #   AND(
        #   NOT(ISPICKVAL( Status__c,"Historical")),
        #   Brands__r.Advertiser__r.CreatedDate >=  DATETIMEVALUE("2015-02-23 00:00:00"),ISPICKVAL( Brands__r.Advertiser__r.Duplicate_Approval__c,"Approved")=False )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please go to the Advertiser page to submit the new advertiser for approval."))

    @api.constrains('sf_external_id')
    def _check_submit_for_new_brand_approval(self):
        # SF Validation Rule: Submit_for_New_Brand_Approval
        # SF condition (fires when TRUE):
        #   AND(
        #   Brands__r.CreatedDate >=  DATETIMEVALUE("2015-01-29 22:00:00"),
        #   NOT(ISPICKVAL( Status__c ,"Historical")),
        #   ISPICKVAL(Brands__r.Approved_Duplicate__c,"Approved")=False
        #   )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please go to the Brand page to submit the new brand for approval."))
