# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvCheckDetail(models.Model):
    _name = 'mv.check_detail'
    _description = 'Check Detail'
    # === Identification ===
    name = fields.Char(string='Check Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    air_date = fields.Date(string='Air Date')  # SF: Air_Date__c
    air_time = fields.Char(string='Air Time', size=10)  # SF: Air_Time__c
    checkid = fields.Char(string='CheckId', size=25)  # SF: CheckId__c
    check_amount = fields.Monetary(string='Check Amount', currency_field='currency_id')  # SF: Check_Amount__c
    check = fields.Many2one(string='Check', comodel_name='mv.check', ondelete='cascade', required=True)  # SF: Check__c
    debit_check = fields.Char(string='Debit Check', size=15)  # SF: Debit_Check__c
    debit_date = fields.Date(string='Debit Date')  # SF: Debit_Date__c
    item_ad_id = fields.Char(string='Item Ad ID', size=30)  # SF: Item_Ad_ID__c
    net_amount = fields.Float(string='Net Amount', digits=(18, 2), compute='_compute_net_amount', store=True)  # SF: Net_Amount__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='set null')  # SF: Schedule__c
    unmatched_reason = fields.Selection(string='Unmatched Reason', selection=[('no_matching_week_long_form_schedule', 'No matching week/long form Schedule'), ('mismatched_rate_on_schedule', 'Mismatched rate on Schedule'), ('mismatched_traffic_on_schedule', 'Mismatched traffic on Schedule'), ('duplicate_schedules', 'Duplicate Schedules'), ('schedule_only_exists_for_non_havas_dealaccount', 'Schedule only exists for Non-Havas DealAccount.')])  # SF: Unmatched_Reason__c | SF Unrestricted picklist
    unmatched = fields.Boolean(string='Unmatched')  # SF: Unmatched__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_net_amount(self):
        # SF formula (verbatim, may need translation):
        #   Check_Amount__c * 0.85
        for rec in self:
            # TODO: translate SF formula to Python
            rec.net_amount = False
