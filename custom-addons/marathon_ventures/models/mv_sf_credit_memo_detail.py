# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSfCreditMemoDetail(models.Model):
    _name = 'mv.sf_credit_memo_detail'
    _description = 'Sf Credit Memo Detail'
    # === Identification ===
    name = fields.Char(string='SF Credit Memo Detail Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.sf_credit_memo_detail.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    account = fields.Char(string='Account', compute='_compute_account', store=True)  # SF: Account__c
    brand = fields.Char(string='Brand', compute='_compute_brand', store=True)  # SF: Brand__c
    comments = fields.Text(string='Comments')  # SF: Comments__c
    credit_amount = fields.Monetary(string='Credit Amount', currency_field='currency_id', required=True)  # SF: Credit_Amount__c
    intacct_si_number = fields.Char(string='Intacct SI Number', compute='_compute_intacct_si_number', store=True)  # SF: Intacct_SI_Number__c
    netsuite_invoice_number = fields.Char(string='NetSuite Invoice Number', compute='_compute_netsuite_invoice_number', store=True)  # SF: NetSuite_Invoice_Number__c
    program = fields.Char(string='Program', compute='_compute_program', store=True)  # SF: Program__c
    sf_credit_memo_detail = fields.Many2one(string='SF Credit Memo Detail', comodel_name='mv.sf_credit_memo', ondelete='cascade', required=True)  # SF: SF_Credit_Memo_Detail__c
    schedule_week = fields.Date(string='Schedule Week', compute='_compute_schedule_week', store=True)  # SF: Schedule_Week__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='restrict', required=True)  # SF: Schedule__c
    unit_length = fields.Float(string='Unit Length', digits=(18, 2), compute='_compute_unit_length', store=True)  # SF: Unit_Length__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_account(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.DealAccount__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account = False

    @api.depends('sf_external_id')
    def _compute_brand(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.DealBrand__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.brand = False

    @api.depends('sf_external_id')
    def _compute_intacct_si_number(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Intacct_SI_Number__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.intacct_si_number = False

    @api.depends('sf_external_id')
    def _compute_netsuite_invoice_number(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.NetSuite_Invoice_Number__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.netsuite_invoice_number = False

    @api.depends('sf_external_id')
    def _compute_program(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.DealProgram__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.program = False

    @api.depends('sf_external_id')
    def _compute_schedule_week(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Week__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.schedule_week = False

    @api.depends('sf_external_id')
    def _compute_unit_length(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.UnitLength__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.unit_length = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_sfcm_complete(self):
        # SF Validation Rule: SFCM_Complete
        # SF condition (fires when TRUE):
        #   SF_Credit_Memo_Detail__r.Complete__c =TRUE
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("This SFCM has been processed and marked off as complete by Finance. Please consult with Finance before making any changes. All changes must be approved by Finance."))

    @api.constrains('sf_external_id')
    def _check_sf_cm_schedules_equal_master_deal(self):
        # SF Validation Rule: SF_CM_Schedules_equal_Master_Deal
        # SF condition (fires when TRUE):
        #   IF( Schedule__r.Deal_Parent__c <>  SF_Credit_Memo_Detail__r.Deal__c  ,true, False)
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Schedule selected does not appear on the list of schedules found on the deal."))
