# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvCase(models.Model):
    _name = 'mv.case'
    _description = 'Case'
    # === Identification ===
    name = fields.Char(string='Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    accountid = fields.Many2one(string='AccountId', comodel_name='res.partner', ondelete='set null')  # SF: AccountId
    assetid = fields.Many2one(string='AssetId', comodel_name='res.partner', ondelete='set null')  # SF: AssetId | TODO: undeclared SF lookup target — verify
    businesshoursid = fields.Many2one(string='BusinessHoursId', comodel_name='res.partner', ondelete='set null')  # SF: BusinessHoursId | TODO: undeclared SF lookup target — verify
    closeddate = fields.Char(string='ClosedDate')  # SF: ClosedDate | SF std field with no <type> in metadata
    comments = fields.Char(string='Comments')  # SF: Comments | SF std field with no <type> in metadata
    contactemail = fields.Char(string='ContactEmail')  # SF: ContactEmail | SF std field with no <type> in metadata
    contactfax = fields.Char(string='ContactFax')  # SF: ContactFax | SF std field with no <type> in metadata
    contactid = fields.Many2one(string='ContactId', comodel_name='res.partner', ondelete='set null')  # SF: ContactId
    contactmobile = fields.Char(string='ContactMobile')  # SF: ContactMobile | SF std field with no <type> in metadata
    contactphone = fields.Char(string='ContactPhone')  # SF: ContactPhone | SF std field with no <type> in metadata
    contactphone2test = fields.Char(string='ContactPhone2Test', compute='_compute_contactphone2test', store=True)  # SF: ContactPhone2Test__c
    description = fields.Char(string='Description')  # SF: Description | SF std field with no <type> in metadata
    isclosedoncreate = fields.Char(string='IsClosedOnCreate')  # SF: IsClosedOnCreate | SF std field with no <type> in metadata
    isescalated = fields.Char(string='IsEscalated')  # SF: IsEscalated | SF std field with no <type> in metadata
    isselfserviceclosed = fields.Char(string='IsSelfServiceClosed')  # SF: IsSelfServiceClosed | SF std field with no <type> in metadata
    isvisibleinselfservice = fields.Char(string='IsVisibleInSelfService')  # SF: IsVisibleInSelfService | SF std field with no <type> in metadata
    language = fields.Char(string='Language')  # SF: Language | SF std field with no <type> in metadata
    origin = fields.Selection(string='Origin', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Origin
    ownerid = fields.Many2one(string='OwnerId', comodel_name='res.users', ondelete='set null')  # SF: OwnerId
    parentid = fields.Many2one(string='ParentId', comodel_name='res.partner', ondelete='set null')  # SF: ParentId | TODO: undeclared SF lookup target — verify
    priority = fields.Selection(string='Priority', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Priority
    reason = fields.Selection(string='Reason', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Reason
    sourceid = fields.Many2one(string='SourceId', comodel_name='res.partner', ondelete='set null')  # SF: SourceId | TODO: undeclared SF lookup target — verify
    status = fields.Selection(string='Status', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Status
    subject = fields.Char(string='Subject')  # SF: Subject | SF std field with no <type> in metadata
    suppliedcompany = fields.Char(string='SuppliedCompany')  # SF: SuppliedCompany | SF std field with no <type> in metadata
    suppliedemail = fields.Char(string='SuppliedEmail')  # SF: SuppliedEmail | SF std field with no <type> in metadata
    suppliedname = fields.Char(string='SuppliedName')  # SF: SuppliedName | SF std field with no <type> in metadata
    suppliedphone = fields.Char(string='SuppliedPhone')  # SF: SuppliedPhone | SF std field with no <type> in metadata
    type = fields.Selection(string='Type', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Type

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_contactphone2test(self):
        # SF formula (verbatim, may need translation):
        #   HYPERLINK("/"& Account.Id, "test")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.contactphone2test = False
