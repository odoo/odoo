# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class CrmLead(models.Model):
    _inherit = 'crm.lead'

    # === SF Metadata ===
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id, required=False)

    # === SF Custom Fields ===
    address = fields.Char(string='Address')  # SF: Address | SF std field with no <type> in metadata
    annualrevenue = fields.Char(string='AnnualRevenue')  # SF: AnnualRevenue | SF std field with no <type> in metadata
    campaignid = fields.Many2one(string='CampaignId', comodel_name='res.partner', ondelete='set null')  # SF: CampaignId | TODO: undeclared SF lookup target — verify
    donotcall = fields.Char(string='DoNotCall')  # SF: DoNotCall | SF std field with no <type> in metadata
    fax = fields.Char(string='Fax')  # SF: Fax | SF std field with no <type> in metadata
    genderidentity = fields.Selection(string='GenderIdentity', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: GenderIdentity
    hasoptedoutofemail = fields.Char(string='HasOptedOutOfEmail')  # SF: HasOptedOutOfEmail | SF std field with no <type> in metadata
    hasoptedoutoffax = fields.Char(string='HasOptedOutOfFax')  # SF: HasOptedOutOfFax | SF std field with no <type> in metadata
    individualid = fields.Many2one(string='IndividualId', comodel_name='res.partner', ondelete='set null')  # SF: IndividualId | TODO: undeclared SF lookup target — verify
    industry = fields.Selection(string='Industry', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Industry
    jigsaw = fields.Char(string='Jigsaw')  # SF: Jigsaw | SF std field with no <type> in metadata
    lasttransferdate = fields.Char(string='LastTransferDate')  # SF: LastTransferDate | SF std field with no <type> in metadata
    leadsource = fields.Selection(string='LeadSource', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: LeadSource
    numberofemployees = fields.Char(string='NumberOfEmployees')  # SF: NumberOfEmployees | SF std field with no <type> in metadata
    pronouns = fields.Selection(string='Pronouns', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Pronouns
    rating = fields.Selection(string='Rating', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Rating
    sfga__correlationid = fields.Char(string='CorrelationID', size=255)  # SF: SFGA__CorrelationID__c
    sfga__correlation_data = fields.Text(string='Correlation Data')  # SF: SFGA__Correlation_Data__c
    sfga__web_source = fields.Char(string='Web Source', size=255)  # SF: SFGA__Web_Source__c
    sfssdupecatcher__override_dupecatcher = fields.Boolean(string='Lead Override DupeCatcher')  # SF: SFSSDupeCatcher__Override_DupeCatcher__c
    status = fields.Selection(string='Status', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Status
    accountid = fields.Many2one(string='AccountId', comodel_name='res.partner', ondelete='set null')  # SF: AccountId
    amount = fields.Char(string='Amount')  # SF: Amount | SF std field with no <type> in metadata
    x_sf_opportunity_campaignid = fields.Many2one(string='CampaignId (Opp)', comodel_name='res.partner', ondelete='set null')  # COLLISION renamed from CampaignId | TODO: undeclared SF lookup target — verify
    closedate = fields.Char(string='CloseDate')  # SF: CloseDate | SF std field with no <type> in metadata
    concurconnect__accruedunapprovedexpensetotal = fields.Monetary(string='Accruing/Unpaid Expense Total', currency_field='currency_id')  # SF: ConcurConnect__AccruedUnApprovedExpenseTotal__c
    concurconnect__accruedunapprovedexpenses = fields.Float(string='Accruing/Unpaid Expense', digits=(5, 2), compute='_compute_concurconnect__accruedunapprovedexpenses', store=True)  # SF: ConcurConnect__AccruedUnApprovedExpenses__c
    concurconnect__finalexpensetotal = fields.Monetary(string='Final/Paid Expense Total', currency_field='currency_id')  # SF: ConcurConnect__FinalExpenseTotal__c
    concurconnect__finalexpense = fields.Float(string='Final/Paid Expenses', digits=(5, 2), compute='_compute_concurconnect__finalexpense', store=True)  # SF: ConcurConnect__FinalExpense__c
    contractid = fields.Many2one(string='ContractId', comodel_name='res.partner', ondelete='set null')  # SF: ContractId | TODO: undeclared SF lookup target — verify
    expectedrevenue = fields.Char(string='ExpectedRevenue')  # SF: ExpectedRevenue | SF std field with no <type> in metadata
    iqscore = fields.Char(string='IqScore')  # SF: IqScore | SF std field with no <type> in metadata
    isprivate = fields.Char(string='IsPrivate')  # SF: IsPrivate | SF std field with no <type> in metadata
    x_sf_opportunity_leadsource = fields.Selection(string='LeadSource (Opp)', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # COLLISION renamed from LeadSource
    nextstep = fields.Char(string='NextStep')  # SF: NextStep | SF std field with no <type> in metadata
    pricebook2id = fields.Many2one(string='Pricebook2Id', comodel_name='product.pricelist', ondelete='set null')  # SF: Pricebook2Id
    x_sf_probability = fields.Char(string='SF: Probability')  # COLLISION renamed from Probability | SF std field with no <type> in metadata
    stagename = fields.Selection(string='StageName', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: StageName
    totalopportunityquantity = fields.Char(string='TotalOpportunityQuantity')  # SF: TotalOpportunityQuantity | SF std field with no <type> in metadata
    x_sf_type = fields.Selection(string='SF: Type', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # COLLISION renamed from Type

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_concurconnect__accruedunapprovedexpenses(self):
        # SF formula (verbatim, may need translation):
        #   IF(AND(ConcurConnect__AccruedUnApprovedExpenseTotal__c > 0, Amount > 0), (ConcurConnect__AccruedUnApprovedExpenseTotal__c / Amount), null)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.concurconnect__accruedunapprovedexpenses = False

    @api.depends('sf_external_id')
    def _compute_concurconnect__finalexpense(self):
        # SF formula (verbatim, may need translation):
        #   IF(AND(ConcurConnect__FinalExpenseTotal__c > 0, Amount > 0), (ConcurConnect__FinalExpenseTotal__c / Amount), null)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.concurconnect__finalexpense = False
