# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvPiPayout(models.Model):
    _name = 'mv.pi_payout'
    _description = 'Pi Payout'
    # === Identification ===
    name = fields.Char(string='PI Payout Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.pi_payout.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    brand = fields.Char(string='Brand', size=250)  # SF: Brand__c
    calls = fields.Integer(string='Calls')  # SF: Calls__c
    market = fields.Many2one(string='Market', comodel_name='mv.market', ondelete='set null')  # SF: Market__c
    payout_amount = fields.Monetary(string='Payout Amount', currency_field='currency_id')  # SF: Payout_Amount__c
    payout_gross = fields.Monetary(string='Payout Gross', currency_field='currency_id', compute='_compute_payout_gross', store=True)  # SF: Payout_Gross__c
    response_time = fields.Datetime(string='Response Time')  # SF: Response_Time__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='cascade', required=True)  # SF: Schedule__c
    station = fields.Many2one(string='[Dep]Station', comodel_name='mv.station', ondelete='set null')  # SF: Station__c
    week = fields.Date(string='[Dep]Week')  # SF: Week__c
    x800_number = fields.Char(string='X800 Number', size=20)  # SF: X800_Number__c
    zip_code = fields.Char(string='Zip Code', size=10, index=True)  # SF: Zip_Code__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_payout_gross(self):
        # SF formula (verbatim, may need translation):
        #   Payout_Amount__c *20/17
        for rec in self:
            # TODO: translate SF formula to Python
            rec.payout_gross = False
