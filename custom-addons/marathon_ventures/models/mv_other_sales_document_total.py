# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvOtherSalesDocumentTotal(models.Model):
    _name = 'mv.other_sales_document_total'
    _description = 'Other Sales Document Total'
    # === Identification ===
    name = fields.Char(string='Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    absolute_value = fields.Monetary(string='Absolute Value', currency_field='currency_id')  # SF: Absolute_Value__c
    description = fields.Char(string='Description', size=80)  # SF: Description__c
    other_sales_document = fields.Many2one(string='Other Sales Document', comodel_name='mv.other_sales_document', ondelete='cascade', required=True)  # SF: Other_Sales_Document__c
    percent_value = fields.Float(string='Percent Value', digits=(5, 2))  # SF: Percent_Value__c
    subtotal = fields.Monetary(string='Subtotal', currency_field='currency_id')  # SF: Subtotal__c
    total = fields.Monetary(string='Total', currency_field='currency_id')  # SF: Total__c
