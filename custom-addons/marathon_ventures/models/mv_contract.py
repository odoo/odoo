# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvContract(models.Model):
    _name = 'mv.contract'
    _description = 'Contract'

    # === Identification ===
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    accountid = fields.Many2one(string='AccountId', comodel_name='res.partner', ondelete='set null')  # SF: AccountId
    activatedbyid = fields.Many2one(string='ActivatedById', comodel_name='res.partner', ondelete='set null')  # SF: ActivatedById | TODO: undeclared SF lookup target — verify
    activateddate = fields.Char(string='ActivatedDate')  # SF: ActivatedDate | SF std field with no <type> in metadata
    billingaddress = fields.Char(string='BillingAddress')  # SF: BillingAddress | SF std field with no <type> in metadata
    companysigneddate = fields.Char(string='CompanySignedDate')  # SF: CompanySignedDate | SF std field with no <type> in metadata
    companysignedid = fields.Many2one(string='CompanySignedId', comodel_name='res.partner', ondelete='set null')  # SF: CompanySignedId | TODO: undeclared SF lookup target — verify
    contractterm = fields.Char(string='ContractTerm')  # SF: ContractTerm | SF std field with no <type> in metadata
    customersigneddate = fields.Char(string='CustomerSignedDate')  # SF: CustomerSignedDate | SF std field with no <type> in metadata
    customersignedid = fields.Many2one(string='CustomerSignedId', comodel_name='res.partner', ondelete='set null')  # SF: CustomerSignedId | TODO: undeclared SF lookup target — verify
    customersignedtitle = fields.Char(string='CustomerSignedTitle')  # SF: CustomerSignedTitle | SF std field with no <type> in metadata
    description = fields.Char(string='Description')  # SF: Description | SF std field with no <type> in metadata
    enddate = fields.Char(string='EndDate')  # SF: EndDate | SF std field with no <type> in metadata
    name = fields.Char(string='Name')  # SF: Name | SF std field with no <type> in metadata
    ownerexpirationnotice = fields.Char(string='OwnerExpirationNotice')  # SF: OwnerExpirationNotice | SF std field with no <type> in metadata
    ownerid = fields.Many2one(string='OwnerId', comodel_name='res.users', ondelete='set null')  # SF: OwnerId
    renewal_option = fields.Selection(string='Renewal Option?', selection=[('yes', 'Yes'), ('no', 'No')])  # SF: Renewal_Option__c
    shippingaddress = fields.Char(string='ShippingAddress')  # SF: ShippingAddress | SF std field with no <type> in metadata
    specialterms = fields.Char(string='SpecialTerms')  # SF: SpecialTerms | SF std field with no <type> in metadata
    startdate = fields.Char(string='StartDate')  # SF: StartDate | SF std field with no <type> in metadata
    status = fields.Selection(string='Status', selection=[('todo', 'TODO: populate from SF Global Value Set')])  # SF: Status
    which_party_can_exercise_option = fields.Selection(string='Which party can exercise option?', selection=[('nosey', 'Nosey'), ('counterparty', 'Counterparty'), ('either', 'Either')])  # SF: Which_party_can_exercise_option__c
