# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvContracts(models.Model):
    _name = 'mv.contracts'
    _description = 'Contracts'
    # === Identification ===
    name = fields.Char(string='Contracts Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    additional_comments = fields.Text(string='Additional Comments')  # SF: Additional_Comments__c
    amount = fields.Monetary(string='Amount', currency_field='currency_id')  # SF: Amount__c
    automatic_renewal_no = fields.Char(string='Automatic Renewal No', size=255)  # SF: Automatic_Renewal_No__c
    automatic_renewal_yes = fields.Text(string='Automatic Renewal Yes')  # SF: Automatic_Renewal_Yes__c
    automatic_renewal = fields.Selection(string='Automatic Renewal', selection=[('yes', 'Yes'), ('no', 'No')])  # SF: Automatic_Renewal__c
    billing_specifics = fields.Text(string='Billing Specifics')  # SF: Billing_Specifics__c
    channel_partner = fields.Char(string='Channel Partner', size=150)  # SF: Channel_Partner__c
    commercial_terms = fields.Text(string='Commercial Terms')  # SF: Commercial_Terms__c
    contract_objective = fields.Selection(string='Contract Objective', selection=[('administrative_contracts', 'Administrative Contracts'), ('advertising_client', 'Advertising Client'), ('ai_tool_licensing', 'AI Tool Licensing'), ('consultants', 'Consultants'), ('content', 'Content'), ('distribution_partners', 'Distribution Partners'), ('network_agreements', 'Network Agreements'), ('nosey_platform_support', 'Nosey platform support'), ('production', 'Production'), ('station_group_agreements', 'Station Group Agreements')])  # SF: Contract_Objective__c
    early_termination_rights = fields.Text(string='Early Termination Rights')  # SF: Early_Termination_Rights__c
    end_date = fields.Date(string='End Date')  # SF: End_Date__c
    entity_name = fields.Selection(string='Entity Name', selection=[('marathon_ventures', 'Marathon Ventures'), ('county_road_media', 'County Road Media'), ('nosey', 'Nosey'), ('noseytv', 'NoseyTV')])  # SF: Entity_Name__c
    estimates_milestones = fields.Text(string='Estimates/Milestones')  # SF: Estimates_Milestones__c
    in_disupte = fields.Selection(string='In Dispute', selection=[('no', 'No'), ('yes', 'Yes')])  # SF: In_Disupte__c
    internal_channel = fields.Selection(string='Internal Channel', selection=[('confess_by_nosey', 'Confess by Nosey'), ('judge_nosey', 'Judge Nosey'), ('nosey', 'Nosey')])  # SF: Internal_Channel__c
    internal_contact = fields.Many2one(string='Internal Contact', comodel_name='res.users', ondelete='set null')  # SF: Internal_Contact__c
    notice_address = fields.Text(string='Notice Address')  # SF: Notice_Address__c
    notification_of_automatic_renewal_term = fields.Date(string='Notification of Automatic Renewal/Term')  # SF: Notification_of_Automatic_Renewal_Term__c
    renewal_termination_comments = fields.Text(string='Renewal/Termination Comments')  # SF: Renewal_Termination_Comments__c
    start_date = fields.Date(string='Start Date')  # SF: Start_Date__c
    status = fields.Selection(string='Status', selection=[('active', 'Active'), ('archived', 'Archived')])  # SF: Status__c
    test_term_months = fields.Integer(string='Term (Months)', compute='_compute_test_term_months', store=True)  # SF: Test_Term_Months__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_test_term_months(self):
        # SF formula (verbatim, may need translation):
        #   (
        #     (YEAR(End_Date__c) - YEAR(Start_Date__c)) * 12
        #   )
        #   +
        #   (
        #     MONTH(End_Date__c) - MONTH(Start_Date__c)
        #   )
        for rec in self:
            # TODO: translate SF formula to Python
            rec.test_term_months = False
