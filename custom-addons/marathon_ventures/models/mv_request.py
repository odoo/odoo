# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvRequest(models.Model):
    _name = 'mv.request'
    _description = 'Request'

    # === Identification ===
    name = fields.Char(string='Request Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.request.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    department = fields.Selection(string='Department', selection=[('sales', 'Sales'), ('finance', 'Finance'), ('operations', 'Operations'), ('pricing_and_planning', 'Pricing and Planning')], required=True)  # SF: Department__c | SF Unrestricted picklist
    description = fields.Text(string='Description')  # SF: Description__c
    reason_for_decline = fields.Text(string='Comments')  # SF: Reason_for_Decline__c
    status = fields.Selection(string='Status', selection=[('new', 'New'), ('declined', 'Declined'), ('escalated', 'Escalated'), ('on_hold', 'On Hold'), ('closed', 'Closed')])  # SF: Status__c | SF Unrestricted picklist
    subject = fields.Text(string='Subject', required=True)  # SF: Subject__c
    type = fields.Selection(string='Type', selection=[('problem', 'Problem'), ('finance', 'Finance'), ('new_feature_request', 'New Feature Request'), ('system_enhancement', 'System Enhancement'), ('question', 'Question'), ('training_request', 'Training Request'), ('order_entry_additional_help', 'Order Entry: Additional Help'), ('deletion_request', 'Deletion Request'), ('deletion_by_product_team', 'Deletion by Product Team')], required=True)  # SF: Type__c | SF Unrestricted picklist

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_laptop_numb_for_laptop_sign_out(self):
        # SF Validation Rule: Laptop_Numb_for_Laptop_Sign_Out
        # SF condition (fires when TRUE):
        #   AND(ISPICKVAL(Type__c , 'Sign Out Laptop'),  ISBLANK(Laptop_Number__c))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("If a Laptop is being singed out, you need to provide the CMIT Laptop #"))
