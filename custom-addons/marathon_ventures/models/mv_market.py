# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvMarket(models.Model):
    _name = 'mv.market'
    _description = 'Market'
    # === Identification ===
    name = fields.Char(string='Market Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    aa_dma_rank = fields.Integer(string='AA DMA Rank', help='A rank based on the amount of African American TV homes the Market can reach relative to the other Markets.')  # SF: AA_DMA_Rank__c
    aa_hh = fields.Float(string='AA HH %', digits=(5, 2), help='% of African American Households the Market can reach.')  # SF: AA_HH__c
    aa_tv_homes = fields.Integer(string='AA TV Homes', help='The amount of African American TV Homes a Market can potentially reach.')  # SF: AA_TV_Homes__c
    ads = fields.Float(string='ADS %', digits=(5, 2))  # SF: ADS__c
    asian_dma_rank = fields.Integer(string='Asian DMA Rank', help='A rank based on the amount of Asian American TV homes the Market can reach relative to the other Markets.')  # SF: Asian_DMA_Rank__c
    asian_homes = fields.Float(string='Asian % Homes', digits=(5, 2), help='The percent of Asian American Homes a Market can reach relative to other Markets.')  # SF: Asian_Homes__c
    asian_tv_homes = fields.Integer(string='Asian TV Homes', help='The amount of Asian American TV Homes a Market can potentially reach.')  # SF: Asian_TV_Homes__c
    dma_rank = fields.Integer(string='DMA Rank', help='The Markets Nielsen Designated Market Area Rank.')  # SF: DMA_Rank__c | SF unique → consider _sql_constraints
    hispanic_or_latinx_dma_rank = fields.Integer(string='Hispanic or Latinx DMA Rank', help='A rank based on the amount ofHispanic or Latinx American TV homes the Market can reach relative to the other Markets.')  # SF: Hispanic_or_Latinx_DMA_Rank__c
    hispanic_or_latinx_homes = fields.Float(string='Hispanic or Latinx % Homes', digits=(5, 2), help='The percent of Hispanic or Latinx American Homes a Market can reach relative to other Markets.')  # SF: Hispanic_or_Latinx_Homes__c
    hispanic_or_latinx_tv_homes = fields.Integer(string='Hispanic or Latinx TV Homes', help='The amount of Hispanic or Latinx TV Homes a Market can potentially reach.')  # SF: Hispanic_or_Latinx_TV_Homes__c
    market_rank = fields.Char(string='Market Rank', compute='_compute_market_rank', store=True, help='Formula field to prepend 0 or 00 to Market Rank.')  # SF: Market_Rank__c
    ota = fields.Float(string='OTA %', digits=(5, 2))  # SF: OTA__c
    tv_homes = fields.Integer(string='TV Homes', help='Amount of all TV homes a Market can potentially reach relative to the other Markets.')  # SF: TV_Homes__c
    time_zone = fields.Selection(string='Time Zone', selection=[('hst', 'HST'), ('akst', 'AKST'), ('pst', 'PST'), ('mst', 'MST'), ('cst', 'CST'), ('est', 'EST'), ('ast', 'AST')], help='Market Time Zone.')  # SF: Time_Zone__c
    us_hh = fields.Float(string='US HH %', digits=(5, 2), help='% of US Households the Market can reach.')  # SF: US_HH__c
    wired_cable = fields.Float(string='Wired Cable %', digits=(5, 2), help='% of Wired Cable Households the Market can reach.')  # SF: Wired_Cable__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_market_rank(self):
        # SF formula (verbatim, may need translation):
        #   if( LEN(text( DMA_Rank__c ))=1,"00"&TEXT( DMA_Rank__c ) ,
        #   if( LEN(text(DMA_Rank__c  ))=2,"0"&TEXT(DMA_Rank__c),
        #   TEXT(DMA_Rank__c)))
        #   &" - "&Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.market_rank = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_market_name_cannot_have_an_ampersand(self):
        # SF Validation Rule: Market_Name_cannot_have_an_Ampersand
        # SF condition (fires when TRUE):
        #   CONTAINS(Name, '&')
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("The Market Name cannot contain an '&', use the word 'and' instead."))
