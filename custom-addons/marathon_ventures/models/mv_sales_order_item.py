# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSalesOrderItem(models.Model):
    _name = 'mv.sales_order_item'
    _description = 'Sales Order Item'
    # === Identification ===
    name = fields.Char(string='Line No', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    department = fields.Char(string='Department', size=80)  # SF: Department__c
    description = fields.Char(string='Description', size=255)  # SF: Description__c
    extended_price = fields.Monetary(string='Extended Price', currency_field='currency_id')  # SF: Extended_Price__c
    item = fields.Char(string='Item', size=40)  # SF: Item__c
    location = fields.Char(string='Location', size=80)  # SF: Location__c
    memo = fields.Char(string='Memo', size=255)  # SF: Memo__c
    opportunity = fields.Many2one(string='Opportunity', comodel_name='crm.lead', ondelete='set null')  # SF: Opportunity__c
    price = fields.Monetary(string='Price', currency_field='currency_id')  # SF: Price__c
    quantity = fields.Float(string='Quantity', digits=(14, 2))  # SF: Quantity__c
    sales_order = fields.Many2one(string='Sales Order', comodel_name='mv.sales_order', ondelete='cascade', required=True)  # SF: Sales_Order__c
    site = fields.Char(string='Site', size=80)  # SF: Site__c
    unit = fields.Char(string='Unit', size=20)  # SF: Unit__c
