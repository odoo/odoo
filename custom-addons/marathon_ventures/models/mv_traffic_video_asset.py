# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvTrafficVideoAsset(models.Model):
    _name = 'mv.traffic_video_asset'
    _description = 'Traffic Video Asset'

    # === Identification ===
    name = fields.Char(string='Traffic Video Asset Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.traffic_video_asset.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    deal = fields.Many2one(string='Deal', comodel_name='mv.deal', ondelete='set null')  # SF: Deal__c
    traffic = fields.Many2one(string='Traffic Instructions', comodel_name='mv.traffic', ondelete='set null')  # SF: Traffic__c
    video_asset = fields.Many2one(string='Video Asset', comodel_name='mv.video_file', ondelete='set null')  # SF: Video_Asset__c
