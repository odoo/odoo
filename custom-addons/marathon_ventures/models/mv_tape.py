# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvTape(models.Model):
    _name = 'mv.tape'
    _description = 'Tape'
    # === Identification ===
    name = fields.Char(string='Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.tape.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    account = fields.Many2one(string='Account', comodel_name='res.partner', ondelete='restrict', required=True)  # SF: Account__c
    advertiser = fields.Char(string='Advertiser', compute='_compute_advertiser', store=True)  # SF: Advertiser__c
    brand = fields.Many2one(string='Brand', comodel_name='mv.brands', ondelete='restrict', required=True)  # SF: Brand__c
    flight_end = fields.Date(string='Flight End')  # SF: Flight_End__c
    flight_start = fields.Date(string='Flight Start')  # SF: Flight_Start__c
    isci_2 = fields.Char(string='ISCI 2', size=100)  # SF: ISCI_2__c
    isci_3 = fields.Char(string='ISCI 3', size=100)  # SF: ISCI_3__c
    isci = fields.Char(string='ISCI', size=100)  # SF: ISCI__c
    length = fields.Selection(string='Length', selection=[('v_30', '30'), ('v_60', '60'), ('v_120', '120'), ('v_300', '300'), ('v_15', '15'), ('v_1710', '1710'), ('v_90', '90'), ('v_180', '180'), ('v_240', '240'), ('v_45', '45'), ('v_75', '75'), ('v_05', '05'), ('v_10', '10')])  # SF: Length__c | SF Unrestricted picklist
    program = fields.Many2one(string='Program', comodel_name='mv.programs', ondelete='restrict', required=True)  # SF: Program__c
    rotation = fields.Selection(string='Rotation', selection=[('spot', 'Spot'), ('v_50_50_split', '50-50 Split'), ('pattern', 'Pattern'), ('limited_ratio', 'Limited Ratio')])  # SF: Rotation__c | SF Unrestricted picklist
    x800_number = fields.Char(string='800 Number')  # SF: X800_Number__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_advertiser(self):
        # SF formula (verbatim, may need translation):
        #   Brand__r.Advertiser__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.advertiser = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_must_enter_start_end_length(self):
        # SF Validation Rule: Must_Enter_Start_End_Length
        # SF condition (fires when TRUE):
        #   OR( ISBLANK(Flight_Start__c)=TRUE,ISBLANK( Flight_End__c)=TRUE,ISBLANK(TEXT(Length__c))=TRUE)
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Must select a Length, Flight Start and Flight End date to save the record"))

    @api.constrains('sf_external_id')
    def _check_prevent_changes_in_start_date(self):
        # SF Validation Rule: Prevent_Changes_in_Start_Date
        # SF condition (fires when TRUE):
        #   ISCHANGED(Flight_Start__c)=TRUE
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Cannot change the flight start date after record is created."))
