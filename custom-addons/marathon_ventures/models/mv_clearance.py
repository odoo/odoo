# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvClearance(models.Model):
    _name = 'mv.clearance'
    _description = 'Clearance'
    # === Identification ===
    name = fields.Char(string='Clearance', default=lambda self: self.env['ir.sequence'].next_by_code('mv.clearance.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    aa_hh = fields.Float(string='AA HH%', digits=(5, 2), compute='_compute_aa_hh', store=True)  # SF: AA_HH__c
    affiliate = fields.Char(string='Affiliate', compute='_compute_affiliate', store=True)  # SF: Affiliate__c
    call_letters = fields.Char(string='Call Letters', compute='_compute_call_letters', store=True)  # SF: Call_Letters__c
    clearance_list = fields.Many2one(string='Clearance List', comodel_name='mv.clearance_list', ondelete='cascade', required=True)  # SF: Clearance_List__c
    daypart = fields.Selection(string='Daypart', selection=[('em', 'EM'), ('day', 'DAY'), ('pr', 'PR'), ('lf', 'LF')])  # SF: Daypart__c | SF Unrestricted picklist
    first_time_period = fields.Char(string='First Time Period', size=255)  # SF: First_Time_Period__c
    launch_date = fields.Date(string='Launch Date', required=True)  # SF: Launch_Date__c
    market = fields.Char(string='Market', compute='_compute_market', store=True)  # SF: Market__c
    off_air_date = fields.Date(string='Off Air Date')  # SF: Off_Air_Date__c
    second_time_period = fields.Char(string='Second Time Period', size=255)  # SF: Second_Time_Period__c
    station = fields.Many2one(string='Station', comodel_name='mv.station', ondelete='restrict', required=True)  # SF: Station__c
    us_hh = fields.Float(string='US HH%', digits=(5, 2), compute='_compute_us_hh', store=True)  # SF: US_HH__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_aa_hh(self):
        # SF formula (verbatim, may need translation):
        #   Station__r.Market__r.AA_HH__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.aa_hh = False

    @api.depends('sf_external_id')
    def _compute_affiliate(self):
        # SF formula (verbatim, may need translation):
        #   Station__r.Affiliate__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.affiliate = False

    @api.depends('sf_external_id')
    def _compute_call_letters(self):
        # SF formula (verbatim, may need translation):
        #   Station__r.Call_Letters__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.call_letters = False

    @api.depends('sf_external_id')
    def _compute_market(self):
        # SF formula (verbatim, may need translation):
        #   Station__r.Market__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.market = False

    @api.depends('sf_external_id')
    def _compute_us_hh(self):
        # SF formula (verbatim, may need translation):
        #   Station__r.Market__r.US_HH__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.us_hh = False
