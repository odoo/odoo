# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSfCreditMemo(models.Model):
    _name = 'mv.sf_credit_memo'
    _description = 'Sf Credit Memo'
    # === Identification ===
    name = fields.Char(string='SF Credit Memo Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.sf_credit_memo.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    account = fields.Char(string='Account', compute='_compute_account', store=True)  # SF: Account__c
    advertiser = fields.Char(string='Advertiser', compute='_compute_advertiser', store=True)  # SF: Advertiser__c
    at_fault = fields.Many2one(string='At Fault', comodel_name='res.users', ondelete='set null')  # SF: At_Fault__c
    brand = fields.Char(string='Brand', compute='_compute_brand', store=True)  # SF: Brand__c
    comments = fields.Text(string='Comments')  # SF: Comments__c
    complete = fields.Boolean(string='Complete')  # SF: Complete__c
    deal = fields.Many2one(string='Deal', comodel_name='mv.deal', ondelete='restrict', required=True)  # SF: Deal__c
    double_check = fields.Many2one(string='Double Check', comodel_name='res.users', ondelete='set null', help='Double check was missed by the user linked to this field')  # SF: Double_Check__c
    error_reason = fields.Many2many(string='ERROR REASON', comodel_name='mv.error_reason.tag', relation='mv_sf_credit_memo_error_reason_rel', help='Populate this field if the SFCM was created due to MVMS error.')  # SF: ERROR_REASON__c
    error_caused_by = fields.Selection(string='Error Caused By', selection=[('agency', 'Agency'), ('marathon_ventures', 'Marathon Ventures'), ('network', 'Network'), ('marathon_ventures_former_employee', 'Marathon Ventures - Former Employee'), ('system_error', 'System Error')], required=True)  # SF: Error_Caused_By__c
    finance_comments = fields.Text(string='Finance Comments')  # SF: Finance_Comments__c
    intacct_cm = fields.Char(string='Intacct CM', size=25)  # SF: Intacct_CM__c
    netsuite_cm = fields.Char(string='Netsuite CM', size=150)  # SF: Netsuite_CM__c
    program = fields.Char(string='Program', compute='_compute_program', store=True)  # SF: Program__c
    request_assistance_comments = fields.Text(string='Request Assistance Comments')  # SF: Request_Assistance_Comments__c
    request_assistance = fields.Boolean(string='Request Assistance')  # SF: Request_Assistance__c
    sf_credit_memo_total = fields.Char(string='SF Credit Memo Total', compute='_compute_sf_credit_memo_total', store=True)  # SF: SF_Credit_Memo_Total__c | TODO unknown SF type: Summary
    status = fields.Selection(string='Status', selection=[('pending', 'Pending'), ('approved_by_ops_manager', 'Approved by Ops Manager'), ('approved_by_finance', 'Approved by Finance'), ('approved_by_op_s_task_force', 'Approved by Op\'s Task Force'), ('approved_by_upper_management', 'Approved by Upper Management'), ('approved_by_partner', 'Approved by Partner')])  # SF: Status__c | SF Unrestricted picklist
    tms = fields.Char(string='TMS', size=10, help='Scripps Only')  # SF: TMS__c
    task_force_comments = fields.Text(string='Task Force Comments')  # SF: Task_Force_Comments__c
    type = fields.Selection(string='Type', selection=[('credit', 'Credit'), ('network_error', 'Network Error'), ('agency_discount', 'Agency Discount'), ('agency_discount_approved_by_network', 'Agency Discount - Approved by Network'), ('network_performance', 'Network Performance'), ('bundles_credit', 'Bundles Credit'), ('bundles_performance', 'Bundles Performance')])  # SF: Type__c | SF Unrestricted picklist

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_account(self):
        # SF formula (verbatim, may need translation):
        #   Deal__r.ContactAccount__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account = False

    @api.depends('sf_external_id')
    def _compute_advertiser(self):
        # SF formula (verbatim, may need translation):
        #   Deal__r.Advertiser__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.advertiser = False

    @api.depends('sf_external_id')
    def _compute_brand(self):
        # SF formula (verbatim, may need translation):
        #   Deal__r.Brands__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.brand = False

    @api.depends('sf_external_id')
    def _compute_program(self):
        # SF formula (verbatim, may need translation):
        #   Deal__r.Program__r.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.program = False

    @api.depends('sf_external_id')
    def _compute_sf_credit_memo_total(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.sf_credit_memo_total = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_at_fault_required_if_mv_error(self):
        # SF Validation Rule: At_Fault_Required_If_MV_Error
        # SF condition (fires when TRUE):
        #   IF(AND(ISPICKVAL(Error_Caused_By__c, "Marathon Ventures"), ISBLANK(At_Fault__c)), True, False)
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("The \"At fault\" field must be populated with a user if the \"Error Caused By\" is Marathon Ventures."))

    @api.constrains('sf_external_id')
    def _check_at_fault_review(self):
        # SF Validation Rule: At_Fault_Review
        # SF condition (fires when TRUE):
        #   AND(OR( ISPICKVAL( Type__c , "Credit - Approved by Network"), ISPICKVAL(Type__c, "Credit")), ISPICKVAL( Error_Caused_By__c , ""))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("If SFCM Type = Credit, Please populate the 'Error Caused By' field."))

    @api.constrains('sf_external_id')
    def _check_cm_field_error(self):
        # SF Validation Rule: CM_field_Error
        # SF condition (fires when TRUE):
        #   AND(ISNEW(),NOT(ISBLANK( Netsuite_CM__c )))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("For new records, Netsuite CM cannot be populated"))

    @api.constrains('sf_external_id')
    def _check_double_check(self):
        # SF Validation Rule: Double_Check
        # SF condition (fires when TRUE):
        #   AND(ISNEW(),ISBLANK(Double_Check__c))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please populate Double Check field with appropriate MVMS Team Member."))

    @api.constrains('sf_external_id')
    def _check_must_enter_comments(self):
        # SF Validation Rule: Must_Enter_Comments
        # SF condition (fires when TRUE):
        #   ISBLANK( Comments__c )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Please add an explanation."))

    @api.constrains('sf_external_id')
    def _check_sfcm_complete(self):
        # SF Validation Rule: SFCM_Complete
        # SF condition (fires when TRUE):
        #   Complete__c = True
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("This SFCM has been processed and marked off as complete by Finance. Please consult with Finance before making any changes. All changes must be approved by Finance."))

    @api.constrains('sf_external_id')
    def _check_scripps_networks_needs_tms_number(self):
        # SF Validation Rule: Scripps_Networks_needs_TMS_number
        # SF condition (fires when TRUE):
        #   AND(ISBLANK(TMS__c),
        #   AND(
        #   OR(
        #   ISPICKVAL(Type__c , "Network Error"),
        #    ISPICKVAL( Type__c ,"Network Performance"),
        #   ISPICKVAL(Type__c , "Agency Discount - Approved by Network")
        #   )),
        #   AND (
        #   OR(
        #   CONTAINS(Program__c, "True Real"),
        #   CONTAINS(Program__c, "Bounce PP"),
        #   CONTAINS(Program__c, "BounceTV"),
        #   CONTAINS(Program__c, "BounceTV Local"),
        #   CONTAINS(Program__c, "CourtTV"),
        #   CONTAINS(Program__c, "CourtTV Mystery"),
        #   CONTAINS(Program__c, "Defy"),
        #   CONTAINS(Program__c, "Grit PP"),
        #   CONTAINS(Program__c, "GritTV"),
        #   CONTAINS(Program__c, "ION Television PP"),
        #   CONTAINS(Program__c, "ION Television"),
        #   CONTAINS(Program__c, "ION Mystery"),
        #   CONTAINS(Program__c, "Laff PP"),
        #   CONTAINS(Program__c, "Laff"),
        #   CONTAINS(Program__c, "Newsy")
        #   )
        #   )
        #   )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("Scripps Networks needs to have a TMS number filled."))
