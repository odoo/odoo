# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvPrelogDataMirror(models.Model):
    _name = 'mv.prelog_data_mirror'
    _description = 'Prelog Data Mirror'
    # === Identification ===
    name = fields.Char(string='Prelog Data Mirror Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.prelog_data_mirror.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    advertiserproduct = fields.Char(string='Advertiser/Product', size=255)  # SF: AdvertiserProduct__c
    agency = fields.Char(string='Agency', size=255)  # SF: Agency__c
    airdate = fields.Date(string='Air Date')  # SF: AirDate__c
    batch_id = fields.Char(string='Batch Id', size=255)  # SF: Batch_Id__c
    breakcode = fields.Char(string='Break Code', size=255)  # SF: BreakCode__c
    commercial_type = fields.Char(string='Commercial Type', size=30)  # SF: Commercial_Type__c
    current_home = fields.Char(string='Current Home', size=255)  # SF: Current_Home__c
    deal = fields.Many2one(string='Deal', comodel_name='mv.deal', ondelete='set null')  # SF: Deal__c
    errorreason = fields.Many2many(string='Error Reason', comodel_name='mv.errorreason.tag', relation='mv_prelog_data_mirror_errorreason_rel')  # SF: ErrorReason__c
    field1 = fields.Char(string='800#', size=255)  # SF: Field1__c
    line = fields.Integer(string='Line')  # SF: Line__c
    log_schedule = fields.Char(string='Log Schedule', size=18)  # SF: Log_Schedule__c
    materialdescription = fields.Char(string='Material Description', size=255)  # SF: MaterialDescription__c
    networkdealnumber = fields.Char(string='Network Deal Number', size=255)  # SF: NetworkDealNumber__c
    network = fields.Char(string='Network', size=255)  # SF: Network__c
    orderproductdescription = fields.Char(string='Order Product Description', size=255)  # SF: OrderProductDescription__c
    prelogdata = fields.Many2one(string='Prelog Data', comodel_name='mv.prelog_data', ondelete='set null')  # SF: PrelogData__c
    problem = fields.Boolean(string='[Dep]Problem')  # SF: Problem__c
    processrequired = fields.Boolean(string='[Dep]Process Required')  # SF: ProcessRequired__c
    product = fields.Char(string='Product', size=255)  # SF: Product__c
    rate = fields.Monetary(string='Rate', currency_field='currency_id')  # SF: Rate__c
    reason_for_unmatched = fields.Char(string='Reason for Unmatched', size=255, help='Reason for non match to schedule and/or solution/correction required for proper match')  # SF: Reason_for_Unmatched__c
    removal_reason = fields.Selection(string='Removal Reason', selection=[('overrun', 'Overrun'), ('out_of_rotation', 'Out of Rotation'), ('not_booked', 'Not Booked'), ('other', 'Other'), ('canceled', 'Canceled'), ('hiatused', 'Hiatused'), ('cia', 'CIA'), ('log_issues', 'LOG issues'), ('rate_change', 'Rate Change'), ('duplicate', 'Duplicate')])  # SF: Removal_Reason__c
    scheduleadid = fields.Char(string='Schedule Ad-ID', size=255)  # SF: ScheduleAdID__c
    schedulelength = fields.Char(string='Schedule Length', size=255)  # SF: ScheduleLength__c
    scheduletime = fields.Char(string='Schedule Time', size=255)  # SF: ScheduleTime__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='set null')  # SF: Schedule__c
    timeperiod = fields.Char(string='Time Period', size=255)  # SF: TimePeriod__c
    version = fields.Integer(string='Version', required=True)  # SF: Version__c
    week = fields.Date(string='Week', compute='_compute_week', store=True)  # SF: Week__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_week(self):
        # SF formula (verbatim, may need translation):
        #   CASE(
        #   MOD( AirDate__c - DATE(1900, 1, 7), 7),
        #   0, AirDate__c - 6,
        #   1, AirDate__c ,
        #   2, AirDate__c -1,
        #   3, AirDate__c - 2,
        #   4,AirDate__c - 3,
        #   5, AirDate__c - 4,
        #   6, AirDate__c - 5, DATE(2001, 1, 1))
        for rec in self:
            # TODO: translate SF formula to Python
            rec.week = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_network_must_exist(self):
        # SF Validation Rule: Network_Must_Exist
        # SF condition (fires when TRUE):
        #   ISBLANK(Network__c)
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("The Network field is empty - contact Product Team for assistance."))

    @api.constrains('sf_external_id')
    def _check_prelog_missing_fields(self):
        # SF Validation Rule: Prelog_Missing_Fields
        # SF condition (fires when TRUE):
        #   OR( ISBLANK( Version__c ), ISBLANK(  NetworkDealNumber__c ), ISBLANK( AirDate__c ), ISBLANK ( ScheduleTime__c ), ISBLANK( Network__c ), ISBLANK( ScheduleLength__c ))
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("You are missing one of the necessary fields for Prelog Data Mirror: Version, Air Date, Air Time, Network, Network Deal Number or Length.  Double check your upload file."))

    @api.constrains('sf_external_id')
    def _check_weeks_must_match(self):
        # SF Validation Rule: Weeks_Must_Match
        # SF condition (fires when TRUE):
        #   if( Week__c  <>  Schedule__r.Week__c , True, False)
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("The schedule you have entered matches to a different week than this record. Please find a schedule in the correct week."))
