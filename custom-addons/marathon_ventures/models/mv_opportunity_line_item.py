# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvOpportunityLineItem(models.Model):
    _name = 'mv.opportunity_line_item'
    _description = 'Opportunity Line Item'

    # === Identification ===
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    lead_id = fields.Many2one('crm.lead', string='Opportunity', ondelete='cascade', required=True)

    # === SF Custom Fields ===
    description = fields.Char(string='Description')  # SF: Description | SF std field with no <type> in metadata
    discount = fields.Char(string='Discount')  # SF: Discount | SF std field with no <type> in metadata
    listprice = fields.Char(string='ListPrice')  # SF: ListPrice | SF std field with no <type> in metadata
    name = fields.Char(string='Name')  # SF: Name | SF std field with no <type> in metadata
    opportunityid = fields.Many2one(string='OpportunityId', comodel_name='crm.lead', ondelete='set null')  # SF: OpportunityId
    product2id = fields.Many2one(string='Product2Id', comodel_name='product.template', ondelete='set null')  # SF: Product2Id
    productcode = fields.Char(string='ProductCode')  # SF: ProductCode | SF std field with no <type> in metadata
    quantity = fields.Char(string='Quantity')  # SF: Quantity | SF std field with no <type> in metadata
    servicedate = fields.Char(string='ServiceDate')  # SF: ServiceDate | SF std field with no <type> in metadata
    serviceenddate = fields.Date(string='Service End Date')  # SF: ServiceEndDate__c
    subtotal = fields.Char(string='Subtotal')  # SF: Subtotal | SF std field with no <type> in metadata
    totalprice = fields.Char(string='TotalPrice')  # SF: TotalPrice | SF std field with no <type> in metadata
    unitprice = fields.Char(string='UnitPrice')  # SF: UnitPrice | SF std field with no <type> in metadata
