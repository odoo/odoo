# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvWorkingLog(models.Model):
    _name = 'mv.working_log'
    _description = 'Working Log'
    # === Identification ===
    name = fields.Char(string='Working Log Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    air_date_air_time = fields.Char(string='Air Date/Air Time', compute='_compute_air_date_air_time', store=True)  # SF: Air_Date_Air_Time__c
    air_date = fields.Date(string='Air Date')  # SF: Air_Date__c
    air_time = fields.Char(string='Air Time', size=18)  # SF: Air_Time__c
    asset_id = fields.Char(string='Asset Id', size=120)  # SF: Asset_Id__c
    broadcast_network = fields.Char(string='Broadcast Network', size=120)  # SF: Broadcast_Network__c
    created_date_time = fields.Datetime(string='Created Date Time', compute='_compute_created_date_time', store=True)  # SF: Created_Date_Time__c
    current_version = fields.Boolean(string='Current Version', compute='_compute_current_version', store=True)  # SF: Current_Version__c
    day_of_week = fields.Char(string='Day of Week', compute='_compute_day_of_week', store=True)  # SF: Day_of_Week__c
    discrepancy_reasoning = fields.Char(string='Discrepancy Reasoning', size=255)  # SF: Discrepancy_Reasoning__c
    discrepancy = fields.Boolean(string='Discrepancy')  # SF: Discrepancy__c
    double_check_pod = fields.Char(string='Double Check Pod', compute='_compute_double_check_pod', store=True)  # SF: Double_Check_Pod__c
    equiv_30 = fields.Float(string='Equiv :30', digits=(18, 1), compute='_compute_equiv_30', store=True)  # SF: Equiv_30__c
    framed_duration = fields.Char(string='Framed Duration', size=255)  # SF: Framed_Duration__c
    hour_of_day = fields.Char(string='Hour of Day', compute='_compute_hour_of_day', store=True)  # SF: Hour_of_Day__c
    isci = fields.Char(string='ISCI', size=255)  # SF: ISCI__c
    locked = fields.Boolean(string='Locked')  # SF: Locked__c
    main_network_temp = fields.Boolean(string='Main Network Temp')  # SF: Main_Network_Temp__c
    main_network = fields.Boolean(string='Main Network', compute='_compute_main_network', store=True)  # SF: Main_Network__c
    pod = fields.Char(string='Pod', size=255)  # SF: Pod__c
    podded_day = fields.Char(string='Podded Day', compute='_compute_podded_day', store=True)  # SF: Podded_Day__c
    prelog_data = fields.Many2one(string='Prelog Data', comodel_name='mv.prelog_data', ondelete='set null')  # SF: Prelog_Data__c
    program_version = fields.Integer(string='Program Version')  # SF: Program_Version__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='cascade', required=True)  # SF: Schedule__c
    segment_duration = fields.Char(string='Segment Duration', size=255)  # SF: Segment_Duration__c
    segment = fields.Integer(string='Segment')  # SF: Segment__c
    series = fields.Char(string='Series', size=255)  # SF: Series__c
    spot_rate = fields.Monetary(string='Spot Rate', currency_field='currency_id', compute='_compute_spot_rate', store=True)  # SF: Spot_Rate__c
    title = fields.Char(string='Title', size=255)  # SF: Title__c
    type = fields.Selection(string='Type', selection=[('media', 'Media'), ('episode', 'Episode')])  # SF: Type__c
    unfilled_time = fields.Integer(string='Unfilled Time')  # SF: Unfilled_Time__c
    version = fields.Integer(string='Version')  # SF: Version__c
    x800_number = fields.Char(string='800 Number', size=155)  # SF: X800_Number__c

    # === Computed / Roll-Up ===

    @api.depends()
    def _compute_air_date_air_time(self):
        # SF formula (verbatim, may need translation):
        #   LPAD(TEXT(MONTH(Air_Date__c)), 2, '0') & "/" &
        #   LPAD(TEXT(DAY(Air_Date__c)), 2, '0') & "/" &
        #   TEXT(YEAR(Air_Date__c)) & " - " &
        #   LPAD(
        #     IF(
        #       AND(
        #         RIGHT(Air_Time__c, 2) = "PM",
        #         VALUE(MID(Air_Time__c, 1, FIND(":", Air_Time__c) - 1)) != 12
        #       ),
        #       TEXT(VALUE(MID(Air_Time__c, 1, FIND(":", Air_Time__c) - 1)) + 12),
        #       IF(
        #         AND(
        #           RIGHT(Air_Time__c, 2) = "AM",
        #           VALUE(MID(Air_Time__c, 1, FIND(":", Air_Time__c) - 1)) = 12
        #         ),
        #         "00",
        #         LPAD(MID(Air_Time__c, 1, FIND(":", Air_Time__c) - 1), 2, '0')
        #       )
        #     ),
        #     2, '0'
        #   ) & ":" &
        #   LPAD(
        #     MID(Air_Time__c, FIND(":", Air_Time__c) + 1, 2),
        #     2, '0'
        #   ) & ":" &
        #   LPAD(
        #     MID(Air_Time__c, FIND(":", Air_Time__c) + 4, 2),
        #     2, '0'
        #   )
        for rec in self:
            # TODO: translate SF formula to Python
            rec.air_date_air_time = False

    @api.depends()
    def _compute_created_date_time(self):
        # SF formula (verbatim, may need translation):
        #   CreatedDate
        for rec in self:
            # TODO: translate SF formula to Python
            rec.created_date_time = False

    @api.depends()
    def _compute_current_version(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Working_Log_Version__c =  Version__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.current_version = False

    @api.depends()
    def _compute_day_of_week(self):
        # SF formula (verbatim, may need translation):
        #   CASE(
        #   MOD(  Air_Date__c  - DATE(1900, 1, 7), 7),
        #   0, "7Sun",
        #   1, "1Mon",
        #   2, "2Tues",
        #   3, "3Wed",
        #   4, "4Th",
        #   5, "5Fri",
        #   6, "6Sat", "Error")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.day_of_week = False

    @api.depends()
    def _compute_double_check_pod(self):
        # SF formula (verbatim, may need translation):
        #   IF(
        #       ISBLANK(Schedule__r.ISCI_CODE__c) || ISBLANK(ISCI__c),
        #       "Missing ISCI on Schedule or Pod",
        #       IF(
        #           NOT(
        #               CONTAINS(
        #                   SUBSTITUTE(Schedule__r.ISCI_CODE__c, "-", ""),
        #                   SUBSTITUTE(ISCI__c, "-", "")
        #               )
        #           ),
        #           "ISCI not present in Schedule ISCI field",
        #           IF(
        #               NOT(
        #                   CONTAINS(
        #                       LOWER(Schedule__r.Pod__c),
        #                       LOWER(Pod__c)
        #                   )
        #               ),
        #               "Podded spot not present in Schedules Pod Field",
        #               IF(
        #                   NOT(
        #                       CONTAINS(
        #                           LOWER(Schedule__r.Days_of_Air__c),
        #                           LOWER(Podded_Day__c)
        #                       )
        #                   ),
        #                   "Podded Day not present in Schedules Day of Air Field",
        #                   ""
        #               )
        #           )
        #       )
        #   )
        for rec in self:
            # TODO: translate SF formula to Python
            rec.double_check_pod = False

    @api.depends()
    def _compute_equiv_30(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.UnitLength__c /30
        for rec in self:
            # TODO: translate SF formula to Python
            rec.equiv_30 = False

    @api.depends()
    def _compute_hour_of_day(self):
        # SF formula (verbatim, may need translation):
        #   IF(LEFT( Air_Time__c , 2)="1:","1",
        #   IF(LEFT( Air_Time__c , 2)="2:","2",
        #   IF(LEFT( Air_Time__c , 2)="3:","3",
        #   IF(LEFT( Air_Time__c , 2)="4:","4",
        #   IF(LEFT( Air_Time__c , 2)="5:","5",
        #   IF(LEFT( Air_Time__c , 2)="6:","6",
        #   IF(LEFT( Air_Time__c , 2)="7:","7",
        #   IF(LEFT( Air_Time__c , 2)="8:","8",
        #   IF(LEFT( Air_Time__c , 2)="9:","9",
        #   IF(LEFT( Air_Time__c , 2)="10","10",
        #   IF(LEFT( Air_Time__c , 2)="11","11",
        #   IF(LEFT( Air_Time__c , 2)="12","12",
        #   IF(LEFT( Air_Time__c , 2)="01","1",
        #   IF(LEFT( Air_Time__c , 2)="02","2",
        #   IF(LEFT( Air_Time__c , 2)="03","3",
        #   IF(LEFT( Air_Time__c , 2)="04","4",
        #   IF(LEFT( Air_Time__c , 2)="05","5",
        #   IF(LEFT( Air_Time__c , 2)="06","6",
        #   IF(LEFT( Air_Time__c , 2)="07","7",
        #   IF(LEFT( Air_Time__c , 2)="08","8",
        #   IF(LEFT( Air_Time__c , 2)="09","9",
        #   "FALSE")))))))))))))))))))))
        #   &
        #   IF( RIGHT( Air_Time__c ,2)="am","am",
        #   IF( RIGHT( Air_Time__c ,2)="AM","am",
        #   IF( RIGHT( Air_Time__c ,2)="pm","pm",
        #   IF( RIGHT( Air_Time__c ,2)="PM","pm",
        #   IF( RIGHT( Air_Time__c ,2)="XM","am",
        #   IF( RIGHT( Air_Time__c ,2)="xm","am",
        #   "False"))))))
        for rec in self:
            # TODO: translate SF formula to Python
            rec.hour_of_day = False

    @api.depends()
    def _compute_main_network(self):
        # SF formula (verbatim, may need translation):
        #   Broadcast_Network__c = Schedule__r.DealProgram__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.main_network = False

    @api.depends()
    def _compute_podded_day(self):
        # SF formula (verbatim, may need translation):
        #   CASE(
        #   MOD( Air_Date__c - DATE(1900, 1, 7), 7),
        #   0, "Su",
        #   1, "M",
        #   2, "Tu",
        #   3, "W",
        #   4, "Th",
        #   5, "F",
        #   6, "Sa", "Error")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.podded_day = False

    @api.depends()
    def _compute_spot_rate(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Rate__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.spot_rate = False

    # === Constraints (translated from SF Validation Rules) ===

    @api.constrains('sf_external_id')
    def _check_version_duplicate(self):
        # SF Validation Rule: Version_Duplicate
        # SF condition (fires when TRUE):
        #   AND(
        #     NOT(ISBLANK(Version__c)),
        #     NOT(ISBLANK(Schedule__r.Working_Log_Version__c)),
        #     NOT(
        #       OR(
        #         Schedule__r.DealProgram__c = "NoseyTV",
        #         Schedule__r.DealProgram__c = "Confess"
        #       )
        #     ),
        #     Version__c <= Schedule__r.Working_Log_Version__c
        #   )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("An upload with this version number has already been uploaded please reach out to Kiyana Dunlock."))

    @api.constrains('sf_external_id')
    def _check_working_logs_locked(self):
        # SF Validation Rule: Working_Logs_Locked
        # SF condition (fires when TRUE):
        #   AND(
        #       Locked__c = TRUE,
        #       Air_Date__c <= TODAY(),
        #       ISCHANGED(Locked__c) = FALSE
        #   )
        for rec in self:
            # TODO: translate SF formula; the condition below is a placeholder
            condition = False
            if condition:
                raise ValidationError(_("This record is locked and cannot be modified."))
