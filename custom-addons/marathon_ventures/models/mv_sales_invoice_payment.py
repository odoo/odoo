# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSalesInvoicePayment(models.Model):
    _name = 'mv.sales_invoice_payment'
    _description = 'Sales Invoice Payment'
    # === Identification ===
    name = fields.Char(string='Sales Invoice Payment', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    amount = fields.Monetary(string='Amount', currency_field='currency_id')  # SF: Amount__c
    document_number = fields.Char(string='Document Number', size=200)  # SF: Document_Number__c
    payment_date = fields.Date(string='Payment Date')  # SF: Payment_Date__c
    payment_method = fields.Char(string='Payment Method', size=50)  # SF: Payment_Method__c
    payment_recordkey = fields.Char(string='Payment Recordkey', size=50)  # SF: Payment_Recordkey__c
    payment_type = fields.Char(string='Payment Type', size=50)  # SF: Payment_Type__c
    posting_date = fields.Date(string='Posting Date')  # SF: Posting_Date__c
    receipt_date = fields.Date(string='Receipt Date')  # SF: Receipt_Date__c
    sales_invoice = fields.Many2one(string='Sales Invoice', comodel_name='mv.sales_invoice', ondelete='cascade', required=True)  # SF: Sales_Invoice__c
