# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

from odoo import models, fields, _


class TagPerformingRightsLicensePaymentTermsTag(models.Model):
    _name = 'mv.performing_rights_license_payment_terms.tag'
    _description = 'Tag for performing_rights_license_payment_terms'

    name = fields.Char(string='Name', default='New', copy=False)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.performing_rights_license_payment_terms.tag.'),
    ]

class TagSilverTeamAccountsTag(models.Model):
    _name = 'mv.silver_team_accounts.tag'
    _description = 'Tag for silver_team_accounts'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.silver_team_accounts.tag.'),
    ]

class TagDaysTag(models.Model):
    _name = 'mv.days.tag'
    _description = 'Tag for days'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.days.tag.'),
    ]

class TagBusinessTypeTag(models.Model):
    _name = 'mv.business_type.tag'
    _description = 'Tag for business_type'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.business_type.tag.'),
    ]

class TagRestrictedProgrammingDnaTag(models.Model):
    _name = 'mv.restricted_programming_dna.tag'
    _description = 'Tag for restricted_programming_dna'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.restricted_programming_dna.tag.'),
    ]

class TagLogTypeTag(models.Model):
    _name = 'mv.log_type.tag'
    _description = 'Tag for log_type'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.log_type.tag.'),
    ]

class TagErrorreasonTag(models.Model):
    _name = 'mv.errorreason.tag'
    _description = 'Tag for errorreason'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.errorreason.tag.'),
    ]

class TagDaysAllowedTag(models.Model):
    _name = 'mv.days_allowed.tag'
    _description = 'Tag for days_allowed'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.days_allowed.tag.'),
    ]

class TagErrorReasonTag(models.Model):
    _name = 'mv.error_reason.tag'
    _description = 'Tag for error_reason'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.error_reason.tag.'),
    ]

class TagErrorTag(models.Model):
    _name = 'mv.error.tag'
    _description = 'Tag for error'

    name = fields.Char(string='Name', required=True)
    code = fields.Char(string='SF Value Code', index=True, copy=False)
    color = fields.Integer(string='Color')

    _sql_constraints = [
        ('code_uniq', 'unique(code)', 'SF value code must be unique within mv.error.tag.'),
    ]