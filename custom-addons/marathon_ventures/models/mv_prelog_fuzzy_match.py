# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvPrelogFuzzyMatch(models.Model):
    _name = 'mv.prelog_fuzzy_match'
    _description = 'Prelog Fuzzy Match'
    # === Identification ===
    name = fields.Char(string='Match Id', default=lambda self: self.env['ir.sequence'].next_by_code('mv.prelog_fuzzy_match.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    best_fit = fields.Char(string='Best Fit', compute='_compute_best_fit', store=True)  # SF: Best_Fit__c
    errors = fields.Text(string='Errors')  # SF: Errors__c
    matchlevel = fields.Float(string='Match Level', digits=(5, 2), help='The % likelihood the schedule matches the mirror')  # SF: MatchLevel__c
    minutes = fields.Integer(string='Number of Minutes')  # SF: Minutes__c
    prelogdatamirrorid = fields.Many2one(string='Prelog Data Mirror', comodel_name='mv.prelog_data_mirror', ondelete='cascade', required=True)  # SF: PrelogDataMirrorId__c
    scheduleid = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='set null')  # SF: ScheduleId__c
    schedule_rate = fields.Monetary(string='Schedule Rate', currency_field='currency_id', compute='_compute_schedule_rate', store=True)  # SF: Schedule_Rate__c
    time_period = fields.Char(string='Time Period', compute='_compute_time_period', store=True)  # SF: Time_Period__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_best_fit(self):
        # SF formula (verbatim, may need translation):
        #   HYPERLINK("/apex/BestFitButton?Id=" +  Id, "Best Fit")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.best_fit = False

    @api.depends('sf_external_id')
    def _compute_schedule_rate(self):
        # SF formula (verbatim, may need translation):
        #   ScheduleId__r.Rate__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.schedule_rate = False

    @api.depends('sf_external_id')
    def _compute_time_period(self):
        # SF formula (verbatim, may need translation):
        #   ScheduleId__r.Start_End_Time__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.time_period = False
