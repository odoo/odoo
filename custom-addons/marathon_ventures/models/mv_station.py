# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvStation(models.Model):
    _name = 'mv.station'
    _description = 'Station'
    # === Identification ===
    name = fields.Char(string='Station Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.station.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    active_station = fields.Boolean(string='Active Station', help='Check this box if the Station is a part of an active Bundle Pricing Unit.')  # SF: Active_Station__c
    affiliate_owner = fields.Selection(string='Affiliate Owner', selection=[('gray', 'Gray'), ('gannett', 'Gannett'), ('pmcm_tv_llc', 'PMCM TV, LLC'), ('tribune', 'Tribune'), ('raycom', 'Raycom'), ('media_general', 'Media General'), ('hearst', 'Hearst'), ('tegna', 'Tegna'), ('american_spirit', 'American Spirit'), ('univision', 'Univision'), ('scripps', 'Scripps')])  # SF: Affiliate_Owner__c | SF Unrestricted picklist
    affiliate = fields.Char(string='Affiliate', size=55)  # SF: Affiliate__c
    call_letters = fields.Char(string='EC Call Letters', size=55, required=True, index=True)  # SF: Call_Letters__c | SF unique → consider _sql_constraints
    group_owner = fields.Many2one(string='Group Owner', comodel_name='res.partner', ondelete='set null', help='The Stations Group Owner(Lookup to Account)')  # SF: Group_Owner__c
    market = fields.Many2one(string='Market', comodel_name='mv.market', ondelete='set null')  # SF: Market__c
    nielsen = fields.Char(string='Nielsen Call Letters', size=18)  # SF: Nielsen__c
    owner_call_letters = fields.Char(string='Owner Call Letters', size=18, help='What the Group Owner calls the Station.')  # SF: Owner_Call_Letters__c
    rank = fields.Integer(string='Market Rank', compute='_compute_rank', store=True)  # SF: Rank__c
    station_affiliate = fields.Many2one(string='Station Affiliate', comodel_name='mv.programs', ondelete='set null', help='The Stations Affiliate(Lookup to Program)')  # SF: Station_Affiliate__c
    station_count = fields.Integer(string='Station Count', compute='_compute_station_count', store=True, help='Station Count Field used for calculations')  # SF: Station_Count__c

    # === Computed / Roll-Up ===

    @api.depends()
    def _compute_rank(self):
        # SF formula (verbatim, may need translation):
        #   Market__r.DMA_Rank__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.rank = False

    @api.depends()
    def _compute_station_count(self):
        # SF formula (verbatim, may need translation):
        #   1
        for rec in self:
            # TODO: translate SF formula to Python
            rec.station_count = False
