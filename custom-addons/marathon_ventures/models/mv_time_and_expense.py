# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvTimeAndExpense(models.Model):
    _name = 'mv.time_and_expense'
    _description = 'Time And Expense'

    # === Identification ===
    name = fields.Char(string='TE Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.time_and_expense.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    intacct_recon = fields.Boolean(string='Intacct Recon', help='Check off if SF = Intacct')  # SF: Intacct_Recon__c
    mv_employee = fields.Many2one(string='MV Employee', comodel_name='res.partner', ondelete='restrict', required=True, help='Report owner - to be reimbursed.')  # SF: MV_Employee__c
    te_period = fields.Date(string='TE Period', required=True, help='Date of Time and Expense Period')  # SF: TE_Period__c
    te_total = fields.Char(string='TE Total', compute='_compute_te_total', store=True)  # SF: TE_Total__c | TODO unknown SF type: Summary

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_te_total(self):
        # SF formula (verbatim, may need translation):
        for rec in self:
            # TODO: translate SF formula to Python
            rec.te_total = False
