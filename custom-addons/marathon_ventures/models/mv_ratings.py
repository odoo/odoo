# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvRatings(models.Model):
    _name = 'mv.ratings'
    _description = 'Ratings'
    # === Identification ===
    name = fields.Char(string='Ratings', default=lambda self: self.env['ir.sequence'].next_by_code('mv.ratings.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    actual_ratings = fields.Many2one(string='Actual Ratings', comodel_name='mv.rating_detail', ondelete='set null')  # SF: Actual_Ratings__c
    daypart_formula = fields.Char(string='Daypart Formula', compute='_compute_daypart_formula', store=True)  # SF: Daypart_Formula__c
    daypart = fields.Selection(string='Daypart', selection=[('da', 'DA'), ('da_mf', 'DA - MF'), ('da_ss', 'DA - SS'), ('em', 'EM'), ('ln', 'LN'), ('on', 'ON'), ('pr', 'PR'), ('total_day', 'Total Day'), ('em_ms_6a_9a', 'EM - MS 6a-9a'), ('da_mf_9a_1p', 'DA - MF 9a-1p'), ('ef_mf_1p_7p', 'EF - MF 1p-7p'), ('da_ss_9a_7p', 'DA - SS 9a-7p'), ('pr_mf_7p_1a', 'PR - MF 7p-1a'), ('pr_ss_7p_1a', 'PR - SS 7p-1a'), ('lf_ms_1a_3a', 'LF - MS 1a-3a'), ('on_ms_3a_6a', 'ON - MS 3a-6a'), ('fr_mf', 'FR - MF'), ('pr_ss', 'PR - SS'), ('pr_mf', 'PR - MF'), ('em_ms', 'EM - MS'), ('pr_ms', 'PR - MS'), ('ln_ms', 'LN - MS'), ('on_ms', 'ON - MS'), ('ef_mf', 'EF - MF'), ('w_ss', 'W - SS'), ('v_1q23_em_ms', '1Q23 EM - MS'), ('v_1q23_pr_ms', '1Q23 PR - MS'), ('v_1q23_ln_ms', '1Q23 LN - MS'), ('v_1q23_ef_mf', '1Q23 EF - MF'), ('v_1q23_w_ss', '1Q23 W - SS'), ('v_1q23_da_mf', '1Q23 DA - MF'), ('em_mf', 'EM - MF'), ('wnba_7p_1a', 'WNBA 7p-1a'), ('nwsl_7p_1a', 'NWSL 7p-1a'), ('da_mf_9a_6p', 'DA - MF 9a-6p'), ('pr_ms_6p_12a', 'PR - MS 6p-12a'), ('ln_ms_12a_3a', 'LN - MS 12a-3a'), ('da_ss_9a_6p', 'DA - SS 9a-6p')])  # SF: Daypart__c
    estimated_ratings = fields.Many2one(string='Estimated Rating', comodel_name='mv.rating_detail', ondelete='set null')  # SF: Estimated_Ratings__c
    hour = fields.Char(string='Hour', size=255)  # SF: Hour__c
    measurement_type = fields.Selection(string='Measurement Type', selection=[('nielsen_big_data', 'Nielsen + Big Data'), ('nielsen_panel', 'Nielsen + Panel'), ('video_amp', 'Video Amp'), ('other', 'Other')])  # SF: Measurement_Type__c
    month = fields.Integer(string='Month', compute='_compute_month', store=True)  # SF: Month__c
    program = fields.Many2one(string='Program', comodel_name='mv.programs', ondelete='restrict', required=True)  # SF: Program__c
    projected_ratings = fields.Many2one(string='Projected Ratings', comodel_name='mv.rating_detail', ondelete='set null')  # SF: Projected_Ratings__c
    quarter = fields.Integer(string='Quarter', compute='_compute_quarter', store=True)  # SF: Quarter__c
    week = fields.Date(string='Week')  # SF: Week__c
    year = fields.Integer(string='Year', compute='_compute_year', store=True)  # SF: Year__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_daypart_formula(self):
        # SF formula (verbatim, may need translation):
        #   TEXT(Daypart__c)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.daypart_formula = False

    @api.depends('sf_external_id')
    def _compute_month(self):
        # SF formula (verbatim, may need translation):
        #   MONTH(Week__c + 6)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.month = False

    @api.depends('sf_external_id')
    def _compute_quarter(self):
        # SF formula (verbatim, may need translation):
        #   IF( Month__c < 4, 1, IF( ((Month__c >3) && (Month__c < 7)), 2, IF( ((Month__c >6) && (Month__c <10)),3, IF(( Month__c >9), 4, 0) ) ) )
        for rec in self:
            # TODO: translate SF formula to Python
            rec.quarter = False

    @api.depends('sf_external_id')
    def _compute_year(self):
        # SF formula (verbatim, may need translation):
        #   YEAR( Week__c + 6)
        for rec in self:
            # TODO: translate SF formula to Python
            rec.year = False
