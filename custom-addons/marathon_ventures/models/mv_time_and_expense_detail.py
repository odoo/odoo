# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvTimeAndExpenseDetail(models.Model):
    _name = 'mv.time_and_expense_detail'
    _description = 'Time And Expense Detail'
    # === Identification ===
    name = fields.Char(string='Time and Expense Detail Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.time_and_expense_detail.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    amount = fields.Monetary(string='Amount', currency_field='currency_id', required=True)  # SF: Amount__c
    attendee_1_agency_name = fields.Char(string='Attendee 1 Agency Name', compute='_compute_attendee_1_agency_name', store=True)  # SF: Attendee_1_Agency_Name__c
    attendee_1 = fields.Many2one(string='Attendee 1', comodel_name='res.partner', ondelete='set null')  # SF: Attendee_1__c
    attendee_2_agency_name = fields.Char(string='Attendee 2 Agency Name', compute='_compute_attendee_2_agency_name', store=True)  # SF: Attendee_2_Agency_Name__c
    attendee_2 = fields.Many2one(string='Attendee 2', comodel_name='res.partner', ondelete='set null')  # SF: Attendee_2__c
    attendee_3_agency_name = fields.Char(string='Attendee 3 Agency Name', compute='_compute_attendee_3_agency_name', store=True)  # SF: Attendee_3_Agency_Name__c
    attendee_3 = fields.Many2one(string='Attendee 3', comodel_name='res.partner', ondelete='set null')  # SF: Attendee_3__c
    attendee_comments = fields.Text(string='Attendee Comments')  # SF: Attendee_Comments__c
    comment = fields.Text(string='Comment', help='Expense details')  # SF: Comment__c
    exp_type = fields.Selection(string='Exp Type', selection=[('airfare', 'Airfare'), ('car_milage', 'Car Milage'), ('car_rental', 'Car Rental'), ('car_service', 'Car Service'), ('charitable_contributions', 'Charitable Contributions'), ('cocktails', 'Cocktails'), ('entertainment', 'Entertainment'), ('gas_or_tolls', 'Gas or Tolls'), ('hotel_lodging', 'Hotel Lodging'), ('meals', 'Meals'), ('mta_bus_or_subway', 'MTA Bus or Subway'), ('office_exp', 'Office Exp'), ('office_supplies', 'Office Supplies'), ('parking', 'Parking'), ('taxi', 'Taxi'), ('trade_show_ent', 'Trade Show Ent.'), ('train', 'Train'), ('wireless_reimbursement', 'Wireless Reimbursement'), ('gifts', 'Gifts')], required=True)  # SF: Exp_Type__c
    time_and_expense = fields.Many2one(string='Time and Expense', comodel_name='mv.time_and_expense', ondelete='cascade', required=True)  # SF: Time_and_Expense__c
    transaction_date_del = fields.Date(string='Transaction Date', help='Receipt Date')  # SF: Transaction_Date_del__c
    vendor_description = fields.Char(string='Vendor Description', size=100)  # SF: Vendor_Description__c

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_attendee_1_agency_name(self):
        # SF formula (verbatim, may need translation):
        #   Attendee_1__r.Account.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.attendee_1_agency_name = False

    @api.depends('sf_external_id')
    def _compute_attendee_2_agency_name(self):
        # SF formula (verbatim, may need translation):
        #   Attendee_2__r.Account.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.attendee_2_agency_name = False

    @api.depends('sf_external_id')
    def _compute_attendee_3_agency_name(self):
        # SF formula (verbatim, may need translation):
        #   Attendee_3__r.Account.Name
        for rec in self:
            # TODO: translate SF formula to Python
            rec.attendee_3_agency_name = False
