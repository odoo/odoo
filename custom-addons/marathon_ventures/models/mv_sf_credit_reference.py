# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSfCreditReference(models.Model):
    _name = 'mv.sf_credit_reference'
    _description = 'Sf Credit Reference'
    # === Identification ===
    name = fields.Char(string='SF Credit Reference Name', default=lambda self: self.env['ir.sequence'].next_by_code('mv.sf_credit_reference.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    average_days_to_pay = fields.Char(string='Average Days to Pay', size=100)  # SF: Average_Days_to_Pay__c
    balance_past_due = fields.Monetary(string='Balance Past Due', currency_field='currency_id')  # SF: Balance_Past_Due__c
    comments = fields.Text(string='Comments')  # SF: Comments__c
    contact_person = fields.Char(string='Contact Person', size=100, required=True)  # SF: Contact_Person__c
    credit_amount_granted = fields.Monetary(string='Credit Amount Granted', currency_field='currency_id')  # SF: Credit_Amount_Granted__c
    credit_application = fields.Many2one(string='Credit Application', comodel_name='mv.credit_application', ondelete='cascade', required=True)  # SF: Credit_Application__c
    credit_terms = fields.Selection(string='Credit Terms', selection=[('cia', 'CIA'), ('due_upon_receipt', 'Due Upon Receipt'), ('net_10', 'Net 10'), ('net_15', 'Net 15'), ('net_30', 'Net 30'), ('net_45', 'Net 45'), ('net_60', 'Net 60')])  # SF: Credit_Terms__c | SF Unrestricted picklist
    current_balance = fields.Monetary(string='Current Balance', currency_field='currency_id')  # SF: Current_Balance__c
    date = fields.Date(string='Date')  # SF: Date__c
    days_past_due = fields.Integer(string='Days Past Due')  # SF: Days_Past_Due__c
    email = fields.Char(string='Email')  # SF: Email__c | SF Email — see _check_email_format
    high_credit_date = fields.Char(string='High Credit Date', size=10)  # SF: High_Credit_Date__c
    high_credit = fields.Monetary(string='High Credit', currency_field='currency_id')  # SF: High_Credit__c
    length_of_business = fields.Char(string='Length of Business', size=100)  # SF: Length_of_Business__c
    phone_number = fields.Char(string='Phone Number')  # SF: Phone_Number__c
    reference_company_website = fields.Char(string='Reference Company Website', required=True)  # SF: Reference_Company_Website__c
    reference_company = fields.Char(string='Reference Company', size=100)  # SF: Reference_Company__c
    title = fields.Char(string='Title', size=100)  # SF: Title__c
    unlimited_credit = fields.Boolean(string='Unlimited Credit', help='This checkbox is marked by Finance when unlimited credit is granted for bookings.')  # SF: Unlimited_Credit__c
