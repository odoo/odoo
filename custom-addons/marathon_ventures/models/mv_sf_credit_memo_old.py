# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvSfCreditMemoOld(models.Model):
    _name = 'mv.sf_credit_memo_old'
    _description = 'Sf Credit Memo Old'
    # === Identification ===
    name = fields.Char(string='SF Credit Memo Old Number', default=lambda self: self.env['ir.sequence'].next_by_code('mv.sf_credit_memo_old.name') or 'New', copy=False, readonly=True)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')
    currency_id = fields.Many2one('res.currency', string='Currency', default=lambda self: self.env.company.currency_id)

    # === SF Custom Fields ===
    account = fields.Char(string='Account', compute='_compute_account', store=True)  # SF: Account__c
    brand = fields.Char(string='Brand', compute='_compute_brand', store=True)  # SF: Brand__c
    comments = fields.Text(string='Comments')  # SF: Comments__c
    complete = fields.Boolean(string='Complete')  # SF: Complete__c
    credit_amount = fields.Monetary(string='Credit Amount', currency_field='currency_id')  # SF: Credit_Amount__c
    credit_date = fields.Date(string='Credit Date')  # SF: Credit_Date__c
    intacct_si_number = fields.Char(string='Intacct SI Number', compute='_compute_intacct_si_number', store=True)  # SF: Intacct_SI_Number__c
    paid_date = fields.Date(string='Paid Date')  # SF: Paid_Date__c
    program = fields.Char(string='Program', compute='_compute_program', store=True)  # SF: Program__c
    schedule_week = fields.Date(string='Schedule Week', compute='_compute_schedule_week', store=True)  # SF: Schedule_Week__c
    schedule = fields.Many2one(string='Schedule', comodel_name='mv.schedules', ondelete='restrict', required=True)  # SF: Schedule__c
    status = fields.Selection(string='Status', selection=[('pending', 'Pending'), ('approved_by_finance', 'Approved by Finance'), ('approved_by_partner', 'Approved by Partner')])  # SF: Status__c | SF Unrestricted picklist
    type = fields.Selection(string='Type', selection=[('agency_discount', 'Agency Discount'), ('agency_discount_approved_by_network', 'Agency Discount - Approved by Network'), ('credit', 'Credit'), ('credit_approved_by_network', 'Credit - Approved by Network'), ('network_error', 'Network Error')])  # SF: Type__c | SF Unrestricted picklist

    # === Computed / Roll-Up ===

    @api.depends('sf_external_id')
    def _compute_account(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Deal_Parent__r.ContactAccount__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.account = False

    @api.depends('sf_external_id')
    def _compute_brand(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.DealBrand__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.brand = False

    @api.depends('sf_external_id')
    def _compute_intacct_si_number(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Intacct_SI_Number__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.intacct_si_number = False

    @api.depends('sf_external_id')
    def _compute_program(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.DealProgram__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.program = False

    @api.depends('sf_external_id')
    def _compute_schedule_week(self):
        # SF formula (verbatim, may need translation):
        #   Schedule__r.Week__c
        for rec in self:
            # TODO: translate SF formula to Python
            rec.schedule_week = False
