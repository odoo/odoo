# -*- coding: utf-8 -*-
# Generated by marathon_ventures Salesforce->Odoo migration generator.
# Do not edit by hand if you plan to regenerate; instead update the SF metadata
# and re-run the generator.

import logging
import re
from odoo import models, fields, api, _
from odoo.exceptions import ValidationError

_logger = logging.getLogger(__name__)


class MvVideoFile(models.Model):
    _name = 'mv.video_file'
    _description = 'Video File'
    # === Identification ===
    name = fields.Char(string='Video File Name', default='New', copy=False)
    sf_external_id = fields.Char(string='SF External ID', index=True, copy=False)
    sf_legacy_data = fields.Json(string='SF Legacy Payload')

    # === SF Custom Fields ===
    account = fields.Char(string='Account', size=255)  # SF: Account__c
    advertiser = fields.Char(string='Advertiser', size=255)  # SF: Advertiser__c
    brand = fields.Char(string='Brand', size=255)  # SF: Brand__c
    file_name = fields.Char(string='File Name', size=255)  # SF: File_Name__c
    file_path = fields.Char(string='File Path', size=255)  # SF: File_Path__c
    isci_no_char = fields.Char(string='ISCI No Char', compute='_compute_isci_no_char', store=True)  # SF: ISCI_No_Char__c
    isci = fields.Char(string='ISCI', size=120, required=True)  # SF: ISCI__c
    length = fields.Selection(string='Length', selection=[('v_15', '15'), ('v_20', '20'), ('v_30', '30'), ('v_45', '45'), ('v_60', '60'), ('v_90', '90'), ('v_120', '120'), ('v_150', '150'), ('v_180', '180'), ('v_240', '240'), ('v_300', '300'), ('v_1710', '1710')])  # SF: Length__c
    unique_ref_id = fields.Char(string='Unique Ref-ID', size=120)  # SF: Unique_Ref_ID__c | SF unique → consider _sql_constraints
    x800_number = fields.Char(string='800 Number', size=120)  # SF: X800_Number__c

    # === Computed / Roll-Up ===

    @api.depends('isci')
    def _compute_isci_no_char(self):
        # SF formula (verbatim, may need translation):
        #   SUBSTITUTE(ISCI__c, "-", "")
        for rec in self:
            # TODO: translate SF formula to Python
            rec.isci_no_char = (rec.isci or "").replace("-", "")
