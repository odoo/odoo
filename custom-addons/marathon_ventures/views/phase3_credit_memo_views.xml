<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Phase 3 — SF Credit Memo approval state machine + amount + approver hint -->
  <record id="view_mv_sf_credit_memo_form_phase3" model="ir.ui.view">
    <field name="name">mv.sf_credit_memo.form.phase3</field>
    <field name="model">mv.sf_credit_memo</field>
    <field name="inherit_id" ref="marathon_ventures.view_mv_sf_credit_memo_form"/>
    <field name="arch" type="xml">
      <xpath expr="//sheet" position="before">
        <header>
          <button name="action_submit_for_approval" type="object" string="Submit for Approval"
                  class="btn-primary"
                  invisible="approval_state not in ('draft','rejected')"/>
          <button name="action_approve" type="object" string="Approve"
                  class="btn-primary"
                  invisible="approval_state != 'submitted'"/>
          <button name="action_reject" type="object" string="Reject"
                  class="btn-secondary"
                  invisible="approval_state != 'submitted'"/>
          <field name="approval_state" widget="statusbar" statusbar_visible="draft,submitted,approved"/>
        </header>
      </xpath>
      <xpath expr="//sheet" position="inside">
        <group string="Approval">
          <field name="sfcm_amount" widget="monetary"/>
          <field name="currency_id" invisible="1"/>
          <field name="submitted_by_id" readonly="1"/>
          <field name="submitted_date" readonly="1"/>
          <field name="approved_by_id" readonly="1"/>
          <field name="approved_date" readonly="1"/>
          <field name="rejection_reason" invisible="approval_state != 'rejected'"/>
        </group>
      </xpath>
    </field>
  </record>
</odoo>
