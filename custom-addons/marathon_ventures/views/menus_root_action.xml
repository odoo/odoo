<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <!-- Intentionally empty.  We do NOT bind an action to the root Marathon
       Ventures menu so the top-of-screen breadcrumb keeps showing
       "Marathon Ventures" instead of the action's name (e.g. "Deal").
       Users navigate via the section sub-menus (Master Data / Sales Operations
       / Operations / Finance). -->
</odoo>
