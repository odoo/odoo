<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="sign_request_send_wizard_view_form" model="ir.ui.view">
        <field name="name">sign.request.send.wizard.form</field>
        <field name="model">sign.request.send.wizard</field>
        <field name="arch" type="xml">
            <form string="Send Signature Request">
                <group>
                    <field name="request_id" readonly="1"/>
                    <field name="signer_ids" widget="many2many_tags"/>
                    <field name="subject"/>
                    <field name="message"/>
                </group>
                <footer>
                    <button name="action_send" string="Send" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <record id="sign_request_send_wizard_action" model="ir.actions.act_window">
        <field name="name">Send Signature Request</field>
        <field name="res_model">sign.request.send.wizard</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="sign_request_send_wizard_view_form"/>
        <field name="target">new</field>
    </record>
</odoo>

