# GAPS

No contract items are currently marked as `[STUB]`.

All required non-stubbable items are implemented:

- `_check_user_can_write()` raises `AccessError`
- `action_toggle_favorite()`
- `_get_first_accessible_article()`
- private/workspace/shared record rules
