# Kodoo Forge Codegen

Filesystem export layer for Forge blueprints.

Initial responsibility:
- render installable addon trees from snapshots
- generate manifests, XML, CSV, and Python scaffolding
- package artifacts for download or repository handoff

This module should consume the same snapshot graph used by runtime publication.
