# Kodoo Forge Runtime

Runtime materialization layer for Forge blueprints.

Initial responsibility:
- project snapshots into managed Odoo runtime objects
- support preview and publish flows
- keep runtime bindings and ownership markers

This module is an output target for `kodoo_forge`, not the schema owner.
