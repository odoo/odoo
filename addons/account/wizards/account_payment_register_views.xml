<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="arch" type="xml">
            <form string="Pay">
                <field name="line_ids" invisible="1" />
                <field name="can_edit_wizard" force_save="1" invisible="1" />
                <field name="can_group_payments" force_save="1" invisible="1" />
                <field
                    name="early_payment_discount_mode"
                    force_save="1"
                    invisible="1" />
                <field name="installments_mode" invisible="1" />
                <field name="installments_switch_amount" invisible="1" />
                <field name="installments_switch_html" invisible="1" />
                <field name="payment_type" force_save="1" invisible="1" />
                <field name="partner_type" force_save="1" invisible="1" />
                <field name="source_amount" force_save="1" invisible="1" />
                <field name="source_amount_currency" force_save="1" invisible="1" />
                <field name="source_currency_id" force_save="1" invisible="1" />
                <field name="company_id" force_save="1" invisible="1" />
                <field name="partner_id" force_save="1" invisible="1" />
                <field name="country_code" force_save="1" invisible="1" />
                <field name="currency_id" invisible="1" />
                <field name="custom_user_amount" invisible="1" />
                <field name="custom_user_currency_id" invisible="1" />

                <field name="show_partner_bank_account" invisible="1" />
                <field name="require_partner_bank_account" invisible="1" />
                <field name="available_journal_ids" invisible="1" />
                <field name="available_payment_channel_ids" invisible="1" />
                <field name="available_bank_account_ids" invisible="1" />
                <field name="company_currency_id" invisible="1" />
                <field name="hide_writeoff_section" invisible="1" />
                <field name="writeoff_is_exchange_account" invisible="1" />
                <field name="untrusted_bank_account_ids" invisible="1" />
                <field name="missing_account_partners" invisible="1" />

                <div
                    role="alert"
                    class="alert alert-info"
                    invisible="not hide_writeoff_section">
                    <p class="m-0">
                        <b>Early Payment Discount of <field name="payment_difference" /> has been
                            applied.</b>
                    </p>
                </div>
                <div
                    role="alert"
                    class="alert alert-warning"
                    invisible="untrusted_payments_count == 0">
                    <span class="fw-bold"><field name="untrusted_payments_count" class="oe_inline" />
                        out of <field name="total_payments_amount" class="oe_inline" /> payments
                        will be skipped due to <button name="action_view_untrusted_bank_accounts" type="object" class="oe_link p-0 align-baseline">untrusted bank
                        accounts</button>.</span>
                </div>
                <div
                    role="alert"
                    class="alert alert-warning"
                    invisible="not missing_account_partners">
                    <span class="fw-bold">Payments related to partners with no bank account
                        specified will be skipped. <button name="action_view_missing_account_partners" type="object" class="oe_link p-0 align-baseline">View
                        Partner(s)</button></span>
                </div>
                <div
                    role="alert"
                    class="alert alert-warning"
                    invisible="not duplicate_payment_ids">
                    <span>This payment has the same partner, amount and date as </span>
                    <field
                        name="duplicate_payment_ids"
                        string="Duplicated Payments"
                        widget="x2many_buttons" />
                </div>
                <div role="alert" invisible="not actionable_errors">
                    <field
                        name="actionable_errors"
                        widget="actionable_errors"
                        class="w-100" />
                </div>
                <group>
                    <group name="group1">
                        <field
                            name="journal_id"
                            options="{'no_open': True, 'no_create': True}"
                            required="1" />
                        <field
                            name="payment_channel_id"
                            context="{'hide_payment_journal_id': 1}"
                            options="{'no_create': True, 'no_open': True}"
                            required="1" />
                        <field
                            name="bank_account_id"
                            placeholder="Account Number"
                            context="{'display_account_trust': True, 'default_partner_ids': [partner_id]}"
                            invisible="not show_partner_bank_account or not can_edit_wizard or (can_group_payments and not group_payment)"
                            readonly="payment_type == 'inbound'"
                            required="require_partner_bank_account and can_edit_wizard and (not can_group_payments or not group_payment)" />
                        <field
                            name="group_payment"
                            invisible="not can_group_payments" />
                        <label
                            for="payment_difference"
                            invisible="not show_payment_difference" />
                        <div invisible="not show_payment_difference">
                            <field name="payment_difference" />
                            <field
                                name="payment_difference_handling"
                                widget="radio"
                                nolabel="1"
                                invisible="is_register_payment_on_draft" />
                            <div
                                class="o_inner_group grid col-lg-12 p-0"
                                invisible="hide_writeoff_section or payment_difference_handling == 'open'">
                                <div
                                    class="o_wrap_field d-flex d-sm-contents flex-column mb-3 mb-sm-0">
                                    <div
                                        class="o_cell o_wrap_label text-break text-900">
                                        <label
                                            for="writeoff_account_id"
                                            string="Post Difference In"
                                            class="oe_edit_only" />
                                    </div>
                                    <div class="o_cell o_wrap_input text-break">
                                        <field
                                            name="writeoff_account_id"
                                            string="Post Difference In"
                                            options="{'no_create': True}"
                                            required="payment_difference_handling == 'reconcile' and not early_payment_discount_mode" />
                                    </div>
                                </div>
                                <div
                                    class="o_wrap_field d-flex d-sm-contents flex-column mb-3 mb-sm-0">
                                    <div
                                        class="o_cell o_wrap_label text-break text-900">
                                        <label
                                            for="writeoff_label"
                                            string="Label"
                                            invisible="writeoff_is_exchange_account"
                                            class="oe_edit_only" />
                                    </div>
                                    <div class="o_cell o_wrap_input text-break">
                                        <field
                                            name="writeoff_label"
                                            invisible="writeoff_is_exchange_account"
                                            required="payment_difference_handling == 'reconcile'" />
                                    </div>
                                </div>
                            </div>
                        </div>
                    </group>
                    <group name="group2">
                        <label for="amount" />
                        <div name="amount_div" class="o_row">
                            <field
                                name="amount"
                                force_save="1"
                                readonly="not can_edit_wizard or can_group_payments and not group_payment" />
                            <field
                                name="currency_id"
                                options="{'no_create': True, 'no_open': True}"
                                groups="base.group_multi_currency"
                                invisible="not can_edit_wizard or can_group_payments and not group_payment"
                                required="1" />
                        </div>
                        <field
                            name="installments_switch_html"
                            string=""
                            widget="account_payment_register_html"
                            invisible="not installments_switch_html" />
                        <field name="payment_date" />
                        <field
                            name="communication"
                            invisible="not can_edit_wizard or (can_group_payments and not group_payment)" />
                    </group>
                    <field name="qr_code" invisible="1" />
                    <div invisible="not qr_code" colspan="2" class="text-center">
                        <field name="qr_code" />
                    </div>
                </group>
                <footer>
                    <button
                        name="action_create_payments"
                        type="object"
                        string="Create Payments"
                        invisible="total_payments_amount == 1"
                        class="oe_highlight"
                        data-hotkey="q" />
                    <button
                        name="action_create_payments"
                        type="object"
                        string="Create Payment"
                        invisible="total_payments_amount != 1"
                        class="oe_highlight"
                        data-hotkey="q" />
                    <button
                        special="cancel"
                        string="Discard"
                        class="btn btn-secondary"
                        data-hotkey="x" />
                </footer>
            </form>
        </field>
    </record>

</odoo>
