<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_users_form_simple_modif_inherit" model="ir.ui.view">
        <field name="name">res.users.preferences.view.form.inherit.account</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form_simple_modif" />
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="unit_id" domain="[('company_unit_id', '=', company_id)]" options="{'no_create': True}" groups="account.group_multi_operating_unit"/>
            </field>
        </field>
    </record>

    <record id="view_users_form_inherit" model="ir.ui.view">
        <field name="name">res.users.view.form.inherit.account</field>
        <field name="model">res.users</field>
        <field name="inherit_id" ref="base.view_users_form" />
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="unit_id" domain="[('company_unit_id', '=', company_id)]" options="{'no_create': True}" groups="account.group_multi_operating_unit"/>
            </field>
        </field>
    </record>
</odoo>
