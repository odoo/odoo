<?xml version="1.0" ?>
<odoo>

    <!-- Mail template are declared in a NOUPDATE block
         so users can freely customize/delete them -->
    <data noupdate="1">
        <!--Email template -->
        <template id="email_template_edi_invoice_view">
            <div>
                <p>Dear <span t-field="object.partner_id.name"/>
                    <t t-set="access_action" t-value="object.with_context(force_website=True).get_access_action()"/>
                    <t t-set="is_online" t-value="access_action and access_action['type'] == 'ir.actions.act_url'"/>
                    <t t-set="access_url" t-value="object.get_mail_url()"/>

                    <t t-if="object.partner_id.parent_id">
                        (<i t-field="object.partner_id.parent_id.name"/>)
                    </t>,
                </p>
                <p>Here is, in attachment, your invoice <strong t-if="object.number" t-field="object.number"/>
                    <t t-if="object.origin">
                    (with reference: <span t-field="object.origin"/>)
                    </t>
                    amounting in <strong t-field="object.amount_total" t-options="{'widget': 'monetary', 'display_currency': object.currency_id}"/>
                    from <span t-field="object.company_id.name"/>.
                </p>
                <t t-if="is_online">
                    <br/><br/>
                    <center>
                        <a t-att-href="access_url" style="background-color: #1abc9c; padding: 20px; text-decoration: none; color: #fff; border-radius: 5px; font-size: 16px;" class="o_default_snippet_text">View Invoice</a>
                    </center>
                </t>
                <br/><br/>
                <p t-if="object.state=='paid'">This invoice is already paid.</p>
                <p t-else="">Please remit payment at your earliest convenience.</p>
                <p>Thank you,</p>
                <p style="color:#888888">
                    <t t-if="object.user_id and object.user_id.signature" t-raw="object.user_id.signature"/>
                </p>
            </div>
        </template>

        <record id="email_template_edi_invoice" model="mail.template">
            <field name="name">Invoicing: Invoice email</field>
            <field name="email_from">${(object.user_id.email and '%s &lt;%s&gt;' % (object.user_id.name, object.user_id.email) or '')|safe}</field>
            <field name="subject">${object.company_id.name} Invoice (Ref ${object.number or 'n/a'})</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="model_id" ref="account.model_account_invoice"/>
            <field name="auto_delete" eval="True"/>
            <field name="report_template" ref="account_invoices"/>
            <field name="report_name">Invoice_${(object.number or '').replace('/','_')}_${object.state == 'draft' and 'draft' or ''}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="template_view" ref="email_template_edi_invoice_view"/>
        </record>
    </data>
</odoo>
