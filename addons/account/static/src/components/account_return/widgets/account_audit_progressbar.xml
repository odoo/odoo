<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="account.AccountAuditProgressBar" t-inherit="web.ProgressBarField" t-inherit-mode="primary">
        <t t-else="" position="replace">
            <t t-else="">
                <t t-if="!this.hasMaxValue">
                  <span class="mx-1">None</span>
                </t>
                <t t-else="">
                    <input
                        t-if="this.isEditable and this.props.isCurrentValueEditable"
                        t-ref="currentValue"
                        class="o_input h-100 text-center"
                        type="text"
                        inputmode="decimal"
                        autocomplete="off"
                        t-on-focus="this.onInputFocus"
                        t-on-blur="this.onInputBlur"
                    />
                    <span t-else="" class="mx-1" t-out="this.formatCurrentValue(true)"/>
                    /
                    <input
                        t-if="this.isEditable and this.props.isMaxValueEditable"
                        t-ref="maxValue"
                        class="o_input h-100 text-center"
                        type="text"
                        inputmode="decimal"
                        autocomplete="off"
                        t-on-focus="this.onInputFocus"
                        t-on-blur="this.onInputBlur"
                    />
                    <span t-else="" class="mx-1" t-out="this.formatMaxValue(true)"/>
                </t>
            </t>
        </t>
    </t>
</templates>
