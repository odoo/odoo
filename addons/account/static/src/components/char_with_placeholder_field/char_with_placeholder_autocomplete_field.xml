<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="account.CharWithPlaceholderAutoCompleteField" t-inherit="web.CharField">
        <xpath expr="//span" position="attributes">
            <attribute name="t-att-class">{'text-muted': !this.props.record.data[props.name]}</attribute>
        </xpath>
        <xpath expr="//input" position="attributes">
            <attribute name="t-att-placeholder">placeholder</attribute>
        </xpath>

        <xpath expr="//t[@t-else='']" position="before">
            <t t-elif="props.record.resModel !== 'res.partner' || props.record.data.company_type === 'company'">
                <AutoComplete
                    value="props.record.data[props.name] || ''"
                    sources="sources"
                    onSelect.bind="onSelect"
                    input="inputRef"
                    placeholder="placeholder || ''"
                />
            </t>
        </xpath>
    </t>

</templates>
