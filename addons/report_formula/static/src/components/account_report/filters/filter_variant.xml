<?xml version="1.0" encoding="UTF-8" ?>
<templates>
    <t t-name="report_formula.AccountReportFilterVariant">
        <Dropdown>
            <button class="btn btn-secondary">
                <i class="fa-solid fa-book me-1"/>Report: <t t-out="this.selectedVariantName"/>
            </button>

            <t t-set-slot="content">
                <t t-foreach="this.controller.cachedFilterOptions.available_variants" t-as="variant" t-key="variant_index">
                    <DropdownItem
                        class="{ 'selected': this.controller.cachedFilterOptions.selected_variant_id === variant.id }"
                        onSelected="() => this.filterVariant(variant.id)"
                        t-out="variant.name"
                    />
                </t>
                <t t-if="this.controller.cachedFilterOptions.has_inactive_variants">
                    <div role="separator" class="dropdown-divider"/>
                    <DropdownItem onSelected="(ev) => this.controller.reportAction(ev, 'action_view_all_variants', {})">
                        Enable more ...
                    </DropdownItem>
                </t>
            </t>
        </Dropdown>
    </t>
</templates>
