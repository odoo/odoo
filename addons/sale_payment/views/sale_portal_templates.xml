<odoo>
    <template id="portal_my_orders_payment" name="Payment on my orders" inherit_id="sale.portal_order_page">
        <xpath expr="//t[@name='portal_confirmation_sign']" position="replace">
            <t t-set="payment_tx_id" t-value="order.get_portal_last_transaction()"/>
            <t t-if="order_valid and not order.signature and order.has_to_be_signed()" name="portal_confirmation_sign">
                <a class="btn btn-primary btn-block" data-toggle="modal" data-target="#modalaccept">
                    <i class="fa fa-arrow-circle-right"/> Accept &amp; Sign
                </a>
            </t>
            <t t-elif="order.amount_total and not invoices and order.state in ('sent', 'sale') and order_valid and order.has_to_be_paid() and payment_tx_id.state != 'done'" name="portal_confirmation_pay">
                <a class="btn btn-primary btn-block" data-toggle="modal" href="#portal_pay" role="button">
                    <i class="fa fa-arrow-circle-right"/> Accept &amp; Pay
                </a>
            </t>
        </xpath>
        <xpath expr="//div[@id='confirm_status']" position="inside">
            <t t-if="invoices and order.state in ('sale', 'done') and portal_confirmation == 'pay'">
                <a role="button" class="btn btn-primary btn-block" disabled="disabled">
                    <span class="fa fa-check-circle"> Done</span>
                </a>
            </t>
        </xpath>

        <xpath expr="//div[@id='quotation_toolbar']/a" position="replace">
            <t t-set="payment_tx_id" t-value="order.get_portal_last_transaction()"/>
            <t t-if="order_valid and not order.signature and order.has_to_be_signed()">
                <a class="btn btn-success" data-toggle="modal" data-target="#modalaccept">
                    <i class="fa fa-arrow-circle-right"/> Accept &amp; Sign
                </a>
            </t>
            <t t-elif="not invoices and order.state in ('sent', 'sale') and order_valid and order.has_to_be_paid() and payment_tx_id.state != 'done'" name="portal_confirmation_pay">
                <a class="btn btn-success" data-toggle="modal" data-target="#portal_pay">
                    <i class="fa fa-arrow-circle-right"/> Accept &amp; Pay
                </a>
            </t>
        </xpath>
    </template>

    <template id="portal_order_payment_confirmation_status" name="Payment on my orders" inherit_id="sale.portal_order_confirmation_status">
        <xpath expr="//t[@t-call='sale.portal_order_success']" position="after">
            <div t-if="order.transaction_ids and not invoices and order.state in ('sent', 'sale') and order.has_to_be_paid() and not success and not error" class="o_sale_payment_tx_status" t-att-data-order-id="order.id">
                <t t-set="payment_tx_id" t-value="order.get_portal_last_transaction()"/>
                <t t-call="payment.payment_confirmation_status"/>
            </div>
        </xpath>
    </template>

    <template id="payment_dialog" name="Payment Modal" inherit_id="sale.portal_order_signature_dialog">
        <xpath expr="//div[@id='modalaccept']" position="after">
            <t t-set="payment_tx_id" t-value="order.get_portal_last_transaction()"/>
            <div class="row" t-if="order.amount_total and not invoices and order.state in ('sent', 'sale') and portal_confirmation == 'pay' and payment_tx_id.state != 'done' and portal_layout" id="pay_with">
                <div class="modal fade" id="portal_pay" tabindex="-1" role="dialog">
                    <div class="modal-dialog">
                        <div class="modal-content">
                            <div class="modal-header">
                                <h3 class="modal-title">Payment Methods</h3>
                                <button type="button" class="close" data-dismiss="modal">&amp;times;</button>
                            </div>
                            <div class="modal-body">
                                <p>
                                    <span>I agree that by signing this proposal, I accept it on the behalf of </span>
                                    <b t-field="order.partner_id.commercial_partner_id"/>
                                    <span>, for an amount of </span>
                                    <b data-id="total_amount" t-field="order.amount_total"
                                        t-options='{"widget": "monetary", "display_currency": order.pricelist_id.currency_id}'/>
                                    <t t-if="order.payment_term_id"><span>with payment terms: </span><b t-field="order.payment_term_id"/>.</t>
                                </p>
                            </div>
                            <div class="modal-footer">
                                <div t-if="pms or s2s_acquirers or form_acquirers" id="payment_method" class="text-left col-lg-12">
                                    <t t-call="payment.payment_tokens_list">
                                        <t t-set="mode" t-value="'payment'"/>
                                        <t t-set="partner_id" t-value="order.partner_id.id"/>
                                        <t t-set="access_token" t-value="access_token"/>
                                        <t t-set="success_url" t-value="'/my/orders/%s?%s' % (order.id, keep_query())"/>
                                        <t t-set="error_url" t-value="'/my/orders/%s?%s' % (order.id, keep_query())"/>
                                        <t t-set="callback_method" t-value="''"/>
                                        <t t-set="submit_txt" t-value="'Pay Now'"/>
                                        <t t-set="icon_class" t-value="'fa-lock'"/>
                                        <t t-set="form_action" t-value="'/pay/sale/' + str(order.id) + '/s2s_token_tx/'"/>
                                        <t t-set="prepare_tx_url" t-value="'/pay/sale/' + str(order.id) + '/form_tx/'"/>
                                    </t>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </xpath>
    </template>

    <template id="portal_my_orders_payment_tx" name="Payment on my orders" inherit_id="sale.portal_order_content">
       <xpath expr="//div[@id='invoices']" position="after">
           <t t-set="tx_ids" t-value="order.transaction_ids.filtered(lambda tx: tx.state in ('pending', 'authorized', 'done'))"/>
           <div t-if="tx_ids">
               <label class="col-md-4 col-4 text-right">Transactions:</label>
               <div class="col-md-8 col-8">
                   <t t-foreach="tx_ids" t-as="tx">
                       <div>
                           <span t-esc="tx.reference"/>
                           <span class="text-muted" t-field="tx.create_date"/>
                           <t t-if="tx.state == 'done'">
                               <span class="label label-success orders_label_text_align"><i class="fa fa-fw fa-check"/> Done</span>
                           </t>
                           <t t-if="tx.state != 'done'">
                               <span class="label label-info orders_label_text_align"><i class="fa fa-fw fa-clock-o"/> Waiting</span>
                               <t t-if="tx.state_message"><br /><span t-esc="tx.state_message"/></t>
                           </t>
                       </div>
                   </t>
               </div>
           </div>
       </xpath>
    </template>

    <template id="portal_order_error" name="Order error display: payment errors"
            inherit_id="sale.portal_order_error">
        <xpath expr="//t[@name='generic']" position="after">
            <t t-set="tx_id" t-value="order.get_portal_last_transaction()"/>
            <t t-if="error == 'pay_sale_invalid_doc'">
                There was an error processing your payment: invalid order.
            </t>
            <t t-if="error == 'pay_sale_invalid_token'">
                There was en error processing your payment: invalid credit card ID.
            </t>
            <t t-if="error == 'pay_sale_tx_fail'">
                There was an error processing your payment: transaction failed.<br />
                <t t-if="tx_id and tx_id.state_message">
                    <t t-esc="tx_id.state_message"/>
                </t>
            </t>
            <t t-if="error == 'pay_sale_tx_state'">
                There was an error processing your payment: transaction issue.<br />
                <t t-if="tx_id and tx_id.state_message">
                    <t t-esc="tx_id.state_message"/>
                </t>
            </t>
            <t t-if="error == 'pay_sale_tx_token'">
                There was an error processing your payment: issue with credit card ID validation.
            </t>
        </xpath>
    </template>

    <template id="portal_order_success" name="Order success display: payment success"
            inherit_id="sale.portal_order_success">
        <xpath expr="//a[hasclass('close')]" position="after">
            <t t-if="success == 'pay_sale'">
                <t t-set="payment_tx_id" t-value="order.get_portal_last_transaction()"/>
                <span t-if='payment_tx_id and payment_tx_id.acquirer_id.done_msg' t-raw="payment_tx_id.acquirer_id.done_msg"/>
                <span t-if='payment_tx_id.acquirer_id.post_msg' t-raw="payment_tx_id.acquirer_id.post_msg"/>
            </t>
        </xpath>
    </template>
</odoo>
