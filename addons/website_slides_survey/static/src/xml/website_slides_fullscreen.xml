<?xml version="1.0" encoding="UTF-8"?>
<templates>

    <t t-name="website.slides.fullscreen.certification">
        <div class="justify-content-center align-self-center text-center">
            <div t-if="this.slide.category === 'certification' &amp;&amp; !this.slide.completed" class="">
                <a class="btn btn-primary" t-att-href="'/slides_survey/slide/get_certification_url?slide_id=' + this.slide.id" target="_blank">
                    <i class="fa fa-graduation-cap"/>
                    <span t-if="this.channel.isMember"> Begin Certification</span>
                    <span t-else="">Test Certification</span>
                </a>
            </div>
            <div t-if="this.slide.category === 'certification' &amp;&amp; this.slide.completed">
                <h4 class="mb-3 text-white">Congratulations, you passed the Certification!</h4>
                <a role="button" class="btn btn-primary" t-att-href="'/survey/' + this.slide.certificationId + '/get_certification'">
                    <i class="fa fa-fw fa-trophy" role="img" aria-label="Download certification" title="Download certification"/> Download certification
                </a>
            </div>
            <div t-if="this.slide.category === 'certification' &amp;&amp; this.channel.canUpload" class="mt-3 h6">
                <a t-att-href="'/odoo/survey.survey/' + this.slide.certificationId">
                    <i class="oi oi-arrow-right me-1"/>Add Questions to this Survey
                </a>
            </div>
        </div>
    </t>
</templates>
