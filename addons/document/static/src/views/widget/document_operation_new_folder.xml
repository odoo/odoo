<?xml version="1.0" encoding="UTF-8"?>
<templates>

    <t t-name="document.DocumentsOperationNewFolder" >
        <button
            t-if="this.documentService.userIsInternal &amp;&amp; this.props.record.data.user_permission === 'edit'"
            t-on-click="() => this.onClick()"
            t-out="this.buttonLabel"
            t-att-disabled="!this.props.record.data.display_name"
            class="btn btn-secondary w-100"
        />
    </t>

</templates>
