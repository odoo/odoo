<odoo>
    <record id="view_partner_property_form" model="ir.ui.view">
        <field name="name">res.partner.property.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@id='invoice_send_settings']" position="inside">
                <field name="routing_scheme" invisible="1"/> <!-- used in computes -->
                <field name="routing_endpoint" invisible="1"/> <!-- used in computes -->
                <field name="routing_identifier_override"
                       placeholder="0088:123456789..."
                       groups="base.group_no_one"/>
            </xpath>
        </field>
    </record>
</odoo>
