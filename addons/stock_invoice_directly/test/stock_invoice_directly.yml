-
  In order to test the stock_invoice_directly module. I will create an outgoing
  picking order which creates an invoice from the picking order itself.
-
  I create an Outgoing Picking order.
-
  !record {model: stock.picking, id: stock_picking_out0}:
    partner_id: base.res_partner_address_22
    invoice_state: 2binvoiced
    move_lines:
      - company_id: base.main_company
        location_id: stock.stock_location_stock
        product_id: product.product_product_3
        product_qty: 3.0
        product_uom: product.product_uom_unit
        location_dest_id: stock.stock_location_customers
    move_type: direct
    type: out
-
  I need to check the availability of the product so I make my picking order for processing later.
-
  !python {model: stock.picking}: |
    record_id = ref('stock.menu_action_picking_tree')

    copy_context = dict(context or {})
    copy_context.update(active_ids=[record_id], active_id=record_id)

    self.draft_force_assign(cr, uid, [ref("stock_picking_out0")], context=copy_context)
-
  I check the product availability. Product is available in the stock and ready to be sent.
-
  !python {model: stock.picking}: |
    record_id = ref('stock.menu_action_picking_tree')
    copy_context = dict(context or {})
    copy_context.update(active_ids=[record_id], active_id=record_id)

    self.action_assign(cr, uid, [ref("stock_picking_out0")], context=copy_context)
-
  I process the delivery
-
  !python {model: stock.partial.picking}: |
    copy_context = dict(context or {})
    copy_context.update(active_model='stock.picking', active_ids=[ref('stock_picking_out0')])
    partial_id = self.create(cr, uid, {}, context=copy_context)
    self.do_partial(cr, uid, [partial_id], context=copy_context)

-
  As the Invoice state of the picking order is To be invoiced. I create invoice for my outgoing picking order.
-
  !python {model: stock.invoice.onshipping}: |
    record_id = ref('stock_picking_out0')

    copy_context = dict(context or {})
    copy_context.update(active_ids=[record_id], active_id=record_id, active_model='stock.picking')
    values = {
      'invoice_date': '2010-08-04',
      'journal_id': str(ref('account.sales_journal')),
    }
    wiz_id = self.create(cr, uid, values, context=copy_context)
    self.create_invoice(cr, uid, [wiz_id], context=copy_context)
-
  I check that the customer invoice is created successfully.
-
  !python {model: account.invoice}: |
    picking_obj = self.pool.get('stock.picking')
    picking = picking_obj.browse(cr, uid, [ref('stock_picking_out0')], context=context)
    partner = picking[0].partner_id.id
    inv_ids = self.search(cr, uid, [('type','=','out_invoice'),('partner_id','=',partner)], context=context)
    assert inv_ids, 'No Invoice is generated!'

