<?xml version="1.0"?>
<odoo>

        <menuitem id="menu_config_claim" name="Claim"
            groups="base.group_no_one"
            parent="base.menu_base_config" sequence="55"/>

        <!-- Claims categories  -->

        <record id="crm_claim_category_form" model="ir.ui.view">
            <field name="name">crm.claim.category.form</field>
            <field name="model">crm.claim.category</field>
            <field name="arch" type="xml">
                <form string="Claim Categories">
                    <group>
                        <field name="name"/>
                        <field name="team_id" groups="base.group_multi_salesteams"/>
                    </group>
                </form>
            </field>
        </record>

        <record id="crm_claim_category_tree" model="ir.ui.view">
            <field name="name">crm.claim.category.tree</field>
            <field name="model">crm.claim.category</field>
            <field name="arch" type="xml">
                <tree string="Claim Categories">
                    <field name="name"/>
                    <field name="team_id" groups="base.group_multi_salesteams"/>
                </tree>
            </field>
        </record>

        <record id="crm_claim_categ_action" model="ir.actions.act_window">
            <field name="name">Claim Categories</field>
            <field name="res_model">crm.claim.category</field>
            <field name="view_type">form</field>
            <field name="view_id" ref="crm_claim_category_tree"/>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Click to create a claim category.
              </p><p>
                Create claim categories to better manage and classify your
                claims. Some example of claims can be: preventive action,
                corrective action.
              </p>
            </field>
        </record>

        <menuitem action="crm_claim_categ_action" name="Categories"
            id="menu_crm_case_claim-act" parent="menu_config_claim" groups="base.group_no_one"/>

        <!-- Claim Stages -->

        <record id="crm_claim_stage_tree" model="ir.ui.view">
            <field name="name">crm.claim.stage.tree</field>
            <field name="model">crm.claim.stage</field>
            <field name="arch" type="xml">
                <tree string="Claim Stages">
                    <field name="sequence"/>
                    <field name="name"/>
                    <field name="on_followup"/>
                </tree>
            </field>
        </record>

        <record id="crm_claim_stage_form" model="ir.ui.view">
            <field name="name">crm.claim.stage.form</field>
            <field name="model">crm.claim.stage</field>
            <field name="arch" type="xml">
                <form string="Claim Stage">
                    <group col="4">
                        <field name="name"/>
                        <field name="case_default"/>
                        <field name="sequence"/>
                        <field name="on_followup"/>
                    </group>
                </form>
            </field>
        </record>

        <record id="crm_claim_stage_act" model="ir.actions.act_window">
            <field name="name">Claim Stages</field>
            <field name="res_model">crm.claim.stage</field>
            <field name="view_type">form</field>
            <field name="view_id" ref="crm_claim_stage_tree"/>
            <field name="help" type="html">
              <p class="oe_view_nocontent_create">
                Click to setup a new stage in the processing of the claims. 
              </p><p>
                You can create claim stages to categorize the status of every
                claim entered in the system. The stages define all the steps
                required for the resolution of a claim.
              </p>
            </field>
        </record>

        <!-- Claims    -->

        <record model="ir.ui.view" id="crm_case_claims_tree_view">
            <field name="name">CRM - Claims Tree</field>
            <field name="model">crm.claim</field>
            <field name="arch" type="xml">
                <tree string="Claims">
                    <field name="name"/>
                    <field name="partner_id"/>
                    <field name="user_id"/>
                    <field name="date"/>
                    <field name="stage_id"/>
                    <field name="date_action_next"/>
                    <field name="action_next"/>
                    <field name="categ_id" string="Type"/>
                    <field name="date_deadline" invisible="1"/>
                    <field name="date_closed" invisible="1"/>
                </tree>
            </field>
        </record>

        <record model="ir.ui.view" id="crm_case_claims_form_view">
            <field name="name">CRM - Claims Form</field>
            <field name="model">crm.claim</field>
            <field name="arch" type="xml">
                <form string="Claim">
                <header>
                    <field name="stage_id" widget="statusbar" clickable="True" options="{'fold_field': 'fold'}"/>
                </header>
                <sheet string="Claims">
                    <div class="oe_right oe_button_box">
                        <button name="action_get_attachment_view"
                            class="oe_stat_button"
                            icon="fa-book"
                            type="object">
                            <field name="attachment_ids" widget="x2many_counter"/>
                        </button>
                        <button name="action_calendar_followup" class="oe_stat_button" icon="fa-calendar" type="object"
                             help="Shows all the follow-up User" context="{'partner_id': active_id}">
                             <field name="meeting_ids" string="Calendar" widget="x2many_counter"/>
                        </button>
                        <button class="oe_inline oe_stat_button" type="object"
                            name="action_claim_phonecall" icon="fa-phone">
                            <field string="Calls" name="phonecall_ids" widget="x2many_counter"/>
                        </button>
                    </div>
                    <div class="oe_title">
                        <h1>
                            <field class="oe_inline oe_left" name="priority" widget="priority"/>
                            <field name="name" placeholder="Claim summary..." class="oe_inline"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="categ_id"/>
                            <field name="user_id" context="{'default_groups_ref': ['base.group_user', 'base.group_partner_manager', 'base.group_sale_salesman_all_leads']}"/>
                            <field name="team_id" groups="base.group_multi_salesteams"/>
                        </group>
                        <group>
                            <field name="date"/>
                            <field name="date_deadline"/>
                            <field name="id" string="Reference"/>
                        </group>
                    </group>
                        <notebook>
                            <page string="Claim">
                                <group col="4" groups="base.group_user" string="Claim Reporter">
                                    <field name="partner_id" string="Name"
                                        on_change="onchange_partner_id(partner_id)"/>
                                    <field name="partner_phone"/>
                                    <field name="email_from" widget="email"/>
                                    <field name="mobile"/>
                                </group>
                                <group string="Claim Subject" groups="base.group_user">
                                    <field name="ref"/>
                                </group>
                                <field name="description" colspan="4" nolabel="1" groups="base.group_user" placeholder="Description..."/>
                            </page>
                            <page string="Action" groups="base.group_user">
                                <separator string="Follow-up"/>
                                    <field name="followup_ids">
                                        <tree string="Follow-up" editable="bottom">
                                            <field name="state" invisible="1"/>
                                            <field name="start_datetime" string="Date" required="1"/>
                                            <field name="stop_datetime" invisible="1"/>
                                            <field name="name" string="Action"/>
                                            <field name="user_id" string="Responsible"/>
                                        </tree>
                                    </field>
                                <group colspan="2" col="2">
                                    <separator colspan="2" string="Resolution Actions"/>
                                    <field name="type_action"/>
                                    <field name="resolution" colspan="2" nolabel="1" placeholder="Action Description, Root Causes..."/>
                                </group>
                            </page>
                        </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" groups="base.group_user"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
                </form>
            </field>
        </record>

        <!-- Claim Calendar view -->

        <record model="ir.ui.view" id="crm_case_claims_calendar_view">
            <field name="name">Claims Actions</field>
            <field name="model">crm.claim</field>
            <field name="priority" eval="2"/>
            <field name="arch" type="xml">
                <calendar string="Claims" date_start="date_action_next" color="user_id">
                    <field name="name"/>
                </calendar>
            </field>
        </record>

        <!-- Claim Kanban view -->

        <record model="ir.ui.view" id="crm_case_claims_kanban_view">
            <field name="name">crm.claim.kanban</field>
            <field name="model">crm.claim</field>
            <field name="arch" type="xml">
                <kanban default_group_by="stage_id">
                    <field name="stage_id"/>
                    <field name="color"/>
                    <field name="user_id"/>
                    <field name="message_unread"/>
                    <field name="message_summary"/>
                    <templates>
                        <t t-name="kanban-box">
                           <div t-attf-class="oe_kanban_color_#{kanban_getcolor(record.color.raw_value)} oe_kanban_card oe_kanban_global_click oe_semantic_html_override">
                                <div class="oe_dropdown_toggle oe_dropdown_kanban" groups="base.group_user">
                                    <i class="fa fa-bars fa-lg"/>
                                    <ul class="oe_dropdown_menu">
                                        <t t-if="widget.view.is_action_enabled('edit')"><li><a type="edit">Edit Claim</a></li></t>
                                        <t t-if="widget.view.is_action_enabled('delete')"><li><a type="delete">Delete</a></li></t>
                                        <br/>
                                        <li><ul class="oe_kanban_colorpicker" data-field="color"/></li>
                                    </ul>
                                </div>
                                <div class="oe_kanban_content">
                                    <div>
                                        <strong><field name="name"/></strong>
                                    </div>
                                    <div>
                                        <field name="partner_id"/><br/>
                                        <field name="date"/><br/>
                                    </div>
                                    <div class="oe_kanban_footer_left">
                                        <field name="categ_id"/>
                                    </div>
                                    <div class="oe_kanban_bottom_left">
                                        <t t-raw="record.message_summary.raw_value"/>
                                    </div>
                                    <div class="oe_kanban_bottom_right">
                                        <img t-att-src="kanban_image('res.users', 'image_small', record.user_id.raw_value)" t-att-title="record.user_id.value" width="24" height="24" class="oe_kanban_avatar pull-right"/>
                                        <div class="pull-left" groups="base.group_user">
                                            <field name="kanban_state" widget="kanban_state_selection"/>
                                            <field name="priority" widget="priority"/>
                                        </div>
                                    </div>
                                </div>
                                <div class="oe_clear"></div>
                            </div>
                        </t>
                    </templates>
                </kanban>
            </field>
        </record>

        <!-- Crm claim Search view -->

        <record id="view_crm_case_claims_filter" model="ir.ui.view">
            <field name="name">CRM - Claims Search</field>
            <field name="model">crm.claim</field>
            <field name="arch" type="xml">
                <search string="Search Claims">
                    <field name="name" string="Claims"/>
                    <separator/>
                    <filter string="Unassigned Claims" icon="terp-personal-" domain="[('user_id','=', False)]" help="Unassigned Claims" />
                    <separator/>
                    <filter string="New Mail" name="message_unread" domain="[('message_unread','=',True)]"/>
                    <field name="partner_id" filter_domain="[('partner_id','child_of',self)]"/>
                    <field name="user_id"/>
                    <field name="stage_id"/>
                    <group expand="0" string="Group By">
                        <filter string="Partner" icon="terp-partner" domain="[]" help="Partner" context="{'group_by':'partner_id'}"/>
                        <filter string="Responsible" icon="terp-personal" domain="[]" help="Responsible User" context="{'group_by':'user_id'}"/>
                        <filter string="Stage" icon="terp-stage" domain="[]" context="{'group_by':'stage_id'}"/>
                        <filter string="Type" icon="terp-stock_symbol-selection" domain="[]" context="{'group_by':'categ_id'}"/>
                        <filter string="Claim Month" icon="terp-go-month" domain="[]" help="Claim Date by Month" context="{'group_by':'date'}"/>
                        <filter string="Deadline" icon="terp-go-month" domain="[]" context="{'group_by':'date_deadline'}"/>
                        <filter string="Closure" icon="terp-go-month" domain="[]" help="Date Closed" context="{'group_by':'date_closed'}"  groups="base.group_no_one"/>
                    </group>
                </search>
            </field>
        </record>
</odoo>
