<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="html_editor.CodeToolbar">
        <div t-att-class="this.props.isActive ? 'show' : ''" class="o_code_toolbar">
            <div data-prevent-closing-overlay="true">
                <Dropdown menuClass="'o_language_selector'">
                    <button class="btn" t-att-title="this.languages[this.props.currentLanguage]" name="language">
                        <span class="px-1" t-esc="this.languages[this.props.currentLanguage]"/>
                        <i class="fa fa-caret-down"></i>
                    </button>
                    <t t-set-slot="content">
                        <t t-foreach="Object.entries(this.languages)" t-as="language" t-key="language[0]">
                            <DropdownItem
                                attrs="{ name: language[1] }"
                                onSelected="() => this.props.onLanguageChange(language[0])" t-on-pointerdown.prevent="() => {}">
                                <t t-esc="language[1]"/>
                            </DropdownItem>
                        </t>
                    </t>
                </Dropdown>
                <CopyButton content="this.props.getContent" copyText.translate="Copy" successText.translate="Code copied to the clipboard."/>
                <button class="text-nowrap btn"
                    t-on-click="this.props.convertToParagraph"
                >
                    <span class="mx-1 fa fa-paragraph" title="Convert to paragraph"></span>
                </button>
            </div>
        </div>
    </t>
</templates>
