<templates xml:space="preserve">
    <t t-name="html_editor.FontFamilySelector">
        <Dropdown menuClass="'o_font_family_selector_menu'" menuRef="this.menuRef">
            <button class="btn btn-light" t-att-title="this.props.title" name="font_family" t-att-disabled="this.props.isDisabled">
                <span class="px-1" t-esc="this.props.currentFontFamily.displayName"/>
            </button>
            <t t-set-slot="content">
                <div data-prevent-closing-overlay="true">
                    <t t-foreach="this.props.fontFamilyItems" t-as="item" t-key="item_index">
                        <DropdownItem
                            attrs="{ name: item.nameShort }"
                            onSelected="() => this.props.onSelected(item)" t-on-pointerdown.prevent="() => {}">
                            <t t-esc="item.name"/>
                        </DropdownItem>
                    </t>
                </div>
            </t>
        </Dropdown>
    </t>
</templates>
