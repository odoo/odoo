<templates xml:space="preserve">
    <t t-name="html_editor.ImageCrop">
        <div
             class="o_we_crop_widget"
             tabindex="-1"
             contenteditable="false"
             t-custom-ref="el"
             t-on-zoom="this.onCropZoom"
        >
            <div class="o_we_cropper_wrapper" t-custom-ref="cropperWrapper">
                <img class="o_we_cropper_img" t-custom-ref="imageRef"/>
                <div class="o_we_crop_buttons text-center mt16 position-fixed o_we_no_overlay" contenteditable="false">
                    <div class="btn-group btn-group-toggle" title="Aspect Ratio" data-bs-toggle="buttons">
                        <t t-foreach="this.aspectRatios" t-as="ratio" t-key="ratio">
                            <t t-set="is_active" t-value="ratio === this.aspectRatio"/>
                            <label t-attf-class="btn #{is_active and 'active' or ''}" t-on-click="() => this.setAspectRatio(ratio)">
                                <input type="radio" /><t t-out="ratio_value.label"/>
                            </label>
                        </t>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="button" title="Zoom In" t-on-click="() => this.onZoom(0.1)"><i class="oi oi-fw" data-icon="zoom_in"/></button>
                        <button type="button" title="Zoom Out" t-on-click="() => this.onZoom(-0.1)"><i class="oi oi-fw" data-icon="zoom_out"/></button>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="button" title="Rotate Left" t-on-click="() => this.onRotate(-90)"><i class="oi oi-fw" data-icon="undo"/></button>
                        <button type="button" title="Rotate Right" t-on-click="() => this.onRotate(90)"><i class="oi oi-fw" data-icon="redo"/></button>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="button" title="Flip Horizontal" t-on-click="() => this.onFlip('scaleX')"><i class="oi" data-icon="arrow_range"/></button>
                        <button type="button" title="Flip Vertical" t-on-click="() => this.onFlip('scaleY')"><i class="oi" data-icon="height"/></button>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="button" title="Reset Image" t-on-click="this.onReset"><i class="oi" data-icon="refresh"/> Reset Image</button>
                    </div>
                    <div class="btn-group" role="group">
                        <button type="button" t-on-click="this.save" class="btn btn-primary"><i class="oi" data-icon="check" role="presentation"/> Apply</button>
                        <button type="button" t-custom-ref="discardButton" t-on-click="this.closeCropper" class="btn btn-danger"><i class="oi" data-icon="close_small" role="presentation"/> Discard</button>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
