<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="report_contributionregister">
    <t t-call="report.html_container">
        <t t-foreach="docs" t-as="o">
            <t t-call="report.external_layout">
                <div class="page">
                    <h2>PaySlip Lines by Contribution Register</h2>


                    <div class="row mt32 mb32">
                        <div class="col-xs-3">
                            <strong>Register Name:</strong>
                            <p t-field="o.name"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Date From:</strong>
                            <p t-esc="data['form']['date_from']"/>
                        </div>
                        <div class="col-xs-3">
                            <strong>Date To:</strong>
                            <p t-esc="data['form']['date_to']"/>
                        </div>
                    </div>

                    <table class="table table-condensed">
                        <thead>
                            <tr>
                                <th>PaySlip Name</th>
                                <th>Code</th>
                                <th>Name</th>
                                <th>Quantity/Rate</th>
                                <th>Amount</th>
                                <th>Total</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr t-foreach="get_payslip_lines" t-as="line">
                                <td><span t-field="line.slip_id.name"/></td>
                                <td><span t-field="line.code"/></td>
                                <td><span t-field="line.name"/></td>
                                <td><span t-field="line.quantity"/></td>
                                <td><span t-field="line.amount"/></td>
                                <td>
                                    <span t-field=" line.total"
                                    t-field-options='{"widget": "monetary", "display_currency": "res_company.currency_id"}'/>
                                </td>
                            </tr>
                        </tbody>
                    </table>

                    <div class="row">
                        <div class="col-xs-4 pull-right">
                            <table class="table table-condensed">
                                <tr class="border-black">
                                    <td><strong>Total</strong></td>
                                    <td class="text-right">
                                        <span t-esc="sum_total" t-esc-options='{"widget": "monetary", "display_currency": "o.company_id.currency_id"}'/>
                                    </td>
                                </tr>
                            </table>
                        </div>
                    </div>
                </div>
            </t>
        </t>
    </t>
</template>

</odoo>
