<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_company_form" model="ir.ui.view">
        <field name="name">res.company.form.inherit.l10n_au</field>
        <field name="model">res.company</field>
        <field name="inherit_id" ref="base.view_company_form"/>
        <field name="arch" type="xml">
            <field name="vat" position="before">
                <label name="ABN" for="vat" string="ABN" invisible="country_code not in ['AU']"/>
            </field>
            <xpath expr="//label[@name='tax_label']" position="attributes">
                <attribute name="invisible" add="country_code == 'AU'" separator=" or "/> 
            </xpath>
            <field name="company_registry" position="before">
                <label name="ACN" for="company_registry" string="ACN" invisible="country_code != 'AU'"/>
            </field>
            <xpath expr="//label[@name='company_label']" position="attributes">
                <attribute name="invisible" add="country_code == 'AU'" separator=" or "/>
            </xpath>
        </field>
    </record>

</odoo>
