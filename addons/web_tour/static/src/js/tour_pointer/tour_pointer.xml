<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="web_tour.TourPointer">
        <div 
            t-ref="anchor"
            class="o_tour_anchor"
        />
        <div
            t-ref="pointer"
            t-if="this.isVisible"
            t-attf-class="
                o_tour_pointer
                {{ this.props.pointerState.onClick ? 'cursor-pointer' : '' }}
            "
            t-attf-style="
                --TourPointer__width: {{ this.constructor.width }}px;
                --TourPointer__height: {{ this.constructor.height }}px;
            "
        >   
            <div
                t-ref="dropzone"
                t-if="this.props.pointerState.isZone"
                class="o_tour_dropzone pe-none"
            />
            <div
                t-if="!this.state.showContent and !this.props.pointerState.isZone"
                t-on-mouseover="() => this.openContent()"
                t-attf-class="o_tour_pointer_tip o_{{ this.state.direction }}"
            />
        </div>
    </t>
    <t t-name="web_tour.TourPointer.Content">
        <div 
            class="o_tour_pointer o_tour_pointer_content px-3 py-2"
            t-on-click="this.props.onClick"
            t-on-mouseleave="this.props.closeContent"
            t-on-mouseover="this.props.openContent"
        >
            <span>
                <t t-out="this.props.content" />
            </span>
            <div class="d-flex justify-content-end">
                <button class="btn btn-link shadow-none" t-on-click="this.onStopClicked">Stop Tour</button>
            </div>
        </div>
    </t>
</templates>
