<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="KanbanActivity">
    <div class="dropdown o_kanban_selection">
        <a class="dropdown-toggle o_activity_btn" data-toggle="dropdown">
            <span t-attf-class="fa o_activity_color_#{widget.activity_state or 'default'} fa-chevron-circle-right fa-lg"/>
        </a>
        <ul class="dropdown-menu o_activity" role="menu">
        </ul>
    </div>
</t>

<t t-name="KanbanActivityLoading">
    <li class="text-center o_no_activity">
        <span class="fa fa-spinner fa-spin fa-2x" />
    </li>
</t>

<t t-name="KanbanActivityDropdown">
    <li t-if="_.isEmpty(records)" class="text-center o_no_activity">
        <span>No activities.</span>
    </li>
    <li t-if="!_.isUndefined(records)">
        <ul class="o_activity_log">
            <t t-foreach="_.keys(records)" t-as="key">
                <t t-set="logs" t-value="records[key]" />
                <li class="o_activity_label">
                    <strong t-attf-class="o_activity_color_#{key}">
                        <t t-esc="selection[key]" /> (<span t-esc="logs.length" />)
                    </strong>
                </li>
                <li t-foreach="logs" t-as="log" t-att-data-activity-id="log.id" class="o_schedule_activity">
                    <div class="pull-left"><span t-attf-class="mt8 fa #{log.icon} fa-lg" /></div>
                    <div class="o_activity_title pull-left">
                        <strong><t t-esc="log.title_action or log.next_activity_id[1]" /></strong>
                        <div>
                            <span class="fa fa-clock-o" />
                            <span t-att-title="log.date_action"><t t-esc="log.day" /></span>
                        </div>
                    </div>
                    <div class="pull-right">
                        <span class="o_mark_as_done o_activity_link fa fa-check-circle fa-2x" t-att-data-activity-id="log.id" title="Mark as done" />
                    </div>
                </li>
            </t>
        </ul>
    </li>
    <li class="o_schedule_activity text-center">
        <strong>Schedule an activity</strong>
    </li>
</t>

</templates>
