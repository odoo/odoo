<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

<t t-name="mail.Composer">
    <t t-set="isChatterComposer" t-value="this.extended and !this.props.composer.message"/>
    <t t-set="partitionedActions" t-value="this.composerActions.partition"/>
    <t t-set="dummy" t-value="this.computeMoreActions(partitionedActions)"/>
    <t t-set="actionsContainerClass" t-value="'o-mail-Composer-actions d-flex o-rounded-bubble'"/>
    <div class="d-print-none" t-custom-ref="root">
        <div class="o-mail-Composer d-grid flex-shrink-0 pt-0 position-relative"
                t-att-class="{
                    'pb-2': this.extended and !this.props.composer.message,
                    'o-extended': this.extended,
                    'o-isUiSmall': this.ui.isSmall,
                    'ps-2 pe-4 pb-2': this.normal and !this.env.inDiscussApp and !this.env.inMeetingView,
                    'ps-0 pe-3 pb-2': this.normal and this.env.inMeetingView,
                    'o-hasSelfAvatar': this.showComposerAvatar,
                    'o-focused': this.props.composer.isFocused,
                    'o-editing': this.props.composer.message,
                    'o-chatWindow mx-2 my-1': this.env.inChatWindow,
                    'mb-2': this.env.inChatWindow and !this.props.composer.message,
                    'o-discussApp pt-1 ps-2 pe-4 o-pb-2_5': this.env.inDiscussApp,
                    'o-disabled': this.props.disabled,
                }" t-attf-class="{{ this.props.className }}">
            <FileUploader t-if="this.allowUpload" multiUpload="this.isMultiUpload" onUploaded.bind="(data) => { this.attachmentUploader.uploadData(data) }">
                <t t-set-slot="toggler">
                    <button t-att-disabled="!this.state.active or this.areAllActionsDisabled" class="d-none" title="Attach files" aria-label="Attach files" t-custom-ref="file-uploader"><i class="fa fa-fw fa-paperclip"/></button>
                </t>
            </FileUploader>
            <div t-if="this.showComposerAvatar" class="o-mail-Composer-sidebarMain flex-shrink-0 d-flex">
                <img class="o-mail-Composer-avatar mx-auto o_avatar rounded-3" t-att-src="(this.thread?.effectiveSelf or this.message.effectiveSelf).avatarUrl" alt="Avatar of user"/>
            </div>
            <div class="o-mail-Composer-coreHeader min-w-0">
                <div t-if="this.props.composer.replyToMessage" class="text-truncate small p-2">
                    <span class="cursor-pointer" t-on-click="() => this.env.messageHighlight?.highlightMessage(this.props.composer.replyToMessage)">
                        Replying to <b t-out="this.props.composer.replyToMessage.authorName"/>
                    </span>
                    <span t-if="this.props.composer.replyToMessage.thread?.notEq(this.props.composer.thread)">
                        on: <b><t t-out="this.props.composer.replyToMessage.thread.displayName"/></b>
                    </span>
                    <i class="fa fa-lg fa-times-circle rounded-circle p-0 ms-1 cursor-pointer" title="Discard" t-on-click="() => (this.props.composer.replyToMessage = undefined)"/>
                </div>
                <span t-if="this.props.composer.message and this.ui.isSmall" class="px-1" t-on-click="this.onClickCancelOrSaveEditText">
                    <button class="btn btn-sm btn-secondary rounded-pill border border-secondary ps-1 pe-2 py-0 mb-1" t-att-data-type="this.EDIT_CLICK_TYPE.CANCEL">
                        <i class="oi oi-close pe-2" t-att-data-type="this.EDIT_CLICK_TYPE.CANCEL"></i>
                        Discard editing
                    </button>
                </span>
            </div>
            <div class="o-mail-Composer-coreMain min-w-0 d-flex flex-nowrap align-items-start flex-grow-1" t-att-class="{ 'flex-column' : this.extended or this.props.composer.message }">
                <div class="o-mail-Composer-inputContainer o-mail-Composer-bg d-flex flex-grow-1 min-w-0 overflow-x-hidden border o-rounded-bubble"
                    t-att-class="{
                        'o-iosPwa': this.isIosPwa,
                        'align-self-stretch' : this.extended,
                        'w-100': this.props.composer.message,
                    }"
                    t-custom-ref="input-container"
                >
                    <div t-if="!this.extended" t-attf-class="{{ actionsContainerClass }}">
                        <t t-if="this.moreActions" t-call="mail.Composer.moreActions"/>
                    </div>
                    <div class="position-relative flex-grow-1 min-w-0">
                        <t t-set="inputClasses" t-value="{
                            'o-mail-Composer-inputStyle form-control border-0 o-rounded-bubble': true,
                            'o-mobile': this.isMobileOS,
                            'ps-2': partitionedActions.other.length === 0,
                            'text-muted': this.props.composer.restoredFromFullComposer,
                        }"/>
                        <Wysiwyg t-if="this.composerService.htmlEnabled" class="'w-100 min-w-0 text-break overflow-auto'" config="this.wysiwygConfig" onLoad.bind="this.onLoadWysiwyg"/>
                        <t t-else="">
                                <textarea class="o-mail-Composer-input o-mail-Composer-bg shadow-none overflow-auto o-scrollbar-thin o-discuss-text-body user-select-auto"
                                t-att-class="inputClasses"
                                t-custom-ref="textarea"
                                t-on-keydown="this.onKeydown"
                                t-on-focusin="this.onFocusin"
                                t-on-focusout="this.onFocusout"
                                t-on-click="(ev) => this.markEventHandled(ev, 'composer.onClickTextarea')"
                                t-on-paste="this.onPaste"
                                t-custom-model="this.props.composer.composerText"
                                t-on-input="(ev) => this.onInput(ev)"
                                t-att-placeholder="this.placeholder"
                                t-att-readOnly="!this.state.active or this.props.composer.restoredFromFullComposer"
                                t-att-disabled="this.props.disabled or this.props.composer.restoredFromFullComposer"
                                tabindex="0"
                            />
                            <!--
                                This is an invisible textarea used to compute the composer
                                height based on the text content. We need it to downsize
                                the textarea properly without flicker.
                            -->
                            <textarea
                                class="o-mail-Composer-fake position-absolute overflow-hidden"
                                t-att-class="inputClasses"
                                t-att-value="this.props.composer.composerText"
                                t-custom-ref="fakeTextarea"
                                disabled="1"
                            />
                        </t>
                    </div>
                    <t t-call="mail.Composer.quickActions"/>
                </div>
            </div>
            <div class="o-mail-Composer-footer min-w-0 overflow-auto o-scrollbar-thin">
                <AttachmentList
                    t-if="this.props.composer.attachments.length > 0"
                    attachments="this.props.composer.attachments"
                    unlinkAttachment.bind="(...args) => this.attachmentUploader.unlink(...args)"/>
                <div t-if="!this.extended and !this.props.composer.message" class="o-mail-Composer-pickerContainer" t-att-class="{ 'o-active': this.composerActions.activePicker and this.ui.isSmall }" t-custom-ref="picker-container">
                    <div t-if="partitionedActions.pickers.length > 1 and this.composerActions.activePicker and this.ui.isSmall" class="o-mail-Composer-pickerSelection btn-group w-100">
                        <button t-foreach="partitionedActions.pickers" t-as="pickerAction" t-key="'PICKER_' + pickerAction.id" class="btn btn-secondary" t-out="pickerAction.pickerName" t-on-click="(ev) => pickerAction.onSelected(ev)" t-att-class="{
                            'active': pickerAction.picker.isOpen
                        }"/>
                    </div>
                    <div t-custom-ref="picker-target"/>
                </div>
                <div t-else="" class="d-flex align-items-center gap-1 w-100 pe-1">
                    <div class="pt-1" t-att-class="{ 'mt-2': !this.props.composer.message }">
                        <span t-if="this.props.composer.message and !this.ui.isSmall" class="text-muted px-1 small" t-out="this.CANCEL_OR_SAVE_EDIT_TEXT" t-on-click="this.onClickCancelOrSaveEditText" />
                        <t t-elif="!this.props.composer.message and !this.props.composer.restoredFromFullComposer">
                            <t t-call="mail.Composer.sendButton"/>
                            <span t-if="!this.isSendButtonDisabled and !this.isMobileOS" class="text-muted small ms-1" t-out="this.OR_PRESS_SEND_KEYBIND"/>
                        </t>
                    </div>
                    <span class="flex-grow-1"/>
                    <t t-if="!this.props.composer.message" t-call="mail.Composer.extraActions"/>
                </div>
            </div>
        </div>
    </div>
    <NavigableList t-if="this.suggestion" class="'o-mail-Composer-suggestionList'" t-props="this.navigableListProps"/>
</t>

<t t-name="mail.Composer.sendButton">
    <button class="o-mail-Composer-send btn"
        t-att-class="{
            'btn-primary btn-sm': this.extended,
            'btn-link rounded-circle p-0': !this.extended,
            'me-2': this.env.inDiscussApp,
            'border-start-0': this.env.inDiscussApp and !this.props.composer.message,
            'border-0': this.props.composer.message,
        }"
        t-on-click="this.sendMessage"
        t-att-disabled="this.isSendButtonDisabled"
        t-att-aria-label="this.SEND_TEXT"
    >
        <t t-if="this.thread and !this.thread.channel" t-out="this.SEND_TEXT"/>
        <t t-else=""><i class="fa fa-fw fa-lg fa-paper-plane-o"/></t>
    </button>
</t>

<t t-name="mail.Composer.quickActions">
    <div class="o-mail-Composer-quickActions o-mail-Composer-bg" t-attf-class="{{ actionsContainerClass }}" t-custom-ref="quick-actions">
        <div class="o-mail-Composer-mainActions d-flex flex-grow-1 align-items-baseline">
            <ActionList actions="partitionedActions.quick" inline="true"/>
        </div>
    </div>
</t>

<t t-name="mail.Composer.moreActions">
    <div class="o-mail-Composer-moreActions o-mail-Composer-bg" t-attf-class="{{ actionsContainerClass }}" t-custom-ref="more-actions">
        <div class="o-mail-Composer-mainActions d-flex flex-grow-1 align-items-baseline">
            <ActionList actions="[this.moreActions]" inline="true"/>
        </div>
    </div>
</t>

<t t-name="mail.Composer.extraActions">
    <div class="o-mail-Composer-extraActions" t-attf-class="{{ actionsContainerClass }}" t-custom-ref="extra-actions">
        <div class="o-mail-Composer-mainActions d-flex flex-grow-1 align-items-baseline">
            <ActionList actions="partitionedActions.other" inline="true"/>
        </div>
    </div>
</t>

    <t t-name="mail.Composer.suggestionSpecial">
        <strong class="px-2 py-1 align-self-center flex-shrink-0 text-truncate">
            <t t-out="option.displayName"/>
        </strong>
        <em class="text-600 text-truncate align-self-center">
            <t t-out="option.description"/>
        </em>
    </t>

    <t t-name="mail.Composer.suggestionPartner">
        <t t-set="partner" t-value="option.partner"/>
        <t t-set="title" t-value="partner.name"/>
        <t t-if="partner.email" t-set="title" t-value="`${title} (${partner.email})`"/>
        <DiscussAvatar t-if="partner" persona="partner" size="24"/>
        <strong class="o-mail-Composer-suggestionPartner-name px-2 py-1 align-self-center flex-shrink-0 text-truncate" t-att-class="{'o-has-email': partner.email, 'mw-100': !partner.email}" t-att-title="title">
            <t t-out="partner.name"/>
            <t t-if="option.thread and partner.name !== option.thread.getPersonaName(partner)">
                "<t t-out="option.thread.getPersonaName(partner)"/>"
            </t>
        </strong>
        <span t-if="partner.email" class="text-600 text-truncate align-self-center opacity-75" t-att-title="title">(<t t-out="partner.email"/>)</span>
    </t>

    <t t-name="mail.Composer.suggestionRole">
        <span class="d-flex justify-content-center opacity-75" style="width:24px;">
            <i class="oi oi-users"/>
        </span>
        <strong class="px-2 py-1 align-self-center flex-shrink-0 text-truncate" t-out="option.label"/>
        <em class="text-600 text-truncate align-self-center opacity-75">
            <t t-if="option.thread?.channel?.channel_type === 'channel'">Notify users with this role who have permission to view this channel</t>
            <t t-else="">Notify users with this role</t>
        </em>
    </t>

    <t t-name="mail.Composer.suggestionChannel">
        <strong class="px-2 py-1 align-self-center flex-shrink-0 text-truncate">
            <i t-attf-class="fa #{option.channel?.parent_channel_id ? 'fa-comments-o' : 'fa-hashtag'} me-2"/>
            <t t-if="option.channel.parent_channel_id">
                <t t-out="option.channel.parent_channel_id.displayName"/>
                <i class="oi oi-chevron-right o-xsmaller mx-1"/>
            </t>
            <t t-out="option.channel.displayName"/>
        </strong>
    </t>

    <t t-name="mail.Composer.suggestionChannelCommand">
        <strong class="px-2 py-1 align-self-center flex-shrink-0 text-truncate">
            <t t-out="option.label"/>
        </strong>
        <span class="text-600 text-truncate align-self-center">
            <t t-out="option.help"/>
        </span>
    </t>

    <t t-name="mail.Composer.suggestionCannedResponse">
        <strong class="px-2 py-1 align-self-center flex-shrink-0 text-truncate mw-100">
            <t t-out="option.source"/>
        </strong>
        <span class="text-600 text-truncate align-self-center">
            <t t-out="option.label"/>
        </span>
    </t>
    <t t-name="mail.Composer.suggestionEmoji">
        <strong class="px-2 align-self-center flex-shrink-1 fs-3">
            <t t-out="option.emoji.codepoints"/>
        </strong>
        <em class="text-600 text-truncate align-self-center">
            <t t-out="option.emoji.shortcodes.join(' ')"/>
        </em>
    </t>


    <t t-name="mail.FullComposerRecoveryPopover">
        <div class="px-2">
            <div>
                <p class="mb-0 fs-5 fw-bold">Continue with Full Composer?</p>
                <p class="mb-0 small text-muted">Formatting can only be restored in Full Composer.</p>
            </div>
            <div class="d-flex align-items-center justify-content-center gap-1 mt-2">
                <button class="btn btn-primary" t-on-click="this.onClickFullRecover"><i class="fa fa-check me-2 opacity-75"/>Yes <span class="small opacity-50">(Keep formatting)</span></button>
                <button class="btn btn-secondary" t-on-click="this.onClickTextRecover"><i class="oi oi-close me-2 opacity-75"/>No <span class="small opacity-50">(Remove formatting)</span></button>
            </div>
        </div>
    </t>

</templates>
