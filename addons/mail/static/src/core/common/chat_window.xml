<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

<t t-name="mail.ChatWindow">
    <div class="o-mail-ChatWindow fixed-bottom overflow-hidden d-flex flex-column shadow bg-100"
        t-att-style="this.style"
        t-att-class="this.attClass"
        t-on-keydown="this.onKeydown"
        tabindex="1"
    >
        <div class="o-mail-ChatWindow-header d-flex align-items-center flex-shrink-0 bg-100 z-1 border-bottom border-secondary" t-on-click="this.onClickHeader" t-att-class="{ 'cursor-pointer': !this.ui.isSmall and !this.props.chatWindow.actionsDisabled }">
            <t t-if="this.hasActionsMenu">
                <div class="d-flex text-truncate bg-inherit">
                    <Dropdown position="this.ui.isSmall ? 'bottom-end' : 'left-start'" onStateChanged="isOpen => this.onActionsMenuStateChanged(isOpen)" menuClass="this.store.discussDropdownMenuClass(this)">
                        <button
                            class="o-mail-ChatWindow-moreActions btn rounded-0 d-flex align-items-center o-ps-1_5 pe-0 py-1 my-0 w-100 rounded-end-0 bg-inherit"
                            t-att-class="{ 'rounded-top-3': !this.ui.isSmall, 'o-active': this.state.actionsMenuOpened, 'o-hover': this.actionsMenuButtonHover.isHover and !this.parentChannelHover.isHover }"
                            t-att-disabled="this.state.editingName or this.props.chatWindow.actionsDisabled"
                            t-att-title="this.actionsMenuTitleText"
                            t-ref="actionsMenuButton"
                        >
                            <t t-call="mail.ChatWindow.headerContent"/>
                            <i t-if="!this.state.editingName" class="fa-solid fa-caret-down"/>
                        </button>
                        <t t-set-slot="content">
                            <t t-set="quickActionsInDropdown" t-value="this.partitionedActions.quick.slice(this.ui.isSmall ? 2 : 4)"/>
                            <ActionList actions="[quickActionsInDropdown, this.partitionedActions.other, ...this.partitionedActions.group]" dropdown="true" thread="this.thread"/>
                        </t>
                    </Dropdown>
                </div>
                <AutoresizeInput
                    t-if="this.state.editingName"
                    className="'text-truncate fw-bold flex-shrink-1 me-1 py-0 ' + (this.ui.isSmall ? 'fs-4' : 'fs-5')"
                    enabled="true"
                    autofocus="true"
                    onCancel.bind="this.cancelRenameThread"
                    onValidate.bind="this.renameThread"
                    value="this.props.chatWindow.displayName"
                />
                <i t-if="this.state.editingName" class="fa-solid fa-caret-down opacity-50"/>
            </t>
            <t t-else="">
                <t t-call="mail.ChatWindow.headerContent"/>
            </t>
            <div t-if="this.thread and this.thread.importantCounter > 0" class="o-mail-ChatWindow-counter mx-1 badge rounded-pill fw-bold o-discuss-badge shadow-sm" t-ref="needactionCounter">
                <t t-out="this.thread.importantCounter"/>
            </div>
            <div class="ms-1 flex-grow-1"/>
            <div class="d-flex flex-shrink-0 me-1">
                <ActionList actions="this.partitionedActions.quick.slice(0, this.ui.isSmall ? 2 : 4).reverse()" inline="true" thread="this.thread"/>
            </div>
            <t t-if="this.store.inPublicPage and !this.store.self_partner">
                <button class="btn ps-1" t-if="!this.state.editingGuestName">
                    <img class="o-mail-DiscussContent-selfAvatar rounded-circle object-fit-cover flex-shrink-0" alt="Avatar" t-att-src="this.store.self.avatarUrl" t-on-click="() => this.state.editingGuestName = true"/>
                </button>
                <AutoresizeInput
                    t-if="this.state.editingGuestName"
                    className="'py-1 me-2'"
                    autofocus="true"
                    onCancel.bind="this.cancelRenameGuest"
                    onValidate.bind="this.renameGuest"
                    value="this.store.self.name"
                />
            </t>
        </div>
        <div t-if="!this.props.chatWindow.folded or this.ui.isSmall" class="d-flex flex-column h-100 overflow-auto o-scrollbar-thin position-relative bg-inherit" t-att-class="{ 'opacity-50': this.state.editingName }" t-ref="content">
            <t name="thread content">
                <div t-if="this.threadActions.activeAction?.actionPanelComponentCondition" class="h-100 bg-inherit" t-attf-class="{{ this.threadActions.activeAction.panelOuterClass }}">
                    <t t-component="this.threadActions.activeAction.actionPanelComponent" t-props="{ ...this.threadActions.activeAction.actionPanelComponentProps, thread: this.thread }"/>
                </div>
                <t t-elif="this.thread">
                    <Thread autofocus="this.autofocusThread" isInChatWindow="true" thread="this.thread" t-key="this.thread.localId" jumpPresent="this.state.jumpThreadPresent" jumpToNewMessage="this.props.chatWindow.jumpToNewMessage"/>
                    <t t-call="mail.ChatWindow.memberTyping"/>
                    <Composer composer="this.thread.composer" autofocus="this.autofocusComposer" mode="'compact'" onPostCallback.bind="() => this.state.jumpThreadPresent++" dropzoneRef="this.contentRef" type="this.composerType"/>
                </t>
            </t>
        </div>
    </div>
</t>

<t t-name="mail.ChatWindow.headerContent">
    <div class="o-mail-ChatWindow-threadAvatar position-relative bg-inherit my-0 me-1" t-att-class="{
        'py-1': !this.thread or this.threadActions.actions.length gt 3,
        'py-2': this.thread and this.threadActions.actions.length lte 3,
        'ms-1': this.thread,
        'ms-3': !this.thread,
        'ps-2': !this.hasActionsMenu,
    }" name="threadAvatar">
        <img t-if="this.thread" class="rounded-3 object-fit-cover" t-att-src="this.thread.parent_channel_id?.avatarUrl ?? this.thread.avatarUrl" alt="Thread Image"/>
        <ImStatus t-if="this.showImStatus" className="'o-mail-ChatWindow-imStatus position-absolute'" member="this.thread.imStatusMember" size="'sm'"/>
    </div>
    <t t-if="this.thread?.parent_channel_id">
        <span class="fw-bold small o-mx-0_5 opacity-75 opacity-100-hover cursor-pointer o-hover-text-underline rounded fs-6" t-out="this.thread.parent_channel_id.displayName" title="Open Channel" t-ref="parentChannel" t-on-click.stop="() => this.thread.parent_channel_id.open({ focus: true })"/>
        <i class="fa-solid fa-chevron-right o-xsmaller ms-1 mt-1 text-muted opacity-75"/>
    </t>
    <CountryFlag t-if="this.thread?.showCorrespondentCountry" country="this.thread.correspondentCountry" class="'o-mail-ChatWindow-country border shadow-sm'"/>
    <div t-if="!this.state.editingName" class="text-truncate fw-bold border border-transparent o-mx-0_5 my-0 py-1" t-att-title="this.props.chatWindow.displayName" t-out="this.props.chatWindow.displayName" t-att-class="{ 'fs-4': this.ui.isSmall, 'fs-5': !this.ui.isSmall }"/>
</t>

<t t-name="mail.ChatWindow.memberTyping">
    <div t-if="this.thread.hasOtherMembersTyping" class="d-flex bg-inherit position-relative">
        <div class="o-mail-ChatWindow-typing d-flex px-3 position-absolute bottom-0 start-0 w-100 bg-inherit align-items-center">
            <Typing channel="this.thread" size="'medium'"/>
        </div>
    </div>
</t>

</templates>
