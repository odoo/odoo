<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
<t t-name="mail.Tabs">
    <div t-attf-class="o-mail-Tabs d-flex border-top bg-view h-100 w-100" t-ref="ref" t-att-class="{'o-mail-Tabs-vertical': this.props.direction === 'v', 'flex-column o-mail-Tabs-horizontal': this.props.direction === 'h'}">
        <div class="position-relative d-flex bg-100 flex-shrink-0" t-att-class="{'border-bottom flex-row mh-25': this.props.direction === 'h', 'border-end flex-column mw-25': this.props.direction === 'v'}">
            <button t-if="this.scrollState.hasScrollbar" t-att-disabled="!this.scrollState.canScrollBefore" class="btn border-transparent o-mail-Tabs-scroller flex-shrink-0 bg-100 rounded-0" t-on-click="() => this.scroll(-1)"><i class="oi smaller" t-att-class="{'oi-chevron-up': this.props.direction === 'v', 'oi-chevron-left': this.props.direction === 'h'}"/></button>
            <nav class="o-mail-Tabs-scrollContainer d-flex flex-grow-1 overflow-auto gap-2 p-1" t-att-class="{'flex-row': this.props.direction === 'h', 'flex-column': this.props.direction === 'v'}" t-ref="nav">
                <t t-slot="headers"/>
            </nav>
            <button t-if="this.scrollState.hasScrollbar" t-att-disabled="!this.scrollState.canScrollAfter" class="btn border-transparent o-mail-Tabs-scroller flex-shrink-0 rounded-0 bg-100" t-on-click="() => this.scroll(1)"><i class="oi smaller" t-att-class="{'oi-chevron-down': this.props.direction === 'v', 'oi-chevron-right': this.props.direction === 'h'}"/></button>
        </div>
        <div class="o-mail-Tabs-content overflow-auto flex-grow-1 o-scrollbar-thin">
            <t t-slot="panels"/>
        </div>
    </div>
</t>

<t t-name="mail.InternalTabHeader">
    <button
        class="o-mail-TabHeader d-flex align-items-center text-start flex-shrink-0 rounded border-0 fw-bold"
        t-att-class="{'o-active': this.isActive, 'bg-transparent text-muted opacity-75': !this.isActive}"
        t-att-title="this.props.title"
        t-on-click="this.onClick"
        t-ref="root"
        t-att-data-header-id="this.props.id"
    >
        <t t-slot="default"/>
    </button>
</t>

<t t-name="mail.TabPanel">
    <div t-if="this.env.tabsContext.isActive(this.props.id)" class="p-2">
        <t t-slot="default"/>
    </div>
</t>

</templates>
