<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">
    <t t-name="mail.NotificationMessage">
        <div class="o-mail-NotificationMessage text-break mx-auto text-muted opacity-75 px-3 text-center smaller" t-on-click="onClickNotificationMessage" t-ref="root">
            <i t-if="message.notificationIcon" t-attf-class="{{ message.notificationIcon }} me-1"/>
            <t t-if="message.notificationType === 'call'">
                <span class="fw-bold" t-esc="callInformation"/> <span class="o-xsmaller" t-esc="message.dateSimpleWithDay"/>
            </t>
            <t t-else="">
                <span class="o-mail-NotificationMessage-author d-inline" t-if="message.author and !message.body.includes(escape(message.author.name))" t-esc="message.author.name"/> <t t-out="message.body"/>
            </t>
        </div>
    </t>
</templates>
