<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.MeetingChat">
        <ActionPanel t-if="this.thread" title.translate="Chat" resizable="true" minWidth="300" initialWidth="400" icon="'fa-solid fa-comments'" contentRef="this.panelContentRef">
            <Thread thread="this.thread" jumpPresent="this.state.jumpPresent"/>
            <t t-call="mail.ChatWindow.memberTyping"/>
            <Composer
                composer="this.thread.composer"
                autofocus="!this.isMobileOS"
                onDiscardCallback="() => (this.thread.composer.replyToMessage = undefined)"
                onPostCallback.bind="() => this.state.jumpPresent++"
                dropzoneRef="this.panelContentRef"
           />
        </ActionPanel>
    </t>
</templates>
