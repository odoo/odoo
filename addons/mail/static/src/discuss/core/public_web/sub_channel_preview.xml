<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.SubChannelPreview">
        <t t-set="message" t-value="this.thread.newestPersistentOfAllMessage ?? this.thread.from_message_id"/>
        <button class="o-mail-SubChannelPreview btn btn-light-subtle d-flex border border-secondary mx-1 px-2 py-1 rounded-2 align-items-center" t-attf-class="{{ this.props.class }}" t-on-click="this.onClick">
            <div class="d-flex flex-column w-100">
                <div class="d-flex w-100">
                    <span class="o-fw-600 text-truncate small" t-out="this.thread.displayName"/>
                    <span class="flex-grow-1 pe-1"/>
                    <span t-if="message" class="text-muted smaller flex-shrink-0" t-out="this.dateText(message)"/>
                </div>
                <div class="o-mail-SubChannelPreview-lastMessage text-start text-muted fw-normal smaller o_line_clamp ms-2">
                    <t t-if="message" t-call="mail.message_preview_prefix" message="message"/>
                    <t t-out="message?.inlineBody"/>
                </div>
            </div>
            <i t-if="this.env.inMessage" class="oi oi-chevron-right ms-2 opacity-50"/>
        </button>
    </t>
</templates>
