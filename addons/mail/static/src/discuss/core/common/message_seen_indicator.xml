<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.MessageSeenIndicator">
        <span t-if="this.props.message.channel_id?.exists()" class="o-mail-MessageSeenIndicator position-relative opacity-25 cursor-pointer" t-att-class="{ 'o-hasEveryoneSeen opacity-75': this.props.message.hasEveryoneSeen }" t-att-title="this.summary" t-attf-class="{{ this.props.className }}" t-on-click="this.openDialog">
            <i class="fa fa-check ps-1"/><i t-if="this.props.message.hasEveryoneSeen" class="o-second start-0 fa fa-check position-absolute"/>
        </span>
    </t>

    <t t-name="mail.MessageSeenIndicatorPopover.card">
        <div class="o-mail-MessageSeenIndicatorPopover-card d-flex align-items-center gap-2">
            <span class="o_avatar position-relative o_card_avatar" style="width: 30px;height:30px;">
                <img t-att-src="this.member.avatarUrl" class="w-100 h-100 rounded object-fit-cover" />
            </span>
            <span class="fw-bold" t-esc="this.member.name"/>
        </div>
    </t>

    <t t-name="mail.MessageSeenIndicatorDialog">
        <Dialog size="'sm'" title.translate="Seen by:" footer="false" withBodyPadding="false">
            <ul class="list-group list-group-flush list-unstyled py-1" t-ref="content">
                <li class="list-group-item py-2" t-foreach="this.props.message.channelMemberHaveSeen" t-as="member" t-key="this.member.id">
                    <t t-call="mail.MessageSeenIndicatorPopover.card"/>
                </li>
            </ul>
        </Dialog>
    </t>

</templates>
