<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.AttachmentImageView" owl="1">
        <t t-if="attachmentImageView">
            <div t-attf-class="{{ className }}" role="menu" t-att-aria-label="attachmentImageView.attachment.displayName" t-ref="root">
                <div class="o_AttachmentImageView d-flex position-relative flex-shrink-0"
                    t-att-class="{
                        'o-isUploading': attachmentImageView.attachment.isUploading,
                    }"
                    t-att-title="attachmentImageView.attachment.displayName ? attachmentImageView.attachment.displayName : undefined"
                    t-att-data-id="attachmentImageView.attachment.localId"
                    tabindex="0"
                    aria-label="View image"
                    role="menuitem"
                    t-on-click="attachmentImageView.onClickImage"
                    t-att-data-mimetype="attachmentImageView.attachment.mimetype"
                >
                    <t t-if="!attachmentImageView.attachment.isUploading">
                        <img class="img img-fluid my-0 mx-auto" t-att-src="attachmentImageView.imageUrl" t-att-alt="attachmentImageView.attachment.name" t-attf-style="max-width: min(100%, {{ attachmentImageView.width }}px); max-height: {{ attachmentImageView.height }}px;"/>
                    </t>
                    <t t-if="attachmentImageView.attachment.isUploading">
                        <div class="o_AttachmentImageUploading position-absolute top-0 bottom-0 start-0 end-0 d-flex align-items-center justify-content-center" title="Uploading">
                            <i class="fa fa-spin fa-spinner"/>
                        </div>
                    </t>
                    <div class="o_AttachmentImageView_imageOverlay position-absolute top-0 bottom-0 start-0 end-0 p-2 text-white opacity-0 opacity-100-hover d-flex align-items-end flax-wrap flex-column">
                        <div t-if="attachmentImageView.attachment.isDeletable" class="o_AttachmentImageView_action o_AttachmentImageView_actionUnlink btn btn-sm btn-dark rounded opacity-75 opacity-100-hover" t-att-class="{'o-pretty': attachmentImageView.attachmentListView.composerViewOwner}" tabindex="0" aria-label="Remove" role="menuitem" t-on-click="attachmentImageView.onClickUnlink" title="Remove">
                            <i class="fa fa-trash"/>
                        </div>
                        <div t-if="attachmentImageView.hasDownloadButton" class="o_AttachmentImageView_action o_AttachmentImageView_actionDownload btn btn-sm btn-dark rounded opacity-75 opacity-100-hover mt-auto" t-on-click="attachmentImageView.onClickDownload" title="Download">
                            <i class="fa fa-download"/>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </t>
</templates>
