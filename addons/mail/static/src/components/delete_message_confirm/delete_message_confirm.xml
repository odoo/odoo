<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.DeleteMessageConfirm" owl="1">
        <Dialog contentClass="'o_DeleteMessageConfirm ' + className" title="title" onClosed="messageActionList and messageActionList.onDeleteConfirmDialogClosed">
            <t t-if="messageActionList">
                <p>Are you sure you want to delete this message?</p>
                <blockquote class="o_DeleteMessageConfirm_blockquote">
                    <Message localId="messageActionList.messageViewForDelete.localId"/>
                </blockquote>
                <small t-if="!messageActionList.message.originThread or messageActionList.message.originThread.model !== 'mail.channel'">Pay attention: The followers of this document who were notified by email will still be able to read the content of this message and reply to it.</small>
                <t t-set-slot="buttons" t-slot-scope="dialog">
                    <button class="btn btn-primary" t-on-click="(ev) => messageActionList.onClickConfirmDelete(ev)">Delete</button>
                    <button class="btn btn-secondary" t-on-click="() => dialog.close()">Cancel</button>
                </t>
            </t>
        </Dialog>
    </t>
</templates>
