<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mail.ComposerSuggestionList" owl="1">
        <div class="o_ComposerSuggestionList" t-att-class="{ 'o-lowPosition': props.isBelow }">
            <div t-att-class="{ 'dropdown': props.isBelow, 'dropup': !props.isBelow }">
                <div class="o_ComposerSuggestionList_list dropdown-menu show">
                    <t t-foreach="composer.__mfield_mainSuggestedRecordsList(mself)" t-as="record" t-key="record.localId">
                        <ComposerSuggestion
                            composerLocalId="props.composerLocalId"
                            isActive="record === composer.__mfield_activeSuggestedRecord(mself)"
                            modelName="composer.__mfield_suggestionModelName(mself)"
                            recordLocalId="record.localId"
                        />
                    </t>
                    <t t-if="composer.__mfield_mainSuggestedRecordsList(mself).length > 0 and composer.__mfield_extraSuggestedRecordsList(mself).length > 0">
                        <div role="separator" class="dropdown-divider"/>
                    </t>
                    <t t-foreach="composer.__mfield_extraSuggestedRecordsList(mself)" t-as="record" t-key="record.localId">
                        <ComposerSuggestion
                            composerLocalId="props.composerLocalId"
                            isActive="record === composer.__mfield_activeSuggestedRecord(mself)"
                            modelName="composer.__mfield_suggestionModelName(mself)"
                            recordLocalId="record.localId"
                        />
                    </t>
                </div>
            </div>
        </div>
    </t>

</templates>
