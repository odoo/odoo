<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="mail.AttachmentImage" owl="1">
        <div>
            <t t-if="attachmentImage and attachmentImage.attachment">
                <div class="o_AttachmentImage d-flex o-details-overlay position-relative"
                     t-att-class="{
                            'o-composer-attachment': attachmentImage.attachment.isLinkedToComposer,
                            'o-isUploading': attachmentImage.attachment.isUploading,
                            'o-viewable': attachmentImage.attachment.isViewable,
                            'o-attachment-viewable': attachmentImage.attachment.isViewable,
                            }"
                     t-att-title="attachmentImage.attachment.displayName ? attachmentImage.attachment.displayName : undefined"
                     t-att-data-id="attachmentImage.attachment.localId"
                     t-on-click="attachmentImage.onClickImage"
                     t-att-data-mimetype="attachmentImage.attachment.mimetype"
                >
                    <img t-att-src="attachmentImage.imageUrl"/>
                    <t t-if="attachmentImage.attachment.isUploading and attachmentImage.attachment.isLinkedToComposer">
                        <div class="o_AttachmentImageUploading d-flex align-items-center justify-content-center position-absolute" title="Uploading">
                            <i class="fa fa-spin fa-spinner"/>
                        </div>
                    </t>
                    <div class="o_AttachmentImage_imageOverlay d-flex flex-row justify-content-end position-absolute">
                        <div class="o_AttachmentImage_details o_AttachmentImage_imageOverlayDetails d-flex flex-column justify-content-end">
                            <t t-if="attachmentImage.attachment.displayName and props.imageSize !== 'composer'">
                                <div class="o_AttachmentImage_filename overflow-hidden">
                                    <t t-esc="attachmentImage.attachment.displayName"/>
                                </div>
                            </t>
                            <t t-if="attachmentImage.attachment.extension">
                                <div class="o_AttachmentImage_extension text-uppercase">
                                    <t t-esc="attachmentImage.attachment.extension"/>
                                </div>
                            </t>
                        </div>
                        <div class="o_AttachmentImage_actions d-flex flex-column">
                            <t t-if="props.isEditable" t-key="'unlink'">
                                <div class="o_AttachmentImage_action o_AttachmentImage_actionUnlink text-center"
                                     t-att-class="{'o-pretty': attachmentImage.attachment.isLinkedToComposer}" t-on-click="attachmentImage.onClickUnlink" title="Remove"
                                >
                                    <i class="fa fa-times"/>
                                </div>
                            </t>
                            <t t-if="!attachmentImage.attachment.isLinkedToComposer and !attachmentImage.attachment.isUploading and !attachmentImage.attachment.linkPreview" t-key="'download'">
                                <div class="o_AttachmentImage_action o_AttachmentImage_actionDownload text-center" t-on-click="attachmentImage.attachment.onClickDownload" title="Download">
                                    <i class="fa fa-download"/>
                                </div>
                            </t>
                        </div>
                    </div>
                    <t t-if="attachmentImage.hasDeleteConfirmDialog">
                        <AttachmentDeleteConfirmDialog
                            attachmentLocalId="attachmentImage.attachment.localId"
                            t-on-dialog-closed="attachmentImage.onDeleteConfirmDialogClosed"
                        />
                    </t>
                </div>
            </t>
        </div>
    </t>
</templates>
