<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mail.ActivityBoxView" owl="1">
            <div class="o_ActivityBoxView" t-attf-class="{{ className }}" t-ref="root">
                <a href="#" role="button" class="o_ActivityBoxView_title btn d-flex align-items-center mt-4 p-0 w-100 fw-bold" t-att-aria-expanded="isActivityListVisible ? 'true' : 'false'" t-on-click="onClickActivityBoxTitle">
                    <hr class="o_ActivityBoxView_titleLine w-auto flex-grow-1 me-3" />
                    <span class="o_ActivityBoxView_titleText">
                        <i class="fa fa-fw" t-att-class="isActivityListVisible ? 'fa-caret-down' : 'fa-caret-right'"/>
                        Planned activities
                    </span>
                    <t t-if="!isActivityListVisible">
                        <span class="o_ActivityBoxView_titleBadges ms-2">
                            <t t-if="chatter.thread.overdueActivities.length > 0">
                                <span class="o_ActivityBoxView_titleBadge me-1 badge text-bg-danger">
                                    <t t-esc="chatter.thread.overdueActivities.length"/>
                                </span>
                            </t>
                            <t t-if="chatter.thread.todayActivities.length > 0">
                                <span class="o_ActivityBoxView_titleBadge me-1 badge text-bg-warning">
                                    <t t-esc="chatter.thread.todayActivities.length"/>
                                </span>
                            </t>
                            <t t-if="chatter.thread.futureActivities.length > 0">
                                <span class="o_ActivityBoxView_titleBadge me-1 badge text-bg-success">
                                    <t t-esc="chatter.thread.futureActivities.length"/>
                                </span>
                            </t>
                        </span>
                    </t>
                    <hr class="o_ActivityBoxView_titleLine w-auto flex-grow-1 ms-3"/>
                </a>
                <t t-if="isActivityListVisible">
                    <div class="o_ActivityBoxView_activityList">
                        <t t-foreach="activityViews" t-as="activityView" t-key="activityView.localId">
                            <ActivityView className="'o_ActivityBoxView_activity'" record="activityView"/>
                        </t>
                    </div>
                </t>
            </div>
    </t>

</templates>
