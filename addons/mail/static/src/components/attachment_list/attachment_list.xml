<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mail.AttachmentList" owl="1">
        <t t-if="attachmentList">
            <div class="d-flex flex-column justify-content-start" t-attf-class="{{ className() }}" t-ref="root">
                <div class="d-flex flex-grow-1 flex-wrap" t-attf-class="{{ className('partialList', 'partialListImages') }}">
                    <t t-foreach="attachmentList.attachmentImages" t-as="attachmentImage" t-key="attachmentImage.localId">
                        <AttachmentImage className="className('attachment') + ' my-1 me-1 mw-100'" localId="attachmentImage.localId"/>
                    </t>
                </div>
                <div class="d-flex flex-grow-1 flex-wrap justify-content-start m-1" t-attf-class="{{ className('partialList', 'partialListNonImages') }}">
                    <t t-foreach="attachmentList.attachmentCards" t-as="attachmentCard" t-key="attachmentCard.localId">
                        <AttachmentCard className="className('attachment') + ' my-1 me-1 mw-100'" localId="attachmentCard.localId"/>
                    </t>
                </div>
            </div>
        </t>
    </t>

</templates>
