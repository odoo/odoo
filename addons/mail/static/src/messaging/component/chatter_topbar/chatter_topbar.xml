<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mail.messaging.component.ChatterTopbar" owl="1">
        <div class="o_ChatterTopbar">
            <t t-if="chatter">
                <t t-if="chatter.hasThread">
                    <button class="btn btn-link o_ChatterTopbar_buttonSendMessage"
                            type="button"
                            t-att-class="{
                                'o-active': chatter.isComposerVisible and !chatter.isComposerLog,
                            }"
                            t-att-disabled="chatter.isDisabled"
                            t-on-click="_onClickSendMessage">
                        Send message
                    </button>
                    <button class="btn btn-link o_ChatterTopbar_buttonLogNote"
                            type="button"
                            t-att-class="{
                                'o-active': chatter.isComposerVisible and chatter.isComposerLog,
                            }"
                            t-att-disabled="chatter.isDisabled"
                            t-on-click="_onClickLogNote">
                        Log note
                    </button>
                </t>
                <t t-if="chatter.hasActivities">
                    <button class="btn btn-link o_ChatterTopbar_buttonScheduleActivity" type="button" t-att-disabled="chatter.isDisabled" t-on-click="_onClickScheduleActivity">
                        <i class="fa fa-clock-o"/>
                        Schedule activity
                    </button>
                </t>
                <button class="btn btn-link o_ChatterTopbar_buttonAttachments" type="button" t-att-disabled="chatter.isDisabled" t-on-click="_onClickAttachments">
                    <i class="fa fa-paperclip"/>
                    <t t-if="chatter.isDisabled or chatter.thread and chatter.thread.areAttachmentsLoaded">
                        <span class="o_ChatterTopbar_buttonCount o_ChatterTopbar_buttonAttachmentsCount" t-esc="chatter.thread ? chatter.thread.allAttachments.length : 0"/>
                    </t>
                    <t t-else="">
                        <i class="o_ChatterTopbar_buttonAttachmentsCountLoader fa fa-spinner fa-spin"/>
                    </t>
                </button>
                <div class="o-autogrow"/>
                <t t-if="chatter.hasFollowers">
                    <div class="o_ChatterTopbar_rightSection">
                        <div class="o_ChatterTopbar_followers">
                            <button class="btn btn-link o_ChatterTopbar_buttonFollow" type="button" t-att-disabled="chatter.isDisabled" t-on-click="_onClickFollow">
                                Follow
                            </button>
                            <button class="btn btn-link o_ChatterTopbar_buttonFollowers" type="button" t-att-disabled="chatter.isDisabled" t-on-click="_onClickFollowers">
                                <i class="fa fa-user"/>
                                <span class="o_ChatterTopbar_buttonCount o_ChatterTopbar_buttonFollowersCount pl-1">0</span>
                            </button>
                        </div>
                    </div>
                </t>
            </t>
        </div>
    </t>

</templates>
