<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="mail.component.DiscussMobileMailboxSelection" owl="1">
        <div class="o_DiscussMobileMailboxSelection">
            <t t-foreach="storeProps.pinnedMailboxList" t-as="mailbox" t-key="mailbox.localId">
                <button class="o_DiscussMobileMailboxSelection_button btn"
                    t-att-class="{
                        'btn-primary': props.activeThreadLocalId === mailbox.localId,
                        'btn-secondary': props.activeThreadLocalId !== mailbox.localId,
                        'o-active': props.activeThreadLocalId === mailbox.localId,
                    }" t-on-click="_onClick" t-att-data-mailbox-local-id="mailbox.localId" type="button"
                >
                    <t t-esc="mailbox.name"/>
                </button>
            </t>
        </div>
    </t>

</templates>
