/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            scroll conservation on toggle discuss
        [Test/model]
            ChatWindowManagerComponent
        [Test/assertions]
            2
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [0]
                    [Record/models]
                        mail.channel
                    [mail.channel/id]
                        20
                {foreach}
                    {Record/insert}
                        [Record/models]
                            Range
                        [start]
                            0
                        [end]
                            10
                .{as}
                    i
                .{do}
                    {entry}
                        [Record/models]
                            mail.message
                        [mail.message/body]
                            not empty
                        [mail.message/model]
                            mail.channel
                        [mail.message/res_id]
                            20
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            @testEnv
            .{Record/insert}
                []
                    [Record/models]
                        ChatWindowManagerComponent
                []
                    [Record/models]
                        MessagingMenuComponent
            @testEnv
            .{Component/afterNextRender}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{MessagingMenu/messagingMenuComponents}
                    .{Collection/first}
                    .{MessagingMenuComponent/toggler}
            @testEnv
            .{Component/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{UI/click}
                        @testEnv
                        .{Record/findById}
                            [Thread/id]
                                20
                            [Thread/model]
                                mail.channel
                        .{Thread/threadPreviewComponents}
                        .{Collection/first}
                [message]
                    should wait until channel 20 scrolled to its last message after opening it from the messaging menu
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    @thread
                                    .{Thread/threadViews}
                                    .{Collection/first}
                                    .{ThreadView/messageListComponents}
                                    .{Collection/first}
                                    .{web.Element/scrollHeight}
                                    .{-}
                                        @thread
                                        .{Thread/threadViews}
                                        .{Collection/first}
                                        .{ThreadView/messageListComponents}
                                        .{Collection/first}
                                        .{web.Element/clientHeight}
            {Dev/comment}
                Set a scroll position to chat window
            @testEnv
            .{Component/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{Record/update}
                        [0]
                            @testEnv
                            .{Record/findById}
                                [Thread/id]
                                    20
                                [Thread/model]
                                    mail.channel
                            .{Thread/threadViews}
                            .{Collection/first}
                            .{ThreadView/messageListComponents}
                            .{Collection/first}
                        [1]
                            [web.Element/scrollTop]
                                142
                [message]
                    should wait until channel 20 scrolled to 142 after setting this value manually
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    142
            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{Discuss/open}
            @testEnv
            .{Component/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{Discuss/open}
                [message]
                    should wait until channel 20 restored its scroll to 142 after opening discuss
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    142
            {Test/assert}
                []
                    @testEnv
                    .{Record/findById}
                        [Thread/id]
                            20
                        [Thread/model]
                            mail.channel
                    .{Thread/threadViews}
                    .{Collection/first}
                    .{ThreadView/messageListComponents}
                    .{Collection/first}
                    .{MessageListComponent/scrollTop}
                    .{=}
                        142
                []
                    chat window scrollTop should still be the same after closing discuss
`;
