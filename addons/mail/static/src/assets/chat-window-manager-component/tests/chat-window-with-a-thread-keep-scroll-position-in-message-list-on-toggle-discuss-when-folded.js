/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            chat window with a thread: keep scroll position in message list on toggle discuss when folded
        [Test/model]
            ChatWindowManagerComponent
        [Test/assertions]
            2
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [0]
                    [Record/models]
                        mail.channel
                    [mail.channel/id]
                        20
                {foreach}
                    {Record/insert}
                        [Record/models]
                            Range
                        [start]
                            0
                        [end]
                            10
                .{as}
                    i
                .{do}
                    {entry}
                        [Record/models]
                            mail.message
                        [mail.message/body]
                            not empty
                        [mail.message/model]
                            mail.channel
                        [mail.message/res_id]
                            20
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            @testEnv
            .{Record/insert}
                []
                    [Record/models]
                        ChatWindowManagerComponent
                []
                    [Record/models]
                        MessagingMenuComponent
            @testEnv
            .{Component/afterNextRender}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{MessagingMenu/messagingMenuComponents}
                    .{Collection/first}
                    .{MessagingMenuComponent/toggler}
            @testEnv
            .{UI/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{UI/click}
                        @testEnv
                        .{MessagingMenu/messagingMenuComponents}
                        .{Collection/first}
                        .{MessagingMenuComponent/notificationList}
                        .{NotificationListComponent/threadPreview}
                        .{Collection/first}
                [message]
                    should wait until channel 20 scrolled to its last message after opening it from the messaging menu
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    @testEnv
                                    .{Record/findById}
                                        [Thread/id]
                                            20
                                        [Thread/model]
                                            mail.channel
                                    .{Thread/messageListComponents}
                                    .{Collection/first}
                                    .{web.Element/scrollHeight}
                                    .{-}
                                        @testEnv
                                        .{Record/findById}
                                            [Thread/id]
                                                20
                                            [Thread/model]
                                                mail.channel
                                        .{Thread/messageListComponents}
                                        .{Collection/first}
                                        .{web.Element/clientHeight}
            {Dev/comment}
                Set a scroll position to chat window
            @testEnv
            .{UI/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{Record/update}
                        [0]
                            @testEnv
                            .{Record/findById}
                                [Thread/id]
                                    20
                                [Thread/model]
                                    mail.channel
                            .{Thread/messageListComponents}
                            .{Collection/first}
                        [1]
                            [web.Element/scrollTop]
                                142
                [message]
                    should wait until channel 20 scrolled to 142 after setting this value manually
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    142
            {Dev/comment}
                fold chat window
            @testEnv
            .{Component/afterNextRender}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{ChatWindowManager/chatWindows}
                    .{Collection/first}
                    .{ChatWindow/chatWindowComponents}
                    .{Collection/first}
                    .{ChatWindowComponent/header}
            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{Record/update}
                    [0]
                        @testEnv
                        .{Env/discuss}
                    [1]
                        [Discuss/discussView]
                            false
            {Test/assert}
                []
                    @testEnv
                    .{ChatWindowManager/ChatWindowManagerComponents}
                    .{Collection/first}
                    .{ChatWindowManagerComponent/chatWindows}
                    .{Collection/length}
                    .{=}
                        0
                []
                    should not have any chat window after opening discuss

            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{Discuss/open}
            {Dev/comment}
                unfold chat window
            @testEnv
            .{UI/afterEvent}
                [eventName]
                    o-component-message-list-scrolled
                [func]
                    @testEnv
                    .{UI/click}
                        @testEnv
                        .{ChatWindowManager/chatWindows}
                        .{Collection/first}
                        .{ChatWindow/chatWindowComponents}
                        .{Collection/first}
                        .{ChatWindowComponent/header}
                [message]
                    should wait until channel 20 restored its scroll position to the last saved value (142)
                [predicate]
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            scrollTop
                            thread
                        [Function/out]
                            @thread
                            .{Thread/model}
                            .{=}
                                mail.channel
                            .{&}
                                @thread
                                .{Thread/id}
                                .{=}
                                    20
                            .{&}
                                @scrollTop
                                .{=}
                                    142
            {Test/assert}
                []
                    @testEnv
                    .{Record/findById}
                        [Thread/id]
                            20
                        [Thread/model]
                            mail.channel
                    .{Thread/messageListComponents}
                    .{Collection/first}
                    .{web.Element/scrollTop}
                    .{=}
                        142
                []
                    chat window scrollTop should still be the same after closing discuss
`;
