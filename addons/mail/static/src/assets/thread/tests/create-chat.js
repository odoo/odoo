/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            create (chat)
        [Test/model]
            Thread
        [Test/assertions]
            15
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{isFalsy}
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Thread/id]
                        200
                    [Thread/model]
                        mail.channel
                .{isFalsy}

            :channel
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Thread
                    [Thread/channelType]
                        chat
                    [Thread/id]
                        200
                    [Thread/members]
                        @testEnv
                        .{Field/add}
                            {Record/insert}
                                [Record/models]
                                    Partner
                                [Partner/email]
                                    demo@example.com
                                [Partner/id]
                                    5
                                [Partner/imStatus]
                                    online
                                [Partner/name]
                                    Demo
                    [Thread/model]
                        mail.channel
            {Test/assert}
                @channel
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Thread/id]
                        200
                    [Thread/model]
                        mail.channel
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
            {Test/assert}
                @channel
                .{=}
                    @testEnv
                    .{Record/findById}
                        [Thread/id]
                            200
                        [Thread/model]
                            mail.channel
            {Test/assert}
                @channel
                .{Thread/model}
                .{=}
                    mail.channel
            {Test/assert}
                @channel
                .{Thread/channelType}
                .{=}
                    chat
            {Test/assert}
                @channel
                .{Thread/id}
                .{=}
                    200
            {Test/assert}
                @channel
                .{Thread/correspondent}
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{=}
                    @channel
                    .{Thread/correspondent}
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{Partner/email}
                .{=}
                    demo@example.com
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{Partner/id}
                .{=}
                    5
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{Partner/imStatus}
                .{=}
                    online
            {Test/assert}
                @testEnv
                .{Record/findById}
                    [Partner/id]
                        5
                .{Partner/name}
                .{=}
                    Demo
`;
