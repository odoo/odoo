/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Dev/comment}
        The command that will be executed.
    {Record/insert}
        [Record/models]
            Field
        [Field/name]
            help
        [Field/model]
            ChannelCommand
        [Field/type]
            attr
`;
