/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            mobile: auto-select "Inbox" when discuss had channel as active thread
        [Test/model]
            DiscussComponent
        [Test/assertions]
            3
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
                    [Env/owlEnv]
                        [browser]
                            [innerHeight]
                                640
                            [innerWidth]
                                360
                        [device]
                            [isMobile]
                                true
            @testEnv
            .{Record/insert}
                [Record/models]
                    mail.channel
                [mail.channel/id]
                    20
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            @testEnv
            .{Record/insert}
                [Record/models]
                    DiscussComponent
            @testEnv
            .{Thread/open}
                @testEnv
                .{Record/findById}
                    [Thread/id]
                        20
                    [Thread/model]
                        mail.channel
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MobileMessagingNavbarComponent
                    .{Collection/first}
                    .{MobileMessagingNavbarComponent/tab}
                    .{Collection/find}
                        {Record/insert}
                            [Record/models]
                                Function
                            [Function/in]
                                tab
                            [Function/out]
                                @tab
                                .{Tab/id}
                                .{=}
                                    channel
                    .{Tab/isActive}
                []
                    'channel' tab should be active initially when loading discuss with channel id as active_id

            @testEnv
            .{Component/afterNextRender}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MobileMessagingNavbarComponent
                    .{Collection/first}
                    .{MobileMessagingNavbarComponent/tab}
                    .{Collection/find}
                        {Record/insert}
                            [Record/models]
                                Function
                            [Function/in]
                                tab
                            [Function/out]
                                @tab
                                .{Tab/id}
                                .{=}
                                    mailbox
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MobileMessagingNavbarComponent
                    .{Collection/first}
                    .{MobileMessagingNavbarComponent/tab}
                    .{Collection/find}
                        {Record/insert}
                            [Record/models]
                                Function
                            [Function/in]
                                tab
                            [Function/out]
                                @tab
                                .{Tab/id}
                                .{=}
                                    mailbox
                    .{Tab/isActive}
                []
                    'mailbox' tab should be selected after click on mailbox tab
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            DiscussMobileMailboxSelectionComponent
                    .{Collection/first}
                    .{DiscussMobileMailboxSelectionComponent/button}
                    .{Collection/find}
                        {Record/insert}
                            [Record/models]
                                Function
                            [Function/in]
                                button
                            [Function/out]
                                @button
                                .{Button/mailbox}
                                .{=}
                                    @testEnv
                                    .{Env/inbox}
                    .{Tab/isActive}
                []
                    'Inbox' mailbox should be auto-selected after click on mailbox tab
`;
