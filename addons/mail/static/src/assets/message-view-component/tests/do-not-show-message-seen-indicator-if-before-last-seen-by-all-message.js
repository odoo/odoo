/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            do not show message seen indicator if before last seen by all message
        [Test/model]
            MessageViewComponent
        [Test/assertions]
            3
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            :currentPartner
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Partner
                    [Partner/displayName]
                        Demo User
                    [Partner/id]
                        @testEnv
                        .{Env/currentPartner}
                        .{Partner/id}
            :thread
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Thread
                    [Thread/channelType]
                        chat
                    [Thread/id]
                        11
                    [Thread/messageSeenIndicators]
                        @testEnv
                        .{Record/insert}
                            [Record/models]
                                MessageSeenIndicator
                            [MessageSeenIndicator/message]
                                @testEnv
                                .{Record/insert}
                                    [Record/models]
                                        Message
                                    [Message/id]
                                        99
                    [Thread/model]
                        mail.channel
            :threadViewer
                @testEnv
                .{Record/insert}
                    [Record/models]
                        ThreadViewer
                    [ThreadViewer/hasThreadView]
                        true
                    [ThreadViewer/thread]
                        @thread
            :lastSeenMessage
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Message
                    [Message/author]
                        @currentPartner
                    [Message/body]
                        <p>You already saw me</p>
                    [Message/id]
                        100
                    [Message/originThread]
                        @thread
            :message
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Message
                    [Message/author]
                        @currentPartner
                    [Message/body]
                        <p>Test</p>
                    [Message/id]
                        99
                    [Message/originThread]
                        @thread
            @testEnv
            .{Record/insert}
                []
                    [Record/models]
                        ThreadPartnerSeenInfo
                    [ThreadPartnerSeenInfo/lastSeenMessage]
                        @lastSeenMessage
                    [ThreadPartnerSeenInfo/partner]
                        @testEnv
                        .{Env/currentPartner}
                    [ThreadPartnerSeenInfo/thread]
                        @thread
                []
                    [Record/models]
                        ThreadpartnerSeenInfo
                    [ThreadPartnerSeenInfo/lastSeenMessage]
                        @lastSeenMessage
                    [ThreadPartnerSeenInfo/partner]
                        @testEnv
                        .{Record/insert}
                            [Record/models]
                                Partner
                            [Partner/id]
                                100
                    [ThreadPartnerSeenInfo/thread]
                        @thread
            @testEnv
            .{Record/insert}
                [Record/models]
                    MessageViewComponent
                [MessageViewComponent/messageView]
                    @message
                    .{Message/messageView}
            {Test/assert}
                []
                    @message
                    .{Message/messageComponents}
                    .{Collection/length}
                    .{=}
                        1
                []
                    should display a message component
            {Test/assert}
                []
                    @message
                    .{Message/messageComponents}
                    .{Collection/first}
                    .{MessageViewComponent/seenIndicator}
                []
                    message component should have a message seen indicator
            {Test/assert}
                []
                    @message
                    .{Message/messageComponents}
                    .{Collection/first}
                    .{MessageViewComponent/seenIndicator}
                    .{MessageSeenIndicatorComponent/icon}
                    .{Collection/length}
                    .{=}
                        0
                []
                    message component should not have any check (V)
`;
