/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Action
        [Action/name]
            Partner/convertData
        [Action/params]
            data
                [type]
                    Object
        [Action/returns]
            Object
        [Action/behavior]
            :data2
                {Record/insert}
                    [Record/models]
                        Object
            {if}
                @data
                .{Dict/hasKey}
                    active
                .{then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Partner/active]
                                @data
                                .{Dict/get}
                                    active
            {if}
                @data
                .{Dict/hasKey}
                    country
            .{then}
                {if}
                    @data
                    .{Dict/get}
                        country
                    .{isFalsy}
                .{then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Partner/country]
                                {Record/empty}
                .{else}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Partner/country]
                                {Record/insert}
                                    [Record/models]
                                        Country
                                    [Country/id]
                                        @data
                                        .{Dict/get}
                                            country
                                        .{Collection/first}
                                    [Country/name]
                                        @data
                                        .{Dict/get}
                                            country
                                        .{Collection/second}
            {if}
                @data
                .{Dict/hasKey}
                    display_name
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Partner/displayName]
                            @data
                            .{Dict/get}
                                display_name
            {if}
                @data
                .{Dict/hasKey}
                    email
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Partner/email]
                            @data
                            .{Dict/get}
                                email
            {if}
                @data
                .{Dict/hasKey}
                    id
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Partner/id]
                            @data
                            .{Dict/get}
                                id
            {if}
                @data
                .{Dict/hasKey}
                    im_status
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Partner/imStatus]
                            data
                            .{Dict/get}
                                im_status
            {if}
                @data
                .{Dict/hasKey}
                    name
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Partner/name]
                            @data
                            .{Dict/get}
                                name
            {Dev/comment}
                relation
            {if}
                @data
                .{Dict/hasKey}
                    user_id
            .{then}
                {if}
                    @data
                    .{Dict/get}
                        user_id
                    .{isFalsy}
                .{then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Partner/user]
                                {Record/empty}
                .{else}
                    {if}
                        {Array/isArray}
                            data
                            .{Dict/get}
                                user_id
                    .{then}
                        :user
                            [Record/models]
                                User
                            [User/displayName]
                                @data
                                .{Dict/get}
                                    user_id
                                .{Collection/second}
                            [User/id]
                                @data
                                .{Dict/get}
                                    user_id
                                .{Collection/first}
                    .{else}
                        :user
                            [Record/models]
                                User
                            [User/id]
                                @data
                                .{Dict/get}
                                    user_id
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Partner/isInternalUser]
                                @data
                                .{Dict/get}
                                    is_internal_user
                            [Partner/user]
                                {Record/insert}
                                    @user
            @data2
`;
