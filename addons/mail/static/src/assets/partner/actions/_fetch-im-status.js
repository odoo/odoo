/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Action
        [Action/name]
            Partner/_fetchImStatus
        [Action/behavior]
            :partnerIds
                {Record/insert}
                    [Record/models]
                        Collection
            {foreach}
                {Record/all}
                    [Record/models]
                        Partner
            .{as}
                partner
            .{do}
                {if}
                    @partner
                    .{Partner/imStatus}
                    .{!=}
                        im_partner
                    .{&}
                        @partner
                        .{Partner/id}
                        .{>}
                            0
                .{then}
                    {Collection/push}
                        [0]
                            @partnerIds
                        [1]
                            @partner
                            .{Partner/id}
            {if}
                @partnerIds
                .{Collection/length}
                .{=}
                    0
            .{then}
                {break}
            :dataList
                @env
                .{Env/owlEnv}
                .{Dict/get}
                    services
                .{Dict/get}
                    rpc
                .{Function/call}
                    [0]
                        [route]
                            /longpolling/im_status
                        [params]
                            [partner_ids]
                                @partnerIds
                    [1]
                        [shadow]
                            true
            {Record/insert}
                [Record/models]
                    Partner
                @dataList
                .{Collection/map}
                    {Record/insert}
                        [Record/models]
                            Function
                        [Function/in]
                            item
                        [Function/out]
                            [Partner/id]
                                @item
                                .{Dict/get}
                                    id
                            [Partner/imStatus]
                                @item
                                .{Dict/get}
                                    im_status
`;
