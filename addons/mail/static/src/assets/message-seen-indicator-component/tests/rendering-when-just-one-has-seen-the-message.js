/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            rendering when just one has seen the message
        [Test/model]
            MessageSeenIndicatorComponent
        [Test/assertions]
            3
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            :thread
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Thread
                    [Thread/id]
                        1000
                    [Thread/messageSeenIndicators]
                        @testEnv
                        .{Record/insert}
                            [Record/models]
                                MessageSeenIndicator
                            [MessageSeenIndicator/channelId]
                                1000
                            [MessageSeenIndicator/messageId]
                                100
                    [Thread/model]
                        mail.channel
                    [Thread/partnerSeenInfos]
                        []
                            @testEnv
                            .{Record/insert}
                                [Record/models]
                                    ThreadPartnerSeenInfo
                                [ThreadPartnerSeenInfo/channelId]
                                    1000
                                [ThreadPartnerSeenInfo/lastFetchedMessage]
                                    @testEnv
                                    .{Record/insert}
                                        [Record/models]
                                            Message
                                        [Message/id]
                                            100
                                [ThreadPartnerSeenInfo/lastSeenMessage]
                                    @testEnv
                                    .{Record/insert}
                                        [Record/models]
                                            Message
                                        [Message/id]
                                            100
                                [ThreadPartnerSeenInfo/partnerId]
                                    10
                        []
                            @testEnv
                            .{Record/insert}
                                [Record/models]
                                    ThreadPartnerSeenInfo
                                [ThreadPartnerSeenInfo/channelId]
                                    1000
                                [ThreadPartnerSeenInfo/lastFetchedMessage]
                                    @testEnv
                                    .{Record/insert}
                                        [Record/models]
                                            Message
                                        [Message/id]
                                            99
                                [ThreadPartnerSeenInfo/partnerId]
                                    100
            :message
                @testEnv
                .{Record/insert}
                    [Record/models]
                        Message
                    [Message/author]
                        @testEnv
                        .{Record/insert}
                            [Record/models]
                                Partner
                            [Partner/displayName]
                                Demo User
                            [Partner/id]
                                @testEnv
                                .{Env/currentPartner}
                                .{Partner/id}
                    [Message/body]
                        <p>Test</p>
                    [Message/id]
                        100
                    [Message/originThread]
                        @thread
            @testEnv
            .{Record/insert}
                [Record/models]
                    MessageSeenIndicatorComponent
                [MessageSeenIndicatorComponent/message]
                    @message
                [MessageSeenIndicatorComponent/thread]
                    @thread
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MessageSeenIndicatorComponent
                    .{Collection/length}
                    .{=}
                        1
                []
                    should display a message seen indicator component
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MessageSeenIndicatorComponent
                    .{Collection/first}
                    .{MessageSeenIndicatorComponent/messageSeenIndicator}
                    .{MessageSeenIndicator/hasEveryoneSeen}
                    .{isFalsy}
                []
                    indicator component should not be considered as all seen
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            MessageSeenIndicatorComponent
                    .{Collection/first}
                    .{MessageSeenIndicatorComponent/icon}
                    .{Collection/length}
                    .{=}
                        2
                []
                    should display two seen indicator icon
`;
