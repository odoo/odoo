/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Dev/comment}
        States the OWL ref of the "dialog" window.
    {Record/insert}
        [Record/models]
            Field
        [Field/name]
            dialogRef
        [Field/model]
            Attachment
        [Field/type]
            attr
        [Field/target]
            Element
`;
