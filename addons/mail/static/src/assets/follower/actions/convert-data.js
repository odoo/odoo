/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Action
        [Action/name]
            Follower/convertData
        [Action/params]
            data
        [Action/behavior]
            :data2
                {Record/insert}
                    [Record/models]
                        Dict
            {if}
                @data
                .{Dict/hasKey}
                    id
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Follower/id]
                            @data
                            .{Dict/get}
                                id
            {if}
                @data
                .{Dict/hasKey}
                    is_active
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Follower/isActive]
                            @data
                            .{Dict/get}
                                is_active
            {if}
                @data
                .{Dict/hasKey}
                    is_editable
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Follower/isEditable]
                            @data
                            .{Dict/get}
                                is_editable
            {if}
                @data
                .{Dict/hasKey}
                    partner_id
            .{then}
                {if}
                    @data
                    .{Dict/get}
                        partner_id
                    .{isFalsy}
                .{then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Follower/partner]
                                {Record/empty}
                .{else}
                    :partnerData
                        {Record/insert}
                            [Record/models]
                                Partner
                            [Partner/displayName]
                                @data
                                .{Dict/get}
                                    display_name
                            [Partner/email]
                                @data
                                .{Dict/get}
                                    email
                            [Partner/id]
                                @data
                                .{Dict/get}
                                    partner_id
                            [Partner/name]
                                @data
                                .{Dict/get}
                                    name
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Follower/partner]
                                @partnerData
            {if}
                @data
                .{Dict/get}
                    partner
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Follower/partner]
                            {Record/insert}
                                [Record/models]
                                    Partner
                                {Follower/convertData}
                                    @data
                                    .{Dict/get}
                                        partner
            @data2
`;
