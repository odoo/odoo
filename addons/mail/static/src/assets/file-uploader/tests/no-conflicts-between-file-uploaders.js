/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Test
        [Test/name]
            no conflicts between file uploaders
        [Test/model]
            FileUploader
        [Test/assertions]
            2
        [Test/scenario]
            :testEnv
                {Record/insert}
                    [Record/models]
                        Env
            @testEnv
            .{Record/insert}
                [Record/models]
                    Server
                [Server/data]
                    @record
                    .{Test/data}
            @testEnv
            .{Record/insert}
                []
                    [Record/models]
                        res.partner
                    [res.partner/id]
                        100
                []
                    [Record/models]
                        res.partner
                    [res.partner/id]
                        101
            :firstChatterContainerComponent
                @testEnv
                .{Record/insert}
                    [Record/models]
                        ChatterContainerComponent
                    [ChatterContainerComponent/threadId]
                        100
                    [ChatterContainerComponent/threadModel]
                        res.partner
            :secondChatterContainerComponent
                @testEnv
                .{Record/insert}
                    [Record/models]
                        ChatterContainerComponent
                    [ChatterContainerComponent/threadId]
                        101
                    [ChatterContainerComponent/threadModel]
                        res.partner
            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            ChatterTopbarComponent
                    .{Collection/first}
                    .{ChatterTopbarComponent/buttonAttachments}
                @testEnv
                .{UI/click}
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            ChatterTopbarComponent
                    .{Collection/second}
                    .{ChatterTopbarComponent/buttonAttachments}
            :file1
                @testEnv
                .{Record/insert}
                    [Record/models]
                        web.File
                    [web.File/content]
                        hello, world
                    [web.File/contentType]
                        text/plain
                    [web.File/name]
                        text1.txt
            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{UI/inputFiles}
                    [0]
                        @firstChatterContainerComponent
                        .{ChatterContainerComponent/chatter}
                        .{Chatter/attachmentBoxView}
                        .{AttachmentBoxView/fileUploader}
                        .{FileUploader/fileInput}
                    [1]
                        @file1
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            Attachment
                    .{Collection/length}
                    .{=}
                        1
                []
                    Uploaded file should be the only attachment created

            :file2
                @testEnv
                .{Record/insert}
                    [Record/models]
                        web.File
                    [web.File/content]
                        hello, world
                    [web.File/contentType]
                        text/plain
                    [web.File/name]
                        text2.txt
            @testEnv
            .{UI/afterNextRender}
                @testEnv
                .{UI/inputFiles}
                    [0]
                        @secondChatterContainerComponent
                        .{ChatterContainerComponent/chatter}
                        .{Chatter/attachmentBoxView}
                        .{AttachmentBoxView/fileUploader}
                        .{FileUploader/fileInput}
                    [1]
                        @file2
            {Test/assert}
                []
                    @testEnv
                    .{Record/all}
                        [Record/models]
                            Attachment
                    .{Collection/length}
                    .{=}
                        2
                []
                    Uploaded file should be the only attachment added
`;
