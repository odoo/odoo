<templates>
    <t t-name="mail.EmailHtmlConverterReferenceIframe">
        <!-- The iframe dimensions are set to trigger media queries 
         for the "desktop" version of the email -->
        <iframe t-att-sandbox="isBrowserSafari() ? 'allow-same-origin allow-scripts' : 'allow-same-origin'"
            aria-hidden="true"
            t-attf-style="
                display: block !important;
                {{ isVisible ? `` : `visibility: hidden !important;` }}
                {{ isVisible ? `` : `pointer-events: none !important;` }}
                {{ isVisible ? `` : `position: absolute !important;` }}
                top: {{ top or `0` }} !important;
                left: {{ left or `-9999px` }} !important;
                width: {{ width or `1320px` }} !important;
                min-width: {{ width or `1320px` }} !important;
                height: {{ height or `1000px` }} !important;
                min-height: {{ height or `1000px` }} !important;
            "/>
    </t>
    <t t-name="mail.EmailHtmlConverterReference">
        <div style="width: 100% !important"/>
    </t>
    <t t-name="mail.EmailHtmlConverterHead">
        <meta http-equiv="Content-Security-Policy" content="script-src 'none';"/>
        <meta charset="utf-8"/>
        <meta name="viewport" content="width=device-width, initial-scale=1, user-scalable=no"/>
    </t>
</templates>
