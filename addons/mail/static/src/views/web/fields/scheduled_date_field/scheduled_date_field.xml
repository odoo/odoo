<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="mail.ScheduledDateField">
    <button t-attf-class="btn btn-light #{this.scheduledDate ? 'text-info' : ''}" data-hotkey="s" t-on-click="this.onClick" title="Send Later">
        <i class="fa fa-clock-o"/>
        <span t-if="this.scheduledDate">
            Sending <t t-out="this.scheduledDate.toLocaleString(this.dateTimeFormat)"/>
        </span>
    </button>
</t>
</templates>
