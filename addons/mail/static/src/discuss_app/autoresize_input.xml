<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

<t t-name="mail.AutoresizeInput" owl="1">
    <input
        class="o-mail-AutoresizeInput text-truncate px-1 border-1 bg-view border-transparent"
        t-attf-class="{{ props.className }}"
        t-attf-placeholder="{{ props.placeholder }}"
        t-att-disabled="!props.enabled"
        t-att-title="state.value"
        t-model="state.value"
        t-on-keydown="onKeydownInput"
        t-ref="input"
        type="text"
    />
</t>

</templates>
