<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!-- wizard view -->
        <record model="ir.ui.view" id="mail_followers_edit_form">
            <field name="name">Edit Followers</field>
            <field name="model">mail.followers.edit</field>
            <field name="arch" type="xml">
                <form string="Edit Followers">
                    <group>
                        <field name="res_model" invisible="1"/>
                        <field name="res_ids" invisible="1"/>
                        <field name="operation" widget="radio" options="{'horizontal': true}"/>
                        <field name="partner_ids" widget="many2many_tags_avatar"
                                placeholder="Contacts to add or remove..."
                                options="{'no_quick_create': True}"
                                context="{'show_email': True, 'form_view_ref': 'base.view_partner_simple_form'}"/>
                        <field name="notify" invisible="operation == 'remove'"/>
                        <field name="message" invisible="not notify"
                               options="{'style-inline': true, 'no-attachment': true}"
                               class="oe-bordered-editor"/>
                    </group>
                    <footer>
                        <button string="Confirm"
                            name="edit_followers" type="object" class="btn-primary" data-hotkey="q"/>
                        <button string="Cancel" class="btn-secondary" special="cancel" data-hotkey="x" />
                    </footer>
                </form>
            </field>
        </record>
    </data>
</odoo>
