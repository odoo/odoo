<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="res_groups_privilege_lunch" model="res.groups.privilege">
        <field name="name">Lunch</field>
        <field name="sequence">16</field>
        <field name="category_id" ref="base.module_category_human_resources" />
    </record>

    <record id="group_lunch_user" model="res.groups">
        <field name="name">User : Order your meal</field>
        <field name="sequence">10</field>
        <field name="privilege_id" ref="res_groups_privilege_lunch" />
    </record>

    <record id="group_lunch_manager" model="res.groups">
        <field name="name">Administrator</field>
        <field name="sequence">20</field>
        <field name="privilege_id" ref="res_groups_privilege_lunch" />
        <field
            name="comment">Be able to create new products, cashmoves and to confirm or cancel orders.</field>
        <field name="implied_ids" eval="[Command.link(ref('group_lunch_user'))]" />
        <field
            name="user_ids"
            eval="[Command.link(ref('base.user_root')), Command.link(ref('base.user_admin'))]" />
    </record>

</odoo>
