<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="lunch_product_view_search" model="ir.ui.view">
        <field name="name">lunch.product.search</field>
        <field name="model">lunch.product</field>
        <field name="arch" type="xml">
            <search string="Product Search">
                <field name="name"/>
                <field name="category_id"/>
                <field name="supplier_id"/>
                <field name="description"/>
                <filter name="inactive" string="Archived" domain="[('active','=',False)]"/>
                <group expand="0" string="Group By">
                    <filter name="supplier" string="Vendor" context="{'group_by': 'supplier_id'}"/>
                    <filter name="category" string="Product Category" context="{'group_by': 'category_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="lunch_product_view_tree" model="ir.ui.view">
        <field name="name">lunch.product.tree</field>
        <field name="model">lunch.product</field>
        <field name="arch" type="xml">
            <tree string="Products Tree">
                <field name="name"/>
                <field name="category_id"/>
                <field name="supplier_id"/>
                <field name="description"/>
                <field name="price"/>
            </tree>
        </field>
    </record>

    <record id="lunch_product_view_form" model="ir.ui.view">
        <field name="name">lunch.product.form</field>
        <field name="model">lunch.product</field>
        <field name="arch" type="xml">
            <form string="Products Form">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object"
                            class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <field name="image" widget="image" class="oe_avatar" options="{'preview_image': 'image_medium'}"/>
                    <label for="name"/>
                    <field name='name'/>
                    <div>
                        <field name="is_topping"/>
                        <label for="is_topping"/>
                    </div>
                    <group>
                        <field name='category_id' attrs="{'invisible': [('is_topping', '=', True)], 'required': [('is_topping', '=', False)]}"/>
                        <field name='supplier_id'/>
                        <field name='price'/>
                    </group>
                    <notebook>
                        <page string="Description" name="description">
                            <label for='description'/>
                            <field name='description'/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_lunch_product_kanban" model="ir.ui.view">
        <field name="name">lunch.product.kanban</field>
        <field name="model">lunch.product</field>
        <field name="arch" type="xml">
            <kanban js_class="lunch_kanban" create="0" class="o_kanban_mobile">
                <field name="id"/>
                <field name="name"/>
                <field name="category_id"/>
                <field name="supplier_id"/>
                <field name="description"/>
                <field name="price"/>
                <field name="image_small"/>
                <field name="already_ordered"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="oe_kanban_global_click o_kanban_record_has_image_fill">
                            <div class="o_kanban_image_fill_left d-none d-md-block flex-grow-0"
                                t-attf-style="background-image:url('#{kanban_image('lunch.product', 'image_medium', record.id.raw_value)}')"/>
                            <div class="d-flex flex-column flex-grow-1">
                                <div class="o_kanban_record_top d-flex flex-row">
                                    <div class="o_kanban_record_headings flex-grow-1">
                                        <i t-if="record.already_ordered.raw_value" class="fa fa-heart mr-1 o_pink"/><strong class="o_kanban_record_title"><t t-esc="record.name.value"/></strong>
                                    </div>
                                    <span class="badge badge-pill">
                                        Price: <t t-esc="record.price.value"/>
                                    </span>
                                </div>
                                <div class="o_kanban_record_bottom d-flex flex-column">
                                    <div>
                                        <t t-esc="record.supplier_id.value"/>
                                    </div>
                                    <div>
                                        <t t-esc="record.description.value"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="lunch_product_category_view_tree" model="ir.ui.view">
        <field name="name">Product category Tree</field>
        <field name="model">lunch.product.category</field>
        <field name="arch" type="xml">
            <tree string="Products List" editable="bottom">
                <field name='name' string="Product Category: "/>
            </tree>
        </field>
    </record>

    <record id="lunch_product_category_view_form" model="ir.ui.view">
        <field name="name">Product category Form</field>
        <field name="model">lunch.product.category</field>
        <field name="arch" type="xml">
            <form string="Products Form">
                <sheet>
                    <group>
                        <field name='name' string="Product Category: "/>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="lunch_product_action" model="ir.actions.act_window">
        <field name="name">Products</field>
        <field name="res_model">lunch.product</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new product for lunch
          </p><p>
            A product is defined by its name, category, price and vendor.
          </p>
        </field>
    </record>

    <record id="lunch_product_action_order" model="ir.actions.act_window">
        <field name="name">Order</field>
        <field name="res_model">lunch.product</field>
        <field name="view_mode">kanban</field>
        <field name="domain">[('is_topping', '=', False), ('supplier_id.available_today', '=', True)]</field>
    </record>

    <record id="lunch_product_category_action" model="ir.actions.act_window">
        <field name="name">Product Categories</field>
        <field name="res_model">lunch.product.category</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Create a new lunch category
          </p><p>
            Here you can access all categories for the lunch products.
          </p>
        </field>
    </record>
</odoo>
