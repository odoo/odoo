<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_resource_calendar_search" model="ir.ui.view">
        <field name="name">resource.calendar.search</field>
        <field name="model">resource.calendar</field>
        <field name="arch" type="xml">
            <search string="Search Working Time">
               <field name="name" string="Working Time"/>
               <field name="company_id" groups="base.group_multi_company"/>
               <separator/>
               <filter string="Archived" name="inactive" domain="[('active', '=', False)]"/>
               <filter string="Partial working schedules" name="partial_working_schedules" domain="[('work_time_rate', '&lt;', 1.0)]"/>
               <group>
                    <filter name="groupby_company" context="{'group_by': 'company_id'}"/>
                </group>
           </search>
        </field>
    </record>

    <record id="resource_calendar_form" model="ir.ui.view">
        <field name="name">resource.calendar.form</field>
        <field name="model">resource.calendar</field>
        <field name="arch" type="xml">
            <form string="Working Time" class="o_resource_form">
                <sheet string="Working Time" class="mw-100 pb-2 d-flex flex-grow-1">
                    <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                    <div class="oe_button_box" name="button_box">
                        <button name="%(resource_calendar_leaves_action_from_calendar)d" type="action"
                                icon="fa-plane"
                                class="oe_stat_button"
                                groups="base.group_no_one">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">Time Off</span>
                                </div>
                        </button>
                        <button name="%(resource_employee_action_from_calendar)d" type="action"
                                icon="fa-cogs"
                                class="oe_stat_button"
                                groups="base.group_user">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">
                                        <field name="employees_count" string="Employees" widget="statinfo"/>
                                    </span>
                                </div>
                        </button>
                        <button name="%(resource_resource_action_from_calendar)d" type="action"
                                icon="fa-cogs"
                                class="oe_stat_button"
                                groups="base.group_user">
                                <div class="o_field_widget o_stat_info">
                                    <span class="o_stat_text">
                                        <field name="work_resources_count" string="Work Resources" widget="statinfo"/>
                                    </span>
                                </div>
                        </button>
                    </div>
                    <div class="alert alert-warning text-start o_form_header" 
                        role="alert" 
                        invisible="not is_duplicate_schedule">
                        <span>Some Working Schedules have the same calendar as this one → </span>
                        <button name="action_view_duplicate_schedules" 
                                type="object" 
                                class="btn btn-link p-0 align-baseline fw-bold" 
                                string="See Working Schedules"/>
                    </div>
                    <group name="resource_header" col="2" class="align-items-end">
                        <group>
                            <field nolabel="True" class="h1" name="name"/>
                        </group>
                        <group>
                            <field name="company_id" options="{'no_create': 1, 'no_quick_create': 1}"
                                   placeholder="Select a company for this working scheduled" domain="[('id', 'in', allowed_company_ids)]"
                                   invisible="context.get('allowed_company_ids', []).length &lt; 2"/>
                        </group>
                    </group>
                    <group name="resource_details">
                        <group style="grid-template-columns: fit-content(50%) minmax(0, 1fr);" col="2">
                            <field name="calendar_type" widget="calendar_type_confirm_radio" options="{'horizontal': true}"/>
                            <field name="days_per_week" style="width: 10ch;" widget="float" digits="[16, 2]" options="{'hide_trailing_zeros': true}" readonly="calendar_type == 'fixed'"/>
                        </group>
                        <group col="2">
                            <label for="hours_per_week"/>
                            <span class="d-flex align-items-baseline">
                                <field name="hours_per_week" nolabel="1" style="width: 10ch; flex: none;" widget="float_time" readonly="calendar_type == 'fixed'"/>
                                <span class="text-muted ms-2">(</span>
                                <field name="hours_per_day" nolabel="1" widget="float_time" readonly="1" class="text-muted" style="width: max-content; flex: none;"/>
                                <span class="text-muted">/d</span><span class="text-muted">)</span>
                            </span>
                        </group>
                    </group>
                    <field name="attendance_ids" widget="resource_calendar_attendance_filtered_one2many" domain_filter="[('date', '=', False)]"
                           invisible="calendar_type != 'fixed'"/>
                    <field nolabel="1"  name="attendance_ids" widget="resource_calendar_attendance_calendar_one2many"
                           class="flex-grow-1" invisible="calendar_type != 'variable'"/>
                </sheet>
            </form>
        </field>
    </record>

    <record id="resource_calendar_form_js" model="ir.ui.view">
        <field name="name">resource.calendar.form.js</field>
        <field name="model">resource.calendar</field>
        <field name="inherit_id" ref="resource.resource_calendar_form"/>
        <field name="arch" type="xml">
            <xpath expr="//form" position="attributes">
                <attribute name="js_class">resource_calendar_form</attribute>
            </xpath>
        </field>
    </record>

    <record id="view_resource_calendar_tree" model="ir.ui.view">
        <field name="name">resource.calendar.list</field>
        <field name="model">resource.calendar</field>
        <field name="arch" type="xml">
            <list string="Working Time">
                <field name="name" string="Working Time"/>
                <field name="hours_per_week" string="Schedule Total Time" optional="hide"/>
                <field name="work_time_rate" widget="percentage"/>
                <field name="full_time_required_hours" widget="float_time" optional="hide"/>
                <field name="employees_count" string="# Employees" optional="hide"/>
                <field name="work_resources_count" string="# Work Resources" optional="hide"/>
                <field name="company_id" groups="base.group_multi_company" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="action_resource_calendar_form" model="ir.actions.act_window">
        <field name="name">Working Schedules</field>
        <field name="res_model">resource.calendar</field>
        <field name="view_mode">list,form</field>
        <field name="domain">['|', ('company_id', '=', False), ('company_id', 'in', allowed_company_ids)]</field>
        <field name="view_id" eval="False"/>
        <field name="search_view_id" ref="view_resource_calendar_search"/>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Define working hours and time table that could be scheduled to your project members
          </p>
        </field>
    </record>
</odoo>
