<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_resource_calendar_attendance_tree" model="ir.ui.view">
        <field name="name">resource.calendar.attendance.list</field>
        <field name="model">resource.calendar.attendance</field>
        <field name="arch" type="xml">
            <list string="Working Time" editable="bottom">
                <field name="sequence" widget="handle"/>
                <field name="date" column_invisible="True"/>
                <field name="display_type" column_invisible="True"/>
                <field name="dayofweek"/>
                <field name="duration_hours" sum="Hours per Week" force_save="1" widget="float_time"/>
                <field name="hour_from" widget="float_time" optional="hidden" decoration-muted="hour_from == hour_to"/>
                <field name="hour_to" widget="float_time" optional="hidden" decoration-muted="hour_from == hour_to"/>
                <field name="week_type" readonly="1" force_save="1" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="view_resource_calendar_attendance_form" model="ir.ui.view">
        <field name="name">resource.calendar.attendance.form</field>
        <field name="model">resource.calendar.attendance</field>
        <field name="arch" type="xml">
            <form string="Working Time">
                <sheet>
                <group>
                    <field name="dayofweek"/>
                    <field name="week_type"/>
                    <label for="hour_from" string="Hours"/>
                    <div id="resource_attendance_hours" class="o_row">
                        <field name="hour_from" widget="float_time" decoration-muted="hour_from == hour_to"/> -
                        <field name="hour_to" widget="float_time" decoration-muted="hour_from == hour_to"/>
                    </div>
                </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_resource_calendar_multi_create_form" model="ir.ui.view">
        <field name="name">resource.calendar.multi.create.form</field>
        <field name="model">resource.calendar.attendance</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <form>
                <group>
                    <field string="Duration" name="duration_hours" widget="float_time"/>
                    <label string="Time" for="hour_from"/>
                    <span class="d-flex align-items-baseline gap-2">
                        <field name="hour_from" nolabel="1" class="o_hr_narrow_field" widget="float_time"/>
                        <i class="fa fa-long-arrow-right" title="to"/>
                        <field name="hour_to" nolabel="1" class="o_hr_narrow_field" widget="float_time"/>
                    </span>
                </group>
            </form>
        </field>
    </record>

    <record id="view_resource_calendar_attendance_calendar" model="ir.ui.view">
        <field name="name">resource.calendar.attendance.calendar</field>
        <field name="model">resource.calendar.attendance</field>
        <field name="arch" type="xml">
            <calendar string="Working Time"
                js_class="resource_calendar_attendance_calendar"
                date_start="date"
                date_stop="date"
                all_day="duration_based"
                mode="week"
                scales="week,month"
                month_overflow="0"
                multi_create_view="resource.view_resource_calendar_multi_create_form"
                show_date_picker="0">
                <field name="display_name"/>
                <field name="hour_from"/>
                <field name="hour_to"/>
                <field name="duration_hours"/>
            </calendar>
        </field>
    </record>
</odoo>
