<?xml version="1.0" encoding="utf-8"?>
<odoo>

        <record id="view_users_form" model="ir.ui.view">
            <field name="name">res.users.form</field>
            <field name="model">res.users</field>
            <field name="inherit_id" ref="calendar.res_users_view_form"/>
            <field name="arch" type="xml">
                <xpath expr="//page[@name='calendar']" position='attributes'>
                    <attribute name="invisible">0</attribute>
                </xpath>
                <group name="calendar_accounts" position="inside">
                    <group string="Google Calendar" class="text-break" invisible="share">
                        <field name="google_calendar_rtoken" readonly="1" groups="base.group_system"/>
                        <field name="google_calendar_token" readonly="1"/>
                        <field name="google_calendar_token_validity" readonly="1"/>
                        <field name="google_calendar_sync_token" readonly="1"/>
                        <field name="google_calendar_cal_id" readonly="1"/>
                        <field name="google_calendar_reset_pending" invisible="1" groups="base.group_system"/> <!-- drives the banner + Reset/Cancel button visibility below -->
                        <div colspan="2" class="alert alert-warning mt-3" role="alert"
                             invisible="not google_calendar_reset_pending">
                            A reset of this Google Calendar account is in progress.
                            Re-linking another account is blocked until the pending Google deletions
                            have been processed.
                        </div>
                        <button
                            string="Reset Account" colspan="2" class="btn btn-secondary mt-3"
                            type="action" name="%(google_calendar_reset_account_action)d"
                            context="{'default_user_id': id}"
                            invisible="google_calendar_reset_pending"/>
                        <button
                            string="Cancel Pending Reset" colspan="2" class="btn btn-warning mt-3"
                            type="object" name="action_cancel_google_calendar_reset"
                            confirm="Cancelling will stop the account resetting. Some events may remain on Google's side. Continue?"
                            invisible="not google_calendar_reset_pending"/>
                    </group>
                </group>
            </field>
        </record>

</odoo>
