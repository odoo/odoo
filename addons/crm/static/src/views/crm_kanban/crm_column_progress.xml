<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="crm.ColumnProgress" t-inherit="web.ColumnProgress" t-inherit-mode="primary">
        <xpath expr="//div[hasclass('o_column_progress')]" position="attributes">
            <attribute name="class" remove="w-75" add="w-50" separator=" "/>
        </xpath>
        <b t-else="" position="after">
            <t t-if="showRecurringRevenue">
                <t t-set="rrmAggregate" t-value="getRecurringRevenueGroupAggregate(props.group)"/>
                <AnimatedNumber t-if="rrmAggregate.value"
                    value="rrmAggregate.value"
                    title="rrmAggregate.title"
                    duration="1000"
                    currency="rrmAggregate.currency"
                    animationClass="'o_animated_grow_huge'"
                >
                    <t t-set-slot="prefix">
                        <strong>+</strong>
                    </t>
                </AnimatedNumber>
            </t>
        </b>
        <AnimatedNumber position="before">
            <t t-set="rottingAggregate" t-value="getRottingGroupCount(props.group)"/>
            <div t-if="rottingAggregate.value > 0" 
                class="badge rounded-pill o_crm_lead_rotting_bg ms-3 me-auto" t-att-title="rottingAggregate.title">
                <div class="text-900 text-nowrap cursor-default">
                    <b t-out="rottingAggregate.value"/>
                </div>
            </div>
            <div class="ms-auto"/>
        </AnimatedNumber>
    </t>
</templates>
