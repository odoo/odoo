<?xml version='1.0' encoding='utf-8' ?>
<odoo>
    <record id="payment_provider_form_aba_payway" model="ir.ui.view">
        <field name="name">ABA PayWay Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <group name="provider_credentials" position='inside'>
                <group invisible="code != 'aba_payway'">
                    <field name="payway_merchant_id"
                           required="code == 'aba_payway' and state != 'disabled'"/>
                    <field name="payway_api_key"
                           required="code == 'aba_payway' and state != 'disabled'"
                           password="True"/>
                </group>
            </group>
        </field>
    </record>
</odoo>
