<odoo>
    <record id="rule_estate_property_unit_ga" model="ir.rule">
        <field name="name">GA (Estate Project Unit)</field>
        <field name="model_id" ref="model_estate_property_unit"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_ht_general_admin'))]"/>
    </record>

    <!-- <record id="rule_estate_property_unit_user" model="ir.rule">
        <field name="name">User - (Property Unit)</field>
        <field name="model_id" ref="model_estate_property_unit"/>
        <field name="domain_force">
            [
                (
                    'project_id.sales_ids.user_id',
                    'in',
                    [user.id]
                )
            ]
        </field>
        <field name="groups" eval="[(4, ref('group_ht_sales_user'))]"/>
    </record> -->
</odoo>