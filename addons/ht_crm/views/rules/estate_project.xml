<odoo>
    <record id="rule_estate_project_ga" model="ir.rule">
        <field name="name">GA (Estate Project)</field>
        <field name="model_id" ref="model_estate_project"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_ht_general_admin'))]"/>
    </record>

    <record id="rule_estate_project_exec" model="ir.rule">
        <field name="name">Ban Điều Hành - (Estate Project)</field>
        <field name="model_id" ref="model_estate_project"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_ht_sales_user'))]"/>
    </record>

    <!-- Sales thấy đồng nghiệp và dự án đang phụ trách -->
    <!-- <record id="rule_estate_project_sales_access" model="ir.rule">
        <field name="name">Estate Project - Sales Access</field>
        <field name="model_id" ref="model_estate_project"/>
        <field name="domain_force">
            [
                '|',
                ('pool_ids.employee_id.user_id', 'in', [user.id]),
                ('phonebook_ids.sales_ids.user_id', 'in', [user.id])
            ]
        </field>
        <field name="groups" eval="[(4, ref('group_ht_sales_user'))]"/>
    </record> -->
</odoo>