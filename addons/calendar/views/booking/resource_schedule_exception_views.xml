<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record
        id="resource_schedule_exception_action_show_appointment_resources"
        model="ir.actions.server">
        <field name="name">Bookable Resource Leaves</field>
        <field name="model_id" ref="resource.model_resource_schedule_exception" />
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field
            name="code">action = env['ir.actions.act_window']._get_action_dict_by_xml_id('resource.action_resource_schedule_exception_tree')
resource_ids = env['resource.resource'].sudo().search([('is_bookable', '=', True)])
calendar_ids = resource_ids.calendar_id
# we do not care about company-wide as it cannot be set from the leaves wizard
action['domain'] = ['|',
                    '&amp;', ('resource_id', '=', False),
                        '|', ('calendar_id', 'in', calendar_ids.ids), ('calendar_id', '=', False),
                    ('resource_id', 'in', resource_ids.ids)]</field>
    </record>

</odoo>
