<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="calendar.WeekDays">
        <div class="o_recurrent_weekdays mb-2 d-flex">
        <t t-foreach="weekdays" t-as="day" t-key="day">
                <div t-attf-class="o_calendar_week_days_rounded {{data[day] ? 'text-bg-action' : 'btn-secondary'}} {{props.readonly ? 'o_readonly' : ''}}"
                    role="checkbox"
                    t-att-aria-checked="data[day] ? 'true' : 'false'"
                    t-att-aria-label="props.record.fields[day].string"
                    t-att-data-tooltip="props.record.fields[day].string"
                    t-att-tabindex="props.readonly ? -1 : 0"
                    t-on-click="this.props.readonly? () => {} : () => this.onChange(day)"
                    t-on-keydown="(ev) => this.onKeydown(ev, day)">
                    <t t-out="props.record.fields[day].string[0]"/>
                </div>
            </t>
        </div>
    </t>
</templates>
