<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="calendar.AttendeeCalendarSidePanel" t-inherit="web.CalendarSidePanel" t-inherit-mode="primary">
        <xpath expr="//div[hasclass('o_calendar_sidepanel_content')]" position="inside">
            <div t-if="showActivityFilter" class="o-checkbox form-check position-relative w-100 mt-3 cursor-pointer">
                <input
                    id="show_activities_checkbox"
                    type="checkbox"
                    class="form-check-input"
                    t-att-checked="showActivities"
                    t-on-change="(ev) => this.onActivityFilterInputChange(ev)"
                />
                <span class="fw-bolder text-truncate flex-grow-1" t-out="activityFilterName"/>
            </div>
        </xpath>
    </t>
</templates>
