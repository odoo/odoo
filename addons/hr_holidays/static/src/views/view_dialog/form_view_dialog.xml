<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="hr_holidays.FormViewDialog.buttons" t-inherit="web.FormViewDialog.ToOne.buttons">
        <xpath expr="//button[hasclass('o_form_button_save')]" position="replace">
            <button
                class="btn btn-primary o_form_button_save"
                t-on-click="this.saveButtonClicked"
                t-if="this.canSave"
                data-hotkey="q">
                <span t-if="this.record.isNew">Submit Request</span>
                <span t-else="">Save</span>
            </button>
        </xpath>

        <xpath expr="//button[hasclass('o_form_button_cancel')]" position="replace">
            <button type="button"
                    class="btn btn-secondary o_form_button_cancel"
                    data-hotkey="j"
                    t-on-click.stop="this.discard"
                    t-if="this.canClose">
                Discard
            </button>
        </xpath>

        <xpath expr="//button[hasclass('o_form_button_cancel')]" position="before">
            <button
                t-if="this.canApprove"
                t-attf-class="btn #{this.canSave ? 'btn-secondary': 'btn-primary'}"
                t-on-click="() => this.onClick('action_approve')"
                data-hotkey="x">Approve</button>
            <button
                t-if="this.canValidate"
                t-attf-class="btn #{this.canSave ? 'btn-secondary': 'btn-primary'}"
                t-on-click="() => this.onClick('action_approve')"
                data-hotkey="x">Validate</button>
            <button
                t-if="this.canRefuse"
                t-attf-class="btn #{(this.canValidate or this.canApprove) ? 'btn-secondary' : 'btn-primary'}"
                t-on-click="() =>this.onClick('action_refuse')"
                data-hotkey="x">Refuse</button>
        </xpath>
        <xpath expr="." position="inside">
            <button class="btn btn-danger ms-auto" t-if="this.canDelete" t-on-click="this.deleteRecord" data-hotkey="v">
                Delete Time Off
            </button>
            <button class="btn btn-danger" t-att-class="{'ms-auto': !this.canDelete}" t-if="this.canCancel" t-on-click="this.cancelRecord" data-hotkey="x">
                Cancel Time Off
            </button>
        </xpath>
    </t>
</templates>
