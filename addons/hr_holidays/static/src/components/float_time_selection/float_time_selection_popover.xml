<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="hr_holidays.FloatTimeSelectionPopover">
        <div class="position-relative d-flex align-items-center flex-sm-row gap-1 gap-md-1 p-2">
            <select class="o_hour_selection form-control form-control-sm w-auto" t-on-change="this.onTimeChange" t-model="this.state.selectedHours">
                <t t-foreach="this.availableHours" t-as="unit" t-key="unit[0]">
                    <option class="text-center" t-att-value="unit[0]" t-esc="unit[1]" t-att-selected="unit[0] == this.state.selectedHours"/>
                </t>
            </select>
            <span>:</span>
            <select class="o_hour_selection form-control form-control-sm w-auto" t-on-change="this.onTimeChange" t-model="this.state.selectedMinutes">
                <t t-foreach="this.availableMinutes" t-as="unit" t-key="unit[0]">
                    <option class="text-center" t-att-value="unit[0]" t-esc="unit[1]" t-att-selected="unit[0] == this.state.selectedMinutes"/>
                </t>
            </select>
        </div>
    </t>
</templates>
