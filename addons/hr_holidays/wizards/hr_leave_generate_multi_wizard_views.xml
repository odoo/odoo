<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="hr_leave_generate_multi_wizard_view_form" model="ir.ui.view">
        <field name="model">hr.leave.generate.multi.wizard</field>
        <field name="arch" type="xml">
            <form string="Generate time off for multiple employees">
                <group>
                    <field name="holiday_status_id" class="w-100" />
                    <field
                        name="allocation_mode"
                        string="Mode"
                        groups="hr_holidays.group_hr_holidays_user" />
                    <field
                        name="employee_ids"
                        placeholder="Everyone"
                        widget="many2many_avatar_employee"
                        invisible="allocation_mode != 'employee'" />
                    <field name="company_id" invisible="allocation_mode != 'company'" />
                    <field
                        name="department_id"
                        invisible="allocation_mode != 'department'"
                        required="allocation_mode == 'department'" />
                    <field
                        name="tag_id"
                        invisible="allocation_mode != 'category'"
                        required="allocation_mode == 'category'" />
                    <label for="date_from" string="Dates" />
                    <field
                        name="date_from"
                        widget="daterange"
                        nolabel="1"
                        options="{'end_date_field': 'date_to'}"
                        class="w-50" />
                    <field name="date_to" invisible="1" />
                </group>
                <field
                    name="name"
                    placeholder="e.g. Extra recuperation, Company unavailability, ..."
                    widget="text" />
                <footer>
                    <button
                        name="action_generate_time_off"
                        type="object"
                        string="Generate Time Off"
                        class="btn-primary"
                        accesskey="c" />
                    <button special="cancel" string="Discard" accesskey="j" close="1" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_hr_leave_generate_multi_wizard" model="ir.actions.act_window">
        <field name="name">Multiple Requests</field>
        <field name="res_model">hr.leave.generate.multi.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

</odoo>
