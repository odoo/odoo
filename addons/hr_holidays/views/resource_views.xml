<?xml version='1.0' encoding='UTF-8' ?>
<odoo>
    <!-- UoM on resource calendar -->
    <record id="resource_calendar_form_inherit" model="ir.ui.view">
        <field name="name">work.calendar.form.inherit</field>
        <field name="model">work.calendar</field>
        <field name="inherit_id" ref="work_resource.resource_calendar_form"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="uom_id" groups="product.group_uom"/>
            </field>
        </field>
    </record>

    <!-- Holiday on resource leave -->
    <record id="resource_calendar_leave_form_inherit" model="ir.ui.view">
        <field name="name">work.calendar.leave.form.inherit</field>
        <field name="model">work.calendar.leave</field>
        <field name="inherit_id" ref="work_resource.resource_calendar_leave_form"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="holiday_id"/>
            </field>
        </field>
    </record>

</odoo>
