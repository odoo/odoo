<?xml version="1.0" ?>
<openerp>
    <data>
        <!--Survey send invitation Email template -->
        <record id="email_template_survey" model="email.template">
            <field name="name">Survey - Send by Email</field>
            <field name="email_from">${object.responsible_id and object.responsible_id.company_id and object.responsible_id.company_id.email or object.responsible_id.email or ''}</field>
            <field name="subject">${object.title}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="model_survey"/>
            <field name="auto_delete" eval="True"/>
            <field name="body_html"><![CDATA[
<div style="font-family: 'Lucica Grande', Ubuntu, Arial, Verdana, sans-serif; font-size: 12px; color: rgb(34, 34, 34); background-color: #FFF; ">
    <p>Hello,</p>
    <p>
        Would you please spent some of your time to fill-in our survey: 
        <a href="#action=survey.action_answer_survey&survey_id=${object.id}&survey_token=__TOKEN__">${object.title}</a>
    </p>
    % if object.responsible_id:
    <p>Thank you for choosing ${object.responsible_id.company_id.name or 'us'}!</p>
    % endif
    <br/>
</div>
            ]]></field>
        </record>

    </data>
</openerp>
