<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <t t-name="survey.SurveySurveyActionHelper">
        <div class="o_view_nocontent">
            <div class="o_nocontent_help container">
                <div class="o_survey_not_found">
                    <h1>No Survey Found</h1>
                    <p class="lead">
                        Pick a sample or
                        <a action="action_load_sample_custom" class="o_survey_load_sample"
                            t-on-click.stop.prevent="() => this.onStartFromScratchClick()">Start from scratch</a>.
                    </p>
                </div>

                <div class="row g-2 g-md-3 mx-auto mt-2 justify-content-center">
                    <t t-set="templateNames" t-value="Object.keys(surveyTemplateData)"/>
                    <div class="col-12 col-md-4 col-xl-3" t-foreach="templateNames" t-as="templateName" t-key="templateName">
                        <t t-set="templateInfo" t-value="surveyTemplateData[templateName]"/>
                        <button
                            type="button"
                            t-attf-class="o_survey_load_sample o_survey_sample_card card flex-row flex-md-column align-items-center w-100 h-100 gap-4 gap-md-3 p-3 border rounded"
                            t-on-click.stop.prevent="() => this.onTemplateClick(templateInfo)"
                            t-attf-aria-label="Select {{templateInfo.title}} template"
                            >
                            <img class="o_image_64_contain mx-auto" t-attf-src="{{templateInfo.icon}}" t-attf-alt="{{templateInfo.title}}"/>
                            <div class="card-body p-0 text-start text-md-center">
                                <h3 class="card-title" t-out="templateInfo.title"/>
                                <p class="card-text" t-out="templateInfo.description"/>
                            </div>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </t>
</odoo>
