<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="sale_management.SaleTemplateDropdown">
        <button
            t-if="!this.state.quotationTemplates.length"
            type="button"
            t-att-class="this.props.newButtonClasses"
            t-att-disabled="this.props.isDisabled"
            t-att-data-hotkey="this.props.hotkey"
            t-on-click.stop="() => this.createQuotation()"
            data-bounce-button=""
        >
            New
        </button>
        <Dropdown t-else="" navigationOptions="this.navigationOptions">
            <button
                t-attf-class="{{ this.props.newButtonClasses }}
                o-dropdown-caret"
                t-att-disabled="this.props.isDisabled"
                t-att-data-hotkey="this.props.hotkey"
                data-bounce-button=""
            >
                New
            </button>
            <t t-set-slot="content">
                <DropdownItem
                    tag="'button'"
                    class="'btn btn-link'"
                    onSelected="() => this.createQuotation()"
                >
                    New Quotation
                </DropdownItem>
                <div role="separator" class="dropdown-divider"/>
                <div class="dropdown-header">Quotation Templates</div>
                <t t-foreach="this.state.quotationTemplates" t-as="template" t-key="template.id">
                    <DropdownItem
                        tag="'span'"
                        class="'btn btn-link o-dropdown-item-indent o_sale_management_template d-flex align-items-center pe-0'"
                        onSelected="() => this.createQuotation({ additionalContext: { default_sale_order_template_id: template.id } })"
                    >
                        <span class="text-truncate" style="max-width: 400px;" t-out="template.name"/>
                        <div class="o_template_icon_group ms-auto d-flex align-items-center gap-1 ps-2 pe-1" t-if="this.state.canManageTemplates">
                            <button type="button" role="menuitem" t-on-click.stop="() => this.onEditClick(template.id)" class="o-navigable oi oi-filled btn btn-link p-1 text-action" data-icon="edit" aria-label="Edit template" title="Edit template"/>
                            <button type="button" role="menuitem" t-on-click.stop="() => this.onDeleteClick(template.id)" class="o-navigable oi oi-filled btn btn-link p-1 text-danger" data-icon="delete" aria-label="Delete template" title="Delete template"/>
                        </div>
                    </DropdownItem>
                </t>
            </t>
        </Dropdown>
    </t>
</templates>
