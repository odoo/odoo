<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="inherit_id" ref="account.view_account_payment_register_form"/>
        <field name="arch" type="xml">
            <group>
                <group>
                    <field name="l10n_latam_check_ids" invisible="payment_method_code not in ['in_third_party_checks', 'out_third_party_checks']"
                      domain="
                            [('payment_method_code', '=', 'new_third_party_checks'), ('l10n_latam_check_current_journal_id', '=', journal_id), ('company_id', '=', company_id)]
                                if payment_type == 'outbound' else
                            [('payment_method_code', '=', 'new_third_party_checks'), ('l10n_latam_check_current_journal_id', '=', False), ('company_id', '=', company_id)]"
                    required="payment_method_code in ['in_third_party_checks', 'out_third_party_checks']"
                        widget="many2many_tags" options="{'no_create': True}"/>
                </group>
                <group name="latam_checks" string="Checks" invisible="payment_method_code not in ['new_third_party_checks', 'own_checks'] and  can_edit_wizard and (not can_group_payments or can_group_payments and group_payment)" colspan="2">
                    <field name="l10n_latam_new_check_ids"  nolabel="1" colspan="2" >
                        <tree editable="bottom">
                            <field name="company_id" column_invisible="True"/>
                            <field name="currency_id" column_invisible="True"/>
                            <field name="name" />
                            <field name="l10n_latam_check_bank_id" column_invisible="parent.payment_method_code == 'own_checks'"/>
                            <field name="l10n_latam_check_issuer_vat" column_invisible="parent.payment_method_code == 'own_checks'"/>
                            <field name="l10n_latam_check_payment_date" />
                            <field name="amount" />
                        </tree>
                    </field>
                </group>
            </group>
        </field>
    </record>

</odoo>
