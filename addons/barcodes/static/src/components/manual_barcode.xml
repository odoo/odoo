<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="barcodes.BarcodeInput">
        <div class="input-group">
            <input id="manual_barcode" t-model="this.state.barcode" type="text" name="barcode"
                    class="form-control" t-ref="manualBarcode"
                    t-att-placeholder="this.props.placeholder" t-on-keydown="this.onKeydown"/>
            <button class="input-group-text" t-att-disabled="!this.state.barcode.trim()"
                    t-on-click.prevent="() => this.submit()">
                Apply
            </button>
        </div>
    </t>

    <t t-name="barcodes.ManualBarcodeScanner" t-inherit="web.BarcodeDialog" t-inherit-mode="primary">
        <Dialog position="attributes">
            <attribute name="footer">true</attribute>
        </Dialog>
        <Dialog position="inside">
            <t t-set-slot="footer">
                <BarcodeInput onSubmit="this.props.onResult" placeholder="this.props.placeholder"/>
            </t>
        </Dialog>
    </t>
</templates>
