<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="hr_recruitment_skills.One2ManyTagsSkillsField">
        <t t-if="!this.anySkills">
            <div name="no_skills" class="skills_helper">
                <p>
                    There are no skills defined for your company in the library.
                </p>
                <button t-on-click="this.skillTypesAction"
                            class="btn btn-secondary align-self-center">
                            Create Skills
                </button>
            </div>
        </t>
        <t t-else="">
            <div class="d-flex align-items-baseline"
                t-attf-class="{{ this.tags.length &amp;&amp; 'gap-1'}}">
                <div class=" d-inline-flex flex-wrap gap-1 mw-100">
                    <t t-foreach="this.tags" t-as="tag" t-key="tag.id">
                        <BadgeTag color="tag.color" text="tag.text" onClick="tag.onClick" onDelete="tag.onDelete"/>
                    </t>
                </div>
                <button t-on-click="() => this.onAdd({ context: { default_skill_type_id: this.defaultSkillType.id }})"
                    class="btn btn-link text-action"
                    style="padding: 0; line-height: 0; height: min-content;">
                    <i class="oi text-info" data-icon="add"/>
                </button>
            </div>
        </t>
    </t>

</templates>
