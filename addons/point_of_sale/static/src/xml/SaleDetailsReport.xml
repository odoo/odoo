<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.SaleDetailsReport">
        <div class="pos-receipt">
            <t t-if="model.data.derived.companyLogoBase64">
                <img class="pos-receipt-logo" t-att-src="model.data.derived.companyLogoBase64" alt="Logo"/>
                <br/>
            </t>
            <t t-if="!model.data.derived.companyLogoBase64" class="pos-receipt-center-align">
                <h1 t-esc="model.company.name" />
                <br/>
            </t>
            <br /><br />

            <div class="orderlines">
                <t t-foreach="products" t-as="line" t-key="line.product_id">
                    <div>
                        <t t-esc="line.product_name.substr(0,20)" />
                        <span class="pos-receipt-right-align">
                            <t t-esc="Math.round(line.quantity * Math.pow(10, model.getDecimalPrecision('Product Unit of Measure').digits)) / Math.pow(10, model.getDecimalPrecision('Product Unit of Measure').digits)" />
                            <t t-if="line.uom !== 'Units'">
                                <t t-esc="line.uom" />
                            </t>
                            x
                            <t t-esc="model.formatCurrencyNoSymbol(line.price_unit)" />
                        </span>
                    </div>
                    <t t-if="line.discount !== 0">
                        <div class="pos-receipt-left-padding">Discount: <t t-esc="line.discount" />%</div>
                    </t>
                </t>
            </div>

            <br/>
            <div>------------------------</div>
            <br/>

            <div>
                Payments:
            </div>
            <div t-foreach="payments" t-as="payment">
                <t t-esc="payment.name" />
                <span t-esc="model.formatCurrencyNoSymbol(payment.total)" class="pos-receipt-right-align"/>
            </div>

            <br/>
            <div>------------------------</div>
            <br/>

            <div>
                Taxes:
            </div>
            <div t-foreach="taxes" t-as="tax">
                <t t-esc="tax.name" />
                <span t-esc="model.formatCurrencyNoSymbol(tax.tax_amount)" class="pos-receipt-right-align"/>
            </div>

            <br/>
            <div>------------------------</div>
            <br/>

            <div>
                Total:
                <span t-esc="model.formatCurrencyNoSymbol(total_paid)" class="pos-receipt-right-align"/>
            </div>

            <br/>
            <div class="pos-receipt-order-data">
                <div><t t-esc="date" /></div>
            </div>
        </div>
    </t>

</templates>
