<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.OrderDetails" owl="1">
        <t t-set="_orderlines" t-value="env.model.getOrderlines(props.order)" />
        <div class="order-container">
            <div t-ref="scrollable" class="order-scroller touch-scrollable">
                <div class="order">
                    <t t-if="!props.order">
                        <div class="order-empty">
                            <i class="fa fa-shopping-cart" role="img" aria-label="Shopping cart"
                                title="Shopping cart" />
                            <h1>Select an order</h1>
                        </div>
                    </t>
                    <t t-elif="_orderlines.length === 0">
                        <div class="order-empty">
                            <i class="fa fa-shopping-cart" role="img" aria-label="Shopping cart"
                                title="Shopping cart" />
                            <h1>Order is empty</h1>
                        </div>
                    </t>
                    <t t-else="">
                        <ul class="orderlines">
                            <t t-foreach="_orderlines" t-as="_orderline" t-key="_orderline.id">
                                <Orderline orderline="_orderline" additionalClasses="{}" />
                            </t>
                        </ul>
                        <OrderSummary order="props.order" />
                    </t>
                </div>
            </div>
        </div>
    </t>

</templates>
