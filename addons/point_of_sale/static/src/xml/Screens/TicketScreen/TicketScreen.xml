<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="TicketScreen" owl="1">
        <div class="ticket-screen screen">
            <div class="screen-content">
                <div class="controls">
                    <div class="buttons" t-att-class="{ 'show-search-bar': state.showSearchBar }">
                        <t t-if="!env.isMobile || !state.showSearchBar">
                            <button class="discard" t-on-click="trigger('close-screen')">
                                <span class="search-icon">
                                    <i class="fa fa-angle-double-left"/>
                                </span>
                                <t t-if="!env.isMobile">
                                    Back
                                </t>
                            </button>
                            <button t-if="showNewTicketButton" class="highlight" t-on-click="createNewOrder">New Order</button>
                        </t>
                        <t t-if="env.isMobile">
                            <t t-if="state.showSearchBar">
                                <button class="arrow-left" t-on-click="state.showSearchBar = !state.showSearchBar;">
                                    <span class="search-icon">
                                        <i class="fa fa-angle-double-left"/>
                                    </span>
                                </button>
                            </t>
                            <t t-else="">
                                <button class="search" t-on-click="state.showSearchBar = !state.showSearchBar;">
                                    <span class="search-icon">
                                        <i class="fa fa-search"/>
                                    </span>
                                </button>
                           </t>
                        </t>
                    </div>
                    <t t-set="placeholder">Search...</t>
                    <SearchBar t-if="state.showSearchBar" config="searchBarConfig" placeholder="placeholder" showSearchIcon="!env.isMobile" />
                </div>
                <div class="orders" t-if="env.isMobile">
                    <t t-foreach="filteredOrderList" t-as="order" t-key="order.cid">
                        <div class="order-table pointer" t-on-click="selectOrder(order)">
                            <div class="order-row">
                                <div class="col">
                                    Date
                                </div>
                                <div class="col end">
                                    <t t-esc="getDate(order)"/>
                                </div>
                            </div>
                            <div class="order-row">
                                <div class="col">
                                    Receipt Number
                                </div>
                                <div class="col end">
                                    <t t-esc="order.name"/>
                                </div>
                            </div>
                            <div class="order-row">
                                <div class="col">
                                    Customer
                                </div>
                                <div class="col end">
                                    <t t-esc="getCustomer(order)"/>
                                </div>
                            </div>
                            <div class="order-row" t-if="showCardholderName()">
                                <div class="col">
                                    Cardholder Name
                                </div>
                                <div class="col end">
                                    <t t-esc="getCardholderName(order)"/>
                                </div>
                            </div>
                            <div class="order-row">
                                <div class="col">
                                    Employee
                                </div>
                                <div class="col end">
                                    <t t-esc="getEmployee(order)"/>
                                </div>
                            </div>
                            <div class="order-row">
                                <div class="col">
                                    Total
                                </div>
                                <div class="col end">
                                    <t t-esc="getTotal(order)"/>
                                </div>
                            </div>
                            <div class="order-row">
                                <div class="col start">
                                    Status
                                </div>
                                <div class="col end">
                                    <t t-esc="getStatus(order)"/>
                                </div>
                            </div>
                            <div class="order-row delete-button" t-if="!hideDeleteButton(order)">
                                <div class="col center very-narrow" name="delete" t-on-click.stop="deleteOrder(order)">
                                    <i class="fa fa-trash" aria-hidden="true"/> Delete
                                </div>
                            </div>
                        </div>
                    </t>
                </div>
                <div class="orders" t-else="">
                    <div class="header-row">
                        <div class="col start wide">Date</div>
                        <div class="col start wide">Receipt Number</div>
                        <div class="col start">Customer</div>
                        <div class="col start wide" t-if="showCardholderName()">Cardholder Name</div>
                        <div class="col start">Employee</div>
                        <div class="col end">Total</div>
                        <div class="col start narrow">Status</div>
                        <div class="col center very-narrow" name="delete"></div>
                    </div>
                    <t t-foreach="filteredOrderList" t-as="order" t-key="order.cid">
                        <div class="order-row pointer" t-on-click="selectOrder(order)">
                            <div class="col start wide">
                                <t t-esc="getDate(order)"></t>
                            </div>
                            <div class="col start wide">
                                <t t-esc="order.name"></t>
                            </div>
                            <div class="col start">
                                <t t-esc="getCustomer(order)"></t>
                            </div>
                            <div t-if="showCardholderName()" class="col start">
                                <t t-esc="getCardholderName(order)"></t>
                            </div>
                            <div class="col start">
                                <t t-esc="getEmployee(order)"></t>
                            </div>
                            <div class="col end">
                                <t t-esc="getTotal(order)"></t>
                            </div>
                            <div class="col start narrow">
                                <t t-esc="getStatus(order)"></t>
                            </div>
                            <div t-if="!hideDeleteButton(order)" class="col center very-narrow delete-button" name="delete" t-on-click.stop="deleteOrder(order)">
                                <i class="fa fa-trash" aria-hidden="true"></i>
                            </div>
                            <div t-else="" class="col center very-narrow delete-button"></div>
                        </div>
                    </t>
                </div>
            </div>
        </div>
    </t>

</templates>
