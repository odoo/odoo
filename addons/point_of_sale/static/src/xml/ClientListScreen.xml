<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">


    <t t-name="ClientLine" owl="1">
        <tr t-attf-class="client-line {{highlight}}" t-att-data-id="props.partner.id"
            t-on-click="trigger('click-client', {client: props.partner})">
            <td>
                <t t-esc="props.partner.name" />
            </td>
            <td>
                <t t-esc="props.partner.address" />
            </td>
            <td>
                <t t-esc="props.partner.phone or props.partner.mobile or ''" />
            </td>
        </tr>
    </t>

    <t t-name="ClientDetails" owl="1">
        <section class="client-details">
            <div class="client-picture">
                <img t-att-src="partnerImageUrl" alt="Partner logo"
                     style="width: 64px; height: 64px; object-fit: cover;" />
            </div>
            <div class="client-name">
                <t t-esc="props.partner.name" />
            </div>
            <div class="edit-buttons">
                <div class="button edit">
                    <i class="fa fa-pencil-square"
                       t-on-click="trigger('activate-edit-mode', {isNewClient: false})" role="img"
                       aria-label="Edit" title="Edit" />
                </div>
            </div>
            <div class="client-details-box clearfix">
                <div class="client-details-left">
                    <div class="client-detail">
                        <span class="label">Address</span>
                        <t t-if="props.partner.address">
                            <span class="detail client-address">
                                <t t-esc="props.partner.address" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail client-address empty">N/A</span>
                        </t>
                    </div>
                    <div class="client-detail">
                        <span class="label">Email</span>
                        <t t-if="props.partner.email">
                            <span class="detail client-email">
                                <t t-esc="props.partner.email" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail client-email empty">N/A</span>
                        </t>
                    </div>
                    <div class="client-detail">
                        <span class="label">Phone</span>
                        <t t-if="props.partner.phone">
                            <span class="detail client-phone">
                                <t t-esc="props.partner.phone" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail client-phone empty">N/A</span>
                        </t>
                    </div>
                </div>
                <div class="client-details-right">
                    <div class="client-detail">
                        <span class="label">Barcode</span>
                        <t t-if="props.partner.barcode">
                            <span class="detail client-id">
                                <t t-esc="props.partner.barcode" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail client-id empty">N/A</span>
                        </t>
                    </div>
                    <div class="client-detail">
                        <span class="label">Tax ID</span>
                        <t t-if="props.partner.vat">
                            <span class="detail vat">
                                <t t-esc="props.partner.vat" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail vat empty">N/A</span>
                        </t>
                    </div>
                    <div t-if="props.pos.pricelists.length lte 1" class="client-detail">
                        <span class='label'>Pricelist</span>
                        <t t-if="props.partner.property_product_pricelist">
                            <span class="detail property_product_pricelist">
                                <t t-esc="props.partner.property_product_pricelist[1]" />
                            </span>
                        </t>
                        <t t-else="">
                            <span class="detail property_product_pricelist empty">N/A</span>
                        </t>
                    </div>
                </div>
            </div>
        </section>
    </t>

    <t t-name="ClientDetailsEdit" owl="1">
        <section class="client-details edit">
            <div class="client-picture">
                <t t-if="partnerImageUrl">
                    <img t-att-src="partnerImageUrl" alt="Partner"
                         style="width: 64px; height: 64px; object-fit: cover;" />
                </t>
                <t t-else="">
                    <i class="fa fa-camera" role="img" aria-label="Picture" title="Picture"></i>
                </t>
                <input type="file" class="image-uploader" t-on-change="uploadImage" />
            </div>
            <input class="detail client-name" name="name" t-att-value="props.partner.name"
                   placeholder="Name" t-on-change="captureChange" />
            <div class="edit-buttons">
                <div class="button undo">
                    <i class="fa fa-undo" t-on-click="trigger('cancel-edit')" role="img"
                       aria-label="Undo" title="Undo" />
                </div>
                <div class="button save">
                    <i class="fa fa-floppy-o" t-on-click="saveChanges" role="img" aria-label="Save"
                       title="Save" />
                </div>
            </div>
            <div class="client-details-box clearfix">
                <div class="client-details-left">
                    <div class="client-detail">
                        <span class="label">Street</span>
                        <input class="detail client-address-street" name="street"
                               t-on-change="captureChange" t-att-value="props.partner.street || ''"
                               placeholder="Street" />
                    </div>
                    <div class="client-detail">
                        <span class="label">City</span>
                        <input class="detail client-address-city" name="city"
                               t-on-change="captureChange" t-att-value="props.partner.city || ''"
                               placeholder="City" />
                    </div>
                    <div class="client-detail">
                        <span class="label">Postcode</span>
                        <input class="detail client-address-zip" name="zip"
                               t-on-change="captureChange" t-att-value="props.partner.zip || ''"
                               placeholder="ZIP" />
                    </div>
                    <div class="client-detail">
                        <span class="label">State</span>
                        <select class="detail client-address-states needsclick" name="state_id"
                                t-on-change="captureChange">
                            <option value="">None</option>
                            <t t-foreach="props.pos.states" t-as="state" t-key="state.id">
                                <option t-if="props.partner.country_id[0] == state.country_id[0]"
                                        t-att-value="state.id"
                                        t-att-selected="props.partner.state_id ? ((state.id === props.partner.state_id[0]) ? true : undefined) : undefined">
                                    <t t-esc="state.name" />
                                </option>
                            </t>
                        </select>
                    </div>
                    <div class="client-detail">
                        <span class="label">Country</span>
                        <select class="detail client-address-country needsclick" name="country_id"
                                t-on-change="captureChange">
                            <option value="">None</option>
                            <t t-foreach="props.pos.countries" t-as="country" t-key="country.id">
                                <option t-att-value="country.id"
                                        t-att-selected="props.partner.country_id ? ((country.id === props.partner.country_id[0]) ? true : undefined) : undefined">
                                    <t t-esc="country.name" />
                                </option>
                            </t>
                        </select>
                    </div>
                </div>
                <div class="client-details-right">
                    <div class="client-detail">
                        <span class="label">Language</span>
                        <select class="detail client-lang needsclick" name="lang"
                                t-on-change="captureChange">
                            <t t-foreach="props.pos.langs" t-as="lang" t-key="lang.id">
                                <option t-att-value="lang.code"
                                        t-att-selected="props.partner.lang ? ((lang.code === props.partner.lang) ? true : undefined) : lang.code === props.pos.user.lang? true : undefined">
                                    <t t-esc="lang.name" />
                                </option>
                            </t>
                        </select>
                    </div>
                    <div class="client-detail">
                        <span class="label">Email</span>
                        <input class="detail client-email" name="email" type="email"
                               t-on-change="captureChange"
                               t-att-value="props.partner.email || ''" />
                    </div>
                    <div class="client-detail">
                        <span class="label">Phone</span>
                        <input class="detail client-phone" name="phone" type="tel"
                               t-on-change="captureChange"
                               t-att-value="props.partner.phone || ''" />
                    </div>
                    <div class="client-detail">
                        <span class="label">Barcode</span>
                        <input class="detail barcode" name="barcode" t-on-change="captureChange"
                               t-att-value="props.partner.barcode || ''" />
                    </div>
                    <div class="client-detail">
                        <span class="label">Tax ID</span>
                        <input class="detail vat" name="vat" t-on-change="captureChange"
                               t-att-value="props.partner.vat || ''" />
                    </div>
                    <div t-if="props.pos.pricelists.length gt 1" class="client-detail">
                        <span class="label">Pricelist</span>
                        <select class="detail needsclick" name="property_product_pricelist"
                                t-on-change="captureChange">
                            <t t-foreach="props.pos.pricelists" t-as="pricelist"
                               t-key="pricelist.id">
                                <option t-att-value="pricelist.id"
                                        t-att-selected="props.partner.property_product_pricelist ? (pricelist.id === props.partner.property_product_pricelist[0] ? true : false) : false">
                                    <t t-esc="pricelist.display_name" />
                                </option>
                            </t>
                        </select>
                    </div>
                </div>
            </div>
        </section>
    </t>

    <t t-name="ClientListScreen" owl="1">
        <div class="clientlist-screen screen" t-on-activate-edit-mode="activateEditMode">
            <div class="screen-content">
                <section class="top-content">
                    <span class="button back"
                          t-on-click="trigger('show-screen', { name: 'ProductScreen' })">
                        <i class="fa fa-angle-double-left"></i>
                        Cancel
                    </span>
                    <span class="searchbox">
                        <input placeholder="Search Customers" t-on-keyup="updateClientList" />
                        <span class="search-clear"></span>
                    </span>
                    <span class="searchbox"></span>
                    <span class="button new-customer" role="img" aria-label="Add a customer"
                          t-on-click="trigger('activate-edit-mode', { isNewClient: true })"
                          title="Add a customer">
                        <i class="fa fa-user"></i>
                        <i class="fa fa-plus"></i>
                    </span>
                    <span t-if="isNextButtonVisible" t-on-click="clickNext"
                          class="button next highlight">
                        <t t-esc="nextButton.text" />
                    </span>
                </section>
                <section class="full-content">
                    <div class="window">
                        <section class="subwindow collapsed">
                            <div class="subwindow-container collapsed">
                                <div t-if="state.isShowDetails"
                                     class="client-details-contents subwindow-container-fix"
                                     t-ref="client-details-ref">
                                    <t t-if="state.isEditMode">
                                        <ClientDetailsEdit t-props="state.editModeProps"
                                                           t-on-cancel-edit="cancelEdit"
                                                           t-on-save-changes="saveChanges" />
                                    </t>
                                    <t t-else="">
                                        <ClientDetails partner="state.selectedClient" pos="pos" />
                                    </t>
                                </div>
                            </div>
                        </section>
                        <section class="subwindow">
                            <div class="subwindow-container">
                                <div class="subwindow-container-fix touch-scrollable scrollable-y"
                                     t-ref="client-list-ref">
                                    <table class="client-list">
                                        <thead>
                                            <tr>
                                                <th>Name</th>
                                                <th>Address</th>
                                                <th>Phone</th>
                                            </tr>
                                        </thead>
                                        <tbody class="client-list-contents">
                                            <t t-foreach="clients" t-as="partner"
                                               t-key="partner.id">
                                                <ClientLine partner="partner"
                                                            selectedClient="state.selectedClient"
                                                            isShowDetails="state.isShowDetails"
                                                            t-on-click-client="clickClient" />
                                            </t>
                                        </tbody>
                                    </table>
                                </div>
                            </div>
                        </section>
                    </div>
                </section>
            </div>
        </div>
    </t>

</templates>
