<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.ScaleScreen">
        <Dialog size="'md'" t-if="this.scale.product">
            <t t-set-slot="header">
                <t t-if="this.env.isSmall">
                    <button class="btn oi oi-arrow-left" data-bs-dismiss="modal" aria-label="Close" t-on-click="this.close"/>
                </t>
                <h4 class="modal-title text-break text-center w-100 ms-4" t-att-class="{ 'me-auto': this.env.isSmall }">
                    <t t-out="this.scale.product.name"/>
                </h4>
                <t t-if="!this.env.isSmall">
                    <button type="button" class="btn-close" aria-label="Close" tabindex="-1" t-on-click="this.close"/>
                </t>
            </t>
            <div class="d-flex flex-column">
                <t t-if="!scale.hardwareTare">
                    <div class="d-flex gap-2 align-items-center mb-2">
                        <div class="w-60 fs-3 me-2">Gross Weight:</div>
                        <div class="gross-weight fs-3 px-5 text-end form-control-plaintext" t-out="this.scale.grossWeightString"/>
                    </div>
                    <div class="d-flex gap-5 align-items-center mb-2">
                    <div class="w-50">
                        <button class="btn btn-primary w-50 fs-3" t-att-disabled="this.scale.loading" t-on-click="() => this.scale.requestTare()">
                            <i t-if="this.scale.tareRequested" class="fa fa-circle-o-notch fa-spin"/>
                            <t t-else="">Tare</t>
                        </button>
                        </div>
                        <div class="d-flex flex-row justify-content-end w-50 align-items-center px-5">
                            <input type="number" class="form-control me-2" t-att-readonly="this.scale.tareRequested" t-custom-model.number="this.scale.tare"/>
                            <span class="text-end fs-3" t-out="this.scale.product.unitOfMeasure"/>
                        </div>
                    </div>
                </t>
                <div class="d-flex gap-2 align-items-center mb-2">
                    <div class="w-60 fs-3 me-2">Net Weight:</div>
                    <div class="text-end fs-3 px-5 form-control-plaintext" t-out="this.scale.netWeightString"/>
                </div>
                <div class="d-flex px-5 flex-row gap-2 m-2 align-items-center">
                    <div class="product-price w-50 fs-2 text-center" t-out="this.scale.unitPriceString"/>
                    <div class="computed-price fd-flex flex-grow-1 p-3 rounded text-center text-bg-info bg-opacity-25 text-info fs-2 fw-bold" t-out="this.scale.totalPriceString"/>
                </div>
            </div>
            <t t-set-slot="footer">
                <div class="d-flex flex-grow-1 gap-2">
                    <button class="btn btn-secondary w-100" t-att-disabled="this.scale.loading" t-on-click="() => this.scale.readWeight()">
                        <i t-if="this.scale.loading and !this.scale.tareRequested" class="fa fa-spinner fa-spin"/>
                        <t t-else="">Get Weight</t>
                    </button>
                    <button class="btn btn-lg btn-primary d-flex justify-content-center align-items-center w-100" t-on-click="this.confirm" t-att-disabled="!this.scale.isWeightValid">
                        Order
                        <i class="fa fa-angle-double-right ms-2"/>
                    </button>
                </div>
            </t>
        </Dialog>
    </t>
</templates>
