<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.OrderlineDetails" owl="1">
        <li class="orderline" t-on-click="() => this.props.onClickOrderline(props.line)" t-att-class="{ selected: props.isSelected }">
            <span class="product-name">
                <t t-out="productName" />
            </span>
            <span class="price">
                <t t-out="totalPrice" />
            </span>
            <ul class="info-list">
                <li class="info">
                    <strong>
                        <t t-out="quantity" />
                    </strong>
                    <span><t t-out="pricePerUnit" /></span>
                </li>
                <t t-if="customerNote">
                    <li class="info orderline-note">
                        <i class="fa fa-sticky-note" role="img" aria-label="Customer Note" title="Customer Note"/>
                        <t t-out="customerNote" />
                    </li>
                </t>
                <t t-if="hasRefundedQty()">
                    <li class="info refund-note">
                        <strong>
                            <t t-out="getFormattedRefundedQty()" />
                        </strong>
                        <span> Refunded</span>
                    </li>
                </t>
                <t t-if="hasToRefundQty()">
                    <li class="info to-refund-highlight refund-note">
                        <t t-set="_destinationOrderUid" t-value="getToRefundDetail().destinationOrderUid"/>
                        <t t-if="_destinationOrderUid">
                            <t t-out="getRefundingMessage()" />
                            <span t-out="_destinationOrderUid" class="order-link" t-on-click.stop="() => this.props.onClickRefundOrderUid(_destinationOrderUid)" />
                        </t>
                        <t t-else="">
                            <t t-out="getToRefundMessage()" />
                        </t>
                    </li>
                </t>
            </ul>
        </li>
    </t>

</templates>
