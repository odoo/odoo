<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.PartnerList">
        <Dialog bodyClass="'partner-list overflow-y-auto'" contentClass="'h-100'">
            <t t-set-slot="header">
                <button t-if="!ui.isSmall" class="btn btn-primary btn-lg lh-lg" role="img" aria-label="Add a customer"
                        t-on-click="() => this.editPartner()"
                        title="Add a customer">
                Create
                </button>
            </t>
            <CustomListView model="'res.partner'" onClickLine.bind="clickPartner" selectedRecord="this.props.partner">
                <t t-set-slot="action" t-slot-scope="scope">
                    <Dropdown>
                        <button class="btn btn-light border float-end">
                            <i class="fa fa-fw fa-bars"/>
                        </button>
                        <t t-set-slot="content">
                            <DropdownItem onSelected="() => this.editPartner(scope.record)">
                                Edit Details
                            </DropdownItem>
                            <DropdownItem onSelected="() => this.goToOrders(scope.record)">
                                All Orders
                            </DropdownItem>
                        </t>
                    </Dropdown>
                </t>
            </CustomListView>
            <t t-set-slot="footer">
                <div class="d-flex justify-content-start flex-wrap gap-2 w-100">
                    <button t-if="ui.isSmall" class="btn btn-primary btn-lg lh-lg" role="img" aria-label="Add a customer"
                        t-on-click="() => this.editPartner()"
                        title="Add a customer">
                    New
                    </button>
                    <button class="btn btn-secondary btn-lg lh-lg o-default-button" t-on-click="() => this.clickPartner(this.props.partner)">Discard</button>
                </div>
            </t>
        </Dialog>
    </t>

</templates>
