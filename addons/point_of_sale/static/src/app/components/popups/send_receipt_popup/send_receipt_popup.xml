<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="point_of_sale.SendReceiptPopup">
        <Dialog title="this.title" footer="false" size="'md'" contentClass="`send-receipt-popup ${!this.ui.isSmall? 'rounded-4' : ''}`" bodyClass="'d-flex flex-column gap-2 py-3'">
            <t t-foreach="this.sendList" t-as="item" t-key="item_index">
                <div t-if="item.show()" class="d-flex">
                    <input type="text" t-attf-class="{{ item.inputClass }} o_input position-relative p-3 border border-end-0 rounded-start-2 text-body" t-att-placeholder="item.placeholder" t-model.proxy="this.state[item.model]" />
                    <div class="d-flex gap-1">
                        <button t-foreach="item.buttons" t-as="button" t-key="button_index" t-att-style="`width: ${this.ui.isSmall ? '4rem' : '8rem'}`" class="btn btn-primary btn-lg lh-lg rounded-start-0 h-100" t-att-disabled="button.disabled()" t-on-click="() => button.click.call()">
                            <i
                                class="oi"
                                t-attf-class="{{button.status === 'loading' ? 'oi-fw oi-spin' : ''}} {{button.iconClass || ''}}"
                                t-attf-data-icon="{{button.status === 'loading' ? 'autorenew' : button.icon}}"
                            />
                        </button>
                    </div>
                </div>
            </t>
        </Dialog>
    </t>

</templates>
