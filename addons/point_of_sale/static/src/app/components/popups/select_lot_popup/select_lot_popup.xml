<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.SelectLotPopup">
        <Dialog title="props.title" size="'md'">
            <t t-set-slot="header">
                <h4 class="modal-title title">
                    <span t-esc="props.title"/> <span class="fst-italic" t-esc="props.name"/>
                </h4>
            </t>
            <div t-ref="root" class="tags-container">
                <t t-if="!props.isSingleItem" t-foreach="state.values" t-as="v" t-key="v.id">
                    <div class="tag-item input-group mb-2">
                        <span class="form-control bg-white" t-esc="v.text"/>
                        <button class="btn btn-danger" t-on-click="() => this.removeItem(v.id)">
                            <i class="fa fa-trash-o"></i>
                        </button>   
                    </div>
                </t>
                <LotAutoComplete
                    placeholder.translate="Serial/Lot Number"
                    sources="getSources()"
                    value="state.value"
                    onSelect.bind="onSelect"
                    dropdown="true"
                    autoSelect="true"
                    autofocus="true"
                />
            </div>
            <t t-set-slot="footer">
                <button class="btn btn-primary btn-lg lh-lg o-default-button" t-on-click="confirm">Ok</button>
            </t>
       </Dialog>
    </t>
</templates>
