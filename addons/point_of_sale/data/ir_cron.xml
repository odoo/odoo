<?xml version="1.0" encoding="utf-8"?>
<odoo noupdate="1">
    <record id="ir_cron_generate_pos_order_invoice" model="ir.cron">
        <field name="name">POS: Generate Order Invoice</field>
        <field name="model_id" ref="model_pos_order"/>
        <field name="state">code</field>
        <field name="code">model._trigger_pos_order_invoice_cron()</field>
        <field name="interval_number">9999</field>  <!-- This cron is used as a background task, should not be run automatically -->
        <field name="interval_type">months</field>
    </record>
</odoo>
