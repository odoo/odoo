<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_assign_serial_numbers_production" model="ir.ui.view">
        <field name="name">mrp_assign_serial_numbers</field>
        <field name="model">stock.assign.serial</field>
        <field name="arch" type="xml">
            <form string="Mass Produce">
                    <group>
                        <field name="show_apply" invisible="1" />
                        <field name="production_id" invisible="1"/>
                        <field name="next_serial_number" invisible="1"/>
                    </group>
                    <group>
                        <group>
                            <field name="next_serial_number"/>
                        </group>
                        <group>
                            <label for="next_serial_count"/>
                            <div class="o_row">
                                <span><field name="next_serial_count"/></span>
                                <button name="generate_serial_numbers_production" type="object" class="btn btn-secondary" title="Generate Serial Numbers">
                                    <span>Generate</span>
                                </button>
                            </div>
                        </group>
                    </group>
                    <span>"To document the consumption of your tracked components at the same time also, write one line per finished product and use the syntax documented below:"</span><br/><br/>
                    <field name="list_of_component" readonly="1"/>
                    <field class="form-control my-2" name="lot_numbers" placeholder="" widget="lot_numbers"/>
                    <footer class="d-flex justify-content-between">
                        <div class="d-flex gap-1">
                            <button name="apply" string="Produce" type="object" class="btn-primary" context="{'make_mo_done': True}" invisible="not show_apply"/>
                            <button name="apply" string="Prepare MO" type="object" class="btn-primary" context="{'make_mo_confirmed': True}" invisible="not show_apply"/>
                            <button string="Cancel" class="btn-secondary" special="cancel" />
                        </div>
                    </footer>
                </form>
        </field>
    </record>

    <record id="act_assign_serial_numbers_production" model="ir.actions.act_window">
        <field name="name">Production Details</field>
        <field name="res_model">stock.assign.serial</field>
        <field name="view_id" ref="view_assign_serial_numbers_production"/>
        <field name="view_mode">form</field>
        <field name="context">{}</field>
        <field name="target">new</field>
    </record>

</odoo>
