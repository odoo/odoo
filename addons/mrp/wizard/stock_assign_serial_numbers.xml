<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_assign_serial_numbers_production" model="ir.ui.view">
        <field name="name">mrp_assign_serial_numbers</field>
        <field name="model">stock.assign.serial</field>
        <field name="arch" type="xml">
            <form string="Mass Produce">
                    <group>
                        <field name="show_apply" invisible="1" />
                        <field name="production_id" invisible="1"/>
                        <field name="next_serial_number" invisible="1"/>
                    </group>
                    <span>Write one line per finished product to produce, with serial numbers as follows:</span><br/><br/>
                    <field name="list_of_component" readonly="1"/>
                    <field class="form-control my-2" name="lot_numbers" placeholder="" widget="lot_numbers"/>
                    <footer class="d-flex justify-content-between">
                        <div class="d-flex gap-1">
                            <button name="apply" string="Produce" type="object" class="btn-primary" context="{'make_mo_done': True}" modifiers="{'invisible': [('show_apply', '=', False)]}" />
                            <button name="apply" string="Prepare MO" type="object" class="btn-primary" context="{'make_mo_confirmed': True}" modifiers="{'invisible': [('show_apply', '=', False)]}" />
                            <button string="Cancel" class="btn-secondary" special="cancel" />
                        </div>
                        <div>
                            <button name="action_open_generate_serial_production" string="Generate Serials" type="object" class="float-end btn-link"/>
                        </div>
                    </footer>
                </form>
        </field>
    </record>

    <record id="act_assign_serial_numbers_production" model="ir.actions.act_window">
        <field name="name">Assign Serial Numbers</field>
        <field name="res_model">stock.assign.serial</field>
        <field name="view_id" ref="view_assign_serial_numbers_production"/>
        <field name="view_mode">form</field>
        <field name="context">{}</field>
        <field name="target">new</field>
    </record>

</odoo>
