<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="sale.ComboConfiguratorDialog">
        <Dialog
            title="this.props.display_name"
            contentClass="'sale-combo-configurator-dialog'"
            bodyClass="'d-flex flex-column gap-4'"
        >
            <div t-foreach="this.allCombos" t-as="combo" t-key="combo.id">
                <div class="d-flex align-items-baseline mb-3">
                    <span name="sale_combo_configurator_title" class="mb-0" t-out="combo.name"/>
                    <h6 class="text-muted ms-2 mb-0 fw-normal fst-normal">
                        <span class="small">
                            <t t-out="this.getSelectedComboItemsText(combo)"/>
                        </span>
                        <span class="small"> included</span>
                    </h6>
                </div>
                <div class="row row-cols-1 row-cols-md-3 g-3">
                    <t t-foreach="combo.combo_items" t-as="comboItem" t-key="comboItem.id">
                        <div t-att-class="{
                            'd-flex flex-column gap-2': true,
                            'align-items-center': comboItem.is_configurable,
                        }">
                            <ProductCard
                                product="this.getSelectedOrProvidedComboItem(comboItem).product"
                                extraPrice="comboItem.extra_price"
                                onClick="() => this.selectComboItem(combo, comboItem)"
                                isSelected="this.getItemQuantity(comboItem.id) > 0"
                                isConfigurable="comboItem.is_configurable"
                            >
                            <t t-if="combo.included_qty > 1" t-set-slot="quantityButtons">
                                <div t-on-click.stop="() => {}">
                                    <SubItemQuantityButtons
                                        quantity="this.getItemQuantity(comboItem.id)"
                                        setQuantity="(qty) => this.setItemQuantity(combo.id, comboItem, qty)"
                                    />
                                </div>
                            </t>
                            </ProductCard>
                        </div>
                    </t>
                </div>
            </div>
            <t t-set-slot="footer">
                <div class="d-flex flex-column-reverse flex-lg-row gap-3 w-100">
                    <div class="d-flex flex-column flex-md-row gap-2">
                        <button
                            name="sale_combo_configurator_confirm_button"
                            class="btn btn-primary w-100 w-lg-auto"
                            t-att-disabled="!this.areAllCombosSelected || this.state.isLoading"
                            t-on-click="this.confirm"
                        >
                            Add to order
                        </button>
                        <button
                            name="sale_combo_configurator_cancel_button"
                            class="btn btn-secondary w-100 w-lg-auto"
                            t-on-click="this.cancel"
                        >
                            Discard
                        </button>
                    </div>
                    <div class="d-flex gap-3 align-items-center">
                        <div t-if="this.props.options?.showQuantity ?? true" class="combo_configurator_quantity ps-lg-3">
                            <QuantityButtons
                                quantity="this.state.quantity"
                                setQuantity="quantity => this.setQuantity(quantity)"
                                isMinusButtonDisabled="this.state.quantity === 1"
                                btnClasses="'d-inline-block w-auto'"
                            />
                        </div>
                        <div t-if="this.props.options?.showPrice ?? true" class="w-100 w-md-auto">
                            <span
                                name="sale_combo_configurator_total"
                                class="h6 mb-0"
                                t-out="this.totalMessage"
                            />
                            <span
                                t-if="this.props.price_info"
                                t-out="this.props.price_info"
                                class="text-muted"
                            />
                        </div>
                    </div>
                </div>
            </t>
        </Dialog>
    </t>
</templates>
