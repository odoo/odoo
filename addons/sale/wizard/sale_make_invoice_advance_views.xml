<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_sale_advance_payment_inv" model="ir.ui.view">
            <field name="name">Invoice Orders</field>
            <field name="model">sale.advance.payment.inv</field>
            <field name="arch" type="xml">
                <form string="Invoice Sales Order">
                    <p class="oe_grey">
                        Invoices will be created in draft so that you can review
                        them before validation.
                    </p>
                    <group>
                        <group colspan="4" class="mb0">
                            <field name="count" invisible="[('count','=',1)]" readonly="True"/>
                            <field name="product_id" context="{'search_default_services': 1, 'default_type': 'service', 'default_invoice_policy': 'order'}" class="oe_inline" invisible="1"/>
                            <field name="advance_payment_method" widget="radio" options="{'horizontal':true}"
                            attrs="{'invisible': [('count','&gt;',1)]}"/> 
                        </group>
                        <group>
                            <div class="text-center ml8 " attrs="{'invisible': ['|', '|', ('advance_payment_method', '!=', 'invoiceable'), ('total_down_payment', '==', 0), ('count','&gt;',1)]}" colspan="2">
                                <field name="deduct_down_payment" class="ml92"/>
                                <label for="deduct_down_payment"/>
                                <field name="currency_id" invisible="1"/>
                                ( <field name="total_down_payment" widget="monetary"/> )
                            </div>
                            <label for="down_payment_method" attrs="{'invisible': [('advance_payment_method', '!=', 'down_payment')]}"/>
                            <div attrs="{'invisible': [('advance_payment_method', '!=', 'down_payment')]}">
                                <field name="down_payment_method" class="oe_inline"/>
                                <field name="amount" attrs="{'required': [('advance_payment_method', '=', 'down_payment')]}" class="ml16 oe_inline"/>
                                <label string="%" attrs="{'invisible': [('down_payment_method', '!=', 'percentage')]}" class="oe_inline"/>
                            </div>
                        </group>
                        <group attrs="{'invisible': ['|', ('advance_payment_method', '!=', 'down_payment'), ('product_id', '!=', False)]}">
                            <field name="deposit_account_id" class="oe_inline" groups="account.group_account_manager"/>
                            <field name="deposit_taxes_id" class="oe_inline" widget="many2many_tags" domain="[('type_tax_use','=','sale')]"/>
                        </group>
                    </group>
                    <footer>
                        <button name="create_invoices" string="Create and View Invoices" type="object"
                            context="{'open_invoices': True}" class="btn-primary"/>
                        <button name="create_invoices" string="Create Invoices" type="object"
                            class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_view_sale_advance_payment_inv" model="ir.actions.act_window">
            <field name="name">Invoice Order</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.advance.payment.inv</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="groups_id" eval="[(4,ref('sales_team.group_sale_salesman'))]"/>
        </record>

        <!-- TODO: check if we need this -->
        <record model="ir.values" id="sale_order_line_make_invoice">
            <field name="model_id" ref="sale.model_sale_order_line" />
            <field name="name">Invoice Orders</field>
            <field name="key2">client_action_multi</field>
            <field name="value" eval="'ir.actions.act_window,' + str(ref('action_view_sale_advance_payment_inv'))" />
            <field name="key">action</field>
            <field name="model">sale.order</field>
        </record>

</odoo>
