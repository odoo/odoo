<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_sale_advance_payment_inv" model="ir.ui.view">
            <field name="name">Invoice Orders</field>
            <field name="model">sale.advance.payment.inv</field>
            <field name="arch" type="xml">
                <form string="Invoice Sales Order">
                    <p class="oe_grey">
                        Invoices will be created in draft so that you can review
                        them before validation.
                    </p>
                    <group>
                        <field name="count" invisible="[('count','=',1)]" readonly="True"/>
                        <field name="sale_order_id" invisible="1"/>
                        <field name="invoice_options" invisible="1"/>
                        <field name="currency_id" invisible="1"/>
                        <field name="product_id"
                            context="{'search_default_services': 1, 'default_type': 'service', 'default_invoice_policy': 'order'}" class="oe_inline"
                            invisible="1"/>
                        <group>
                            <field name="advance_payment_method" class="oe_inline" widget="radio" options="{'visibility_field': 'invoice_options'}" attrs="{'invisible': [('count','&gt;',1)]}"/>
                            <label for="amount" attrs="{'invisible': [('advance_payment_method', 'not in', ('fixed', 'percentage'))]}"/>
                            <div attrs="{'invisible': [('advance_payment_method', 'not in', ('fixed', 'percentage'))]}">
                                <field name="amount" attrs="{'required': [('advance_payment_method', 'in', ('fixed', 'percentage'))]}" class="oe_inline"/>
                                <label string="%%" attrs="{'invisible': [('advance_payment_method', '!=', 'percentage')]}" class="oe_inline"/>
                            </div>
                            <field name="deposit_account_id" class="oe_inline" attrs="{'invisible': ['|', ('advance_payment_method', 'not in', ('fixed', 'percentage')), ('product_id', '!=', False)]}" groups="account.group_account_manager"/>
                            <field name="deposit_taxes_id" class="oe_inline" widget="many2many_tags" domain="[('type_tax_use','=','sale')]" attrs="{'invisible': ['|', ('advance_payment_method', 'not in', ('fixed', 'percentage')), ('product_id', '!=', False)]}"/>
                        </group>
                        <group class="oe_subtotal_footer oe_subtotal_footer_left" attrs="{'invisible': [('count','&gt;',1)]}">
                            <field name="order_total" widget="monetary"/>
                            <field name="upsell_downsell"  widget="monetary" help="Price changes or extra lines added in previous invoices" attrs="{'invisible': [('upsell_downsell', '=', 0)]}"/>
                            <div class="oe_subtotal_footer_separator o_td_label" attrs="{'invisible': [('upsell_downsell', '=', 0)]}">
                                <label for="total_to_invoice"/>
                            </div>
                            <field name="total_to_invoice" widget="monetary" nolabel="1" class="oe_subtotal_footer_separator" attrs="{'invisible': [('upsell_downsell', '=', 0)]}"/>
                            <field name="downpayment_total" widget="monetary" attrs="{'invisible': [('downpayment_total', '=', 0)]}"/>
                            <field name="already_invoiced" widget="monetary" attrs="{'invisible': [('already_invoiced', '=', 0)]}"/>
                            <div class="oe_subtotal_footer_separator o_td_label" attrs="{'invisible': [('undelivered_products', '=', 0)]}">
                                <label for="unbilled_total"/>
                            </div>
                            <field name="unbilled_total" widget="monetary" nolabel="1" class="oe_subtotal_footer_separator" attrs="{'invisible': [('undelivered_products', '=', 0)]}"/>
                            <field name="undelivered_products" widget="monetary" help="Products to invoice after the delivery" attrs="{'invisible': ['|',('undelivered_products', '=', 0),('ready_to_invoice', '==', 0)]}"/>
                            <div class="oe_subtotal_footer_separator o_td_label" attrs="{'invisible': [('ready_to_invoice', '==', 0)]}">
                                <label for="ready_to_invoice"/>
                            </div>
                            <field name="ready_to_invoice" widget="monetary" nolabel="1" class="oe_subtotal_footer_separator" attrs="{'invisible': [('ready_to_invoice', '==', 0)]}"/>
                        </group>
                    </group>
                    <footer>
                        <button name="create_invoices" string="Create and View Invoices" type="object"
                            context="{'open_invoices': True}" class="btn-primary"/>
                        <button name="create_invoices" string="Create Invoices" type="object"
                            class="btn-primary"/>
                        <button string="Cancel" class="btn-default" special="cancel"/>
                    </footer>
                </form>
            </field>
        </record>

        <record id="action_view_sale_advance_payment_inv" model="ir.actions.act_window">
            <field name="name">Invoice Order</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">sale.advance.payment.inv</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="target">new</field>
            <field name="groups_id" eval="[(4,ref('sales_team.group_sale_salesman'))]"/>
            <!-- TODO: check if we need this -->
            <field name="binding_model_id" ref="sale.model_sale_order" />
        </record>

</odoo>
