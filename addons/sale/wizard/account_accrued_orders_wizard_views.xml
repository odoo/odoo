<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="action_accrued_revenue_entry_sale_order_line" model="ir.actions.server">
        <field name="name">Accrued Revenue Entry</field>
        <field name="model_id" ref="sale.model_sale_order_line"/>
        <field name="binding_model_id" ref="sale.model_sale_order_line"/>
        <field name="state">code</field>
        <field name="group_ids" eval="[(4, ref('account.group_account_user'))]"/>
        <field name="code">
if not records:
    raise UserError(env._("Please select at least one item from the list."))

action = records.action_open_accrual_wizard()
        </field>
    </record>

</odoo>
