<?xml version="1.0" encoding="utf-8"?>
<odoo>
     <record id="sale_report_view_pivot" model="ir.ui.view">
         <field name="name">sale.report.pivot</field>
         <field name="model">sale.report</field>
         <field name="arch" type="xml">
            <pivot string="Sales Analysis">
                <field name="name" type="row"/>
                <field name="price_total" string="Total Price" type="measure"/>
                <field name="team_id"/>
                <field name="confirmation_date" interval="month" type="row"/>
            </pivot>
         </field>
    </record>
    <record id="sale_report_view_search" model="ir.ui.view">
        <field name="name">sale.report.search</field>
        <field name="model">sale.report</field>
        <field name="arch" type="xml">
            <search string="Sales Analysis">
                <field name="date_order"/>
                <filter name="Quotations" string="Quotations" domain="[('state','in', ('draft', 'sent'))]"/>
                <filter name="Sales" string="Sales" domain="[('state','not in',('draft', 'cancel', 'sent'))]"/>
                <filter string="Current Year Sales" name="current_year" domain="[('date_order','&gt;=',time.strftime('%%Y-01-01'))]"/>
                <filter string="Confirmed This Month" name="confirmed_current_month" domain="[('confirmation_date','&gt;=', time.strftime('%%Y-%m-01'))]"/>
                <filter string="Confirmed Last Month" name="confirmed_last_month" domain="[('confirmation_date','&lt;',time.strftime('%Y-%m-01')),('confirmation_date','&gt;=', datetime.datetime.now().replace(month=(datetime.datetime.now().month - 1)).strftime('%Y-01-%m'))]"/>
                <filter string="Confirmed This Year" name="confirmed_current_year" domain="[('confirmation_date','&gt;=',time.strftime('%Y-01-01'))]"/>
                <separator/>
                <field name="partner_id"/>
                <field name="product_id"/>
                <field name="user_id"/>
                <field name="categ_id" filter_domain="[('categ_id', 'child_of', self)]"/>
                <field name="company_id" groups="base.group_multi_company"/>
                <group expand="1" string="Group By">
                    <filter string="Salesperson" name="User" context="{'group_by':'user_id'}"/>
                    <filter string="Sales Channel" name="sales_channel" context="{'group_by':'team_id'}"/>
                    <filter string="Partner's Country" name="country_id" context="{'group_by':'country_id'}"/>
                    <filter string="Customer" name="Customer" context="{'group_by':'partner_id'}"/>
                    <filter string="Product Category" name="Category" context="{'group_by':'categ_id'}"/>
                    <filter name="status" string="Status" context="{'group_by':'state'}"/>
                    <filter string="Company" name="company" groups="base.group_multi_company" context="{'group_by':'company_id'}"/>
                    <separator/>
                    <filter string="Order Month" name="order_month" context="{'group_by':'date:month'}" help="Ordered date of the sales order"/>
                </group>
            </search>
        </field>
    </record>

    <record id="sale_report_action" model="ir.actions.act_window">
        <field name="name">Sales Analysis</field>
        <field name="res_model">sale.report</field>
        <field name="view_mode">pivot</field>
        <field name="view_id" ref="sale_report_view_pivot"/>
        <field name="search_view_id" ref="sale_report_view_search"/>
        <field name="context">{'search_default_team_id': 1, 'search_default_current_year': 1}</field>
    </record>
</odoo>
