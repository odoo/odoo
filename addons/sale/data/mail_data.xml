<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Activities -->
        <record id="mail_act_sale_upsell" model="mail.activity.type">
            <field name="name">Order Upsell</field>
            <field name="icon">fa-line-chart</field>
            <field name="res_model_id" ref="sale.model_sale_order"/>
        </record>

        <!-- Sale-related subtypes for messaging / Chatter -->
        <record id="mt_order_sent" model="mail.message.subtype">
            <field name="name">Quotation sent</field>
            <field name="res_model">sale.order</field>
            <field name="default" eval="False"/>
            <field name="description">Quotation sent</field>
        </record>
        <record id="mt_order_confirmed" model="mail.message.subtype">
            <field name="name">Sales Order Confirmed</field>
            <field name="res_model">sale.order</field>
            <field name="default" eval="False"/>
            <field name="description">Quotation confirmed</field>
        </record>

        <!-- Salesteam-related subtypes for messaging / Chatter -->
        <record id="mt_salesteam_order_sent" model="mail.message.subtype">
            <field name="name">Quotation sent</field>
            <field name="sequence">20</field>
            <field name="res_model">crm.team</field>
            <field name="default" eval="True"/>
            <field name="parent_id" eval="ref('sale.mt_order_sent')"/>
            <field name="relation_field">team_id</field>
        </record>
        <record id="mt_salesteam_order_confirmed" model="mail.message.subtype">
            <field name="name">Sales Order Confirmed</field>
            <field name="sequence">21</field>
            <field name="res_model">crm.team</field>
            <field name="default" eval="True"/>
            <field name="parent_id" eval="ref('sale.mt_order_confirmed')"/>
            <field name="relation_field">team_id</field>
        </record>
        <record id="mt_salesteam_invoice_created" model="mail.message.subtype">
            <field name="name">Invoice Created</field>
            <field name="sequence">22</field>
            <field name="res_model">crm.team</field>
            <field name="parent_id" eval="ref('account.mt_invoice_created')"/>
            <field name="relation_field">team_id</field>
        </record>
        <record id="mt_salesteam_invoice_confirmed" model="mail.message.subtype">
            <field name="name">Invoice Confirmed</field>
            <field name="sequence">23</field>
            <field name="res_model">crm.team</field>
            <field name="parent_id" eval="ref('account.mt_invoice_validated')"/>
            <field name="relation_field">team_id</field>
        </record>
    </data>
    <!-- Template and notification section -->
    <data noupdate="1">
        <template id="" inherit_id="mail.mail_notification_paynow" name="oezirhgoi">
            <xpath expr="//t[@t-set='access_code']" position="after">
                <t t-set="access_code" t-value="(record._name == 'sale.order' and record.state in ('draft', 'sent') and record.get_portal_confirmation_action()) or access_code"/>
            </xpath>
            <xpath expr="//t[@t-set='access_name']" position="after">
                <t t-if="record._name == 'sale.order' and access_code != 'view'">
                    <t t-set="access_name">Accept and sign online</t>
                </t>
                <t t-if="record._name == 'sale.order' and access_code == 'sign' and record.has_to_be_paid()">
                    <t t-set="access_name">Sign and pay online</t>
                </t>
            </xpath>
        </template>

        <record id="email_template_edi_sale" model="mail.template">
            <field name="name">Sales Order: Send by email</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="subject">${object.company_id.name} ${object.state in ('draft', 'sent') and 'Quotation' or 'Order'} (Ref ${object.name or 'n/a' })</field>
            <field name="body_html" type="html">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set doc_name = 'quotation' if object.state in ('draft', 'sent') else 'order'
        Dear ${object.partner_id.name}
        % if object.partner_id.parent_id:
            (${object.partner_id.parent_id.name})
        % endif
        <br /><br />
        Here is
        % if ctx.get('proforma')
            in attachment your pro-forma invoice
        % else
            the ${doc_name} <strong>${object.name}</strong>
        % endif
        % if object.origin:
            (with reference: ${object.origin} )
        % endif
        amounting in <strong>${format_amount(object.amount_total, object.pricelist_id.currency_id)}</strong>
        from ${object.company_id.name}.
        <br /><br />
        Do not hesitate to contact us if you have any question.
    </p>
</div>
            </field>
            <field name="report_template" ref="action_report_saleorder"/>
            <field name="report_name">${(object.name or '').replace('/','_')}${object.state == 'draft' and '_draft' or ''}</field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="user_signature" eval="False"/>
            <field name="auto_delete" eval="True"/>
        </record>

        <record id="order_confirmation_mail_template" model="mail.template">
            <field name="name">Order confirmation e-mail</field>
            <field name="model_id" ref="sale.model_sale_order"/>
            <field name="email_from">${object.user_id.email_formatted |safe}</field>
            <field name="email_to">${(object.partner_id.email or '')|safe}</field>
            <field name="subject">Your order ${object.name}</field>
            <field name="body_html" type="xml">
<div style="margin: 0px; padding: 0px;">
    <p style="margin: 0px; padding: 0px; font-size: 13px;">
        % set transaction = ctx['transaction']
        Hello,<br/><br/>
        Your order ${object.name} amounting in ${object.currency_id.symbol}${object.amount_total}
        % if transaction.state != 'pending':
            has been confirmed.
        % else:
            will be validated when the payment is received.
        % endif
    </p><br/>
    % if transaction.state == 'pending':
        <strong>PAYMENT INSTRUCTIONS</strong><br/>
        ${transaction.acquirer_id.post_msg | safe}<br/>
        Communication: ${object.reference}<br/><br/>
    % endif
    <a href="/my/orders/${object.id}" target="_blank" style="background-color: #44a19e; padding: 8px 16px; text-decoration: none; color: #fff; border-radius: 5px;">View Your Order</a>
    <br/><br/>
    % if transaction.state != 'pending':
        <h5><strong>Order Summary:</strong></h5>
        <table width="100%" style="padding: 10px; color: #454748; font-size: 11px;">
            <thead>
                <tr>
                    <th width="18%">Products</th>
                    <th></th>
                    <th align="left">Quantity</th>
                    <th align="left">Unit Price</th>
                    <th align="left">Price</th>
                </tr>
            </thead>
            <tbody>
                % for line in object.order_line:
                    <tr>
                        <td style="width: 150px;">
                             <img src="/web/image/product.product/${line.product_id.id}/image_small" width="64" alt="Product image"></img>
                        </td>
                        <td align="left">${line.product_id.name}</td>
                        <td>${line.product_uom_qty}</td>
                        <td>${line.price_unit}</td>
                        <td>${object.currency_id.symbol} ${line.price_subtotal}</td>
                    </tr>
                % endfor
            </tbody>
        </table>
        <table width="100%" style="padding: 10px; color: #454748;">
            <tr>
                <td>
                    <strong>Ship to:</strong>
                </td>
            </tr>
            <tr>
                <td style="font-size: 13px;">
                    ${object.partner_shipping_id.street}<br/>
                    ${object.partner_shipping_id.city}
                    ${object.partner_shipping_id.state_id.name}
                    ${object.partner_shipping_id.zip}<br/>
                    ${object.partner_shipping_id.country_id.name}
                </td>
            </tr>
            % if ctx['has_carrier']:
                <tr>
                    <td style="font-size: 13px;">
                        <strong>Shipping Method:</strong>
                        ${object.carrier_id.name} (${object.currency_id.symbol}${object.delivery_price})
                    </td>
                </tr>
            % endif
            <tr>
                <td style="padding-top: 10px;">
                    <strong>Bill to:</strong>
                </td>
            </tr>
            <tr>
                <td style="font-size: 13px;">
                    ${object.partner_invoice_id.street}<br/>
                    ${object.partner_invoice_id.city}
                    ${object.partner_invoice_id.state_id.name}
                    ${object.partner_invoice_id.zip}<br/>
                    ${object.partner_invoice_id.country_id.name}
                </td>
            </tr>
            <tr>
                <td style="font-size: 13px;">
                    <strong>Payment Method:</strong>
                    ${transaction.acquirer_id.name} (${transaction.currency_id.symbol}${transaction.amount})
                </td>
            </tr>
        </table>
    % endif
</div></field>
            <field name="lang">${object.partner_id.lang}</field>
            <field name="auto_delete" eval="True"/>
            <field name="user_signature" eval="False"/>
        </record>
    </data>
</odoo>
