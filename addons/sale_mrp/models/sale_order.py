from odoo import _, api, fields, models


class SaleOrder(models.Model):
    _inherit = "sale.order"

    mrp_production_count = fields.Count(
        count_of="mrp_production_ids",
        string="Count of MO generated",
        groups="mrp.group_mrp_user",
    )
    mrp_production_ids = fields.Many2many(
        comodel_name="mrp.production",
        string="Manufacturing Orders",
        compute="_compute_mrp_production_ids",
        groups="mrp.group_mrp_user",
    )

    @api.depends(
        "reference_ids.production_ids",
        "reference_ids.production_ids.state",
        "reference_ids.production_ids.production_group_id.parent_ids",
        "reference_ids.production_ids.picking_type_id.active",
    )
    def _compute_mrp_production_ids(self):
        for sale in self:
            mos = sale.reference_ids.production_ids
            sale.mrp_production_ids = mos.filtered(
                lambda mo: (
                    not mo.production_group_id.parent_ids
                    and mo.state != "cancel"
                    and mo.picking_type_id.active
                )
            )

    def action_view_mrp_production(self):
        self.check_singleton()
        action = {
            "res_model": "mrp.production",
            "type": "ir.actions.act_window",
        }
        if len(self.mrp_production_ids) == 1:
            action.update(
                {
                    "view_mode": "form",
                    "res_id": self.mrp_production_ids.id,
                }
            )
        else:
            action.update(
                {
                    "name": _("Manufacturing Orders Generated by %s", self.name),
                    "domain": [("id", "in", self.mrp_production_ids.ids)],
                    "view_mode": "list,form",
                }
            )
        return action
