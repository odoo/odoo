<?xml version="1.0" encoding="utf-8"?>
<!-- © 2026 Jbnegoc SPA - Todos los derechos reservados -->
<odoo>

    <!-- Categoría de módulos -->
    <record id="module_category_pos_audit" model="ir.module.category">
        <field name="name">Auditoría POS</field>
        <field name="description">Gestión de auditoría de eliminaciones en POS</field>
        <field name="sequence">50</field>
    </record>

    <!-- Grupo: Puede eliminar registros de auditoría -->
    <record id="group_pos_audit_delete" model="res.groups">
        <field name="name">Auditoría POS: Eliminar Registros</field>
        <field name="category_id" ref="module_category_pos_audit"/>
        <field name="comment">
            Permite eliminar registros de auditoría de productos eliminados.
            Este permiso debe otorgarse solo a gerentes o supervisores.
        </field>
    </record>

    <!-- Reglas de acceso por registro (Record Rules) -->

    <!-- Regla: Usuarios normales solo pueden ver registros de auditoría (lectura) -->
    <record id="pos_audit_deleted_rule_read" model="ir.rule">
        <field name="name">POS Audit Deleted: Read Access</field>
        <field name="model_id" ref="model_pos_audit_deleted"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Regla: Manager puede crear registros -->
    <record id="pos_audit_deleted_rule_create" model="ir.rule">
        <field name="name">POS Audit Deleted: Create Access</field>
        <field name="model_id" ref="model_pos_audit_deleted"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Regla: Solo usuarios con permiso específico pueden eliminar -->
    <record id="pos_audit_deleted_rule_unlink" model="ir.rule">
        <field name="name">POS Audit Deleted: Delete Access</field>
        <field name="model_id" ref="model_pos_audit_deleted"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('group_pos_audit_delete'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Regla: Justificaciones - todos los usuarios del POS pueden leer -->
    <record id="pos_deletion_reason_rule" model="ir.rule">
        <field name="name">POS Deletion Reason: All Users</field>
        <field name="model_id" ref="model_pos_deletion_reason"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_user'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Regla: Justificaciones - managers pueden gestionar -->
    <record id="pos_deletion_reason_rule_manager" model="ir.rule">
        <field name="name">POS Deletion Reason: Manager</field>
        <field name="model_id" ref="model_pos_deletion_reason"/>
        <field name="domain_force">[(1, '=', 1)]</field>
        <field name="groups" eval="[(4, ref('point_of_sale.group_pos_manager'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="True"/>
    </record>

    <!-- Reglas de multi-compañía (si aplica) -->
    <record id="pos_audit_deleted_company_rule" model="ir.rule">
        <field name="name">POS Audit Deleted: Multi-company</field>
        <field name="model_id" ref="model_pos_audit_deleted"/>
        <field name="domain_force">['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]</field>
        <field name="global" eval="True"/>
    </record>

</odoo>
