<?xml version="1.0" encoding="UTF-8" ?>
<templates xml:space="preserve">

  <t t-name="web.NavBar" owl="1">
    <header>
    <section>
      <div class="container py-4 my-4">

        <!-- DEMO  -->

        <h3>General Alignment</h3>
        <hr/>
        <div class="row mb-4 pb-4">
          <div class="col">
            <h1><Icon name="'pencil'" class="mr-1"/>Heading 1</h1>
            <h2><Icon name="'pencil'" class="mr-1"/>Heading 2</h2>
            <h3><Icon name="'pencil'" class="mr-1"/>Heading 3</h3>
            <h4><Icon name="'pencil'" class="mr-1"/>Heading 4</h4>
            <h5><Icon name="'pencil'" class="mr-1"/>Heading 5</h5>
            <h6><Icon name="'pencil'" class="mr-1"/>Heading 6</h6>
          </div>
          <div class="col">
            <p><Icon name="'pencil'"/> Paragraph lorem ipsum dolor sit amet <Icon name="'pencil'"/></p>
            <button class="btn btn-sm btn-primary mr-2">Button <Icon name="'pencil'"/></button>
            <button class="btn btn-primary mr-2">Button <Icon name="'pencil'"/></button>
            <button class="btn btn-lg btn-primary mr-2">Button LG <Icon name="'pencil'"/></button>
          </div>
        </div>
        <h3>Two-tone Icons, eg: <code>&lt;Icon name="'bucket'" tone="'#ff7eb6'"/></code></h3>
        <hr/>
        <div class="btn-group mb-4 pb-4" role="group" aria-label="Basic example">
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#000'" class="fa-2x"/>
          </button>
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#be95ff'" class="fa-2x"/>
          </button>
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#0bd9d9'" class="fa-2x"/>
          </button>
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#ff7eb6'" class="fa-2x"/>
          </button>
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#0f62fe'" class="fa-2x"/>
          </button>
          <button type="button" class="btn btn-lg btn-secondary">
            <Icon name="'bucket'" tone="'#f44336'" class="fa-2x"/>
          </button>
        </div>
        <h3>Animations <small>(triggered on JS events)</small></h3>
        <hr/>
        <div class="fa-2x card d-inline-block px-3 mr-4">
          <Icon name="'list_checked'" tone="'green'" class="fa-2x"/>
        </div>
        <div class="fa-2x card d-inline-block px-3">
          <Icon name="'cube'" tone="'green'" class="fa-2x"/>
        </div>

      </div>
    </section>
      <nav
        class="o_main_navbar"
        data-command-category="navbar"
      >
        <!-- Apps Menu -->
        <t t-call="web.NavBar.AppsMenu">
          <t t-set="apps" t-value="menuService.getApps()" />
        </t>

        <!-- App Brand -->
        <DropdownItem
          t-if="currentApp and !env.isSmall"
          href="getMenuItemHref(currentApp)"
          t-esc="currentApp.name"
          class="o_menu_brand"
          dataset="{ menuXmlid: currentApp.xmlid, section: currentApp.id }"
          onSelected="() => this.onNavBarDropdownItemSelection(currentApp)"
        />

        <!-- Current App Sections -->
        <t t-if="currentAppSections.length and !env.isSmall" t-call="web.NavBar.SectionsMenu">
          <t t-set="sections" t-value="currentAppSections" />
        </t>

        <!-- Systray -->
        <div class="o_menu_systray" role="menu">
          <t t-foreach="systrayItems" t-as="item" t-key="item.key">
            <NotUpdatable>
                <ErrorHandler onError="error => this.handleItemError(error, item)">
                    <t t-component="item.Component" t-props="item.props" />
                </ErrorHandler>
            </NotUpdatable>
          </t>
        </div>
      </nav>
    </header>
  </t>

  <t t-name="web.NavBar.AppsMenu" owl="1">
    <Dropdown hotkey="'h'" title="'Home Menu'" class="o_navbar_apps_menu">
      <t t-set-slot="toggler">
        <i class="fa fa-th-large" />
      </t>
      <DropdownItem
        t-foreach="apps"
        t-as="app"
        t-key="app.id"
        class="o_app"
        t-att-class="{ focus: menuService.getCurrentApp() === app }"
        href="getMenuItemHref(app)"
        t-esc="app.name"
        dataset="{ menuXmlid: app.xmlid, section: app.id }"
        onSelected="() => this.onNavBarDropdownItemSelection(app)"
      />
    </Dropdown>
  </t>

  <t t-name="web.NavBar.SectionsMenu" owl="1">
    <div class="o_menu_sections" t-ref="appSubMenus" role="menu">

      <t t-foreach="sections" t-as="section" t-key="section.id">
        <t
          t-set="sectionsVisibleCount"
          t-value="(sections.length - currentAppSectionsExtra.length)"
        />

        <t t-if="section_index lt Math.min(10, sectionsVisibleCount)">
          <t t-set="hotkey" t-value="((section_index + 1) % 10).toString()" />
        </t>
        <t t-else="">
          <t t-set="hotkey" t-value="undefined" />
        </t>

        <t t-if="!section.childrenTree.length">
          <DropdownItem
            title="section.name"
            class="o_nav_entry"
            href="getMenuItemHref(section)"
            hotkey="hotkey"
            t-esc="section.name"
            dataset="{ menuXmlid: section.xmlid, section: section.id }"
            onSelected="() => this.onNavBarDropdownItemSelection(section)"
          />
        </t>
        <t t-else="">
          <MenuDropdown
            hotkey="hotkey"
            title="section.name"
            xmlid="section.xmlid"
          >
            <t t-set-slot="toggler">
              <span t-esc="section.name" t-att-data-section="section.id" />
            </t>
            <t t-call="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
              <t t-set="items" t-value="section.childrenTree" />
            </t>
          </MenuDropdown>
        </t>
      </t>

      <t t-if="currentAppSectionsExtra.length" t-call="web.NavBar.SectionsMenu.MoreDropdown">
        <t t-set="sections" t-value="currentAppSectionsExtra" />
        <t t-if="sectionsVisibleCount lt 10">
          <t t-set="hotkey" t-value="(sectionsVisibleCount + 1 % 10).toString()" />
        </t>
      </t>

    </div>
  </t>

  <t t-name="web.NavBar.SectionsMenu.Dropdown.MenuSlot" owl="1">
    <t t-foreach="items" t-as="item" t-key="item.id">
      <DropdownItem
        t-if="!item.childrenTree.length"
        href="getMenuItemHref(item)"
        class="dropdown-item"
        t-esc="item.name" 
        dataset="{ menuXmlid: item.xmlid, section: item.id }"
        onSelected="() => this.onNavBarDropdownItemSelection(item)"
      />

      <t t-else="">
        <div class="dropdown-menu_group dropdown-header" t-esc="item.name" />
        <DropdownItem
          t-foreach="item.childrenTree"
          t-as="subItem"
          t-key="subItem.id"
          class="o_dropdown_menu_group_entry dropdown-item"
          href="getMenuItemHref(subItem)"
          t-esc="subItem.name"
          dataset="{ menuXmlid: subItem.xmlid, section: subItem.id }"
          onSelected="() => this.onNavBarDropdownItemSelection(subItem)"
        />
      </t>
    </t>
  </t>

  <t t-name="web.NavBar.SectionsMenu.MoreDropdown" owl="1">
    <Dropdown class="o_menu_sections_more" title="'More Menu'" hotkey="hotkey">
      <t t-set-slot="toggler">
        <i class="fa fa-plus" />
      </t>
      <t t-foreach="sections" t-as="section" t-key="section.id">

        <t t-if="!section.childrenTree.length">
          <DropdownItem
            class="o_more_dropdown_section"
            href="getMenuItemHref(section)"
            t-esc="section.name"
            dataset="{ menuXmlid: section.xmlid, section: section.id }"
            onSelected="() => this.onNavBarDropdownItemSelection(section)"
          />
        </t>
        <t t-else="">
          <div
            class="o_more_dropdown_section o_more_dropdown_section_group dropdown-header bg-100"
            t-esc="section.name"
          />
          <t t-call="web.NavBar.SectionsMenu.Dropdown.MenuSlot">
            <t t-set="items" t-value="section.childrenTree" />
          </t>
        </t>
      </t>
    </Dropdown>
  </t>

</templates>
