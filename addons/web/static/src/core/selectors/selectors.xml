<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.AutoCompleteWithSources" >
        <AutoComplete
            value="props.value"
            autoSelect="true"
            resetOnSelect="props.multiSelect"
            class="props.className"
            sources="sources"
            onSelect.bind="onSelect"
            onChange.bind="onChange"
        />
    </t>

    <t t-name="web.TagsSelector" >
        <div class="o_input d-flex flex-wrap h-100 gap-1 o_tags_selector">
            <TagsList tags="tags"/>
            <AutoCompleteWithSources
                resModel="props.resModel"
                value="''"
                domain="props.domain"
                context="props.context"
                className="'o_input o_domain_leaf_value_input flex-grow-1'"
                fieldString="props.fieldString"
                multiSelect="true"
                getIds.bind="getIds"
                update.bind="update"
            />
        </div>
    </t>

    <t t-name="web.ValueSelector" >
        <AutoCompleteWithSources
            resModel="props.resModel"
            value="displayName"
            domain="props.domain"
            context="props.context"
            className="'o_input o_domain_leaf_value_input h-100 o_tag_selector'"
            fieldString="props.fieldString"
            multiSelect="false"
            getIds="() => []"
            update.bind="update"
        />
    </t>

</templates>
