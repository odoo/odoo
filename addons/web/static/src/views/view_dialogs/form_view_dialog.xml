<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

  <t t-name="web.FormViewDialog">
    <Dialog size="this.props.size" title="this.props.title" withBodyPadding="false" modalRef="this.modalRef" onExpand="this.onExpandCallback">
      <View t-props="this.viewProps"/>
      <t t-set-slot="footer">
        <t t-if="this.props.preventEdit and this.props.preventCreate">
          <button class="btn btn-primary" t-on-click="() => this.props.close()" data-available-offline="">Close</button>
        </t>
        <t t-else="">
          <button class="btn btn-primary o-default-button" t-on-click="() => this.props.close()" data-available-offline="">Ok</button>
        </t>
      </t>
    </Dialog>
  </t>

  <t t-name="web.FormViewDialog.ToMany.buttons" t-inherit="web.FormView.DialogButtons">
    <xpath expr="//button[hasclass('o_form_button_save')]" position="replace">
      <button class="btn btn-primary o_form_button_save" t-on-click="this.saveButtonClicked" data-hotkey="c">Save</button>
      <button t-if="!this.env.isSmall" class="btn btn-primary o_form_button_save_new" t-on-click="() => this.saveButtonClicked({saveAndNew: true})" data-hotkey="n">Save &amp; New</button>
    </xpath>
  </t>

  <t t-name="web.FormViewDialog.ToOne.buttons" t-inherit="web.FormView.DialogButtons">
    <xpath expr="//button[hasclass('o_form_button_save')]" position="replace">
      <button class="btn btn-primary o_form_button_save" t-on-click="this.saveButtonClicked" data-hotkey="c">Save</button>
    </xpath>
  </t>

</templates>
