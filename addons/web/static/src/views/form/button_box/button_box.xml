<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

<t t-name="web.Form.ButtonBox" >
    <div class="o-form-buttonbox d-print-none position-relative d-flex w-md-auto" t-attf-class="{{ isFull ? 'o_full w-100' : 'o_not_full'}} {{this.props.class}}">
        <t t-slot="{{ button_value }}" t-foreach="visibleButtons" t-as="button" t-key="button_value"/>
        <div t-if="additionalButtons.length" class="oe_stat_button btn position-relative p-0 border-0">
            <Dropdown t-if="!env.isSmall" position="'bottom-end'" menuClass="'o-form-buttonbox p-0 border-0 o_dropdown_more'">
                <button class="o_button_more btn d-flex justify-content-center align-items-center" t-att-class="'o-dropdown-caret btn-outline-secondary'">
                    <span>More</span>
                </button>
                <t t-set-slot="content">
                    <DropdownItem t-foreach="additionalButtons" t-as="button" t-key="button_value" class="'d-flex flex-column p-0'">
                        <t t-slot="{{ button_value }}" />
                    </DropdownItem>
                </t>
            </Dropdown>
            <div t-if="env.isSmall" class="oe_stat_button btn position-relative p-0 border-0">
                <button class="btn d-flex justify-content-center align-items-center btn-secondary"
                    t-on-click="openBottomSheet">
                    <i class="fa fa-fw fa-bolt lh-base" aria-hidden="true"/>
                </button>
            </div>
        </div>
    </div>
</t>

</templates>
