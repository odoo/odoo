<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.FormView" owl="1">
        <div t-att-class="props.className" t-ref="root">
            <Layout className="model.useSampleModel ? 'o_view_sample_data' : ''">
                <t t-set-slot="layout-buttons">
                    <t t-if="footerArchInfo and env.inDialog">
                        <t t-component="props.Renderer" record="model.root" Compiler="props.Compiler" archInfo="footerArchInfo"/>
                    </t>
                    <t t-else="">
                        <t t-call="{{ props.buttonTemplate }}"/>
                    </t>
                </t>
                <t t-set-slot="control-panel-bottom-left">
                    <t t-if="props.info.actionMenus and !model.root.isVirtual">
                        <ActionMenus
                            activeIds="[model.root.resId]"
                            context="props.context"
                            items="getActionMenuItems()"
                            isDomainSelected="model.root.isDomainSelected"
                            resModel="model.root.resModel"/>
                    </t>
                </t>
                <t t-set-slot="control-panel-bottom-right"> </t>
                <t t-component="props.Renderer" record="model.root" Compiler="props.Compiler" archInfo="archInfo"/>
            </Layout>
        </div>
    </t>

    <t t-name="web.FormView.Buttons" owl="1">
        <div class="o_cp_buttons" role="toolbar" aria-label="Main actions" t-ref="cpButtons">
            <div t-if="model.root.isInEdition" class="o_form_buttons_edit">
                <button type="button" class="btn btn-primary o_form_button_save" data-tooltip="Save record" data-hotkey="s" t-on-click.stop="save">
                    Save
                </button>
                <button type="button" class="btn btn-secondary o_form_button_cancel" data-tooltip="Discard changes" data-hotkey="j" t-on-click.stop="discard">
                    Discard
                </button>
            </div>
            <div t-else="" class="o_form_buttons_view">
                <t t-if="!canEdit and !canCreate">
                    <button type="button" class="btn btn-primary o_form_button_cancel" data-tooltip="Discard changes" data-hotkey="j" t-on-click.stop="discard">
                        Discard
                    </button>
                </t>
                <t t-else="">
                    <button t-if="canEdit" type="button" class="btn btn-primary o_form_button_edit" data-tooltip="Edit record" data-hotkey="a" data-bounce-button="" t-on-click.stop="edit">
                        Edit
                    </button>
                    <button t-if="canCreate" type="button" class="btn btn-secondary o_form_button_create" data-tooltip="Create record" data-hotkey="c" t-on-click.stop="create">
                        Create
                    </button>
                </t>
            </div>
        </div>
    </t>

</templates>
