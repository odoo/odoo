<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.KanbanExamplesNotebookTemplate">
        <div t-if="this.props.description" class="o_kanban_examples_description" t-out="this.props.description"/>
        <div class="o_kanban_examples d-flex p-2">
            <t t-foreach="this.columns" t-as="column" t-key="this.column.title">
                <div class="o_kanban_examples_group flex-grow-1 m-3">
                    <h6><b t-esc="this.column.title"/></h6>
                    <div t-foreach="this.column.records" t-as="record" t-key="this.record.id" class="o_kanban_examples_ghost p-2 border bg-white" t-att-class="{ 'o_collapse': record_index > 0 }">
                        <div class="o_ghost_content pb-3 bg-300"/>
                        <div class="o_ghost_content o_ghost_tag d-inline-block w-50 mt-3 pb-3 bg-300"/>
                        <span class="ms-2" t-if="this.record.bullet" t-out="this.record.bullet"/>
                    </div>
                </div>
            </t>
        </div>
    </t>

    <!-- Note: this dialog isn't responsive, but it is not accessible on mobile -->
    <t t-name="web.KanbanColumnExamplesDialog">
        <Dialog contentClass="'o_kanban_examples_dialog'" title.translate="Kanban Examples" size="'xl'">
            <Notebook orientation="'vertical'" pages="this.pages" onPageUpdate.bind="this.onPageUpdate" />
            <t t-set-slot="footer">
                <button class="btn btn-primary" t-on-click="this.applyExamples" t-esc="this.props.applyExamplesText"/>
                <button class="btn btn-secondary" t-on-click="this.props.close">Close</button>
            </t>
        </Dialog>
    </t>

</templates>
