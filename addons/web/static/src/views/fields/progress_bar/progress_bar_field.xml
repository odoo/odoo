<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.ProgressBarField">
        <div class="o_progressbar w-100 d-flex align-items-center" t-ref="numpadDecimal">
            <div t-if="this.props.title" class="o_progressbar_title text-nowrap pe-1"><t t-esc="this.props.title"/></div>
            <div class="o_progress align-middle overflow-hidden" aria-valuemin="0" t-att-aria-valuemax="this.maxValue" t-att-aria-valuenow="this.currentValue">
                <div t-attf-class="{{ this.progressBarColorClass }} h-100" t-att-style="'width: min(' + 100 * this.currentValue / this.maxValue + '%, 100%)'"></div>
            </div>
            <div class="o_progressbar_value d-flex">
                <t t-if="this.isPercentage">
                    <input
                        t-if="this.isEditable and this.props.isCurrentValueEditable"
                        t-ref="currentValue"
                        class="o_input h-100 text-center"
                        type="text"
                        inputmode="decimal"
                        autocomplete="off"
                        t-att-required="this.props.required"
                        t-on-focus="this.onInputFocus"
                        t-on-blur="this.onInputBlur"
                    />
                    <span t-else="" class="mx-1" t-esc="this.formatCurrentValue(true)"/>
                    <span>%</span>
                </t>
                <t t-else="">
                    <input
                        t-if="this.isEditable and this.props.isCurrentValueEditable"
                        t-ref="currentValue"
                        class="o_input h-100 text-center"
                        type="text"
                        inputmode="decimal"
                        autocomplete="off"
                        t-on-focus="this.onInputFocus"
                        t-on-blur="this.onInputBlur"
                    />
                    <span t-else="" class="mx-1" t-esc="this.formatCurrentValue(true)"/>
                    /
                    <input
                        t-if="this.isEditable and this.props.isMaxValueEditable"
                        t-ref="maxValue"
                        class="o_input h-100 text-center"
                        type="text"
                        inputmode="decimal"
                        autocomplete="off"
                        t-on-focus="this.onInputFocus"
                        t-on-blur="this.onInputBlur"
                    />
                    <span t-else="" class="mx-1" t-esc="this.formatMaxValue(true)"/>
                </t>
            </div>
        </div>
    </t>

</templates>
