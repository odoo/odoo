<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.ImageField">
        <div class="d-inline-block position-relative opacity-trigger-hover">
            <div t-if="!this.props.readonly and this.fieldType !== 'many2one'" class="o_image_uploader_container" t-attf-class="position-absolute d-flex justify-content-between w-100 bottom-0 opacity-0 opacity-100-hover {{this.isMobile ? 'o_mobile_controls' : ''}}" aria-atomic="true" t-att-style="this.sizeStyle">
                <FileUploader
                    acceptedFileExtensions="this.props.acceptedFileExtensions"
                    t-key="this.props.record.resId"
                    onUploaded.bind="this.onFileUploaded"
                >
                    <t t-set-slot="toggler">
                        <button
                            class="o_select_file_button btn btn-light border-0 rounded-circle m-1 p-1"
                            data-tooltip="Edit"
                            aria-label="Edit">
                            <i class="fa fa-pencil fa-fw"/>
                        </button>
                    </t>
                    <button
                        t-if="this.props.record.data[this.props.name] and this.state.isValid"
                        class="o_clear_file_button btn btn-light border-0 rounded-circle m-1 p-1"
                        data-tooltip="Clear"
                        aria-label="Clear"
                        t-on-click="this.onFileRemove">
                        <i class="fa fa-trash-o fa-fw"/>
                    </button>
                </FileUploader>
            </div>
            <img
                loading="lazy"
                t-att-class="this.imgClass"
                t-att-alt="this.imgAlt"
                t-att-src="this.getUrl(this.props.previewImage or this.props.name)"
                t-att-name="this.props.name"
                t-att-height="this.props.height"
                t-att-width="this.props.width"
                t-att-style="this.sizeStyle"
                t-on-error.stop="this.onLoadFailed"
                t-att-data-tooltip-template="this.hasTooltip and this.tooltipAttributes.template"
                t-att-data-tooltip-info="this.hasTooltip and this.tooltipAttributes.info"
                t-att-data-tooltip-delay="this.hasTooltip and this.props.zoomDelay"
            />
        </div>
    </t>

    <t t-name="web.ImageZoomTooltip">
        <div class="o_image_zoom">
            <img t-att-src="this.url" />
        </div>
    </t>

</templates>
