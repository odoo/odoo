<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.EmbeddedActionsBar">
        <div class="o_embedded_actions_bar d-contents" t-ref="root">
            <Transition visible="state.embeddedInfos.showEmbedded" name="'o-fade'" t-slot-scope="transition" leaveDuration="500">
                <div class="o_embedded_actions d-flex flex-wrap w-100 align-items-center justify-content-center gap-2" t-att-class="transition.className">
                    <t t-foreach="state.embeddedInfos.embeddedActions" t-as="action" t-key="action.id">
                        <t t-if="_isEmbeddedActionVisible(action)">
                            <button class="btn btn-secondary o_draggable"
                                    t-att-class="{ 'active': state.embeddedInfos.currentEmbeddedAction?.id === action.id}"
                                    t-on-click="() => props.embeddedActions.openAction(action)"
                                    t-att-data-embedded-index="action_index"
                            >
                                <span t-out="action.name"/>
                            </button>
                        </t>
                    </t>
                    <EmbeddedActionsDropdown embeddedActions="props.embeddedActions" state="dropdownState"/>
                </div>
            </Transition>
        </div>
    </t>

    <t t-name="web.EmbeddedActionsDropdown">
        <Dropdown state="dropdownState" menuClass="'o_embedded_actions_dropdown_menu'">
            <button name="openEmbeddedActions" class="btn btn-secondary" title="Embedded actions" aria-label="Embedded actions">
                <i class="fa-solid fa-fw fa-sliders"/>
            </button>
            <t t-set-slot="content">
                <t t-foreach="state.embeddedInfos.embeddedActions" t-as="action" t-key="action.id">
                    <DropdownItem
                        class="this.getDropdownClass(action)"
                        onSelected="() => this.env.isSmall ? this.onEmbeddedActionClick(action) : this.setVisibility(action.id)"
                        closingMode="'none'"
                    >
                        <div class="d-flex p-0 pe-1 align-items-center justify-content-between">
                            <span t-out="action.name"/>
                            <i t-if="action.is_deletable" class="o_icon_right btn btn-link text-danger p-0 position-absolute end-0 fa-regular fa-trash-can" role="button" tabindex="0" title="Delete item" aria-label="Delete item" t-on-click.stop="() => this.openConfirmationDialog(action)" t-on-keydown="(ev) => this.onDeleteKeydown(ev, action)" />
                        </div>
                    </DropdownItem>
                </t>
                <div role="separator" class="dropdown-divider"/>
                <AccordionItem class="'o_save_current_view'" description.translate="Save View">
                    <div class="px-2 py-2">
                        <input type="text"
                            t-ref="newActionNameRef"
                            class="o_input mb-2"
                            t-model.trim="state.embeddedInfos.newActionName"
                        />
                        <CheckBox value="state.embeddedInfos.newActionIsShared" onChange.bind="onShareCheckboxChange">
                            <span data-tooltip="Make this embedded action available to other users">Shared</span>
                        </CheckBox>
                    </div>
                    <div class="px-3 py-2">
                        <button class="o_save_favorite btn btn-primary w-100" t-on-click="saveNewAction">
                            Save
                        </button>
                    </div>
                </AccordionItem>
            </t>
        </Dropdown>
    </t>

</templates>
