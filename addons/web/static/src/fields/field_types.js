// @ts-check

/** @module @web/fields/field_types - Constants for x2many field type identification */

/** Field types that represent x2many relations (one2many, many2many). */
export const X2M_TYPES = ["one2many", "many2many"];
