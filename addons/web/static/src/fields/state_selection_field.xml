<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.StateSelectionField" owl="1">
        <t t-if="props.readonly">
            <button class="d-flex align-items-center btn btn-link disabled">
                <span t-attf-class="{{ 'mx-2 o_status ' + colorClass }} "/>
                <span t-if="!props.hideLabel" class="o_status_label" t-esc="label"/>
            </button>
        </t>
        <t t-else="">
            <Dropdown title="currentValue" togglerClass="'btn btn-link d-flex'">
                <t t-set-slot="toggler">
                    <div class="d-flex align-items-center">
                        <span t-attf-class="{{ 'mx-2 o_status ' + colorClass }} "/>
                        <span t-if="!props.hideLabel" class="o_status_label" t-esc="label"/>
                    </div>
                </t>
                <t t-foreach="props.options" t-as="option" t-key="option[0]">
                    <DropdownItem t-if="option[0] != currentValue" onSelected="() => this.onChange(option[0])">
                        <div class="d-flex align-items-center">
                            <span t-attf-class="{{ 'mx-2 o_status ' + option[0] }} "/>
                            <span t-esc="option[1]"/>
                        </div>
                    </DropdownItem>
                </t>
            </Dropdown>
        </t>
    </t>

</templates>
