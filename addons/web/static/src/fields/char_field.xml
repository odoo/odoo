<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <t t-name="web.CharField" owl="1">
        <t t-if="props.readonly">
            <span class="o-char-field" t-esc="formattedValue" />
        </t>
        <t t-else="">
            <input
                class="o-char-field o_input"
                t-att-type="isPassword ? 'password' : 'text'"
                t-att-value="props.value or ''"
                t-att-autocomplete="props.autocomplete or (isPassword ? 'new-password' : 'off')"
                t-att-maxlength="maxLength > 0 and maxLength"
                t-att-placeholder="props.placeholder"
                t-on-change="onChange"
            />
        </t>
    </t>

</templates>
