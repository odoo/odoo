<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="product_document_form" model="ir.ui.view">
        <field name="name">product.document.form.sale</field>
        <field name="model">product.document</field>
        <field name="inherit_id" ref="product.product_document_form"/>
        <field name="arch" type="xml">
            <field name="attached_on_sale" position="after">
                <label for="form_field_ids" invisible="not form_field_ids"/>
                <div class="d-flex align-items-center gap-2 flex-wrap" invisible="not form_field_ids">
                    <field name="form_field_ids" widget="many2many_tags" class="oe_inline"/>
                    <button
                        name="action_open_pdf_form_fields"
                        type="object"
                        string="Configure"
                        help="Configure the path to fill the form fields."
                        class="btn btn-secondary btn-sm flex-shrink-0 ms-5"
                        groups="base.group_system"
                    />
                </div>
            </field>
        </field>
    </record>
</odoo>
