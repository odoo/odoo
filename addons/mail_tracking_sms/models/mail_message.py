from odoo import fields, models


class MailMessage(models.Model):
    _inherit = 'mail.message'

    # generation tracking
    source_sms_template_id = fields.Many2one(
        'sms.template', 'Generated by (SMS template)',
        index='btree_not_null', ondelete='set null',
    )
