<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="ir_translation_view_tree_transifex" model="ir.ui.view">
        <field name="name">ir.translation.transifex</field>
        <field name="model">ir.translation</field>
        <field name="inherit_id" ref="base.view_translation_tree"/>
        <field name="arch" type="xml">
            <field name="lang" position="after">
                <field name="transifex_url"
                       widget="link_button"
                       string="Transifex" />
                <field name="is_valid"
                       invisible="True" />
            </field>
            <tree position="attributes">
                <attribute name="decoration-warning">not is_valid</attribute>
            </tree>
        </field>
    </record>

    <record id="ir_translation_dialog_view_tree_transifex" model="ir.ui.view">
        <field name="name">ir.translation.transifex</field>
        <field name="model">ir.translation</field>
        <field name="inherit_id" ref="base.view_translation_dialog_tree"/>
        <field name="arch" type="xml">
            <field name="lang" position="after">
                <field name="transifex_url"
                       widget="link_button"
                       string="Transifex" />
            </field>
        </field>
    </record>

    <record id="action_check_translations" model="ir.actions.server">
        <field name="name">Find Bad Translations</field>
        <field name="type">ir.actions.server</field>
        <field name="model_id" ref="base.model_ir_translation"/>
        <field name="binding_model_id" ref="base.model_ir_translation"/>
        <field name="state">code</field>
        <field name="code">action = records.action_compute_validity()</field>
    </record>

</odoo>
