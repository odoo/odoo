<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="assets_frontend" inherit_id="website.assets_frontend" name="Account Manager">
      <xpath expr="." position="inside">
          <link rel='stylesheet' href='/website_portal/static/src/css/website_portal.less'/>
          <script type="text/javascript" src="/website_portal/static/src/js/website_portal.js"></script>
      </xpath>
    </template>

    <template id="account" name="Account">
        <t t-call="website.layout">
            <div id="wrap">
                <div class="container">
                    <div class="row">
                        <h1 class="text-center">Your Account</h1>
                        <h2 class="text-center text-muted" id="page_subtitle">Manage your sales documents</h2>
                    </div>
                    <div class="col-md-9" id="documents_list">
                        <t t-call="website_portal.quotations" />
                        <t t-call="website_portal.sale_orders" />
                        <t t-call="website_portal.invoices" />
                    </div>
                    <div t-if="sales_rep" class="col-md-3">
                      <t t-call="website_portal.contact" />
                      <div class="oe_structure"/>
                    </div>
                    <div class="col-md-3">
                        <h3 class="page-header">Your Details <a href="account/details" class="btn btn-default btn-xs">Change</a></h3>
                        <div t-field="user.partner_id" t-field-options='{"widget": "contact", "fields": ["email", "phone", "address", "name"]}'/>
                    </div>
                    <div class="oe_structure"/>
                </div>
            </div>
        </t>
    </template>

    <template id="website_portal.custom_panel" inherit_id='website_portal.account' customize_show="True" name="Editable Panel" active="False">
        <xpath expr="//div[@class='col-md-3']" position="after">
            <div class="col-md-3">
              <h3 class="page-header">You Custom Title</h3>
              <p>Place your content here.</p>
            </div>
        </xpath>
    </template>

    <template id="contact" name="Contact">
        <h3 class="page-header">Contact</h3>
          <h5 t-field="sales_rep.name"/>
          <p><a t-att-href="'mailto:'+sales_rep.email" t-esc="sales_rep.email"/></p>
          <p t-field="sales_rep.phone"/>
          <p t-field="sales_rep.city"/>
    </template>

    <template id="quotations" name="Quotations">
        <h3 class="page-header">Your Quotes</h3>
        <t t-if="not cust_quotations">
            <p>There are currently no quotes for your account.</p>
        </t>
        <t t-if="cust_quotations">
            <table class="table table-hover">
                <thead>
                    <th width="50%">Quotation #</th>
                    <th width="25%">Valid Until</th>
                    <th width="25%">Total</th>
                </thead>
                <t t-foreach="cust_quotations" t-as="quotation">
                    <tr>
                        <td>                                   
                            <a t-att-href="'/account/orders/'+str(quotation.id)"><t t-esc="quotation.name"/></a>
                            <t t-if="quotation.state == 'cancel'">
                              <span class="label label-default"><i class="fa fa-fw fa-remove"/> Cancelled</span>
                            </t>
                        </td>
                        <td><span t-field="quotation.validity_date"/></td>
                        <td><span t-field="quotation.amount_total" t-field-options='{"widget": "monetary", "display_currency": "quotation.company_id.currency_id"}'/>
                        </td>
                    </tr>
                </t>
            </table>
        </t>
    </template>

    <template id="sale_orders" name="Sale Orders">
        <h3 class="page-header">Your Orders</h3>
        <t t-if="not cust_orders">
            <p>There are currently no orders for your account.</p>
        </t>
        <t t-if="cust_orders">
            <table class="table table-hover">
                <thead>
                    <th width="40%">Sale Order #</th>
                    <th width="20%">Confirmation Date</th>
                    <th width="20%">Total</th>
                    <th width="20%">Status</th>
                </thead>
                <t t-foreach="cust_orders" t-as="order">
                    <tr>
                        <td>
                            <a t-att-href="'/account/orders/'+str(order.id)"><t t-esc="order.name"/></a>
                        </td>
                        <td><t t-esc="order.date_confirm"/></td>
                        <td><span t-field="order.amount_total" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/></td>
                        <td>
                            <t t-if="order.state == 'progress'">
                                <span class="label label-info"><i class="fa fa-fw fa-clock-o"/> Invoiced</span>
                            </t>
                            <t t-if="order.state in ['shipping_except','invoice_except']">
                                <span class="label label-danger"><i class="fa fa-fw fa-warning"/> Problem</span>
                            </t>
                            <t t-if="order.state == 'done'">
                                <span class="label label-success"><i class="fa fa-fw fa-check"/> Done</span>
                            </t>
                        </td>
                    </tr>
                </t>
            </table>
        </t>
    </template>

    <template id="sale_backend_link" inherit_id='website_portal.sale_orders' customize_show="True" name="Link to Sales Backend" active="False">
        <xpath expr="//h3[@class='page-header']" position="inside">
            <a href="/web#action=portal_sale.action_orders_portal" target="_blank" class="btn btn-default btn-xs pull-right"> Access the complete Sale application</a>
        </xpath>
    </template>

    <template id="invoices" name="Invoices and Payments"> 
        <h3 class="page-header">Your Invoices and Payments</h3>
        <t t-if="not cust_invoices">
            <p>There are currently no invoices for your account.</p>
        </t>
        <t t-if="cust_invoices">
            <table class="table table-hover">
                <thead>
                    <th>Invoice #</th>
                    <th>Invoice Date</th>
                    <th>Total</th>
                    <th>Due Date</th>
                    <th>Status</th>
                </thead>
                <t t-foreach="cust_invoices" t-as="invoice">
                    <tr>
                        <td>
                            <a t-att-href="'report/pdf/account.report_invoice/'+str(invoice.id)"><t t-esc="invoice.number"/></a>
                        </td>
                        <td><t t-esc="invoice.date_invoice"/></td>
                        <td><span t-field="invoice.amount_total" t-field-options='{"widget": "monetary", "display_currency": "invoice.company_id.currency_id"}'/></td>
                        <td><t t-esc="invoice.date_due"/></td>
                        <td>
                            <t t-if="invoice.state == 'open'">
                                <span class="label label-info"><i class="fa fa-fw fa-clock-o"/> Waiting for Payment</span>
                            </t>
                            <t t-if="invoice.state == 'paid'">
                                <span class="label label-success"><i class="fa fa-fw fa-check"/> Paid</span>
                            </t>
                            <t t-if="invoice.state == 'cancel'">
                                <span class="label label-default"><i class="fa fa-fw fa-remove"/> Cancelled</span>
                            </t>
                        </td>
                    </tr>
                </t>
            </table>
        </t>
    </template>

    <template id="my_account_link" name="Link to frontend portal" inherit_id="website.layout">
        <xpath expr="//li[@class='dropdown']/ul/li[contains(.,'Logout')]" position="before">
            <li><a href="/account" role="menuitem">My Account</a></li>
        </xpath>
    </template>

    <template id="orders_followup" name="Odoo - Follow your Orders">
      <t t-call="website.layout">
        <div id="wrap">
            <div class="container">
              <div class="oe_structure">
                <div class="row">
                    <div class="col-sm-4">
                        <ol class="breadcrumb mt8">
                            <li><a href="/account">My Account</a></li>
                            <li>
                              <t t-if="order.state == 'sent'">
                                  Quotation
                              </t>
                              <t t-if="order.state != 'sent'">
                                  Order
                              </t>
                              <t t-esc="order.name"/>
                            </li>
                        </ol>
                    </div>
                </div>
                <h1 class="text-center">
                  Odoo Orders History
                </h1>
                <h2 class="text-center text-muted">
                  Follow your Orders
                </h2>
              </div>
              <div id="optional_placeholder"></div>
              <div class="container">

                  <div class='row'>
                    <div class="panel panel-default">
                      <div class="panel-heading">
                        <div class="row">
                          <div class="col-md-12">
                            <h4>
                              <t t-if="order.state == 'sent'">
                                Quotation 
                              </t>
                              <t t-if="order.state != 'sent'">
                                Order 
                              </t>
                              <span t-field="order.name"/>
                            </h4>
                          </div>
                        </div>
                      </div>
                      <div class="panel-body">
                        <!-- <hr/> -->
                        <div class="mb8">
                            <strong>Date:</strong> <span t-field="order.create_date" t-field-options='{"widget": "date"}'/>
                        </div>
                        <div class='row'>
                          <div class="col-md-6">
                            <div>
                              <strong>Invoicing Address</strong>
                            </div>
                            <div>
                              <address t-field="order.partner_invoice_id" t-field-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'/>
                            </div>
                            <t t-set="invoices" t-value="[i for i in order.invoice_ids if i.state not in ['draft', 'cancel']]"/>
                            <t t-if="invoices">
                              <div>
                                <strong>Invoices</strong>
                              </div>
                              <div>
                                <t t-foreach="invoices" t-as="i">
                                  <t t-set="report_url" t-value="'/report/pdf/account.report_invoice/%s' % i.id"/>
                                  <div>
                                    <a t-att-href="report_url"><span class="fa fa-download"/></a>
                                    <a t-att-href="report_url"><span t-field="i.number"/></a>
                                    <span class="text-muted" t-field="i.date_invoice"/>
                                    <t t-if="i.state == 'paid'">
                                      <span class="label label-success orders_label_text_align"><i class="fa fa-fw fa-check"/> Paid</span>
                                    </t>
                                    <t t-if="i.state != 'paid'">
                                      <span class="label label-info orders_label_text_align"><i class="fa fa-fw fa-clock-o"/> Waiting</span>
                                    </t>
                                  </div>
                                </t>
                              </div>
                            </t>
                          </div>
                          <div id="shipping_address" class="col-md-6">
                            <div>
                              <strong>Shipping Address</strong>
                            </div>
                            <div>
                              <address t-field="order.partner_shipping_id" t-field-options='{"widget": "contact", "fields": ["address", "name"], "no_marker": true}'/>
                            </div>
                          </div>
                        </div>

                        <hr/>

                        <div class="row">
                          <div class="col-md-8 col-md-offset-1">
                            <strong>Product</strong>
                          </div>
                          <div class="col-md-1 text-right">
                            <strong>Unit Price</strong>
                          </div>
                          <div class="col-md-1 text-right">
                            <strong>Quantity</strong>
                          </div>
                          <div class="col-md-1 text-right">
                            <strong>Subtotal</strong>
                          </div>
                        </div>
                        <t t-foreach="order.order_line" t-as="ol">
                          <div class="row orders_vertical_align">
                            <div class="col-md-1 text-center">
                              <a t-att-href="'/shop/product/%s' % str(ol.product_id.product_tmpl_id.id)">
                                <img t-att-src="'/website/image/product.product/%s/image_small/48x48' % ol.product_id.id"/>
                              </a>
                            </div>
                            <div id='product_name' class="col-md-8">
                              <a t-att-href="'/shop/product/%s' % str(ol.product_id.product_tmpl_id.id)"><span t-field="ol.product_id.name"/></a>
                            </div>
                            <div class="col-md-1 text-right">
                              <span t-field="ol.price_unit" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/>
                            </div>
                            <div class="col-md-1 text-right">
                              <t t-if="ol._name == 'sale.order.line'">
                                <span t-field="ol.product_uom_qty"/>
                              </t>
                              <t t-if="ol._name == 'account.invoice.line'">
                                <span t-field="ol.quantity"/>
                              </t>
                            </div>
                            <div class="col-md-1 text-right">
                              <span t-field="ol.price_subtotal" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/>
                            </div>
                          </div>
                        </t>

                        <hr/>

                        <div class="row">
                          <div class="col-md-6">
                            <div>
                              <strong>Contact</strong>
                            </div>
                            <div t-field="order.user_id.partner_id" t-field-options='{"widget": "contact", "fields": ["email", "phone"]}'/>
                          </div>
                          <div class="col-md-6">
                            <div class="row">
                              <div class="col-md-10 text-right">
                                Untaxed Amount:
                              </div>
                              <div class="col-md-2 text-right">
                                <span t-field="order.amount_untaxed" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/>
                              </div>
                            </div>
                            <div class="row">
                              <div class="col-md-10 text-right">
                                Taxes:
                              </div>
                              <div class="col-md-2 text-right">
                                <span t-field="order.amount_tax" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/>
                              </div>
                            </div>
                            <div class="row">
                              <div class="col-md-10 text-right">
                                <strong>Total:</strong>
                              </div>
                              <div class="col-md-2 text-right">
                                <strong><span t-field="order.amount_total" t-field-options='{"widget": "monetary", "display_currency": "order.company_id.currency_id"}'/></strong>
                              </div>
                            </div>
                          </div>
                        </div>
                      </div>
                    </div>
                  </div>

              </div>
              <div class="oe_structure mb32"/>
          </div>
        </div>
      </t>
    </template>

    <template id="sales_backend_access" inherit_id='website_portal.orders_followup' customize_show="True" name="Backend Access" active="False">
      <xpath expr="//div[@id='optional_placeholder']" position="inside">
        <h4 class="text-muted text-center mt64 mb64">
          If you wish to access the complete application to manage your orders, you can follow
          <a href="/web#action=portal_sale.action_orders_portal">this link</a>.
        </h4>
      </xpath>
    </template>

    <template id="details">
        <t t-call="website.layout">
            <t t-set="additional_title">Contact Details</t>
            <div id="wrap">
                <div class="container">
                  <div class="row">
                        <div class="col-sm-6">
                            <ol class="breadcrumb mt8">
                                <li><a href="/account">My Account</a></li>
                                <li>Details</li>
                            </ol>
                        </div>
                    </div>
                    <h1>Contact Details</h1>
                    <form action="/account/details" method="post">
                        <div class="row oe_website_portal">
                            <div class="col-md-8">
                                <div class="row">
                                    <div class="col-md-12">
                                      <div t-if="error_message" class="alert alert-danger">
                                          <t t-foreach="error_message" t-as="err"><t t-esc="err"/><br /></t>
                                      </div>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('name') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="contact_name">Your Name</label>
                                        <input type="text" name="name" class="form-control" t-att-value="name or partner.name" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('email') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="contact_name">Email</label>
                                        <input type="email" name="email" class="form-control" t-att-value="email or partner.email" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('phone') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="phone">Phone</label>
                                        <input type="tel" name="phone" class="form-control" t-att-value="phone or partner.phone" />
                                    </div>

                                    <div class="clearfix" />
                                    <div t-attf-class="form-group #{error.get('street') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="street">Company Name</label>
                                        <input type="text" name="street" class="form-control" t-att-value="street or partner.street"/>
                                    </div>
                                    <div t-if="has_check_vat" t-attf-class="form-group #{error.get('vat') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="vat">VAT Number</label>
                                        <input type="text" name="vat" class="form-control" t-att-value="vat or partner.vat" />
                                    </div>

                                    <div class="clearfix" />
                                    <div t-attf-class="form-group #{error.get('street') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="street">Street</label>
                                        <input type="text" name="street2" class="form-control" t-att-value="street2 or partner.street2" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('city') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="city">City</label>
                                        <input type="text" name="city" class="form-control" t-att-value="city or partner.city" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('zip') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="zipcode">Zip / Postal Code</label>
                                        <input type="text" name="zipcode" class="form-control" t-att-value="zipcode or partner.zip" />
                                    </div>
                                    <div t-attf-class="form-group #{error.get('country_id') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label" for="country_id">Country</label>
                                        <select name="country_id" class="form-control">
                                            <option value="">Country...</option>
                                            <t t-foreach="countries or []" t-as="country">
                                                <option t-att-value="country.id" t-att-selected="country.id == partner.country_id.id">
                                                    <t t-esc="country.name" />
                                                </option>
                                            </t>
                                        </select>
                                    </div>
                                    <div t-attf-class="form-group #{error.get('state_id') and 'has-error' or ''} col-lg-6">
                                        <label class="control-label label-optional" for="state_id">State / Province</label>
                                        <select name="state_id" class="form-control">
                                            <option value="">select...</option>
                                            <t t-foreach="states or []" t-as="state">
                                                <option t-att-value="state.id" style="display:none;" t-att-data-country_id="state.country_id.id" t-att-selected="state.id == partner.state_id.id">
                                                    <t t-esc="state.name" />
                                                </option>
                                            </t>
                                        </select>
                                    </div>
                                </div>
                                <div class="clearfix">
                                    <a class="btn btn-default btn-primary pull-right mb32 contract-submit">
                                        Confirm
                                        <span class="fa fa-long-arrow-right" />
                                    </a>
                                </div>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </t>
    </template>


</odoo>