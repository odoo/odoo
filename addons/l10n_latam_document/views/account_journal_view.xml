<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_journal_form" model="ir.ui.view">
        <field name="model">account.journal</field>
        <field name="name">account.journal.form</field>
        <field name="inherit_id" ref="account.view_account_journal_form"/>
        <field name="arch" type="xml">
            <form>
                <field name="l10n_latam_country_code" invisible="1"/>
            </form>
            <field name="type" position="after">
                <field name="l10n_latam_use_documents" attrs="{'invisible': [('type','not in',['purchase','sale','purchase_refund','sale_refund'])]}"/>
            </field>
            <notebook position="before">
                <button name="create_document_type_sequences" type="object" string="Create Sequences" groups="base.group_no_one"/>
            </notebook>
        </field>
    </record>

</odoo>
