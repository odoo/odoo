<?xml version="1.0"?>
<odoo>
    <!-- Action to open check-in list -->
    <act_window id="action_checkin"
    name="Check-In"
    res_model="hr.checkin"
    view_mode="tree,form"
    />
    <!-- Menu item to open check-in list -->
    <menuitem id="menu_checkin"
    name="checkin-history"
    action="action_checkin"
    />

    <!-- <record id="view_checkin_tree" model="ir.ui.view">
        <field name="name">Check-In History</field>
        <field name="model">hr.checkin</field>
        <field name="arch" type="xml">
            <tree string="Check-In" decoration-bf="message_needaction==True">
                <field name="employee_name"/>
                <field name="employee_id"/>
                <field name="checkin_status"/>
                <field name="time_checkin"/>
            </tree>
        </field>
    </record> -->
</odoo>