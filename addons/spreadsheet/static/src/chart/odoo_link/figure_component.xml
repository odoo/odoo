<odoo>
    <div t-name="spreadsheet.FigureComponent" t-inherit="o-spreadsheet-FigureComponent" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('o-figure-menu-item')]" position="before">
            <div
                t-if="this.hasOdooLink and !this.env.model.getters.isDashboard()"
                class="o-figure-menu-item o-chart-external-link"
                t-on-click="(ev, isMiddleClick) => this.navigateToOdooLink(isMiddleClick)"
                t-on-auxclick="(ev, isMiddleClick) => this.navigateToOdooLink(isMiddleClick)"
                >
                <span class="fa fa-external-link" />
            </div>
        </xpath>
    </div>
    <div t-name="spreadsheet.ScorecardChart" t-inherit="o-spreadsheet-ScorecardChart" t-inherit-mode="extension">
        <xpath expr="//canvas[1]" position="attributes">
            <attribute name="t-on-click">(ev) => this.onClick(ev, false)</attribute>
            <attribute name="t-on-auxclick">(ev) => this.onClick(ev, true)</attribute>
            <attribute name="t-on-mousemove">(ev) => this.onMouseMove(ev)</attribute>
            <attribute name="t-on-mouseleave">(ev) => this.onMouseLeave(ev)</attribute>
            <attribute name="t-att-role">this.env.model.getters.isDashboard() and this.hasOdooLink ? "button" : ""</attribute>
        </xpath>
    </div>
    <div t-name="spreadsheet.GaugeChartComponent" t-inherit="o-spreadsheet-GaugeChartComponent" t-inherit-mode="extension">
        <xpath expr="//canvas[1]" position="attributes">
            <attribute name="t-on-click">() => this.onClick()</attribute>
            <attribute name="t-att-role">this.env.model.getters.isDashboard() and this.hasOdooLink ? "button" : ""</attribute>
        </xpath>
    </div>
</odoo>
