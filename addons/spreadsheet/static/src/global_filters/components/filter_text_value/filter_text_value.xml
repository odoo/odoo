<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="spreadsheet.TextFilterValue">
        <AutoComplete
            t-if="props.options?.length"
            value="props.value"
            sources="sources"
            placeholder.translate="Choose a value..."
            onSelect="(option) => props.onValueChanged(option.value)"
        />
        <input
            t-else=""
            type="text"
            class="o_input o-global-filter-text-value text-truncate"
            t-att-placeholder="translate(props.label)"
            t-att-value="props.value"
            t-on-change="(e) => props.onValueChanged(e.target.value)" />
    </t>
</templates>
