<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="spreadsheet_edition.FilterValue"
       >
        <div class="o-filter-value d-flex align-items-start w-100">
            <div t-if="isDateFilter" class="w-100">
                <select t-if="filter.operator === 'relative'"
                    t-on-change="(e) => this.onDateInput(filter.id, e.target.value || undefined)"
                    class="date_filter_values o_input me-3"
                    required="true">
                    <option value="">Select period...</option>
                    <t t-foreach="relativeDateRangesTypes"
                        t-as="range"
                        t-key="range.type">
                        <option t-att-selected="range.type === filterValue"
                            t-att-value="range.type">
                            <t t-esc="range.description"/>
                        </option>
                    </t>
                </select>
                <DateFromToValue t-elif="filter.operator === 'from_to'"
                    from="filterValue?.from"
                    to="filterValue?.to"
                    onFromToChanged.bind="setFilterValue"/>

                <DateFilterValue t-else=""
                    period="filterValue?.period"
                    yearOffset="filterValue?.yearOffset"
                    disabledPeriods="filter.disabledPeriods"
                    onTimeRangeChanged.bind="setFilterValue"/>
            </div>
            <div t-elif="!filter.target" class="w-100">
                <TextFilterValue
                    value="filterValue"
                    label="filter.label"
                    options="textAllowedValues"
                    onValueChanged.bind="setFilterValue"
                />
            </div>
            <span t-elif="filter.relation and filter.operator !== 'ilike'" class="w-100">
                <MultiRecordSelector
                    t-if="isValid"
                    placeholder="' ' + translate(filter.label)"
                    resModel="filter.relation"
                    resIds="filterValue || []"
                    domain="relationalAllowedDomain"
                    update.bind="setFilterValue" />
                <ValidationMessages t-else="1"
                                    messages="[invalidModel]"
                                    msgType="'error'"/>
            </span>
            <div t-elif="filter.operator === 'ilike'" class="w-100">
                <TextFilterValue
                    value="filterValue"
                    label="filter.label"
                    options="textAllowedValues"
                    onValueChanged.bind="setFilterValue"
                />
            </div>
            <i t-if="props.model.getters.isGlobalFilterActive(filter.id)"
                class="fa fa-times btn btn-link text-muted o_side_panel_filter_icon ms-1"
                title="Clear"
                t-on-click="() => this.setFilterValue()"/>
        </div>
    </t>
</templates>
