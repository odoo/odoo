<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        standard, bold, boxed, striped and folder all t-call web.external_layout_footer;
        inheriting the footer itself reaches the five of them at once. bubble and wave
        keep their own overrides below: their footers are inline, not shared.
    -->
    <template
        id="l10n_ph_external_layout_footer"
        inherit_id="web.external_layout_footer">
        <xpath expr="//div[@name='footer_content']" position="before">
            <t t-if="is_l10n_ph_disbursement_voucher">
                <t t-call="l10n_ph.report_disbursement_voucher_document_footer" />
            </t>
        </xpath>
    </template>

    <template
        id="l10n_ph.external_layout_bubble"
        inherit_id="web.external_layout_bubble">
        <xpath
            expr="//div[hasclass('border-top')][@t-field='layout.report_footer']"
            position="before">
            <t t-if="is_l10n_ph_disbursement_voucher">
                <t t-call="l10n_ph.report_disbursement_voucher_document_footer" />
            </t>
        </xpath>
    </template>

    <template id="l10n_ph.external_layout_wave" inherit_id="web.external_layout_wave">
        <xpath expr="//div[@t-field='layout.report_footer']" position="before">
            <t t-if="is_l10n_ph_disbursement_voucher">
                <t t-call="l10n_ph.report_disbursement_voucher_document_footer" />
            </t>
        </xpath>
    </template>

</odoo>
