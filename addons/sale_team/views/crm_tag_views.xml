<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="sales_team_crm_tag_view_form" model="ir.ui.view">
        <field name="name">sales.team.crm.tag.view.form</field>
        <field name="model">crm.tag</field>
        <field name="arch" type="xml">
            <form string="Tags">
                <sheet>
                    <div class="oe_title">
                        <label for="name" />
                        <h1>
                            <field name="name" placeholder="e.g. Services" />
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="color" widget="color_picker" required="True" />
                            <field name="parent_id" domain="[('id', '!=', id)]" />
                            <field name="active" widget="boolean_toggle" />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="sales_team_crm_tag_view_tree" model="ir.ui.view">
        <field name="name">sales.team.crm.tag.view.list</field>
        <field name="model">crm.tag</field>
        <field name="arch" type="xml">
            <list string="Tags" editable="bottom" multi_edit="1" sample="1">
                <field name="name" />
                <field
                    name="parent_id"
                    string="Parent Tag"
                    domain="[('id', '!=', id)]" />
                <field name="color" widget="color_picker" />
            </list>
        </field>
    </record>

    <record id="sales_team_crm_tag_view_search" model="ir.ui.view">
        <field name="name">sales.team.crm.tag.view.search</field>
        <field name="model">crm.tag</field>
        <field name="arch" type="xml">
            <search string="Search Tags">
                <field name="name" string="Name" />
                <field name="display_name" string="Tag" />
                <separator />
                <filter
                    name="inactive"
                    string="Archived"
                    domain="[('active', '=', False)]" />
                <group>
                    <filter
                        name="group_parent_id"
                        string="Parent Tag"
                        context="{'group_by': 'parent_id'}" />
                    <filter
                        name="group_color"
                        string="Color"
                        context="{'group_by': 'color'}" />
                </group>
            </search>
        </field>
    </record>

    <!-- Tags Configuration -->
    <record id="sales_team_crm_tag_action" model="ir.actions.act_window">
        <field name="name">Tags</field>
        <field name="res_model">crm.tag</field>
        <field name="view_id" ref="sales_team_crm_tag_view_tree" />
        <field name="search_view_id" ref="sales_team_crm_tag_view_search" />
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
            Create CRM Tags
            </p><p>
            Use Tags to manage and track your Opportunities (product structure, sales type, ...)
            </p>
        </field>
    </record>

</odoo>
