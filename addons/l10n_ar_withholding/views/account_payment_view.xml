<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">account.payment.form.inherited</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="l10n_latam_check.view_account_payment_form_inherited" />
        <field name="arch" type="xml">
            <group name="latam_checks" position="replace">
                <notebook>
                    <page name="latam_checks_page" string="Checks" invisible="payment_method_code not in ['new_third_party_checks', 'own_checks']">
                       <field name="l10n_latam_new_check_ids" nolabel="1" colspan="2" readonly="state != 'draft'">
                            <tree name="new_checks" editable="bottom">
                                <field name="company_id" column_invisible="True"/>
                                <field name="currency_id" column_invisible="True"/>
                                <field name="name" />
                                <field name="bank_id" column_invisible="parent.payment_method_code == 'own_checks'"/>
                                <field name="issuer_vat" column_invisible="parent.payment_method_code == 'own_checks'"/>
                                <field name="payment_date" required="parent.payment_method_code == 'own_checks'"/>
                                <field name="amount" />
                                <button type="object" name="get_formview_action" icon="fa-pencil-square-o" title="open" help="Open" column_invisible="parent.state != 'posted'"/>
                            </tree>
                        </field>
                    </page>
                    <page name="withholdings_page" string="Withholdings" invisible="country_code != 'AR'">
                        <field name="l10n_ar_withholding_ids" nolabel="1" colspan="2"
                            readonly="True" invisible="is_internal_transfer">
                            <tree>
                                <field name="move_name" column_invisible="True"/>
                                <field name="tax_line_id" string="Tax"/>
                                <field name="name" string="Withholding Number" required="1"/>
                                <field name="amount_currency" readonly="0" required="1" string="Amount" sum="Total"/>
                            </tree>
                        </field>
                    </page>
                </notebook>
            </group>
        </field>
    </record>
</odoo>
