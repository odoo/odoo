<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_account_payment_register_form" model="ir.ui.view">
        <field name="name">account.payment.register.form</field>
        <field name="model">account.payment.register</field>
        <field name="inherit_id" ref="account.view_account_payment_register_form"/>
        <field name="arch" type="xml">
            <group>
                <group name="withholdings" colspan="2" 
                    invisible="country_code != 'AR'">
                    <field name="withholding_ids" invisible="not can_edit_wizard or (can_group_payments and not group_payment)">
                        <tree editable="bottom">
                            <field name="tax_id" domain="[('l10n_ar_withholding', '=', parent.partner_type), ('company_id', '=', parent.company_id)]" options="{'no_open': True, 'no_create': True}"/>
                            <field name="name" column_invisible="parent.partner_type == 'supplier'"/>
                            <field name="base_amount"/>
                            <field name="amount"/>
                            <field name="currency_id" invisible="True"/>
                        </tree>
                    </field>
                    <field name="net_amount" invisible="not can_edit_wizard or (can_group_payments and not group_payment)"/>
                    <div colspan="2" class="o_row"
                        invisible="(can_edit_wizard and not can_group_payments) or (can_group_payments and group_payment)">
                    <p class="alert alert-warning" role="alert">You can't register withholdings when paying invoices of different partners or same partner without grouping</p>
                    </div>
                </group>
            </group>
        </field>
    </record>
</odoo>
