<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="mail.AvatarCardPopover" t-inherit-mode="extension">
        <xpath expr="//a[hasclass('o-mail-avatar-card-tel')]" position="after">
            <span t-if="employee?.work_location_name" class="text-muted" data-tooltip="Work Location">
                <i t-if="employee.work_location_type === 'office'" class="me-1 fa fa-fw fa-building-o"/>
                <i t-elif="employee.work_location_type === 'home'" class="me-1 fa fa-fw fa-home"/>
                <i t-else="" class="me-1 fa fa-fw fa-map-marker"/>
                <t t-out="employee.work_location_name"/>
            </span>
        </xpath>
    </t>

    <t t-name="hr.avatarCardEmployeeInfos">
        <small class="text-muted" t-if="employee?.job_title" t-out="employee.job_title"/>
        <span class="text-muted" t-if="employee?.department_id" data-tooltip="Department" t-out="employee.department_id.display_name"/>
    </t>
</templates>
