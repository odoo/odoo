<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="hr_homeworking.RadioImageField">
        <div role="radiogroup" class="d-flex flex-wrap" t-att-aria-label="this.string">
            <t t-foreach="this.items" t-as="item" t-key="item[0]">
                <t t-if="['office', 'home', 'other'].includes(item[0])">
                    <div class="form-check o_radio_item me-1" aria-atomic="true">
                        <input
                            type="radio"
                            class="form-check-input o_radio_input"
                            t-att-checked="item[0] === this.value"
                            t-att-disabled="this.props.readonly"
                            t-att-name="this.id"
                            t-att-data-value="item[0]"
                            t-att-data-index="item_index"
                            t-att-id="`${this.id}_${item[0]}`"
                            t-on-change="() => this.onChange(item)"
                        />
                        <t t-if="item[0] === 'office'">
                            <i class="fa-regular fa-building fa-2x" role="img"/>
                        </t>
                        <t t-elif="item[0] === 'home'">
                            <i class="fa-solid fa-house fa-2x" role="img"/>
                        </t>
                        <t t-else="">
                            <i class="fa-solid fa-location-dot fa-2x" role="img"/>
                        </t>
                    </div>
                </t>
            </t>
        </div>
    </t>


</templates>
