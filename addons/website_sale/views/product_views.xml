<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="product_template_search_view_website" model="ir.ui.view">
        <field name="name">product.template.search.published</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_search_view"/>
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='consumable']" position="after">
                <separator/>
                <filter string="Published" name="published" domain="[('website_published', '=', True)]"/>
            </xpath>
        </field>
    </record>

    <record id="product_template_action_website" model="ir.actions.act_window">
        <field name="name">Products</field>
        <field name="res_model">product.template</field>
        <field name="view_type">form</field>
        <field name="view_mode">kanban,tree,form,activity</field>
        <field name="search_view_id" ref="product_template_search_view_website"/>
        <field name="context">{'search_default_published': 1, 'default_website_published': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new product
            </p><p>
                A product can be either a physical product or a service that you sell to your customers.
            </p>
        </field>
    </record>

    <record model="ir.ui.view" id="product_pricelist_view">
        <field name="name">product.pricelist.website.form</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="before">
                <field name="code"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="product_product_website_tree_view">
        <field name="name">product.product.website.tree</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_product_tree_view"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="website_id" groups="website.group_multi_website"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="product_template_website_tree_view">
        <field name="name">product.template.website.tree</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <field name="website_id" groups="website.group_multi_website"/>
            </field>
        </field>
    </record>

    <record model="ir.ui.view" id="product_template_form_view_invoice_policy">
        <field name="name">product.template.invoice.policy</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="sale.product_template_form_view_invoice_policy"/>
        <field name="arch" type="xml">
            <xpath expr="//group[@name='invoicing']" position="attributes">
                <attribute name="invisible">0</attribute>
            </xpath>
        </field>
    </record>

    <record model="ir.ui.view" id="product_template_form_view">
        <field name="name">product.template.product.website.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <!-- add state field in header -->
            <xpath expr="//group[@name='email_template_and_project']" position="attributes">
                <attribute name="invisible">0</attribute>
            </xpath>
            <xpath expr="//group[@name='description']" position="attributes">
                <attribute name="invisible">0</attribute>
            </xpath>
            <xpath expr="//button[@name='toggle_active']" position="before">
                <button class="oe_stat_button" name="website_publish_button"
                    type="object" icon="fa-globe" attrs="{'invisible': [('sale_ok','=',False)]}">
                    <field name="is_published" widget="website_button"/>
                </button>
            </xpath>
            <xpath expr="//page[@name='sales']" position="after">
                <page name="shop" string="eCommerce" attrs="{'invisible': [('sale_ok','=',False)]}">
                    <group name="shop">
                        <group string="Shop">
                            <field name="website_url" invisible="1"/>
                            <field name="website_id" options="{'no_create': True}" groups="website.group_multi_website"/>
                            <field name="public_categ_ids" widget="many2many_tags" string="Categories"/>
                            <field name="alternative_product_ids" widget="many2many_tags"/>
                            <field name="accessory_product_ids" widget="many2many_tags"/>
                            <field name="website_style_ids" widget="many2many_tags" groups="base.group_no_one"/>
                        </group>
                    </group>
                    <group name="product_images" string="Product Images">
                        <!-- TODO SEB decide if we want to show here all images (inc variant) or only generic images  -->
                        <field name="product_image_ids" class="o_website_sale_image_list" context="{'default_name': name, 'default_product_tmpl_id': id, 'hide_product_template': 1}" nolabel="1" mode="kanban"/>
                    </group>
                </page>
            </xpath>
        </field>
    </record>

    <record id="product_product_view_form_easy_inherit_website_sale" model="ir.ui.view">
        <field name="name">product.product.view.form.easy.inherit.website_sale</field>
        <field name="model">product.product</field>
        <field name="inherit_id" ref="product.product_variant_easy_edit_view"/>
        <field name="arch" type="xml">
            <sheet position="inside">
                <group name="variant_images" string="Variant Images">
                    <field name="product_tmpl_id" invisible="1"/>
                    <!-- TODO SEB decide if we want to show here all images or only variant specific -->
                    <field name="variant_image_ids" class="o_website_sale_image_list" context="{'default_name': name, 'default_product_tmpl_id': active_id, 'default_product_product_id': id, 'hide_product_template': 1, 'hide_product_variant': 1}" nolabel="1" mode="kanban"/>
                </group>
            </sheet>
        </field>
    </record>

    <!-- Product Public Categories -->
    <record id="product_public_category_form_view" model="ir.ui.view">
        <field name="name">product.public.category.form</field>
        <field name="model">product.public.category</field>
        <field name="arch" type="xml">
            <form string="Website Public Categories">
                <sheet>
                    <field name="image_medium" widget='image' class="oe_avatar"/>
                    <div class="oe_left">
                        <group>
                            <field name="name"/>
                            <field name="parent_id"/>
                            <field name="website_id" options="{'no_create': True}" groups="website.group_multi_website"/>
                            <field name="sequence"/>
                        </group>
                    </div>
                </sheet>
            </form>
        </field>
    </record>
    <record id="product_public_category_tree_view" model="ir.ui.view">
        <field name="name">product.public.category.tree</field>
        <field name="model">product.public.category</field>
        <field name="field_parent" eval="False"/>
        <field name="arch" type="xml">
            <tree string="Product Public Categories">
                <field name="sequence" widget="handle"/>
                <field name="display_name"/>
                <field name="website_id" groups="website.group_multi_website"/>
            </tree>
        </field>
    </record>
    <record id="product_public_category_action" model="ir.actions.act_window">
        <field name="name">eCommerce Categories</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">product.public.category</field>
        <field name="view_type">form</field>
        <field name="view_mode">tree,form</field>
        <field name="view_id" eval="False"/>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Define a new category
          </p><p>
            Categories are used to browse your products through the
            touchscreen interface.
          </p>
        </field>
    </record>

    <record id="website_sale_pricelist_form_view" model="ir.ui.view">
        <field name="name">website_sale.pricelist.form</field>
        <field name="inherit_id" ref="product.product_pricelist_view" />
        <field name="model">product.pricelist</field>
        <field name="arch" type="xml">
            <field name='currency_id' position='after'>
                <field name="selectable"/>
                <field name="website_id" options="{'no_create': True}" string="Allow to use on " placeholder="None website"/>
            </field>
        </field>
    </record>

    <record id="website_sale_pricelist_tree_view" model="ir.ui.view">
        <field name="name">product.pricelist.tree.inherit.product</field>
        <field name="model">product.pricelist</field>
        <field name="inherit_id" ref="product.product_pricelist_view_tree"/>
        <field name="arch" type="xml">
            <field name="currency_id" position="after">
                <field name="selectable" />
                <field name="website_id" groups="website.group_multi_website"/>
            </field>
        </field>
    </record>

    <record id="product_image_view_form" model="ir.ui.view">
        <field name="name">product.image.view.form</field>
        <field name="model">product.image</field>
        <field name="arch" type="xml">
            <form string="Product Images">
                <div class="row o_website_sale_image_modal">
                    <div class="col-md-6 col-xl-5">
                        <div class="oe_title">
                            <label class="small text-muted" for="name" string="Image Name"/>
                            <h2><field name="name" placeholder="Image Name" required="True"/></h2>

                            <div name="product_template" invisible="context.get('hide_product_template')">
                                <label class="small text-muted" for="product_tmpl_id" string="For product"/><br/>
                                <field name="product_tmpl_id" options="{'no_open': True, 'no_create': True}"/><br/>
                            </div>

                            <div name="product_variant" groups="product.group_product_variant" invisible="context.get('hide_product_variant')">
                                <label class="small text-muted" for="product_product_id" string="For variant"/><br/>
                                <field name="product_product_id" domain="[('product_tmpl_id', '=', product_tmpl_id),]" options="{'no_open': True, 'no_create': True}"/><br/>
                            </div>

                            <label class="small text-muted" for="sequence" string="Sequence"/>
                            <field name="sequence"/>
                        </div>
                    </div>
                    <div class="col-md-6 col-xl-7 text-center o_website_sale_image_modal_container">
                        <field name="image_raw" widget="image"/>
                    </div>
                </div>
            </form>
        </field>
    </record>

    <record id="product_image_view_kanban" model="ir.ui.view">
        <field name="name">product.image.kanban</field>
        <field name="model">product.image</field>
        <field name="arch" type="xml">
            <kanban string="Product Images" default_order="sequence">
                <field name="id"/>
                <field name="name"/>
                <field name="image_raw_byte_size"/>
                <field name="image_raw"/>
                <field name="sequence"/>
                <field name="product_product_id"/>
                <templates>
                    <t t-name="kanban-box">
                        <div class="card oe_kanban_global_click p-0">
                            <div class="o_squared_image">
                                <!-- TODO SEB not always image/png even if it works... -->
                                <!-- TODO SEB uploading an svg is not ok -->
                                <img class="card-img-top" t-attf-src="data:image/png;base64,{{record.image_raw.raw_value}}" t-att-alt="record.name"/>
                            </div>
                            <div class="card-body p-0">
                                <h4 class="card-title p-2 m-0 bg-200">
                                    <small><field name="name"/></small>
                                </h4>
                                <p t-if="record.product_product_id.value &amp;&amp; !context.hide_product_variant" class="card-text p-2">
                                    <span t-attf-class="badge">Variant</span> <t t-esc="record.product_product_id.value"/>
                                </p>
                            </div>
                            <t t-if="record.image_raw_byte_size.raw_value &lt; 100*1000">
                                <t t-set="size_status" t-value="'success'"/>
                                <t t-set="message">Acceptable file size</t>
                            </t>
                            <t t-elif="record.image_raw_byte_size.raw_value &lt; 1000*1000">
                                <t t-set="size_status" t-value="'warning'" />
                                <t t-set="message">Huge file size. The image should be optimized/reduced.</t>
                            </t>
                            <t t-else="1">
                                <t t-set="size_status" t-value="'danger'"/>
                                <t t-set="message">Optimization required! Reduce the image size or increase your compression settings.</t>
                            </t>
                            <span t-attf-class="badge badge-#{size_status} o_product_image_size" t-esc="record.image_raw.value" t-att-title="message"/>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

</odoo>
