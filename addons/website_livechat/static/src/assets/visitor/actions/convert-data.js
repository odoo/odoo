/** @odoo-module **/

import { Define } from '@mail/define';

export default Define`
    {Record/insert}
        [Record/models]
            Action
        [Action/name]
            Visitor/convertData
        [Action/params]
            data
                [type]
                    Object
        [Action/returns]
            Object
        [Action/behavior]
            :data2
                {Record/insert}
                    [Record/models]
                        Object
            {if}
                @data
                .{Dict/hasKey}
                    country_id
            .{then}
                {if}
                    @data
                    .{Dict/get}
                        country_id
                .{then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Visitor/country]
                                {Record/insert}
                                    [Record/models]
                                        Country
                                    [Country/id]
                                        @data
                                        .{Dict/get}
                                            country_id
                                    [Country/code]
                                        @data
                                        .{Dict/get}
                                            country_code
                .{else}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Visitor/country]
                                {Record/empty}
            {if}
                @data
                .{Dict/hasKey}
                    history
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/history]
                            @data
                            .{Dict/get}
                                history
            {if}
                @data
                .{Dict/hasKey}
                    id
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/id]
                            @data
                            .{Dict/get}
                                id
            {if}
                @data
                .{Dict/hasKey}
                    is_connected
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/isConnected]
                            @data
                            .{Dict/get}
                                is_connected
            {if}
                @data
                .{Dict/hasKey}
                    lang_name
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/langName]
                            @data
                            .{Dict/get}
                                lang_name
            {if}
                @data
                .{Dict/hasKey}
                    display_name
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/displayName]
                            @data
                            .{Dict/get}
                                display_name
            {if}
                @data
                .{Dict/hasKey}
                    partner_id
            .{then}
                {if}
                    @data.partner_id
                {then}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Visitor/partner]
                                {Record/insert}
                                    [Record/models]
                                        Partner
                                    [Partner/id]
                                        @data
                                        .{Dict/get}
                                            partner_id
                .{else}
                    {Record/update}
                        [0]
                            @data2
                        [1]
                            [Visitor/partner]
                                {Record/empty}
            {if}
                @data
                .{Dict/hasKey}
                    website_name
            .{then}
                {Record/update}
                    [0]
                        @data2
                    [1]
                        [Visitor/websiteName]
                            @data
                            .{Dict/get}
                                website_name
            @data2
`;
