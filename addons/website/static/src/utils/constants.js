// Utilities for managing the number of elements per row in dynamic carousel
// snippets.
export const DEFAULT_NUMBER_OF_ELEMENTS_FOR_TITLE_LEFT = 2;
export const DEFAULT_NUMBER_OF_ELEMENTS = 4;
export const DEFAULT_NUMBER_OF_ELEMENTS_SM = 1;
