<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="website.prompt">
        <div role="dialog" class="modal o_technical_modal" tabindex="-1">
                <div class="modal-dialog">
                <div class="modal-content">
                    <header class="modal-header" t-if="this.window_title">
                        <h3 class="modal-title"><t t-out="this.window_title"/></h3>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </header>
                    <main class="modal-body">
                        <form role="form" t-att-id="this.id">
                            <div class="row mb0">
                                <label for="page-name" class="col-md-3 col-form-label">
                                    <t t-out="this.field_name"/>
                                </label>
                                <div class="col-md-9">
                                    <input t-if="this.field_type == 'input'" type="text" class="form-control" required="required"/>
                                    <textarea t-if="this.field_type == 'textarea'" class="form-control" required="required" rows="5"></textarea>
                                    <select t-if="this.field_type == 'select'" class="form-select"></select>
                                </div>
                            </div>
                        </form>
                    </main>
                    <footer class="modal-footer">
                        <button type="button" class="btn btn-primary btn-continue"><t t-out="this.btn_primary_title"/></button>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal" aria-label="Discard"><t t-out="this.btn_secondary_title"/></button>
                    </footer>
                </div>
            </div>
        </div>
    </t>
</templates>
