<?xml version="1.0" encoding="utf-8"?>
<templates id="template" xml:space="preserve">
<div t-name="website.TranslatorInfoDialog">
    <p>You are about to enter the translation mode.</p>
    <p>Here are the visuals used to help you translate efficiently:</p>
    <ul class="oe_translate_examples">
        <li data-oe-translation-state="to_translate">Content to translate</li>
        <li data-oe-translation-state="translated">Translated content</li>
    </ul>
    <p>
        In this mode, you can only translate texts. To change the structure of the page, you must edit the master page.
        Each modification on the master page is automatically applied to all translated versions.
    </p>
</div>

<div t-name="website.AttributeTranslateDialog">
    <t t-foreach="widget.translation" t-as="attr">
        <t t-set="value" t-value="widget.translation[attr].innerHTML"/>
        <t t-set="attribute_data" t-value="widget.attributeData[attr]"/>
        <div class="form-group row">
            <label class="col-md-3 col-form-label"><strong><t t-esc="attribute_data.string or attr"/></strong> <small class="text-muted"><t t-esc="attribute_data.substring"/></small></label>
            <input t-attf-class="form-control col-md-8 #{attr === 'src' ? 'o_hidden' : ''}" t-att-value="value" t-att-data-attr="attr" t-att-title="attribute_data.title"/>
            <a t-if="attr === 'src'" href="#" class="thumbnail translate_img_preview col-md-4 p-0">
                <img t-att-src="value" t-att-title="attribute_data.title" class="img img-fluid"/>
            </a>
        </div>
    </t>
</div>
</templates>
