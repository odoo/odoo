import {BaseOptionComponent} from "@html_builder/core/utils";

export class SnippetOptions extends BaseOptionComponent {
    static template = "multi_classes_ids.SnippetOptions";

    setup() {
        super.setup();
    }
}