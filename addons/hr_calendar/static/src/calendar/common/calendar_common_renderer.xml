<?xml version="1.0" encoding="UTF-8"?>

<templates>
    <t t-name="calendar.AttendeeCalendarCommonRenderer.HeaderTemplate" t-inherit="calendar.AttendeeCalendarCommonRenderer.HeaderTemplate" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('location_div')]" position="inside">
            <t t-if="scale != 'month'">
                <div class="o_homework_multi align-self-start d-flex gap-1 ms-1">
                    <t t-foreach="Object.keys(workLocation)" t-as="location" t-key="location">
                        <t t-foreach="workLocation[location]" t-as="wl" t-key="wl.id">
                            <div class="o_homeworking_content o_worklocation_btn wl_action" t-att-data-location="location" t-att-data-id="wl.id" t-att-data-employee="wl.employeeId">
                                <i t-attf-class="o_homework_content rounded-circle fa {{iconMap[wl.icon] || 'fa-map-marker'}} wl_color_{{wl.colorIndex}}"/>
                            </div>
                        </t>
                        <span class="fw-bolder o_worklocation_btn" t-esc="workLocation[location][0].title"/>
                    </t>
                    <button t-if="!workLocationSetForCurrentUser" class="o_worklocation_btn bg-info bg-opacity-25 border-0 rounded-pill px-lg-2 py-0 text-nowrap opacity-0 opacity-100-hover wl_action" data-create="1">
                        <i class="add_wl fa fa-map-marker me-lg-1" aria-hidden="true"></i>
                    </button>
                </div>
            </t>
        </xpath>
    </t>
</templates>
