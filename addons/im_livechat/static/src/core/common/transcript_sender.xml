<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="im_livechat.TranscriptSender">
        <div class="o-livechat-TranscriptSender">
            <div class="input-group">
                <input t-custom-ref="input" t-on-keydown="this.onKeydown" t-custom-model="this.state.email" t-att-disabled="this.isInputDisabled" type="text" class="form-control" t-att-class="{'bg-view': !this.isInputDisabled}" placeholder="mail@example.com" />
                <button class="btn btn-primary" type="button" data-action="sendTranscript" t-att-disabled="this.isButtonDisabled" t-on-click="this.onClickSend">
                    <i class="oi" t-att-class="{
                        'oi-spin': this.state.status === this.STATUS.SENDING,
                        'oi-filled': this.state.status === this.STATUS.IDLE,
                    }"
                    t-attf-data-icon="{{ this.state.status == this.STATUS.SENDING ? 'autorenew' : this.state.status == this.STATUS.SENT ? 'check' : this.state.status == this.STATUS.IDLE ? 'send' : this.state.status == this.STATUS.FAILED ? 'redo' : '' }}"/>
                </button>
            </div>
            <div class="form-text ms-1">
                <t t-if="this.state.status === this.STATUS.SENT">The conversation was sent.</t>
                <t t-elif="this.state.status === this.STATUS.FAILED">An error occurred. Please try again.</t>
            </div>
        </div>
    </t>
</templates>
