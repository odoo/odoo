<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="discuss.ChannelInvitation-selectableItem" t-inherit-mode="extension">
        <xpath expr="//*[@name='selectableUserName']" position="inside">
            <span t-if="props.channel?.channel_type === 'livechat' and selectableUser.user_livechat_username" t-esc="selectableUser.user_livechat_username" class="text-truncate text-muted smaller mx-2"/>
            <span t-if="props.channel?.channel_type === 'livechat' and selectableUser.is_in_call" class="flex-shrink-0 opacity-75 smaller mx-2">
                <i class="fa fa-volume-up o-discuss-inCallIconColor me-1"/>
                <span class="o-discuss-ChannelInvitation-inCallTextColor">in a call</span>
            </span>
        </xpath>
        <xpath expr="//*[@name='selectableUserDetail']" position="inside">
            <div t-if="props.channel?.channel_type === 'livechat' and selectableUser.lang_name" class="d-flex flex-wrap align-items-center gap-1">
                <span class="d-flex text-start fs-6 gap-1">
                    <span class="badge rounded text-bg-primary" t-esc="selectableUser.lang_name"/>
                    <t t-foreach="selectableUser.livechat_languages" t-as="language" t-key="language_index">
                        <span class="badge rounded text-bg-primary" t-esc="language"/>
                    </t>
                </span>
                <span class="d-flex text-start fs-6 gap-1">
                    <i t-if="selectableUser.livechat_expertise_ids.length" class="fa fa-fw fa-graduation-cap" title="Expertise"/>
                    <t t-foreach="selectableUser.livechat_expertise_ids" t-as="expertise" t-key="expertise.id">
                        <span class="badge rounded text-bg-info bg-opacity-75 o-text-white" t-esc="expertise.name"/>
                    </t>
                </span>
            </div>
        </xpath>
    </t>
</templates>
