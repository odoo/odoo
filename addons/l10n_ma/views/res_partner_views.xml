<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_property_form" model="ir.ui.view">
        <field name="name">res.partner.form.inherit.l10n_ma</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <field name="company_registry" position="attributes">
                <attribute name="invisible" add="country_code == 'MA'" separator="or"/>
            </field>
            <xpath expr="//field[@name='vat']" position="after">
                <field name="company_registry" invisible="parent_id or not is_company or country_code != 'MA'" options="{'label_field': 'company_registry_label'}"/>
            </xpath>
        </field>
    </record>
</odoo>
