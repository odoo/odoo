<?xml version="1.0" encoding="utf-8"?>
<openerp>
    <data>
   		<record id="view_change_standard_price" model="ir.ui.view">
            <field name="name">Change Standard Price</field>
            <field name="model">stock.change.standard.price</field>
            <field name="type">form</field>
            <field name="arch" type="xml">
              <form string="Change Standard Price">
             		<field name="new_price" on_change="onchange_price(new_price, context)" context="{'active_id': active_id}"/>
              		<newline/>
                    <field name="stock_account_input" domain="[('type','&lt;&gt;','view'),('type','&lt;&gt;','consolidation')]" attrs="{'readonly':[('enable_stock_in_out_acc','=',False)]}"/>
              		<newline/>
                    <field name="stock_account_output" domain="[('type','&lt;&gt;','view'),('type','&lt;&gt;','consolidation')]" attrs="{'readonly':[('enable_stock_in_out_acc','=',True)]}"/>
              		<newline/>
              		<field name="stock_journal"/>
              		<field name="enable_stock_in_out_acc" invisible="1"/>
              		<group col="2" colspan="4">
	              		<button special="cancel" string="Cancel" icon="gtk-cancel"/>
	              		<button name="change_price" string="Change" type="object" icon="gtk-ok"/>
              		</group>
              </form>
            </field>
		</record>

		<record id="action_view_change_standard_price" model="ir.actions.act_window">
            <field name="name">Change Standard Price</field>
            <field name="type">ir.actions.act_window</field>
            <field name="res_model">stock.change.standard.price</field>
            <field name="view_type">form</field>
            <field name="view_mode">form</field>
            <field name="view_id" ref="view_change_standard_price"/>
            <field name="target">new</field>            
        </record>

    </data>
</openerp>

