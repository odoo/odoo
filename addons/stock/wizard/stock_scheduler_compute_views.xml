<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_procurement_compute_wizard" model="ir.ui.view">
        <field name="name">Run Schedulers Manually</field>
        <field name="model">stock.scheduler.compute</field>
        <field name="arch" type="xml">
            <form string="Parameters">
                <p>
                This will run the schedulers.  Which will try to reserve not available deliveries, 
                try to see if it can solve any exceptions in the stock flow that might have been solved 
                and will run the reordering rules. 
                </p>
                <field name="orderpoints_only" groups="base.group_no_one"/>
                <footer>
                    <button name="procure_calculation" string="Run Schedulers" type="object" class="btn-primary"/>
                    <button string="Cancel" class="btn-default" special="cancel" />
                </footer>
            </form>
        </field>
    </record>

    <record id="action_procurement_compute" model="ir.actions.act_window">
        <field name="name">Run Schedulers</field>
        <field name="res_model">stock.scheduler.compute</field>
        <field name="view_type">form</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>

    <menuitem id="menu_stock_sched" name="Run Schedulers" parent="stock.menu_stock_root" groups="group_stock_manager" sequence="50"/>
    <menuitem action="action_procurement_compute" id="menu_procurement_compute" parent="menu_stock_sched"/>


</odoo>
