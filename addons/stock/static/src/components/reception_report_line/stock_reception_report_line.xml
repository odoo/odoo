<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">

    <tr t-name="stock.ReceptionReportLine" class="align-middle">
        <td t-esc="data.product.display_name"/>
        <td>
            <t t-esc="formatFloat(data.quantity)"/> <t t-if="props.showUom" t-esc="data.uom"/>
            <button type="button" class="btn btn-link fa-solid fa-chart-area"
                title="Forecast Report" aria-label="Forecast Report"
                t-on-click="onClickForecast" name="forecasted_report_link"/>
        </td>
        <td t-if="props.hasMovesIn">
            <t t-if="data.is_qty_assignable">
                <button t-if="!data.is_assigned"
                    t-on-click="onClickAssign"
                    t-att-disabled="opGuard.busy"
                    class="btn btn-sm btn-primary"
                    name="assign_link">
                    Assign
                </button>
                <button t-else=""
                    t-on-click="onClickUnassign"
                    t-att-disabled="opGuard.busy"
                    class="btn btn-sm btn-primary"
                    name="unassign_link">
                    Unassign
                </button>
            </t>
        </td>
        <td>
            <button t-if="data.is_qty_assignable &amp;&amp; data.source"
                class="btn btn-sm btn-primary"
                t-att-disabled="!data.is_assigned or opGuard.busy"
                name="print_label"
                t-on-click="onClickPrint">
                <span class="d-sm-block">Print Label</span>
            </button>
        </td>
    </tr>

</templates>
