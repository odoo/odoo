<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_stock_move_search" model="ir.ui.view">
        <field name="name">stock.move.search</field>
        <field name="model">stock.move</field>
        <field name="priority" eval="3" />
        <field name="arch" type="xml">
            <search string="Stock Moves">
                <field
                    name="reference"
                    string="Location"
                    filter_domain="['|',('location_id', 'ilike', self),('location_dest_id', 'ilike', self)]"
                    groups="stock.group_stock_multi_locations" />
                <field name="product_id" />
                <field
                    name="origin"
                    string="Reference"
                    filter_domain="['|', '|', ('origin', 'ilike', self), ('reference', 'ilike', self), ('picking_id', 'ilike', self)]" />
                <field
                    name="location_id"
                    string="Source Location"
                    groups="stock.group_stock_multi_locations" />
                <field
                    name="location_dest_id"
                    string="Destination Location"
                    groups="stock.group_stock_multi_locations" />
                <field
                    name="partner_id"
                    string="Partner"
                    filter_domain="[('picking_id.partner_id', 'child_of', self)]" />
                <filter
                    name="ready"
                    string="Ready"
                    help="Stock moves that are Available (Ready to process)"
                    domain="[('state','=','assigned')]" />
                <filter
                    name="future"
                    string="To Do"
                    help="Stock moves that are Confirmed, Available or Waiting"
                    domain="[('state','in',('assigned','confirmed','waiting'))]" />
                <filter
                    name="done"
                    string="Done"
                    help="Stock moves that have been processed"
                    domain="[('state','=','done')]" />
                <separator />
                <filter
                    name="incoming"
                    string="Incoming"
                    domain="[('location_id.usage', 'not in', ('internal', 'transit')), ('location_dest_id.usage', 'in', ('internal', 'transit'))]" />
                <filter
                    name="outgoing"
                    string="Outgoing"
                    domain="[('location_id.usage', 'in', ('internal', 'transit')), ('location_dest_id.usage', 'not in', ('internal', 'transit'))]" />
                <filter
                    name="inventory"
                    string="Inventory"
                    domain="[('is_inventory', '=', True)]" />
                <separator />
                <filter
                    name="today"
                    string="Date"
                    help="Scheduled or processing date"
                    date="date" />
                <group>
                    <filter
                        name="by_product"
                        string="Product"
                        context="{'group_by': 'product_id'}" />
                    <filter
                        name="groupby_picking_type_id"
                        string="Operation Type"
                        context="{'group_by': 'picking_type_id'}" />
                    <filter
                        name="groupby_picking_id"
                        string="Picking"
                        context="{'group_by': 'picking_id'}" />
                    <filter
                        name="groupby_location_id"
                        string="Source Location"
                        context="{'group_by': 'location_id'}"
                        groups="stock.group_stock_multi_locations" />
                    <filter
                        name="groupby_dest_location_id"
                        string="Destination Location"
                        context="{'group_by': 'location_dest_id'}"
                        groups="stock.group_stock_multi_locations" />
                    <filter
                        name="status"
                        string="Status"
                        context="{'group_by': 'state'}" />
                    <filter
                        name="groupby_create_date"
                        string="Creation Date"
                        context="{'group_by': 'create_date'}"
                        groups="base.group_no_one" />
                    <filter
                        name="groupby_date"
                        string="Scheduled Date"
                        context="{'group_by': 'date'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_stock_move_list" model="ir.ui.view">
        <field name="name">stock.move.list</field>
        <field name="model">stock.move</field>
        <field name="priority" eval="8" />
        <field name="arch" type="xml">
            <list
                string="Moves"
                default_order="date desc"
                editable="bottom"
                create="0"
                duplicate="0">
                <field
                    name="date"
                    optional="show"
                    readonly="1"
                    decoration-danger="(state not in ('cancel','done')) and date &gt; current_date" />
                <field name="reference" />
                <field name="picking_type_id" column_invisible="True" readonly="1" />
                <field name="location_usage" column_invisible="True" readonly="1" />
                <field
                    name="location_dest_usage"
                    column_invisible="True"
                    readonly="1" />
                <field name="product_id" readonly="1" />
                <field
                    name="location_id"
                    string="From"
                    options="{'no_create': True}"
                    readonly="1"
                    decoration-muted="location_usage not in ('internal','transit')" />
                <field
                    name="location_dest_id"
                    string="To"
                    options="{'no_create': True}"
                    readonly="1"
                    decoration-muted="location_dest_usage not in ('internal','transit')" />
                <field name="product_uom_qty" sum="Total Demand" readonly="1" />
                <field
                    name="quantity"
                    string="Quantity"
                    sum="Total Quantity"
                    readonly="1"
                    decoration-danger="(location_usage in ('internal','transit')) and (location_dest_usage not in ('internal','transit'))"
                    decoration-success="(location_usage not in ('internal','transit')) and (location_dest_usage in ('internal','transit'))" />
                <field
                    name="product_uom_id"
                    widget="many2one_uom"
                    options="{'no_open': True, 'no_create': True}"
                    groups="uom.group_uom"
                    readonly="1" />
                <field
                    name="company_id"
                    groups="base.group_multi_company"
                    readonly="1" />
                <field
                    name="state"
                    widget="badge"
                    optional="show"
                    readonly="1"
                    decoration-danger="state=='cancel'"
                    decoration-info="state== 'assigned'"
                    decoration-muted="state == 'draft'"
                    decoration-success="state == 'done'"
                    decoration-warning="state not in ('draft','cancel','done','assigned')" />
            </list>
        </field>
    </record>

    <record id="view_stock_move_list_picking" model="ir.ui.view">
        <field name="name">stock.picking.move.list</field>
        <field name="model">stock.move</field>
        <field name="priority" eval="50" />
        <field name="arch" type="xml">
            <list
                string="Stock Moves"
                editable="bottom"
                decoration-muted="scrap_id or state == 'cancel' or (state == 'done' and is_locked == True)">
                <field name="company_id" column_invisible="True" />
                <field name="date" column_invisible="True" />
                <field name="state" column_invisible="True" readonly="0" />
                <field name="picking_type_id" column_invisible="True" />
                <field name="location_id" column_invisible="True" />
                <field name="location_dest_id" column_invisible="True" />
                <field name="picking_code" column_invisible="True" />
                <field name="show_details_visible" column_invisible="True" />
                <field name="additional" column_invisible="True" />
                <field name="move_lines_count" column_invisible="True" />
                <field name="is_locked" column_invisible="True" />
                <field
                    name="product_id"
                    readonly="(state != 'draft' and not additional) or move_lines_count &gt; 0"
                    required="1" />
                <field
                    name="quantity_packaging_uom"
                    optional="hide"
                    groups="uom.group_uom" />
                <field name="packaging_uom_id" optional="hide" groups="uom.group_uom" />
                <field name="is_initial_demand_editable" column_invisible="True" />
                <field name="is_quantity_done_editable" column_invisible="True" />
                <field
                    name="product_uom_qty"
                    string="Demand"
                    readonly="not is_initial_demand_editable" />
                <field
                    name="quantity"
                    string="Quantity"
                    readonly="not is_quantity_done_editable" />
                <field name="picked" />
                <field
                    name="product_uom_id"
                    widget="many2one_uom"
                    options="{'no_open': True, 'no_create': True}"
                    groups="uom.group_uom"
                    readonly="state != 'draft' and id" />
            </list>
        </field>
    </record>

    <record id="view_stock_move_kanban" model="ir.ui.view">
        <field name="name">stock.move.kanban</field>
        <field name="model">stock.move</field>
        <field name="priority">10</field>
        <field name="arch" type="xml">
            <kanban class="o_kanban_mobile">
                <field name="product_qty" force_save="0" readonly="1" />
                <field name="is_inventory" />
                <templates>
                    <t t-name="card">
                        <field
                            name="product_id"
                            readonly="state == 'done'"
                            class="fw-bold fs-5" />
                        <a
                            name="action_show_details"
                            title="Show details"
                            class="o_icon_button fa-solid fa-list"
                            type="object"
                            invisible="not show_details_visible" />
                        <div invisible="not is_inventory">
                            <span>Initial Demand <field name="product_uom_qty" readonly="state == 'done'" /></span>
                            <br />
                            <span>Quantity <field name="quantity" /></span>
                        </div>
                        <span invisible="is_inventory"> Quantity <field name="quantity" /> / <field name="product_uom_qty" readonly="state != 'draft'" />
                            <field
                                name="product_uom_id"
                                groups="uom.group_uom"
                                class="ms-1" />
                        </span>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_stock_move_pivot" model="ir.ui.view">
        <field name="name">stock.move.pivot</field>
        <field name="model">stock.move</field>
        <field name="arch" type="xml">
            <pivot string="Stock Moves Analysis" sample="1">
                <field name="picking_type_id" type="col" />
                <field name="date" type="row" interval="month" />
            </pivot>
        </field>
    </record>

    <record id="view_stock_move_graph" model="ir.ui.view">
        <field name="name">stock.move.graph</field>
        <field name="model">stock.move</field>
        <field name="arch" type="xml">
            <graph string="Stock Moves Analysis" sample="1">
                <field name="product_id" />
                <field
                    name="location_dest_id"
                    groups="stock.group_stock_multi_locations" />
                <field name="product_uom_qty" type="measure" />
            </graph>
        </field>
    </record>

    <record id="view_stock_move_form_operations" model="ir.ui.view">
        <field name="name">stock.move.operations.form</field>
        <field name="model">stock.move</field>
        <field name="priority">1000</field>
        <field name="arch" type="xml">
            <form string="Move Detail">
                <field name="sequence" invisible="1" />
                <field name="company_id" invisible="1" />
                <field name="state" invisible="1" />
                <field name="location_id" invisible="1" />
                <field name="location_dest_id" invisible="1" />
                <field name="picking_id" invisible="1" readonly="state == 'done'" />
                <field name="picking_type_id" invisible="1" />
                <field name="is_locked" invisible="1" />
                <field name="show_lot_actions" invisible="1" />
                <field name="picking_code" invisible="1" />
                <field name="has_tracking" invisible="1" />
                <field name="show_quant" invisible="1" />
                <field name="show_lots_text" invisible="1" />
                <field name="show_lots_m2o" invisible="1" />
                <field name="product_qty" invisible="1" />
                <field name="quantity" invisible="1" />
                <group name="product_qty">
                    <group>
                        <field name="product_id" readonly="id or move_line_ids" />
                        <label for="product_uom_qty" />
                        <div class="o_row">
                            <span>
                                <field
                                    name="product_uom_qty"
                                    nolabel="1"
                                    readonly="state != 'draft'" />
                            </span>
                            <span>
                                <field
                                    name="product_uom_id"
                                    widget="many2one_uom"
                                    nolabel="1"
                                    options="{'no_open': True}"
                                    groups="uom.group_uom"
                                    readonly="state != 'draft'" />
                            </span>
                        </div>
                    </group>
                    <group />
                </group>
                <div class="d-flex">
                    <widget
                        name="generate_serials"
                        invisible="not show_lot_actions"
                        class="btn btn-link btn-group" />
                    <widget
                        name="import_lots"
                        invisible="not show_lot_actions"
                        class="btn btn-link btn-group" />
                </div>
                <field
                    name="move_line_ids"
                    widget="sml_x2_many"
                    context="{'list_view_ref': 'stock.view_stock_move_line_operation_tree', 'form_view_ref': 'stock.view_stock_move_line_form_mobile', 'default_picking_id': picking_id, 'default_move_id': id, 'default_product_id': product_id, 'default_location_id': location_id, 'default_location_dest_id': location_dest_id, 'default_company_id': company_id, 'active_picking_id': picking_id}"
                    readonly="state == 'cancel' or (state == 'done' and is_locked) or not product_id" />
            </form>
        </field>
    </record>

    <record id="view_stock_move_form" model="ir.ui.view">
        <field name="name">stock.move.form</field>
        <field name="model">stock.move</field>
        <field name="priority" eval="1" />
        <field name="arch" type="xml">
            <form string="Stock Moves" create="0" edit="0">
                <field name="company_id" invisible="1" />
                <header>
                    <field
                        name="state"
                        widget="statusbar"
                        statusbar_visible="draft,confirmed,assigned,done" />
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                    </div>
                    <group>
                        <group name="main_grp" colspan="2">
                            <group name="main_grp_col1">
                                <field name="reference" />
                                <field
                                    name="location_id"
                                    options="{'no_create': True}" />
                                <field
                                    name="location_dest_id"
                                    options="{'no_create': True}" />
                                <field
                                    name="company_id"
                                    groups="base.group_multi_company" />
                            </group>
                            <group name="main_grp_col2">
                                <field name="product_id" readonly="state == 'done'" />
                                <label for="product_uom_qty" />
                                <div class="o_row">
                                    <field
                                        name="product_uom_qty"
                                        readonly="state == 'done'" />
                                    <field
                                        name="product_uom_id"
                                        widget="many2one_uom"
                                        options="{'no_open': True, 'no_create': True}"
                                        groups="uom.group_uom" />
                                </div>
                                <field
                                    name="date"
                                    string="Scheduled Date"
                                    help="Scheduled time for the first part of the shipment to be processed."
                                    invisible="state == 'done'"
                                    readonly="1" />
                                <field
                                    name="date"
                                    string="Move Date"
                                    help="The actual date when the transfer was processed."
                                    invisible="state != 'done'"
                                    readonly="1" />
                                <field name="date_deadline" force_save="1" />
                            </group>
                        </group>
                        <group
                            name="origin_grp"
                            string="Origin"
                            groups="base.group_no_one">
                            <field name="origin" />
                            <field
                                name="procure_method"
                                groups="stock.group_adv_location"
                                readonly="state != 'draft'" />
                        </group>
                        <group
                            name="linked_group"
                            string="Linked Moves"
                            colspan="2"
                            groups="base.group_no_one">
                            <field
                                name="move_orig_ids"
                                string="Origin Moves"
                                readonly="1">
                                <list>
                                    <field name="location_id" />
                                    <field name="location_dest_id" />
                                    <field
                                        name="product_uom_qty"
                                        readonly="state == 'done'" />
                                    <field name="product_uom_id" />
                                    <field name="state" />
                                </list>
                            </field>
                            <field
                                name="move_dest_ids"
                                string="Destination Moves"
                                readonly="1">
                                <list>
                                    <field name="location_id" />
                                    <field name="location_dest_id" />
                                    <field
                                        name="product_uom_qty"
                                        readonly="state == 'done'" />
                                    <field name="product_uom_id" />
                                    <field name="state" />
                                </list>
                            </field>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="stock_move_action" model="ir.actions.act_window">
        <field name="name">Moves Analysis</field>
        <field name="res_model">stock.move</field>
        <field name="path">moves-analysis</field>
        <field name="view_id" ref="view_stock_move_list" />
        <field name="search_view_id" ref="view_stock_move_search" />
        <field
            name="context">{
            'search_default_done': 1,
            'pivot_measures': ['quantity', '__count__'],
            }</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No stock move found
            </p>
            <p>
                This menu gives you the full traceability of inventory
                operations on a specific product. You can filter on the product
                to see all the past or future movements for the product.
            </p>
        </field>
    </record>

    <record id="action_stock_move_tree_all" model="ir.actions.act_window.view">
        <field name="sequence" eval="3" />
        <field name="view_mode">list</field>
        <field name="view_id" ref="view_stock_move_list" />
        <field name="act_window_id" ref="stock_move_action" />
    </record>

    <record id="action_stock_move_form_all" model="ir.actions.act_window.view">
        <field name="sequence" eval="3" />
        <field name="view_mode">form</field>
        <field name="view_id" ref="view_stock_move_form" />
        <field name="act_window_id" ref="stock_move_action" />
    </record>

    <record id="action_stock_move_pivot_all" model="ir.actions.act_window.view">
        <field name="sequence" eval="1" />
        <field name="view_mode">pivot</field>
        <field name="view_id" ref="view_stock_move_pivot" />
        <field name="act_window_id" ref="stock_move_action" />
    </record>

    <record id="action_stock_move_graph_all" model="ir.actions.act_window.view">
        <field name="sequence" eval="3" />
        <field name="view_mode">graph</field>
        <field name="view_id" ref="view_stock_move_graph" />
        <field name="act_window_id" ref="stock_move_action" />
    </record>

    <record id="action_stock_move_kanban_all" model="ir.actions.act_window.view">
        <field name="sequence" eval="4" />
        <field name="view_mode">kanban</field>
        <field name="view_id" ref="view_stock_move_kanban" />
        <field name="act_window_id" ref="stock_move_action" />
    </record>

</odoo>
