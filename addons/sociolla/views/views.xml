<openerp>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="sociolla.list">
      <field name="name">sociolla list</field>
      <field name="model">sociolla.sociolla</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="sociolla.action_window">
      <field name="name">sociolla window</field>
      <field name="res_model">sociolla.sociolla</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="sociolla.action_server">
      <field name="name">sociolla server</field>
      <field name="model_id" ref="model_sociolla_sociolla"/>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="sociolla" id="sociolla.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="sociolla.menu_1" parent="sociolla.menu_root"/>
    <menuitem name="Menu 2" id="sociolla.menu_2" parent="sociolla.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="sociolla.menu_1_list" parent="sociolla.menu_1"
              action="sociolla.action_window"/>
    <menuitem name="Server to list" id="sociolla" parent="sociolla.menu_2"
              action="sociolla.action_server"/>
    -->
  </data>
</openerp>