<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <data noupdate="1">

        <record id="calendar_event_rule_for_microsoft_sync_user" model="ir.rule">
            <field name="name">Event Permissions for Microsoft Sync User</field>
            <field ref="model_calendar_event" name="model_id"/>
            <field name="domain_force">[('user_id', '=', False), ('microsoft_sync_user_id', '=', user.id)]</field>
            <field name="groups" eval="[(4, ref('base.group_user'))]"/>
        </record>

    </data>
</odoo>
