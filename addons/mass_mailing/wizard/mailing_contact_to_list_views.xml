<?xml version="1.0"?>
<odoo>

    <record id="mailing_contact_to_list_view_form" model="ir.ui.view" >
        <field name="name">mailing.contact.to.list.view.form</field>
            <field name="model">mailing.contact.to.list</field>
            <field name="arch" type="xml">
                <form string="Send a Sample Mail">
                    <group>
                        <field name="mailing_list_ids" widget="many2many_tags"
                            options="{'no_create_edit': True, 'no_open': True, 'color_field': 'color'}"
                            placeholder="Select lists..."/>
                        <field name="contact_ids" invisible="1"/>
                        <field name="partner_ids" invisible="1"/>
                    </group>
                    <footer>
                        <button string="Add to Lists" name="action_add_contacts" type="object" class="btn-primary" data-hotkey="q"/>
                        <button class="btn-secondary" special="cancel" data-hotkey="x"/>
                    </footer>
                </form>
            </field>
    </record>

    <record id="mailing_contact_to_list_action" model="ir.actions.act_window">
        <field name="name">Add Mailing Contacts to a Mailing List</field>
        <field name="res_model">mailing.contact.to.list</field>
        <field name="view_mode">form</field>
        <field name="context">{"dialog_size": "medium"}</field>
        <field name="target">new</field>
    </record>

    <record id="res_partner_to_list_action" model="ir.actions.act_window">
        <field name="name">Add to Mailing List</field>
        <field name="res_model">mailing.contact.to.list</field>
        <field name="view_mode">form</field>
        <field name="context">{
            "dialog_size": "medium",
            "default_partner_ids": active_ids,
        }</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="base.model_res_partner"/>
        <field name="binding_view_types">form,list,kanban</field>
        <field name="group_ids" eval="[Command.link(ref('mass_mailing.group_mass_mailing_user'))]"/>
    </record>

    <record id="res_partner_to_list_results_action" model="ir.actions.client">
        <field name="name">Add Contacts to Mailing List results</field>
        <field name="tag">res_partner_to_list_results</field>
    </record>

</odoo>
