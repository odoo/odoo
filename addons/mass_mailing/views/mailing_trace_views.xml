<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--  MAILING TRACE !-->
    <record model="ir.ui.view" id="mailing_trace_view_search">
        <field name="name">mailing.trace.search</field>
        <field name="model">mailing.trace</field>
        <field name="arch" type="xml">
           <search string="Mail Statistics">
                <field name="mail_mail_id_int"/>
                <field name="message_id"/>
                <field name="email"/>
                <field name="mass_mailing_id"/>
                <filter string="Scheduled" name="filter_scheduled" domain="[('trace_status', '=', 'outgoing')]"/>
                <filter string="Canceled" name="filter_canceled" domain="[('trace_status', '=', 'cancel')]"/>
                <filter string="Sent" name="filter_sent" domain="[('sent_datetime', '!=', False)]"/>
                <filter string="Delivered" name="filter_delivered" domain="[('sent_datetime', '!=', False), ('trace_status', 'not in', ['error', 'cancel'])]"/>
                <separator/>
                <filter string="Opened" name="filter_opened" domain="[('trace_status', 'in', ['open', 'reply'])]"/>
                <filter string="Clicked" name="filter_clicked" domain="[('links_click_done', '!=', False)]"/>
                <filter string="Replied" name="filter_replied" domain="[('trace_status', '=', 'reply')]"/>
                <filter string="Bounced" name="filter_bounced" domain="[('trace_status', '=', 'bounce')]"/>
                <filter string="Failed" name="filter_failed" domain="[('trace_status', '=', 'error')]"/>
                <group expand="0" string="Group By">
                    <filter string="State" name="state" domain="[]" context="{'group_by': 'trace_status'}"/>
                    <filter string="Open Date" name="group_open_date" domain="[('trace_status', 'in', ['open', 'reply'])]" context="{'group_by': 'trace_status_update:day'}"/>
                    <filter string="Reply Date" name="group_reply_date" domain="[('trace_status', '=', 'reply')]" context="{'group_by': 'trace_status_update:day'}"/>
                    <filter string="Last State Update" name="state_update" domain="[]" context="{'group_by': 'trace_status_update'}"/>
                    <filter string="Mass Mailing"  name="mass_mailing" domain="[]" context="{'group_by': 'mass_mailing_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record model="ir.ui.view" id="mailing_trace_view_tree">
        <field name="name">mailing.trace.tree</field>
        <field name="model">mailing.trace</field>
        <field name="arch" type="xml">
            <tree string="Mail Statistics" create="0">
                <field name="mass_mailing_id"/>
                <field name="email"/>
                <field name="message_id"/>
                <field name="sent_datetime"/>
                <field name="trace_status"/>
                <field name="trace_status_update"/>
                <field name="links_click_done"/>
                <field name="failure_type"/>
            </tree>
        </field>
    </record>

    <record model="ir.ui.view" id="mailing_trace_view_form">
        <field name="name">mailing.trace.form</field>
        <field name="model">mailing.trace</field>
        <field name="arch" type="xml">
            <form string="Mail Statistics" create="0">
                <header>
                    <field name="trace_status" widget="statusbar"/>
                </header>
                <sheet>
                    <div class="alert alert-info text-center" role="alert" name="alert_mail_exception"
                        attrs="{'invisible': [('trace_status', 'not in', ['error', 'bounce'])]}">
                        <p>
                            <strong><span name="trace_type_name_mail">This email</span>
                            <span attrs="{'invisible': [('trace_status', '!=', 'error')]}"> could not be sent</span>
                            <span attrs="{'invisible': [('trace_status', '!=', 'bounce')]}"> appears to be invalid</span>
                            </strong>
                        </p>
                    </div>
                    <group>
                        <group string="Recipient">
                            <field name="trace_type" invisible="1"/>
                            <field name="email"/>
                            <field name="mail_mail_id_int"/>
                            <field name="message_id"/>
                        </group>
                        <group string="Document">
                            <field name="model"/>
                            <field name="res_id"/>
                            <field name="trace_status_update"/>
                        </group>
                    </group>
                    <group string="Marketing">
                        <group>
                            <field name="mass_mailing_id"/>
                            <field name="campaign_id"/>
                            <field name="sent_datetime"/>
                            <field name="links_click_done"/>
                        </group>
                        <group>
                            <field name="trace_status_update"/>
                            <field name="failure_type" attrs="{'invisible': [('trace_status', '!=', 'error')]}"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="view_mail_mail_statistics_graph" model="ir.ui.view">
        <field name="name">Mail Statistics Graph</field>
        <field name="model">mailing.trace</field>
        <field name="arch" type="xml">
            <graph string="Mail Statistics" type="bar" stacked="True" interval="day">
                <field name="trace_status_update" type="row" interval="day"/>
                <field name="trace_status" type="row"/>
            </graph>
        </field>
    </record>

    <record id="mailing_trace_action" model="ir.actions.act_window">
        <field name="name">Mailing Traces</field>
        <field name="res_model">mailing.trace</field>
        <field name="view_mode">tree,form,graph,pivot</field>
        <field name="domain">[]</field>
    </record>

    <record id="action_view_mail_mail_statistics_mailing" model="ir.actions.act_window">
        <field name="name">Mail Statistics</field>
        <field name="res_model">mailing.trace</field>
        <field name="view_mode">graph,tree,form,pivot</field>
        <field name="domain">[]</field>
        <field name="context">{'search_default_mass_mailing_id': active_id}</field>
    </record>

    <!-- Add in Technical/Email -->
    <menuitem id="menu_email_statistics"
        name="Mailing Traces"
        parent="mass_mailing.mailing_mailing_menu_technical" sequence="2"
        action="mailing_trace_action"/>
</odoo>
