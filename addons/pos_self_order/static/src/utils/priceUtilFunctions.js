/**
 * @param {number} base_price 
 * @param {number} tax_id 
 * @param {Object} tax_list_object 
 * @returns {number}
 */
// export function findListPriceBasedOnTax( base_price, tax_id, tax_list_object ) {
//     tax_rate = tax_list_object[tax_id] ?? 0;
//     return base_price * (1 + tax_rate / 100);
// }
