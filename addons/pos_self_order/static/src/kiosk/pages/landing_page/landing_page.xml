<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.LandingPage" owl="1">
        <KioskTemplate headerHeight="0" contentHeight="90" footerHeight="10">
            <t t-set-slot="content">
                <div t-if="languages.length > 1" t-on-click="openLanguages" class="o_kiosk_language_selector position-absolute top-0 end-0 m-4 rounded p-4 bg-white shadow-lg">
                    <img class="rounded" t-attf-src="{{currentLanguage.flag_image_url}}" />
                    <span t-esc="currentLanguage.display_name" class="ms-3"></span>
                </div>
                <div t-if="selfOrder.kiosk_image_home.length > 0" t-on-click="start" class="o_kiosk_home_page d-flex flex-column vh-100 align-items-center overflow-hidden">
                    <div id="carouselAutoplaying" t-ref="carousel" class="carousel slide h-100" data-bs-ride="true">
                        <div class="carousel-inner h-100">
                            <div t-foreach="selfOrder.kiosk_image_home" t-as="image" t-key="image.id" t-attf-class="carousel-item h-100 {{activeImage}}">
                                <img t-attf-src="data:image/png;base64,{{image.data}}" class="o_kiosk-landing_image d-block h-100 w-100" />
                            </div>
                        </div>
                    </div>
                </div>
            </t>
            <t t-set-slot="footer">
                <button class="btn btn-primary h-100 w-100 p-0 m-0 fs-1 text-uppercase" t-on-click="start">TOUCH TO START</button>
            </t>
        </KioskTemplate>
    </t>
</templates>
