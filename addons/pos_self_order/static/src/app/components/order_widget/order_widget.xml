<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.OrderWidget">
        <div
            class="page-buttons bg-view z-1"
            t-att-class="{
                'border-top' : !props.removeTopClasses

            }">
            <div class="o_self_footer container o_self_container px-kiosk-p-4 d-flex flex-nowrap py-3 gap-2">
                <button class="btn btn-secondary btn-lg h-auto w-auto opacity-75 py-2 px-3 d-sm-none" t-att-class="leftButton.icon" t-on-click="onClickleftButton" />
                <button class="btn btn-secondary btn-lg d-none d-sm-inline text-nowrap btn-back btn-cancel" t-on-click="onClickleftButton" t-esc="leftButton.name" />
                <div class="d-flex flex-nowrap ms-2 ms-sm-4 w-100 justify-content-end gap-2">
                    <div class="to-order cart text-bg-primary-faded text-truncate d-flex align-items-center px-4 rounded-4" t-esc="lineNotSend.count" t-if="this.selfOrder.router.activeSlot === 'product_list'"/>
                    <button t-if="buttonToShow.label" t-attf-class="cart btn btn-primary btn-lg text-truncate {{ buttonToShow.disabled ? 'disabled' : '' }}" t-on-click="props.action" t-esc="buttonToShow.label"/>
                </div>
            </div>
        </div>
    </t>
</templates>
