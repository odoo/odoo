<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.PresetInfoPopup">
        <Dialog header="false"
            contentClass="'h-50 w-100 m-0 position-absolute self_order_pills_selection_popup_dialog'"
            bodyClass="'p-4 pb-3 m-0'"
            title="this.props.title">
            <div class="select_popup_preset_info w-100 d-flex flex-column h-100 overflow-hidden">
                <div class="d-flex justify-content-between align-items-center px-2 mt-2 mb-4">
                    <h2 class="mb-0">We need more info</h2>
                    <button class="btn-close" aria-label="close" t-on-click="close"/>
                </div>
                <div class="d-flex flex-column gap-3 overflow-y-auto overflow-x-hidden flex-grow-1">
                    <select t-if="preset.needsPartner and existingPartners.length"
                        class="partner-select form-select form-select-lg"
                        t-on-change="selectExistingPartner"
                        t-custom-model="state.selectedPartnerId">
                        <option value="">
                            Select your address
                        </option>
                        <t t-foreach="existingPartners" t-as="partner" t-key="partner.id">
                            <option t-att-value="partner.id" t-out="partner.name + ' ' + partner.address" />
                        </t>
                    </select>
                    <input t-if="preset.needsName || preset.needsPartner"
                        type="text"
                        class="form-control form-control-lg"
                        t-att-placeholder="partnerIsSelected ? '*************' : 'Name'"
                        t-custom-model="state.name"
                        t-att-disabled="partnerIsSelected" />
                    <input t-if="preset.needsEmail || preset.needsPartner"
                        type="text"
                        class="form-control form-control-lg"
                        t-att-placeholder="partnerIsSelected ? '*************' : 'Email'"
                        t-custom-model="state.email"
                        t-att-disabled="partnerIsSelected" />
                    <t t-if="preset.needsName || preset.needsPartner">
                        <input type="text"
                            class="form-control form-control-lg"
                            t-att-placeholder="partnerIsSelected ? '*************' : 'Phone'"
                            t-custom-model="state.phone"
                            t-att-disabled="partnerIsSelected"/>
                        <p t-if="!checkPhoneFormat()" class="text-danger">Enter a valid phone number starting with the country code (e.g. +32)</p>
                    </t>
                    <t t-if="preset.needsPartner">
                        <select class="partner-select form-select form-select-lg" t-custom-model="state.countryId" t-att-disabled="partnerIsSelected">
                            <option value="">
                                <t t-if="partnerIsSelected">*************</t>
                                <t t-else="">Select a country</t>
                            </option>
                            <t t-foreach="this.countries" t-as="country" t-key="country.id">
                                <option t-att-value="country.id" t-out="country.name" />
                            </t>
                        </select>
                        <div class="d-flex gap-3">
                            <input type="text"
                                class="form-control form-control-lg w-75"
                                t-att-placeholder="partnerIsSelected ? '*************' : 'Street and Number'"
                                t-custom-model="state.street"
                                t-att-disabled="partnerIsSelected"/>
                            <input
                                type="text"
                                class="form-control form-control-lg w-25"
                                t-att-placeholder="partnerIsSelected ? '*************' : 'Zip'"
                                t-custom-model="state.zip"
                                t-att-disabled="partnerIsSelected"/>
                        </div>
                        <div class="d-flex gap-3">
                            <t t-set="availableState" t-value="this.states" />
                            <select t-if="availableState.length" class="partner-select form-select form-select-lg" t-custom-model="state.stateId" t-att-disabled="partnerIsSelected">
                                <option value="">
                                    <t t-if="partnerIsSelected">*************</t>
                                    <t t-else="">Select a state</t>
                                </option>
                                <t t-foreach="availableState" t-as="state" t-key="state.id">
                                    <option t-att-value="state.id" t-out="state.name" />
                                </t>
                            </select>
                            <input type="text"
                                class="form-control form-control-lg"
                                t-att-placeholder="partnerIsSelected ? '*************' : 'City'"
                                t-custom-model="state.city"
                                t-att-disabled="partnerIsSelected"/>
                        </div>
                    </t>
                </div>
                <div class="w-100 d-flex mt-3">
                    <button class="btn btn-secondary flex-grow-1 me-1"
                        t-on-click="props.close">
                        Cancel
                    </button>
                    <button class="btn btn-primary flex-grow-1 ms-1"
                        t-on-click="setInformations"
                        t-att-disabled="!this.validSelection">
                        Continue
                    </button>
                </div>
            </div>
        </Dialog>
    </t>
</templates>
