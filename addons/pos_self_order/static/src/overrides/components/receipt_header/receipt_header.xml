<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.ReceiptHeader" t-inherit="point_of_sale.ReceiptHeader" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('pos-receipt-contact')]" position="before">
            <t t-if="this.order.config._self_order_pos">
                <div t-if="this.order.preset_id or this.order.config.self_ordering_mode !== 'nothing'" class="picking-service text-center pt-3">
                    <span t-if="this.order.preset_id?.identification == 'none' or (!this.order.preset_id and this.order.config.self_ordering_service_mode == 'table')">Service at Table</span>
                    <span t-elif="this.order.preset_id?.identification == 'name' or (!this.order.preset_id and this.order.config.self_ordering_service_mode != 'table')">Pickup At Counter</span>
                    <span t-else="">Delivery</span>
                </div>
                <h1 t-if="this.order.tracking_number and this.order.config.displayBigTrackingNumber" class="tracking-number text-center" style="font-size: 100px" t-out="this.order.tracking_number" />
                <div t-if="this.order.config.self_ordering_mode !== 'nothing' and this.order.table_stand_number" class="table-tracker text-center">
                    Table Tracker:
                    <span class="pt-3" t-out="this.order.table_stand_number" />
                </div>
            </t>
        </xpath>
    </t>
</templates>
