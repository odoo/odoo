<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_self_order.NavBar" owl="1">
        <nav t-attf-class="o_self_order_navbar d-flex align-items-center p-2 border-bottom bg-white">
            <button class="btn btn-link text-dark"
                    t-on-click="() => selfOrder.setPage(props.goBack)">
                <i class="fa fa-fw fa-chevron-left"/>
            </button>
            <div class="mx-auto overflow-hidden">
                <h3 class="m-0 text-primary text-truncate" 
                    t-esc="props.customText or selfOrder.company_name"
                    t-on-click="() => selfOrder.setPage('/')"
                    />
            </div>
            <span class="px-3 text-center" t-esc="selfOrder?.table?.name"/>
        </nav>
    </t>
</templates>
