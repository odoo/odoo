<templates id="template" xml:space="preserve">

    <!-- Template for the widget SignatureForm. -->
    <t t-name="portal.SignatureForm">
        <div t-ref="root">
            <div t-if="this.state.success" class="alert alert-success" role="status">
                <span t-if="this.state.success.message" t-out="this.state.success.message"/>
                <span t-else="">Thank You!</span>
                <a t-if="this.state.success.redirect_url" t-att-href="this.state.success.redirect_url">
                    <t t-if="this.state.success.redirect_message" t-out="this.state.success.redirect_message"/>
                    <t t-else="">Click here to see your document.</t>
                </a>
            </div>
            <t t-else="">
                <NameAndSignature t-props="this.nameAndSignatureProps"/>
                <form method="POST" t-on-submit.prevent="this.onClickSubmit">
                    <div class="o_web_sign_name_and_signature"/>
                    <div class="o_portal_sign_controls my-3">
                        <div t-if="this.state.error" class="o_portal_sign_error_msg alert alert-danger" role="status">
                            <t t-out="this.state.error"/>
                        </div>
                        <div class="text-end my-3">
                            <button type="submit" class="o_portal_sign_submit btn btn-primary" t-att-disabled="this.state.submitting || this.signature.isSignatureEmpty">
                                <i class="fa-solid me-1" t-att-class="this.state.submitting ? 'fa-spinner fa-spin' : 'fa-check'"/>
                                <t t-out="this.sendLabel"/>
                            </button>
                        </div>
                    </div>
                </form>
            </t>
        </div>
    </t>

</templates>
