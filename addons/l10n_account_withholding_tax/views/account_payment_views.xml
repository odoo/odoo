<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_account_payment_form" model="ir.ui.view">
        <field name="name">account.payment.form</field>
        <field name="model">account.payment</field>
        <field name="inherit_id" ref="account.view_account_payment_form"/>
        <field name="arch" type="xml">
            <field name="memo" position="after">
                <field name="should_withhold_tax" readonly="state != 'draft'"/>
                <field name="withhold_base_amount" invisible="not should_withhold_tax" readonly="state != 'draft'"/>
                <field name="withhold_tax_id" invisible="not should_withhold_tax" readonly="state != 'draft'"/>
                <field name="withhold_tax_amount" invisible="not should_withhold_tax" readonly="state != 'draft'"/>
            </field>
        </field>
    </record>
</odoo>
