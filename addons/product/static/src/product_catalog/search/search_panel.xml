<?xml version="1.0" encoding="UTF-8"?>

<templates id="template" xml:space="preserve">
    <t
        t-name="ProductCatalog.SearchPanelContent"
        t-inherit="web.SearchPanelContent"
        t-inherit-mode="primary"
    >
        <section position="before">
            <section t-if="showSections" class="o_search_panel_sections mt-5">
                <div class="d-flex align-items-center mb-3">
                    <i class="fa fa-filter me-2"/>
                    <span class="me-2">Selected products</span>
                    <div class="form-check form-switch">
                        <input
                            class="form-check-input"
                            type="checkbox"
                            role="switch"
                            t-att-checked="selectedSection.filtered ? true : false"
                            t-on-click="() => this.setSelectedSection(
                                selectedSection.sectionId, !selectedSection.filtered
                            )"
                        />
                    </div>
                </div>
                <ul class="list-group d-block">
                    <li
                        draggable="true"
                        t-if="this.state.sectionOfSections.size > 1"
                        t-foreach="[...this.state.sectionOfSections.keys()]"
                        t-as="secId"
                        t-key="secId"
                        t-att-class="'list-group-item p-0 mb-1 border-0 cursor-pointer ' + (selectedSection.sectionId == secId ? 'o_selected_section' : '')"
                        t-on-dragstart="(e)=>onDragStart(secId, e)"
                        t-on-dragover="(e)=>onDragOver(e)"
                        t-on-drop="(e)=>onDrop(secId, e)"
                    >
                        <div class="d-flex align-items-center ms-2">
                            <t t-set="section" t-value="this.state.sectionOfSections.get(secId)"/>
                            <span
                                t-att-class="secId
                                    ? 'o_row_handle oi oi-draggable ui-sortable-handle'
                                    : 'd-none'"
                            />
                            <div
                                t-att-class="'w-100 d-flex align-items-center ' + (secId ? 'ms-2' : 'ms-4')"
                                t-on-click="() => this.setSelectedSection(
                                    secId, selectedSection.filtered
                                )"
                                t-att-title="section.name"
                            >
                                <span class="o_section_name text-truncate" t-out="section.name"/>
                                <span class="ms-1">(<t t-out="section.line_count"/>)</span>
                            </div>
                        </div>
                    </li>
                </ul>
                <input
                    t-if="this.state.isAddingSection"
                    class="o_section_input"
                    type="text"
                    placeholder="Enter a description"
                    t-model="this.state.newSectionName"
                    t-on-keydown="onSectionInputKeydown"
                    t-on-blur="createSection"
                />
                <button
                    t-else=""
                    class="btn btn-link"
                    type="button"
                    t-on-click="enableSectionInput"
                >
                    + Add Section
                </button>
            </section>
        </section>
    </t>
    <t
        t-name="ProductCatalog.SearchPanel.Regular"
        t-inherit="ProductCatalog.SearchPanelContent"
        t-inherit-mode="primary"
    />
    <t t-name="ProductCatalog.SearchPanel" t-inherit="web.SearchPanel" t-inherit-mode="primary">
        <xpath expr="//t[@t-call='web.SearchPanel.Regular']" position="attributes">
            <attribute name="t-call">ProductCatalog.SearchPanel.Regular</attribute>
        </xpath>
    </t>
    <t t-name="ProductCatalogSearchPanel.FiltersGroup" t-inherit="web.SearchPanel.FiltersGroup" t-inherit-mode="primary">
        <li t-foreach="[...values.keys()]" position="replace">
            <li t-foreach="[...this.state.sectionOfTags.keys()]" t-as="tagName" t-key="tagName"
                class="o_search_panel_filter_value list-group-item p-0 mb-1 border-0 o_cursor_pointer"
                t-att-class="{ 'ps-2' : isChildList }">
                <t t-set="tagCount" t-value="this.state.sectionOfTags.get(tagName).get('count')"/>
                <t t-set="tagIds" t-value="this.state.sectionOfTags.get(tagName).get('ids')"/>
                <div class="form-check w-100">
                    <input type="checkbox"
                           t-attf-id="{{ section.id }}_input_{{ tagName }}"
                           class="form-check-input"
                           t-on-click="ev => this.toggleTagFilterValue(section.id, tagIds, ev)"/>
                    <label class="o_search_panel_label form-check-label d-flex align-items-center justify-content-between w-100 o_cursor_pointer"
                           t-attf-for="{{ section.id }}_input_{{ tagName }}"
                           t-att-title="(group and group.tooltip) or false">
                        <span class="o_search_panel_label_title text-truncate" t-esc="tagName"/>
                        <span t-if="section.enableCounters and tagCount gt 0"
                              class="o_search_panel_counter text-muted mx-2 small"
                              t-esc="tagCount"/>
                    </label>
                </div>
            </li>
        </li>
    </t>
</templates>
