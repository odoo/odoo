<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="product_document_form" model="ir.ui.view">
        <field name="name">product.document.form</field>
        <field name="model">product.document</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <field name="res_model" invisible="True"/>
                    <h1>
                        <field name="name" class="w-75" readonly="not raw and type != 'url'"/>
                    </h1>
                    <group>
                        <group>
                            <field name="type" readonly="raw or id" class="oe_inline"/>
                            <field
                                name="raw"
                                string="File"
                                filename="name"
                                invisible="type == 'url'"
                                required="type == 'binary'"
                            />
                            <field name="url" widget="url" invisible="type == 'binary'" required="type == 'url'"/>
                            <field name="res_name" string="Product Variant" invisible="res_model != 'product.product'"/>
                            <field
                                name="company_id"
                                groups="base.group_multi_company"
                                options="{'no_create': True}"
                                placeholder="Visible to all"
                            />
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="product_document_kanban" model="ir.ui.view">
        <field name="name">product.document.kanban</field>
        <field name="model">product.document</field>
        <field name="arch" type="xml">
            <kanban js_class="product_documents_kanban" can_open="0">
                <field name="sequence" widget="handle"/>
                <field name="ir_attachment_id"/>
                <field name="mimetype"/>
                <field name="type"/>
                <field name="name"/>
                <field name="active"/>
                <field name="res_model"/>
                <templates>
                    <t t-name="menu">
                        <a t-if="widget.editable" type="open" class="dropdown-item">Edit</a>
                        <a t-if="widget.deletable" type="delete" class="dropdown-item">Delete</a>
                        <a t-attf-href="/web/content/#{record.ir_attachment_id.raw_value}?download=true" download="" class="dropdown-item">Download</a>
                    </t>
                    <t t-name="card" class="o_kanban_attachment flex-row">
                        <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                        <t t-set="previewable" t-value="record.type.raw_value === 'binary' and new RegExp('(image|video|application/pdf|text)').test(record.mimetype.value)"/>
                        <aside class="o_kanban_image m-2">
                            <div t-attf-class="o_kanban_image_wrapper d-flex align-items-center justify-content-center #{previewable ? 'o_kanban_previewer' : ''}">
                                <div t-if="record.type.raw_value == 'url'" class="oi oi-3x text-muted" data-icon="link" title="Link"/>
                                <img t-elif="new RegExp('image.*(gif|jpeg|jpg|png|webp)').test(record.mimetype.value)"
                                    t-attf-src="/web/image/#{record.ir_attachment_id.raw_value}/80x80"
                                    t-att-alt="record.name.value" class="o_attachment_image object-fit-contain"/>
                                <div t-else="" class="o_image" t-att-data-mimetype="record.mimetype.value"/>
                            </div>
                        </aside>
                        <main class="ms-2 p-2">
                            <field name="name" class="fw-bolder fs-5"/>
                            <field  t-if="record.type.raw_value == 'url'" name="url" widget="url"/>
                            <field
                                name="variant_attribute_value_ids"
                                widget="many2many_tags_color_dot"
                                groups="product.group_product_variant"
                                options="{'color_field': 'html_color'}"
                                context="{'display_attribute_name': False}"
                                invisible="res_model != 'product.product'"
                                readonly="1"
                            />
                        </main>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="product_document_list" model="ir.ui.view">
        <field name="name">product.document.list</field>
        <field name="model">product.document</field>
        <field name="arch" type="xml">
            <list editable="top" multi_edit="true">
                <field name="res_model" column_invisible="True"/>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field
                    name="variant_attribute_value_ids"
                    widget="many2many_tags_color_dot"
                    groups="product.group_product_variant"
                    options="{'color_field': 'html_color'}"
                    context="{'display_attribute_name': False}"
                    invisible="res_model != 'product.product'"
                    readonly="1"
                />
                <field name="company_id" groups="base.group_multi_company" optional="show"/>
            </list>
        </field>
    </record>

    <record id="product_document_search" model="ir.ui.view">
        <field name="name">product.document.search</field>
        <field name="model">product.document</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <filter name="archived" string="Archived" domain="[('active', '=', False)]"/>
                <filter name="all" string="All" domain="['|', ('active', '=', True), ('active', '=', False)]"/>
            </search>
        </field>
    </record>

</odoo>
