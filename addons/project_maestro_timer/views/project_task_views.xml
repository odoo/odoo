<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Extend task form: add timer widget in the Timesheets tab -->
    <record id="view_project_task_form_timer" model="ir.ui.view">
        <field name="name">project.task.form.timer</field>
        <field name="model">project.task</field>
        <field name="inherit_id" ref="project.view_task_form2"/>
        <field name="arch" type="xml">

            <!-- Timer widget inserted right after the task title (top of form) -->
            <xpath expr="//div[@name='button_box']" position="after">
                <field name="timer_start"
                       widget="task_timer"
                       nolabel="1"
                       class="o_task_timer_wrapper ms-2"
                       groups="hr_timesheet.group_hr_timesheet_user"/>
            </xpath>

        </field>
    </record>


</odoo>
