<?xml version="1.0" encoding="utf-8"?>
<odoo>
        <record id="view_partner_form" model="ir.ui.view">
            <field name="name">res.partner.vat.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='vat']" position="replace">
                </xpath>

                <!--TODO OCO les xpath ne se font visiblement pas par ordre de dépendance, je suis obligé de bouger le champ ici plutôt que dans autocomplete :/-->

                <xpath expr="//label[@for='street']" position="before">
                    <field name="base_vat_vies_status" invisible="1"/>
                    <field name="vat" string="VAT" style="color:Red;" attrs="{'invisible': [('base_vat_vies_status', '!=', 'failed')]}"/>
                    <field name="vat" string="VAT" style="color:Green;" attrs="{'invisible': [('base_vat_vies_status', '!=', 'passed')]}"/>
                    <field name="vat" string="VAT" style="color:Orange;" attrs="{'invisible': [('base_vat_vies_status', '!=', 'exception')]}"/>
                    <field name="vat" string="VAT" attrs="{'invisible': [('base_vat_vies_status', '!=', 'unchecked')]}"/>
                </xpath>

                <xpath expr="//header" position="inside">
                    <button id="base_vat_recheck_vies" string="Recheck VIES" type="object" name="compute_base_vat_vies_status" attrs="{'invisible': [('base_vat_vies_status', '!=', 'exception')]}" class="oe_read_only"/>
                </xpath>
            </field>
        </record>

        <record id="view_partner_short_form" model="ir.ui.view">
            <field name="name">res.partner.short.vat.inherit</field>
            <field name="model">res.partner</field>
            <field name="inherit_id" ref="base.view_partner_short_form"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='vat']" position="attributes">
                    <attribute name="string">VAT</attribute>
                </xpath>
                <!--TODO OCO voir où ce truc apparaît et si je dois y toucher-->
            </field>
        </record>
</odoo>
