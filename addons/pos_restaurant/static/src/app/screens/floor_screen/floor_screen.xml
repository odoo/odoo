<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="pos_restaurant.FloorScreen">
        <div class="floor-screen screen h-100 position-relative d-flex flex-column flex-nowrap m-0 bg-300 text-start overflow-hidden">
            <t t-set="editButtonClass" t-value="'btn btn-lg'" />
            <t t-set="hasSelectedTable" t-value="this.selectedTables.length > 0" />
            <t t-set="firstSelectedTable" t-value="this.selectedTables.length ? this.selectedTables[0] : null" />
            <div t-attf-class="d-flex flex-row {{this.pos.isEditMode ? 'flex-wrap' : ''}} justify-content-between px-2 gap-1 border-bottom bg-view">
                <button t-if="!this.pos.isEditMode" class="new-order btn btn-lg lh-lg my-2" t-attf-class="{{ this.pos.getOrder()?.isDirectSale ? 'btn-secondary' : 'btn-primary'}}"
                    t-on-click="() => this.clickNewOrder()">
                    <t t-if="this.pos.getOrder()?.isDirectSale">
                        <i class="fa-solid fa-angle-double-left" role="img" aria-label="Back" title="Back"/>
                        <span t-if="!this.ui.isSmall" class="ms-1">Back</span>
                    </t>
                    <t t-else="">
                        <i class="fa-solid fa-plus" role="img" aria-label="New Order" title="New Order"/>
                        <span t-if="!this.ui.isSmall" class="ms-1">New Order</span>
                    </t>
                </button>
                <!-- Center Side Div -->
                <div class="floor-selector d-flex gap-1 py-2 overflow-auto">
                    <t t-foreach="this.pos.models['restaurant.floor'].filter(f => f.active !== false)" t-as="floor" t-key="floor.id">
                        <button class="button button-floor btn btn-outline-secondary btn-lg px-3 lh-lg text-nowrap text-body" t-attf-class="{{ floor.id === this.state.selectedFloorId ? 'active' : '' }}" t-on-click="() => this.selectFloor(floor)">
                            <t t-out="floor.name" />
                            <t t-set="changeCount" t-value="this.getFloorChangeCount(floor)"/>
                            <span t-if="changeCount > 0" class="badge rounded-pill text-bg-danger ms-2 py-1 smaller fw-bolder" t-out="changeCount"/>
                        </button>
                    </t>
                    <button t-attf-class="{{editButtonClass}} btn-secondary lh-lg" t-if="this.pos.isEditMode or this.pos.config.floor_ids?.length === 0" t-on-click="this.addFloor" title="Add Floor" >
                        <i class="fa-solid fa-plus" role="img" aria-label="Add Floor" />
                    </button>
                </div>
                <!-- Right Side Div -->
                <div t-if="this.pos.isEditMode" class="edit-buttons d-flex flex-grow-1 justify-content-center justify-content-md-end gap-2 m-2 px-1 px-sm-0 overflow-x-auto">
                    <div class="d-flex gap-1 p-1 rounded-3 bg-200">
                        <t t-if="hasSelectedTable">
                            <span t-if="!this.ui.isSmall" class="mx-2 align-self-center text-uppercase smaller fw-bolder">Table <t t-out="firstSelectedTable.table_number" /></span>
                            <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="this.changeSeatsNum" t-att-disabled="!hasSelectedTable" title="Seats">
                                <i class="fa-solid fa-user" role="img" aria-label="Seats" />
                            </button>
                            <t t-if="this.selectedTables.some((t) => t.shape === 'square')">
                                <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="() => this.changeShape('round')" t-att-disabled="!hasSelectedTable" title="Round Shape">
                                    <i class="fa-regular fa-circle" role="img" aria-label="Make Round" />
                                </button>
                            </t>
                            <t t-else="">
                                <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="() => this.changeShape('square')" t-att-disabled="!hasSelectedTable" title="Square Shape">
                                    <i class="fa-regular fa-square" role="img" aria-label="Make Square" />
                                </button>
                            </t>
                        </t>
                        <Dropdown menuClass="'pos-dropdown-menu px-1'">
                            <button t-attf-class="{{editButtonClass}} btn-light" title="Change Floor Background">
                                <i class="fa-solid fa-paint-brush" role="img" aria-label="Change Floor Background"/>
                            </button>
                            <t t-set-slot="content">
                                <div class="d-grid gap-1 py-2 px-1" style="grid-template-columns: repeat(3, 1fr);">
                                    <t t-foreach="Object.entries(this.getColors())" t-as="color" t-key="color[0]">
                                        <t t-set="adaptColor" t-value="!hasSelectedTable ? this.getLighterShade(color[0]) : color[0]" />
                                        <t t-set="key" t-value="color[0]"/>
                                        <DropdownItem closingMode="'none'" onSelected="() => this.setColor(hasSelectedTable, adaptColor, key)">
                                            <button
                                                class="p-4 border-1 rounded"
                                                t-attf-style="background-color: {{adaptColor}}"
                                            />
                                        </DropdownItem>
                                    </t>
                                    <DropdownItem closingMode="'none'">
                                        <button class="floor-picture border-1 rounded position-relative text-center overflow-hidden d-flex flex-column align-items-center justify-content-center">
                                            <i class="fa-solid fa-camera" role="img" aria-label="Picture" title="Picture"></i>
                                            File
                                            <input type="file" class="image-uploader" t-on-change="this.uploadImage" />
                                        </button>
                                    </DropdownItem>
                                </div>
                            </t>
                        </Dropdown>
                        <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="() => this.rename(hasSelectedTable)" title="Rename">
                            <i class="fa-regular fa-pen-to-square" role="img" aria-label="Rename"/>
                        </button>
                        <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="() => this.duplicate(hasSelectedTable)" title="Clone">
                            <i class="fa-solid fa-copy" role="img" aria-label="Clone"/>
                        </button>
                        <button t-attf-class="{{editButtonClass}} btn-light" t-on-click.stop="() => this.delete(hasSelectedTable)" title="Delete">
                            <i class="fa-solid fa-trash-can" role="img" aria-label="Delete"/>
                        </button>
                    </div>
                    <button t-attf-class="btn btn-outline-secondary btn-lg d-flex align-items-center justify-content-center gap-2 lh-lg" t-on-click.stop="this.doCreateTable.call" t-att-disabled="this.doCreateTable.status === 'loading'">
                        <t t-if="this.doCreateTable.status === 'loading'">
                            <i class="fa-solid fa-circle-notch fa-spin icon-button" role="img" aria-label="Loading" title="Loading"></i>
                        </t>
                        <t t-else="">
                            <i class="fa-solid fa-plus-circle" role="img" aria-label="Add Table" title="Add Table"/>
                            <t t-if="!this.ui.isSmall">Table</t>
                        </t>
                    </button>
                    <button t-attf-class="btn btn-primary btn-lg" t-on-click.stop="this.closeEditMode">
                        <t t-if="!this.ui.isSmall">Save</t>
                        <i t-else="" class="fa-regular fa-floppy-disk" role="img" aria-label="Save" title="Save"/>
                    </button>
                </div>
                <div t-else="" class="right-buttons d-flex gap-1 p-2 pe-0">
                    <NumpadDropdown/>
                </div>
            </div>
            <t t-set="isKanban" t-value="this.pos.floorPlanStyle == 'kanban'"/>
            <div
                t-ref="floor-map-scroll"
                class="overflow-auto flex-grow-1 flex-shrink-1 flex-basis-0 w-auto"
                t-att-class="{
                    'floor-grid': this.pos.isEditMode,
                    'bg-view': !this.activeFloor?.background_color
                }"
                t-attf-style="background-color: rgba({{this._getColors()[this.activeFloor?.background_color]}},.75)">
                <div t-on-click="this.onClickFloorMap" t-on-touchstart="this._onPinchStart" t-on-touchmove="this._onPinchMove" t-on-touchend="this._onPinchEnd"
                    t-attf-class="floor-map position-relative w-100 h-100"
                    t-ref="floor-map-ref"
                    t-attf-style="
                        -webkit-touch-callout: none;
                        height: {{this.state.floorHeight}} !important;
                        width: {{this.state.floorWidth}} !important;
                        {{ this.activeFloor?.floor_background_image and !isKanban ?
                            'background-image: url(' + this.floorBackround + '); background-size: auto; background-repeat: no-repeat; background-attachment: local;' :
                            ''
                        }}">
                    <t t-if="this.pos.config.floor_ids?.length > 0">
                        <div t-if="!this.activeTables?.length" class="empty-floor d-flex align-items-center justify-content-center h-100 fs-3 text-center text-muted" t-ref="map">
                            <span>Oops! No tables available.<br/>Add a new table to get started.</span>
                        </div>
                        <div t-else="" t-ref="map" t-att-class="{'floor-kanban d-grid gap-1 p-2': isKanban, 'h-100': !isKanban, 'h-50 h-sm-auto overflow-y-auto': isKanban and this.pos.tableSelectorState}">
                           <t t-foreach="this.activeTables.sort((a,b)=>a.id-b.id)" t-as="table" t-key="table.id" >
                                <t t-set="isOccupied" t-value="this.pos.tableHasOrders(table)"/>
                                <t t-set="isIntersecting" t-value="this.state.potentialLink?.child?.id === table.id"/>
                                <t t-set="isIntersected" t-value="this.state.potentialLink?.parent?.id === table.id"/>
                                <div
                                    t-on-click="(ev) => this.onClickTable(table, ev)"
                                    class="floor-table table o_draggable d-flex flex-column align-items-center justify-content-between cursor-pointer"
                                    t-att-class="{
                                        'position-relative m-0': isKanban,
                                        'position-absolute': this.pos.floorPlanStyle !== 'kanban',
                                        'selected': this.state.selectedTableIds.includes(table.id),
                                        'occupied': isOccupied,
                                    }"
                                    t-attf-class="tableId-{{table.id}}"
                                    t-attf-style="
                                                border: 3px solid {{table.color || 'var(--tableOccupied-bg-color, #35d374)'}};
                                                border-radius: {{table.shape === 'round' ? 1000 : 6}}px;
                                                background: {{isOccupied ? table.color || 'var(--tableOccupied-bg-color, #35d374)' : 'var(--table-bg-color, #ffffff65)'}};
                                                color: {{isOccupied ? 'var(--tableOccupied-color, #000000)' : 'var(--table-color, #FFFFFF)'}};
                                                opacity: {{this.state.potentialLink ? (isIntersecting or isIntersected ? 1 : 0.25) : 1}};
                                                {{isKanban ?
                                                    `
                                                        width: 100%;
                                                        min-height: 120px;
                                                    ` :
                                                    `
                                                        width: ${table.width}px;
                                                        height: ${table.height}px;
                                                        top: ${table.getY() +  this.state.floorMapOffset.y}px;
                                                        left: ${table.getX() + this.state.floorMapOffset.x}px;
                                                    `
                                                }}
                                            "
                                    >
                                    <t t-set="offset" t-value="this.getTableHandleOffset(table)"/>
                                    <div
                                        class="info position-relative w-100 h-100 overflow-hidden"
                                        t-att-class="{'opacity-25': table.parent_id}"
                                        t-attf-style="border-radius: {{table.shape === 'round' ? 1000 : 3}}px;"
                                    >
                                        <div t-out="table.table_number" class="label fw-bolder fs-4 position-absolute top-50 start-50 translate-middle" />
                                    </div>
                                    <t t-set="data" t-value="this.getChangeCount(table)"/>
                                    <div
                                        t-if="data.changes > 0"
                                        t-out="this.env.utils.formatProductQty(data.changes, false)"
                                        t-att-class="{
                                            'text-bg-danger': data.changes
                                        }"
                                        class="order-count d-flex align-items-center justify-content-center position-absolute top-0 end-0 rounded-3 smaller fw-bolder z-2 badge"
                                    />
                                    <t t-if="this.state.selectedTableIds.includes(table.id)">
                                        <span t-attf-class="tableId-{{table.id}}" class="table-handle position-absolute top left" t-attf-style="top: {{offset}}px; left: {{offset}}px"/>
                                        <span t-attf-class="tableId-{{table.id}}" class="table-handle position-absolute top right" t-attf-style="top: {{offset}}px; right: {{offset}}px"/>
                                        <span t-attf-class="tableId-{{table.id}}" class="table-handle position-absolute bottom right" t-attf-style="bottom: {{offset}}px; right: {{offset}}px"/>
                                        <span t-attf-class="tableId-{{table.id}}" class="table-handle position-absolute bottom left" t-attf-style="bottom: {{offset}}px; left: {{offset}}px"/>
                                    </t>
                                </div>
                            </t>
                        </div>
                    </t>
                    <div t-else="" class="empty-floor d-flex align-items-center justify-content-center h-100 fs-3 text-center text-muted" t-ref="map">
                        <span>Oops! No floors available.<br/>Add a new floor to get started.</span>
                    </div>
                </div>
            </div>
        </div>
    </t>
</templates>
