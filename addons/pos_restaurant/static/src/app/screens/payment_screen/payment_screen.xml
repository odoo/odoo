<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="pos_restaurant.PaymentScreenDue" t-inherit="point_of_sale.PaymentScreenDue" t-inherit-mode="extension">
        <xpath expr="//section[hasclass('paymentlines-container')]" position="inside">
            <div t-if="this.currentOrder.getCustomerCount() > 1" t-attf-class="message text-center {{this.ui.isSmall ? 'fs-2' : 'fs-1'}}" >
                <PriceFormatter price="this.env.utils.formatCurrency(this.currentOrder.amountPerGuest())" /> / Guest
                <button class="btn rounded-lg p" t-on-click="this.clickTableGuests" style="background-color: #ced4da;">
                    <span class="text-black fs-4 fw-bolder" t-esc="this.currentOrder?.getCustomerCount() || 0"/>
                </button>
            </div>
        </xpath>
    </t>

</templates>
