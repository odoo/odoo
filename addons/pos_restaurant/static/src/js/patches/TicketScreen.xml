<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">

    <t t-name="pos_restaurant.TicketScreen" t-inherit="point_of_sale.TicketScreen" t-inherit-mode="extension" owl="1">
        <xpath expr="//div[hasclass('header-row')]//div[@name='delete']" position="before">
            <div t-if="env.model.ifaceFloorplan" class="col start" name="table">Table</div>
            <div t-if="isTippingFilter()" class="col end narrow" name="tip">Tip</div>
        </xpath>
        <xpath expr="//div[hasclass('order-row')]//div[@name='delete']" position="before">
            <div t-if="env.model.ifaceFloorplan" class="col start" name="table">
                <t t-esc="env.model.getTable(order)"></t>
            </div>
            <div t-if="isTippingFilter()" class="col end narrow" name="tip">
                <TipCell order="order" />
            </div>
        </xpath>
        <xpath expr="//div[hasclass('buttons')]" position="inside">
            <button class="settle-tips" t-if="isTippingFilter()" t-on-click="env.model.actionHandler({ name: 'actionSettleTips', args: [filteredOrderList] })">
                Settle
            </button>
        </xpath>
    </t>

</templates>
