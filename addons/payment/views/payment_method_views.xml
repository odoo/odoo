<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_method_form" model="ir.ui.view">
        <field name="name">payment.method.form</field>
        <field name="model">payment.method</field>
        <field name="arch" type="xml">
            <form string="Payment Method">
                <sheet>
                    <field name="image" widget="image" class="oe_avatar"/>
                    <div class="oe_title">
                        <h1><field name="name" placeholder="Name"/></h1>
                    </div>
                    <group>
                        <field name="parent_id"
                               attrs="{'invisible': [('id', '!=', False), ('parent_id', '=', False)]}"
                        />
                    </group>
                    <notebook>
                        <page string="Providers list" name="providers">
                            <field nolabel="1" name="provider_ids"/>
                        </page>
                        <page string="Child pms list (to rename)" name="child_pms">
                            <field nolabel="1" name="child_payment_method_ids"/>
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="payment_method_tree" model="ir.ui.view">
        <field name="name">payment.method.tree</field>
        <field name="model">payment.method</field>
        <field name="arch" type="xml">
            <tree>
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="child_payment_method_ids" widget="many2many_tags"/>
            </tree>
        </field>
    </record>

     <record id="payment_method_search" model="ir.ui.view">
        <field name="name">payment.method.search</field>
        <field name="model">payment.method</field>
        <field name="arch" type="xml">
            <search>
                <field name="name" string="Name"/>
                <filter string="Parent only (to rename)" name="filter_parent" domain="[('parent_id', '=', False)]"/>
            </search>
        </field>
    </record>

    <record id="action_payment_method" model="ir.actions.act_window">
        <field name="name">Payment Methods</field>
        <field name="res_model">payment.method</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{'search_default_filter_parent': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a payment method
            </p>
        </field>
    </record>

</odoo>
