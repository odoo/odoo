<?xml version="1.0" encoding="utf-8"?>
<odoo>

<template id="event_track_main" name="Event Exhibitor">
    <t t-call="website_event.layout">
        <div class="o_wevent_online o_wevent_online_bg o_wesession_index">
            <!-- Options -->
            <t t-set="option_widescreen" t-value="option_widescreen or False"/>
            <t t-set="option_tracks_wishlist" t-value="not event.is_done and request.website.viewref('website_event_track_session.session_topbar_wishlist').active"/>
            <!-- Drag/Drop Area -->
            <div id="oe_structure_wesession_track_index_1" class="oe_structure"/>
            <!-- Content -->
            <div t-att-class="'o_wevent_online_page_container %s' % ('container pb-3' if not option_widescreen else 'pb-3')">
                <div class="row mx-0 mb-5">
                    <t t-call="website_event_track_session.event_track_aside"/>
                    <t t-call="website_event_track_session.event_track_content"/>
                </div>
            </div>
            <!-- Drag/Drop Area -->
            <div id="oe_structure_wesession_track_index_2" class="oe_structure"/>
        </div>
    </t>
</template>

<!-- ============================================================ -->
<!-- CONTENT: MAIN TEMPLATES -->
<!-- ============================================================ -->

<template id="event_track_content" name="Track: Main Description">
    <div t-att-class="'col-12 o_wevent_online_page_main o_wesession_track_main bg-white p-0 %s' % ('col-md-9 col-lg-10' if option_widescreen else 'col-md-8 col-lg-9')">
        <!-- LIVE INFORMATIONS -->
        <div t-if="not track.event_id.is_ongoing" class="pt-3">
            <div class="mx-3 alert alert-warning">
                Event <span t-esc="track.event_id.name" class="font-weight-bold"/>
                <span t-if="track.event_id.start_today">
                    starts in
                    <span t-esc="track.event_id.start_remaining"
                        t-options="{'widget': 'duration', 'digital': False, 'unit': 'minute', 'round': 'minute'}"/>
                </span>
                <span class="my-0" t-else="track.event_id.start_today">
                    starts at
                    <span t-field="track.event_id.with_context(tz=track.event_id.date_tz).date_begin"
                        t-options="{'format': 'medium'}"/>
                </span>
            </div>
        </div>
        <div t-elif="track.track_start_remaining" class="pt-3">
            <div class="mx-3 alert alert-warning">
                Starting in
                <span t-esc="track.track_start_remaining"
                    t-options="{'widget': 'duration', 'digital': False, 'unit': 'second', 'round': 'minute'}"/>
            </div>
        </div>
        <!-- TRACK DESCRIPTION -->
        <div class="mx-3 pt-3 o_wesession_track_main_description">
            <div class="d-flex justify-content-between">
                <span class="h4" t-esc="track.name"/>
                <t t-if="option_tracks_wishlist" t-call="website_event_track_online.track_widget_reminder"/>
            </div>
            <div class="text-muted">
                <span t-field="track.date"
                    t-options='{"hide_seconds":"True", "format": "short"}'/>
                -
                <span t-field="track.date_end"
                    t-options='{"hide_seconds":"True", "format": "short"}'/>
                <t t-if="event.date_tz">(<span t-field="event.date_tz"/>)</t>
                ( <span t-field="track.duration"
                    t-options='{"widget": "duration", "unit": "hour", "round": "minute"}'/>
                )
            </div>
            <div class="text-muted" t-if="track.location_id">
                <strong>Location:</strong><span t-field="track.location_id"/>
            </div>
            <hr class="mt-2 pb-1 mb-1"/>

            <!-- ABOUT AUTHOR -->
            <div class="row mt-2">
                <div class="col-md-2">
                    <span t-if="track.image" t-field="track.image" t-options="{'widget': 'image', 'class': 'rounded-circle'}"/>
                    <span t-else="" t-field="track.partner_id.sudo().image_128" t-options='{"widget": "image", "class": "rounded-circle"}'/>
                </div>
                <div class="col-md-10 d-flex flex-column">
                    <span t-field="track.partner_name" class="font-weight-bold mb-2"/>
                    <span class="mb-2" t-if="track.partner_id.website">
                        <i class="fa fa-home mr-2"/><a t-att-href="track.partner_id.website"><span t-field="track.partner_id.website"/></a>
                    </span>
                    <span class="mb-2" t-if="track.partner_email">
                        <i class="fa fa-envelope mr-2"/><a t-att-mailto="track.partner_email"><span t-field="track.partner_email"/></a>
                    </span>
                    <div t-if="not is_html_empty(track.partner_biography)"
                        t-field="track.partner_biography" class="oe_no_empty"/>
                    <div t-else=""
                        t-field="track.partner_id.website_description" class="oe_no_empty"/>
                </div>
            </div>
            <hr class="mt-2 pb-1 mb-1"/>
            <div class="d-flew">
                <div t-field="track.description" class="oe_no_empty"/>
            </div>
        </div>
    </div>
</template>

<!-- ============================================================ -->
<!-- ASIDE: CONTROL PANEL -->
<!-- ============================================================ -->

<template id="event_track_aside" name="Track: Aside">
    <div t-att-class="'col-12 pl-0 o_wevent_online_page_aside o_wesession_track_aside %s' % ('col-md-3 col-lg-2' if option_widescreen else 'col-md-4 col-lg-3')">
        <div class="bg-white pt-3 o_wevent_online_page_aside_content">
            <div class="mx-2" t-if="track.website_cta">
                <t t-set="cta_coundown" t-value="bool(track.website_cta_start_remaining)"/>
                <div t-if="cta_coundown" class="text-center w-100 btn btn-primary d-none">
                    <t t-call="website_event_online.display_timer_widget">
                        <t t-set="pre_remaining_time" t-value="int(track.track_start_remaining)"/>
                        <!--<t t-set="pre_countdown_text" t-value="'Talk starts in'"/>-->
                        <t t-set="pre_countdown_display" t-value="bool(False)"/>
                        <t t-set="main_remaining_time" t-value="int(track.website_cta_start_remaining)"/>
                        <!--<t t-set="main_countdown_text" t-value="'Magic happens in'"/>-->
                        <t t-set="main_countdown_display" t-value="bool(False)"/>
                        <t t-set="display_class" t-value="'.o_event_cta_action'"/>
                    </t>
                </div>
                <div t-att-class="'o_event_cta_action %s' % ('d-none' if cta_coundown else '')">
                    <a t-att-href="track.website_cta_url"
                        class="btn btn-primary w-100 mb-3">
                        <span t-esc="track.website_cta_title"/>
                    </a>
                </div>
            </div>
            <div class="d-flex align-items-center justify-content-between mx-2">
                <ul class="nav nav-tabs o_wesession_track_aside_nav d-flex" role="tablist">
                    <li class="nav-item flex-grow-1">
                        <a href="#track_list" aria-controls="track_list" class="nav-link active" role="tab" data-toggle="tab">
                            Track
                        </a>
                    </li>
                </ul>
                <a href="#collapse_track_aside" data-toggle="collapse" class="d-lg-none p-2 text-decoration-none o_wevent_online_page_aside_collapse">
                    <i class="fa fa-chevron-down d-lg-none"/>
                </a>
            </div>
            <div id="collapse_track_aside"
                class="tab-content mx-2 collapse d-lg-block o_wesession_track_aside_tabs">
                <div class="tab-pane fade show active" id="track_list" role="tabpanel">
                    <ul class="list-unstyled">
                        <li t-foreach="tracks_other" t-as="track_other"
                            class="mt-2">
                            <div>
                                <a t-att-href="track_other.website_url"
                                    class="w-100"
                                    t-esc="track_other.name"/>
                                <div class="d-flex align-items-center">
                                    <small class="text-muted" t-esc="track_other.partner_id.name"/>
                                    <div class="d-inline-block ml-auto o_wesession_track_aside_info">
                                        <small t-if="track_other.is_track_live and not track_other.is_track_done"
                                            class="badge badge-danger">Live</small>
                                        <small t-elif="track_other.is_track_done"
                                            class="badge badge-light">Done</small>
                                        <small t-elif="track_other.track_start_remaining"
                                            class="badge badge-light">
                                            In <span t-esc="track_other.track_start_remaining"
                                                t-options="{'widget': 'duration', 'digital': False, 'unit': 'second', 'round': 'minute', 'format': 'narrow'}"/>
                                        </small>
                                    </div>
                                </div>
                            </div>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</template>

</odoo>
