<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
<data>

    <record id="survey_survey_view_form_inherit" model="ir.ui.view">
        <field name="name">survey.survey.view.form.inherit</field>
        <field name="model">survey.survey</field>
        <field name="inherit_id" ref="survey.survey_survey_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_survey_user_input_completed']" position="after">
                <button name="action_survey_user_input_leads"
                    groups="sales_team.group_sale_salesman"
                    type="object"
                    class="oe_stat_button"
                    invisible="not (survey_type in ['survey', 'live_session', 'custom'] and lead_count != 0)"
                    icon="fa-star">
                    <field string="Leads" name="lead_count" widget="statinfo"/>
                </button>
            </xpath>

            <xpath expr="//field[@widget='survey_description_page']" position="after">
                <button type="object" icon="fa-star" 
                    title="This question generates leads" invisible="not (survey_type in ['survey', 'live_session', 'custom'] and is_lead_generating)"/>
            </xpath>
        </field>
    </record>

    <record id="survey_survey_view_kanban_inherit" model="ir.ui.view">
        <field name="name">survey.survey.view.kanban.inherit</field>
        <field name="model">survey.survey</field>
        <field name="inherit_id" ref="survey.survey_survey_view_kanban"/>
        <field name="arch" type="xml">
            <xpath expr="//div[a[@name='action_survey_user_input_completed']]" position="after">
                <div t-if="!selection_mode and record.lead_count.raw_value > 0" class="col-lg-1 col-sm-4 col-6 py-0 my-2">
                    <a type="object"
                       name="action_survey_user_input_leads"
                       class="fw-bold">
                        <field name="lead_count"/><br/>
                        <span class="text-muted" t-if="record.lead_count.raw_value == 1">Lead</span>
                        <span class="text-muted" t-elif="record.lead_count.raw_value > 1">Leads</span>
                    </a>
                </div>
            </xpath>
        </field>
    </record>

</data>
</odoo>
