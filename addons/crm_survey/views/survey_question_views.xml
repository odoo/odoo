<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
<data>

    <record id="survey_question_form_inherit" model="ir.ui.view">
        <field name="name">survey.question.form.inherit</field>
        <field name="model">survey.question</field>
        <field name="inherit_id" ref="survey.survey_question_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@required='not value_image']" position="after">
                <field name="create_lead" string="Create Leads" column_invisible="parent.survey_type not in ['survey', 'live_session', 'custom']"/>
            </xpath>
        </field>
    </record>

</data>
</odoo>
