<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="payment_provider_form_razorpay" model="ir.ui.view">
        <field name="name">Razorpay Provider Form</field>
        <field name="model">payment.provider</field>
        <field name="inherit_id" ref="payment.payment_provider_form"/>
        <field name="arch" type="xml">
            <xpath expr="//sheet" position="before">
                <header>
                    <button
                        name="action_start_onboarding"
                        string="Connect"
                        type="object"
                        class="btn-primary"
                        invisible="not oauth_supported or razorpay_account_id"
                    />
                    <button
                        name="action_reset_credentials"
                        string="Disconnect"
                        confirm="Are you sure you want to disconnect?"
                        type="object"
                        class="btn-secondary"
                        invisible="not oauth_supported or not razorpay_account_id"
                        readonly="True"
                    /> 
                </header>
            </xpath>
            <group name="provider_credentials" position='inside'>
                <group
                    name="razorpay_credentials"
                    invisible="code != 'razorpay'"
                >
                    <field name="razorpay_account_id" 
                        string="Account ID"
                        invisible="not oauth_supported"
                        readonly="True"
                    />
                    <field
                        name="razorpay_key_id"
                        string="Key Id"
                        invisible="oauth_supported and (not razorpay_key_id or not razorpay_key_secret)"
                    />
                    <field
                        name="razorpay_key_secret"
                        string="Key Secret"
                        password="True"
                        invisible="oauth_supported and (not razorpay_key_id or not razorpay_key_secret)"
                    />
                    <label for="razorpay_webhook_secret" invisible="not oauth_supported"/>
                    <div class="o_row">
                        <field
                            name="razorpay_webhook_secret"
                            string="Webhook Secret"
                            password="True"
                            invisible="not oauth_supported"
                            readonly="True"
                        />
                        <button
                            string="Generate your webhook"
                            type="object"
                            name="action_razorpay_create_webhook"
                            class="btn-primary ms-2"
                            invisible="not oauth_supported or not razorpay_account_id or razorpay_webhook_secret"
                        />
                    </div>
                </group>
            </group>
            <group name="provider_credentials" position="after">
                <div
                    class="alert alert-warning"
                    role="alert"
                    invisible="code != 'razorpay' or not razorpay_key_id or razorpay_account_id or not oauth_supported"
                >
                    You are currently connected to Razorpay through the credentials method, which is
                    deprecated. Click the "Connect" button above to use the recommended OAuth
                    method.
                </div>
            </group>
            <field name="allow_tokenization" position="after">
                <div invisible="code != 'razorpay' or not allow_tokenization" colspan="2">
                    <widget
                        label="Enable recurring payments on Razorpay"
                        name="documentation_link"
                        path="/applications/finance/payment_providers/razorpay.html#payment-providers-razorpay-recurring-payments"
                        icon="oi oi-fw o_button_icon oi-arrow-right"
                        colspan="2"
                        class="mx-2"
                    />
                </div>
            </field>
        </field>
    </record>

</odoo>
