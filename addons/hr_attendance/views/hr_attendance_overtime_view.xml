<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_attendance_overtime_list" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.list</field>
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <list edit="0" create="0" js_class="extra_hours_list_view">
                <field name="date"/>
                <field name="employee_id"/>
                <field name="duration" widget="float_time"/>
            </list>
        </field>
    </record>

    <record id="view_attendance_overtime_line_search" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.line.search</field>
        <field name="model">hr.attendance.overtime.line</field>
        <field name="arch" type="xml">
            <search>
                <field name="employee_id"/>
                <group>
                    <filter string="Date" name="groupby_date" context="{'group_by': 'date:day'}"/>
                    <filter string="Employee" name="employee" context="{'group_by': 'employee_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <!-- TODO probably remove -->
    <record id="view_attendance_overtime_line_list" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.line.list</field>
        <field name="model">hr.attendance.overtime.line</field>
        <field name="arch" type="xml">
            <list editable="top" create="0">
                <field name="date" readonly="1"/>
                <field name="employee_id" readonly="1"/>
                <field name="manual_duration" widget="float_time" optional="hide"/>
                <field name="duration" widget="float_time" readonly="1" optional="show"/>
                <field name="status" widget="badge" readonly="1"/>
                <button
                    class="oe_stat_button"
                    string="Approve"
                    invisible="not is_manager or status not in ['to_approve', 'refused']"
                    name="action_approve"
                    icon="fa-check"
                    type="object"
                    groups="hr_attendance.group_hr_attendance_officer"/>
                <button
                    class="oe_stat_button"
                    string="Refuse"
                    invisible="not is_manager or status not in ['to_approve', 'approved']"
                    name="action_refuse"
                    icon="fa-times"
                    type="object"
                    groups="hr_attendance.group_hr_attendance_officer"/>
                <field name="rules_display" readonly="1"/>
            </list>
        </field>
    </record>

    <record id="view_attendance_overtime_search" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.search</field>
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <search>
                <field name="employee_id"/>
                <field name="duration" filter_domain="[('duration', '>=', self)]"/>
                <filter string="Last 3 Months" invisible="1" name="last_three_months" domain="[('date','&gt;=', '-3m')]"/>
                <group>
                    <filter string="Date" name="groupby_date" context="{'group_by': 'date:week'}"/>
                    <filter string="Employee" name="employee" context="{'group_by': 'employee_id'}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="view_attendance_overtime_graph" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.graph</field>
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <graph string="Overtime" type="bar" stacked="0" sample="1">
                <field name="employee_id" type="row"/>
                <field name="date" interval="week" type="col"/>
                <field name="duration" type="measure" widget="float_time"/>
            </graph>
        </field>
    </record>

    <record id="hr_attendance_overtime_view_pivot" model="ir.ui.view">
        <field name="name">hr.attendance.overtime.pivot</field>
        <field name="model">hr.attendance.overtime</field>
        <field name="arch" type="xml">
            <pivot string="Worked Hours">
                <field name="employee_id" type="row"/>
                <field name="date" type="col" interval="month"/>
                <field name="duration" type="measure" widget="float_time"/>
            </pivot>
        </field>
    </record>

    <record id="hr_attendance_overtime_action" model="ir.actions.act_window">
        <field name="name">Extra Hours</field>
        <field name="res_model">hr.attendance.overtime</field>
        <field name="view_mode">graph,pivot,list</field>
        <field name="context">
            {
                "search_default_groupby_date": 1,
                "search_default_employee": 1,
                "search_default_last_three_months": 1
            }
        </field>
        <field name="help" type="html">
            <p class="o_view_nocontent_empty_folder">
                No overtime records found
            </p><p>
                The overtime records of your employees will be displayed here.
            </p>
        </field>

    </record>

    <record id="hr_attendance_overtime_line_action" model="ir.actions.act_window">
        <field name="name">Overtime Details</field>
        <field name="res_model">hr.attendance.overtime.line</field>
        <field name="view_mode">list</field>
    </record>

    <menuitem id="menu_hr_attendance_view_overtime_line" name="Overtime Details" action="hr_attendance_overtime_line_action" parent="menu_hr_attendance_root" sequence="1000"/>
    
</odoo>
