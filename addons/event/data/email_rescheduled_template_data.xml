<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="event_rescheduled_mail" model="mail.template">
            <field name="name">Event: Rescheduled Event</field>
            <field name="model_id" ref="event.model_event_registration"/>
            <field name="email_from">${(object.event_id.organizer_id.email or object.event_id.user_id.email or '')|safe}</field>
            <field name="email_to">${object.partner_id.email or object.email|safe}</field>
            <field name="reply_to">${object.event_id.reply_to|safe}</field>
            <field name="subject">Rescheduled Event: ${object.event_id.name}</field>
            <field name="body_html"><![CDATA[<p>Dear ${object.name},</p>
<p>The event created by ${object.event_id.user_id.name} is now Scheduled on: ${object.event_id.date_begin} To ${object.event_id.date_end}
% if object.event_id.date_tz:
    (${object.event_id.date_tz})
% endif
.</p>
% if object.event_id.address_id:
    <p>Location: ${object.event_id.address_id.contact_address}.</p>
% endif
<p>Thank you for your participation!</p>
<p>Best regards</p>]]></field>
        </record>

    </data>
</odoo>
