import { registry } from "@web/core/registry";

registry.category("mail.log_document_icons").add("event.event", {
    icon: "calendar_today",
    filled: true,
});
registry.category("mail.log_document_icons").add("event.registration", {
    icon: "confirmation_number",
    filled: true,
});
