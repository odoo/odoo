<?xml version="1.0" encoding="utf-8"?>
<odoo>

  <template id="portal_my_home_menu_purchase" name="Portal layout : purchase menu entries" inherit_id="portal.portal_breadcrumbs" priority="25">
    <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
      <li t-if="page_name == 'rfq' or order and order.state == 'sent'" t-attf-class="breadcrumb-item #{'active ' if not order else ''}">
        <a t-if="order" t-attf-href="/my/rfq?{{ keep_query() }}">Requests for Quotation</a>
        <t t-else="">Requests for Quotation</t>
      </li>
      <li t-if="page_name == 'purchase' or order and order.state != 'sent'" t-attf-class="breadcrumb-item #{'active ' if not order else ''}">
        <a t-if="order" t-attf-href="/my/purchase?{{ keep_query() }}">Purchase Orders</a>
        <t t-else="">Purchase Orders</t>
      </li>
      <li t-if="page_name == 'bank_accounts'" class="breadcrumb-item active">
        Bank Accounts
      </li>
      <li t-if="order" class="breadcrumb-item active">
        <t t-esc="order.name"/>
      </li>
    </xpath>
  </template>

  <template id="portal_my_home_purchase" name="Show Requests for Quotation / Purchase Orders" customize_show="True" inherit_id="portal.portal_my_home" priority="25">
    <xpath expr="//div[hasclass('o_portal_docs')]" position="before">
          <t t-set="portal_vendor_category_enable" t-value="True"/>
      </xpath>
      <div id="portal_vendor_category" position="inside">
          <t t-call="portal.portal_docs_entry">
              <t t-set="icon" t-value="'/purchase/static/src/img/rfq.svg'"/>
              <t t-set="text">Follow your Requests for Quotation</t>
              <t t-set="title">Requests for Quotation</t>
              <t t-set="url" t-value="'/my/rfq'"/>
              <t t-set="placeholder_count" t-value="'rfq_count'"/>
          </t>
          <t t-call="portal.portal_docs_entry">
              <t t-set="icon" t-value="'/purchase/static/src/img/calculator.svg'"/>
              <t t-set="text">Follow orders you have to fulfill</t>
              <t t-set="title">Our Orders</t>
              <t t-set="url" t-value="'/my/purchase'"/>
              <t t-set="placeholder_count" t-value="'purchase_count'"/>
          </t>
      </div>
  </template>

  <template id="portal_my_purchase_rfqs" name="My Requests For Quotation">
      <t t-call="portal.portal_layout">
          <t t-set="breadcrumbs_searchbar" t-value="True"/>

          <t t-call="portal.portal_searchbar">
              <t t-set="title" >Requests For Quotation</t>
          </t>
          <t t-if="not rfqs">
              <p class="alert alert-warning">There are currently no requests for quotation for your account.</p>
          </t>
          <t t-if="rfqs" t-call="portal.portal_table">
              <thead>
                  <tr class="active">
                    <th>
                        <span class='d-none d-md-inline'>Request for Quotation #</span>
                        <span class='d-block d-md-none'>Ref.</span>
                    </th>
                      <th class="text-end">Order Deadline</th>
                      <th class="text-end">Total</th>
                  </tr>
              </thead>
              <t t-foreach="rfqs" t-as="rfq">
                  <tr>
                      <td><a t-att-href="rfq.get_portal_url()"><t t-esc="rfq.name"/></a></td>
                      <td class="text-end">
                          <span t-field="rfq.date_order" t-options="{'widget': 'date'}"/>&amp;nbsp;
                          <span class='d-none d-md-inline' t-field="rfq.date_order" t-options="{'time_only': True}"/>
                      </td>
                      <td class="text-end">
                          <span t-field="rfq.amount_total"/>
                      </td>
                  </tr>
              </t>
          </t>
      </t>
  </template>

  <template id="portal_my_purchase_orders" name="My Purchase Orders">
      <t t-call="portal.portal_layout">
          <t t-set="breadcrumbs_searchbar" t-value="True"/>

          <t t-call="portal.portal_searchbar">
              <t t-set="title">Purchase Orders</t>
          </t>
          <t t-if="not orders">
              <p class="alert alert-warning">There are currently no purchase orders for your account.</p>
          </t>
          <t t-if="orders" t-call="portal.portal_table">
              <thead>
                  <tr class="active">
                      <th>
                          <span class='d-none d-md-inline'>Purchase Order #</span>
                          <span class='d-block d-md-none'>Ref.</span>
                      </th>
                      <th class="text-end">
                          <span class='d-none d-md-inline'>Confirmation Date</span>
                          <span class='d-block d-md-none'>Confirmation</span>
                      </th>
                      <th class="text-center"/>
                      <th class="text-end">Total</th>
                  </tr>
              </thead>
              <t t-foreach="orders" t-as="order">
                  <tr>
                      <td><a t-att-href="order.get_portal_url()"><t t-esc="order.name"/></a></td>
                      <td class="text-end">
                          <span t-field="order.date_approve" t-options="{'widget': 'date'}"/>&amp;nbsp;
                          <span class='d-none d-md-inline' t-field="order.date_approve" t-options="{'time_only': True}"/>
                      </td>
                      <td class="text-center">
                          <span t-if="order.invoice_status == 'to invoice'" class="badge rounded-pill text-bg-info">
                              <i class="fa fa-fw fa-file-text" role="img" aria-label="Waiting for Bill" title="Waiting for Bill"></i><span class="d-none d-md-inline"> Waiting for Bill</span>
                          </span>
                          <span t-if="order.state == 'cancel'" class="badge rounded-pill text-bg-secondary">
                              <i class="fa fa-fw fa-remove" role="img" aria-label="Cancelled" title="Cancelled"></i><span class="d-none d-md-inline"> Cancelled</span>
                          </span>
                          <span t-if="order.state == 'done'" class="badge rounded-pill text-bg-success">
                              <i class="fa fa-fw fa-check" role="img" aria-label="Done" title="Done"></i><span class="d-none d-md-inline"> Done</span>
                          </span>
                      </td>
                      <td class="text-end"><span t-field="order.amount_total"/></td>
                  </tr>
              </t>
          </t>
      </t>
  </template>

  <template id="portal_my_purchase_order" name="Purchase Order" inherit_id="portal.portal_sidebar" primary="True">
      <xpath expr="//div[hasclass('o_portal_sidebar')]" position="inside">
          <t t-set="o_portal_fullwidth_alert" groups="purchase.group_purchase_manager">
              <t t-call="portal.portal_back_in_edit_mode">
                  <t t-set="backend_url" t-value="'/web#model=%s&amp;id=%s&amp;action=%s&amp;view_type=form' % (order._name, order.id, order.env.ref('purchase.purchase_rfq').id)"/>
              </t>
          </t>
          <t t-set="invoices" t-value="order.invoice_ids.filtered(lambda i: i.state != 'cancel')"/>

          <div class="row o_portal_purchase_sidebar">
              <!-- Sidebar -->
              <t t-call="portal.portal_record_sidebar">
                  <t t-set="classes" t-value="'col-lg-3 col-xl-4 d-print-none'"/>

                  <t t-set="title">
                      <h2 t-field="order.amount_total" data-id="total_amount"/>
                  </t>
                  <t t-set="entries">
                      <div class="d-flex flex-column gap-4">
                          <div class="flex-grow-1" t-if="order.company_id.allow_vendor_bill_upload">
                              <div class="btn-toolbar flex-sm-nowrap" t-if="order._portal_has_upload_bill_button(request.env.user.partner_id)">
                                  <div class="btn-group flex-grow-1 mb-2">
                                      <t t-set="modal_target" t-value="'#modal_verify_email'"/>
                                      <t t-if="verified_session">
                                          <t t-if="invoices" t-set="modal_target" t-value="'#modal_upload_bill_warning'"/>
                                          <t t-else="invoices" t-set="modal_target" t-value="'#modal_upload_bill'"/>
                                      </t>
                                      <button
                                         t-att-disabled="not is_logged_in_user_customer or invoices.filtered(lambda i: i.state == 'posted')"
                                         t-att-title="'You must be logged in as the customer to upload a bill' if not is_logged_in_user_customer else ''"
                                         t-attf-class="btn {{'btn-secondary' if invoices else 'btn-primary'}}"
                                         data-bs-toggle="modal"
                                         t-att-data-bs-target="modal_target"
                                         href="#">
                                          Upload Your Bill
                                      </button>
                                  </div>
                              </div>

                              <t t-set="used_bank_accounts" t-value="invoices.mapped('partner_bank_id')"/>
                              <a t-if="len(used_bank_accounts) == 1"
                                 t-att-href="my_bank_account_link"
                                 t-att-class="'o_disabled' if not is_logged_in_user_customer else ''"
                                 t-att-data-bs-toggle="'modal' if not verified_session else ''"
                                 t-att-data-bs-target="'#modal_verify_email' if not verified_session else ''"
                              >
                                  <div class="card">
                                      <div class="card-body d-flex flex-column flex-lg-row align-items-lg-center gap-3 border-start border-5 border-info">
                                          <i class="fa fa-lg fa-university"/>
                                          <div>
                                              <b>Your Bank Account</b>
                                              <div t-out="used_bank_accounts[0].bank_id.name" class="mt-2"/>
                                              <div t-out="used_bank_accounts[0].masked_acc_number"/>
                                          </div>
                                      </div>
                                  </div>
                              </a>
                              <a t-elif="is_logged_in_user_customer"
                                 role="button"
                                 t-attf-class="btn col-12 {{'btn-secondary' if invoices else 'btn-primary'}}"
                                 t-att-href="my_bank_account_link"
                                 t-att-data-bs-toggle="'modal' if not verified_session else ''"
                                 t-att-data-bs-target="'#modal_verify_email' if not verified_session else ''"
                              >
                                  Manage Bank Accounts
                              </a>
                              <button t-else=""
                                      disabled="disabled"
                                      title="You must be logged in as the customer to manage bank accounts'}}"
                                      t-attf-class="btn col-12 {{'btn-secondary' if invoices else 'btn-primary'}}"
                              >
                                  Manage Bank Accounts
                              </button>
                          </div>

                          <div class="flex-grow-1">
                              <div class="o_download_pdf btn-toolbar flex-sm-nowrap">
                                  <div class="btn-group flex-grow-1 mb-1">
                                      <a class="btn btn-secondary btn-block o_print_btn o_portal_invoice_print" t-att-href="order.get_portal_url(report_type='pdf')" id="print_invoice_report" title="View Details" target="_blank"><i class="fa fa-print"/> View Details</a>
                                  </div>
                              </div>
                          </div>

                          <div class="navspy flex-grow-1 ps-0" t-ignore="true" role="complementary">
                              <ul class="nav flex-column bs-sidenav"></ul>
                          </div>

                          <div t-if="order.user_id">
                              <h6><small class="text-muted">Purchase Representative</small></h6>
                              <div class="o_portal_contact_details d-flex flex-column gap-2">
                                <div class="d-flex justify-content-start align-items-center gap-2">
                                    <img class="o_avatar o_portal_contact_img rounded" t-att-src="image_data_uri(order.user_id.avatar_128)" alt="Contact"/>
                                    <div>
                                        <h6 class="mb-0" t-out="order.user_id.name"/>
                                        <a href="#discussion" class="small fw-bold">Send message</a>
                                    </div>
                                </div>
                                <div t-field="order.user_id" t-options='{"widget": "contact", "fields": ["phone"], "no_marker": False}'/>
                            </div>
                          </div>
                      </div>
                  </t>
              </t>

              <!-- Page content -->
              <div id="quote_content" class="o_portal_content col-12 col-lg-9 col-xl-8">
                  <t t-if="order.company_id.allow_vendor_bill_upload">
                      <t t-if="verified_session">
                          <t t-call="purchase.upload_bill_warning_modal"/>
                          <t t-call="purchase.upload_bill_modal"/>
                      </t>
                      <t t-else="" t-call="purchase.verify_email_modal"/>
                  </t>

                  <!-- status messages -->
                  <div t-if="order.state == 'cancel'" class="alert alert-danger alert-dismissable d-print-none" role="alert">
                      <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="close"></button>
                      <strong>This purchase has been cancelled.</strong>
                  </div>

                  <!-- main content -->
                  <div id="portal_purchase_content">
                      <div t-call="purchase.purchase_order_portal_content"/>
                  </div>

                  <!-- chatter -->
                  <hr/>
                  <div id="purchase_order_communication">
                      <h3>Communication history</h3>
                      <t t-call="portal.message_thread"/>
                  </div>
              </div><!-- // #quote_content -->
          </div>
      </xpath>
  </template>

  <template id="bill_trashcan">
      <i class="fa fa-trash"
         title="Delete invoice"
         data-bs-toggle="tooltip"
         data-bs-placement="top"
         data-bs-delay="0"
      />
  </template>

  <template id="purchase_order_portal_content" name="Purchase Order Portal Content">
      <!-- Intro -->
      <div id="introduction" class="mt-5 mt-lg-0 pb-2 pt-0">
        <h2 class="my-0">
          <t t-if="order.state in ['draft', 'sent']">Request for Quotation</t>
          <t t-else="1">
            Purchase Order
          </t>
          <em t-esc="order.name"/>
        </h2>
      </div>

      <div>
          <!-- Informations -->
          <div id="informations">
              <div class="row" id="po_date">
                  <div class="mb-3 col-6">
                    <t t-if="order.state in ['draft', 'sent']">
                      <strong>Request For Quotation Date:</strong>
                    </t>
                    <t t-if="order.state in ['purchase', 'done', 'cancel']">
                      <strong>Order Date:</strong>
                    </t>
                    <span t-field="order.date_order" t-options='{"widget": "date"}'/>
                  </div>
              </div>
              <div class="row">
                  <div class="col-lg-6">
                    <strong class="d-block mb-1">From:</strong>
                    <address t-field="order.company_id.partner_id" t-options='{"widget": "contact", "fields": ["address", "name", "vat"], "no_marker": True}'/>
                    <strong>Confirmation Date:</strong> <span t-field="order.date_approve" t-options='{"widget": "date"}'/><br/>
                    <div t-att-class="'d-inline' if order.date_planned else 'd-none'">
                      <strong>Receipt Date:</strong><span class="ms-1" t-field="order.date_planned" t-options='{"widget": "date"}'/>
                    </div>
                  </div>
                  <div class="col-lg-6">
                    <strong class="d-block mb-1">Your Address:</strong>
                    <address t-field="order.partner_id" t-options='{"widget": "contact", "fields": ["address", "name", "vat"], "no_marker": True}'/>
                    <a t-if="request.env.user.partner_id == order.partner_id" href="/my/account">Edit</a>
                  </div>
              </div>

              <div t-if="invoices" class="row">
                  <div class="col">
                      <strong class="d-block mb-1">Invoices</strong>
                      <ul class="list-group mb-4">
                          <t t-foreach="invoices" t-as="i">
                              <t t-set="report_url" t-value="i.get_portal_url(report_type='pdf', download=True)"/>
                              <div class="d-flex flex-wrap align-items-center justify-content-between">
                                  <div>
                                      <a t-att-href="report_url">
                                          <span t-esc="i.display_name"/>
                                      </a>
                                      <div t-if="i.invoice_date" class="small d-lg-inline-block">Date: <span class="text-muted" t-field="i.invoice_date"/></div>
                                  </div>
                                  <span t-if="i.payment_state in ('paid', 'in_payment')" class="badge text-bg-success small"><i class="fa fa-fw fa-check"/>Paid</span>
                                  <span t-elif="i.payment_state == 'reversed'" class="badge text-bg-success small"><i class="fa fa-fw fa-check"/>Reversed</span>
                                  <div t-elif="i._can_be_cancelled_by_vendor_partner(request.env.user.partner_id)">
                                      <form t-if="verified_session" t-attf-action="/my/bill/{{ i.id }}/cancel?{{ keep_query(purchase_order_id=order.id) }}" method="POST" class="d-inline">
                                          <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                          <button id="bill_deletion_submit_button" type="submit" class="btn btn-link">
                                              <t t-call="purchase.bill_trashcan"/>
                                          </button>
                                      </form>
                                      <button t-elif="is_logged_in_user_customer"
                                              id="bill_deletion_verification_button"
                                              class="btn btn-link"
                                              data-bs-toggle="modal"
                                              data-bs-target="#modal_verify_email">
                                          <t t-call="purchase.bill_trashcan"/>
                                      </button>
                                      <div class="badge text-bg-info small">Draft</div>
                                  </div>
                                  <span t-else="" class="small badge text-bg-primary"><i class="fa fa-fw fa-clock-o"/>Waiting Payment</span>
                              </div>
                          </t>
                      </ul>
                  </div>
              </div>
          </div>

          <section id="details" style="page-break-inside: auto;" class="mt32">
              <h3 id="details">Pricing</h3>

              <div class="table-responsive">
                <table t-att-data-order-id="order.id" t-att-data-token="order.access_token" class="table table-sm" id="purchase_order_table">
                    <thead class="bg-100">
                        <tr>
                            <th class="text-start">Products</th>
                            <th class="text-end">Quantity</th>
                            <th t-if="update_dates" class="text-end">Scheduled Date</th>
                            <th t-if="not update_dates and order.state in ['purchase', 'done']" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">Unit Price</th>
                            <th t-if="not update_dates and order.state in ['purchase', 'done']" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>Taxes</span>
                            </th>
                            <th class="text-end" t-if="order.state in ['purchase', 'done']" >
                                <span>Amount</span>
                            </th>
                        </tr>
                    </thead>
                    <tbody class="purchase_tbody">

                          <t t-set="current_subtotal" t-value="0"/>

                          <t t-foreach="order.order_line" t-as="line">

                              <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal"/>

                            <tr t-att-class="'bg-200 fw-bold o_line_section' if line.display_type == 'line_section' else 'fst-italic text-break' if line.display_type == 'line_note' else ''">
                                <t t-if="not line.display_type">
                                    <td id="product_name">
                                        <img t-att-src="image_data_uri(resize_to_48(line.product_id.image_1024))" alt="Product" class="d-none d-lg-inline"/>
                                        <span t-field="line.name"/>
                                    </td>
                                    <td class="text-end">
                                        <div id="quote_qty">
                                            <span t-field="line.product_qty"/>
                                            <span t-field="line.product_uom" groups="uom.group_uom"/>
                                        </div>
                                    </td>
                                    <td t-if="update_dates" class="text-end">
                                        <div class="container">
                                          <div class="mb-3">
                                            <div class="input-group date">
                                                <input type="text"
                                                    class="form-control datetimepicker-input o-purchase-datetimepicker text-end"
                                                    t-att-data-access-token="order.access_token"
                                                    t-att-data-order-id="order.id"
                                                    t-att-data-line-id="line.id"
                                                    t-att-data-value="line.date_planned.isoformat()"
                                                />
                                            </div>
                                          </div>
                                        </div>
                                    </td>
                                    <td t-if="not update_dates and order.state in ['purchase', 'done']" t-attf-class="text-end {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-field="line.price_unit"
                                            class="text-end"
                                        />
                                    </td>
                                    <td t-if="not update_dates and order.state in ['purchase', 'done']" t-attf-class="text-end {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.taxes_id))"/>
                                    </td>
                                    <td class="text-end" t-if="not update_dates and order.state in ['purchase', 'done']">
                                        <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                                <t t-if="line.display_type == 'line_section'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                    <t t-set="current_section" t-value="line"/>
                                    <t t-set="current_subtotal" t-value="0"/>
                                </t>
                                <t t-if="line.display_type == 'line_note'">
                                    <td colspan="99">
                                        <span t-field="line.name"/>
                                    </td>
                                </t>
                            </tr>

                            <t t-if="current_section and (line_last or order.order_line[line_index+1].display_type == 'line_section') and order.state in ['purchase', 'done']">
                                <tr class="is-subtotal text-end">
                                    <td colspan="99">
                                        <strong class="mr16">Subtotal</strong>
                                        <span
                                            t-esc="current_subtotal"
                                            t-options='{"widget": "monetary", "display_currency": order.currency_id}'
                                        />
                                    </td>
                                </tr>
                            </t>
                        </t>
                    </tbody>
                </table>
              </div>

              <div id="total" t-if="order.state in ['purchase', 'done']" class="row" name="total" style="page-break-inside: avoid;">
                  <div t-attf-class="#{'col-4' if report_type != 'html' else 'col-sm-7 col-md-5'} ms-auto">
                      <t t-call="purchase.purchase_order_portal_content_totals_table"/>
                  </div>
              </div>
          </section>

          <section id="terms" class="mt-5" t-if="order.notes">
              <h3 class="">Terms &amp; Conditions</h3>
              <hr class="mt-0 mb-1"/>
              <em t-field="order.notes"/>
          </section>

          <section class="mt-5" t-if="order.payment_term_id">
              <h3 class="">Payment terms</h3>
              <hr class="mt-0 mb-1"/>
              <span t-field="order.payment_term_id"/>
          </section>
      </div>
  </template>

  <template id="purchase_order_portal_content_totals_table">
      <table class="table table-sm">
          <t t-set="tax_totals" t-value="order.tax_totals"/>
          <t t-call="purchase.document_tax_totals"/>
      </table>
  </template>

  <template id="portal_my_purchase_order_update_date" name="Portal: My Purchase Order Update Dates" inherit_id="purchase.portal_my_purchase_order" primary="True">
    <xpath expr="////div[@id='portal_purchase_content']" position="replace">
      <div id="portal_purchase_content">
        <t t-set="update_dates" t-value="True"/>
        <div t-call="purchase.purchase_order_portal_content"/>
      </div>
    </xpath>
  </template>

    <template id="verify_email_modal">
        <div role="dialog" class="modal fade" id="modal_verify_email">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header pb-0">
                        <h4 class="modal-title">Verify your email address</h4>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        <t t-if="order.partner_id.email">
                            Verify your email address at <t t-out="order.partner_id.email"/>. Click "Verify" and keep
                            an eye on your inbox!
                        </t>
                        <t t-else="">
                            There is no email address specified for this vendor. Please send us a message at the bottom of
                            this page to configure a trusted email address.
                        </t>
                    </div>
                    <div class="modal-footer justify-content-start">
                        <form t-if="order.partner_id.email" t-attf-action="/my/purchase/{{ order.id }}/verify?{{ keep_query() }}" method="POST">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                            <button type="submit" class="btn btn-primary">Verify</button>
                        </form>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancel</button>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <template id="upload_bill_warning_modal">
        <div role="dialog" class="modal fade" id="modal_upload_bill_warning">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header pb-0">
                        <h4 class="modal-title">Bill for <t t-out="order.name"/></h4>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <div class="modal-body">
                        This action will erase your current bill.
                    </div>
                    <div class="modal-footer justify-content-start">
                        <button type="submit" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#modal_upload_bill">Continue</button>
                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                    </div>
                </div>
            </div>
        </div>
    </template>

    <template id="upload_bill_modal">
        <div role="dialog" class="modal fade" id="modal_upload_bill">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header pb-0">
                        <h4 class="modal-title">Upload your bill for <t t-out="order.name"/></h4>
                        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                    </div>
                    <form id="upload_bill" method="POST" enctype="multipart/form-data">
                        <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                        <div t-if="bank_accounts" class="modal-body">
                            <div>
                                Make sure your information is correct and address your bill to:
                            </div>
                            <address t-field="order.company_id.partner_id"
                                     t-options='{"widget": "contact", "fields": ["address", "name", "vat"], "no_marker": True}'
                                     class="small"/>
                            <input type="file" class="form-control mb-2" accept="application/pdf" id="bill_pdf"
                                   name="bill_pdf" required="required"></input>
                            <label for="bank_account">Payment will be done on:</label>
                            <select name="bank_account" id="bank_account" class="form-select" required="required">
                                <option t-foreach="bank_accounts" t-as="account" t-att-value="account.id"
                                        t-out="account.portal_display_name"/>
                            </select>
                        </div>
                        <div t-else="" class="modal-body">
                            Create a bank account to which payment will be done.
                        </div>
                        <div class="modal-footer justify-content-start">
                            <button type="submit" class="btn btn-primary" t-if="bank_accounts">Upload Your
                                Bill</button>
                            <a role="button"
                               t-attf-class="btn {{'btn-secondary' if bank_accounts else 'btn-primary'}}"
                               t-att-href="my_bank_account_link">
                                Create a Bank Account
                            </a>
                            <button type="button" class="btn btn-secondary d-none d-sm-block"
                                    data-bs-dismiss="modal">Close</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </template>

  <!-- /my/bank_accounts -->
    <template id="portal_bank_accounts" name="Bank Accounts">
        <t t-call="portal.portal_layout">
            <t t-set="page_title" t-value="'Bank Accounts'"/>
            <t t-set="additional_title"><t t-out="page_title"/></t>
            <div class="wrap ">
                <div class="container">
                    <div class="row justify-content-center">
                        <div class="col-lg-7">
                            <h4 t-if="bank_accounts" class="fs-6 small text-uppercase fw-bolder my-3">Your bank accounts</h4>
                            <ul class="list-group">
                                <li t-foreach="bank_accounts" t-as="account"
                                    class="list-group-item d-flex flex-column gap-2 py-3 px-2">
                                    <div class="d-flex gap-3 align-items-start align-items-md-center">
                                        <!-- query is kept so when deleting it will still redirect to the same purchase order when they save afterward -->
                                        <form t-attf-action="/my/bank_account/{{ account.id }}/archive?{{ keep_query() }}" method="POST">
                                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>
                                            <button type="submit" class="btn btn-link">
                                                <i class="fa fa-trash"
                                                   title="Delete bank account"
                                                   data-bs-toggle="tooltip"
                                                   data-bs-placement="top"
                                                   data-bs-delay="0"
                                                />
                                            </button>
                                        </form>
                                        <div class="col text-break" t-out="account.portal_display_name"/>
                                        <span class="col px-2">
                                            Saved on
                                            <time t-field="account.create_date" class="text-nowrap"
                                                  t-options="{'widget': 'datetime', 'date_only': 'true', 'format': 'medium'}"/>
                                        </span>
                                        <span class="col-3 d-none d-md-block">
                                            <p class="small text-600 mb-0">
                                                <span><i class="fa fa-lock"/> Secured by Odoo</span>
                                            </p>
                                        </span>
                                    </div>
                                </li>
                            </ul>

                        <h4 class="fs-6 small text-uppercase fw-bolder my-3">
                            Add a bank account
                        </h4>
                        <form id="bank_account_form" method="POST" enctype="multipart/form-data">
                            <input type="hidden" name="csrf_token" t-att-value="request.csrf_token()"/>

                            <!-- related to res.bank -->
                            <div class="row row-gap-2">
                                <div class="col-xl-12">
                                    <label for="bic">Bank SWIFT/BIC</label>*
                                    <input required="required" type="text" name="bic" id="bic"
                                           class="form-control js-select2-swift"></input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="name">Bank name</label>*
                                    <input required="required" type="text" name="name" id="name"
                                           maxlength="70"
                                           class="form-control"></input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="street">Street</label>*
                                    <input required="required" type="text" name="street" id="street" maxlength="50"
                                           class="form-control">
                                    </input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="street2">Suite, Building or Floor</label>
                                    <input type="text" name="street2" id="street2" maxlength="50" class="form-control">
                                    </input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="city">City</label>*
                                    <input required="required" type="text" name="city" id="city" maxlength="50"
                                           class="form-control"></input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="state">State</label>*
                                    <select name="state" id="state" class="form-select">
                                        <option t-foreach="states" t-as="state" t-att-value="state.id"
                                                t-att-country="state.country_id.id" t-out="state.name"/>
                                    </select>
                                </div>

                                <div class="col-xl-6">
                                    <label for="zip">Zip</label>*
                                    <input required="required" type="text" name="zip" id="zip" maxlength="10"
                                           class="form-control"></input>
                                </div>

                                <div class="col-xl-6">
                                    <label for="country">Country</label>*
                                    <select required="required" name="country" id="country" class="form-select">
                                        <option t-foreach="countries" t-as="country" t-att-value="country.id"
                                                t-out="country.name"/>
                                    </select>
                                </div>
                            </div>

                            <!-- related to res.partner.bank -->
                            <div class="row row-gap-2 pt-2">
                                <div class="col-xl-6"
                                     t-foreach="bank_account_fields"
                                     t-as="bank_account_field">
                                    <label t-att-for="bank_account_field.name"
                                           t-out="bank_account_field.label + ('*' if bank_account_field.required else '')"/>
                                    <t t-set="input_accept" t-value="'application/pdf' if bank_account_field.type == 'file' else None"/>
                                    <input t-att-type="bank_account_field.type"
                                           t-att-accept="input_accept"
                                           t-att-name="bank_account_field.name"
                                           t-att-id="bank_account_field.name"
                                           t-att-required="bank_account_field.required"
                                           t-att-data-country-id="bank_account_field.country_id"
                                           class="form-control o_country_specific_bank_field"/>
                                </div>
                            </div>

                            <div class="d-flex justify-content-end flex-column flex-md-row gap-2 my-2">
                                <button type="submit" class="btn btn-primary w-100 w-md-auto ms-auto px-5">Save</button>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </t>
  </template>

</odoo>
