<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_purchase_order_search" model="ir.ui.view">
        <field name="name">purchase.order.search</field>
        <field name="model">purchase.order</field>
        <field name="priority" eval="15" />
        <field name="arch" type="xml">
            <search string="Search Purchase Order">
                <field
                    name="name"
                    string="Order"
                    filter_domain="['|', '|', ('name', 'ilike', self), ('partner_ref', 'ilike', self), ('partner_id', 'child_of', self)]" />
                <field name="partner_id" operator="child_of" />
                <field name="user_id" />
                <field name="product_id" />
                <field name="tag_ids" />
                <field name="activity_user_id" string="Activities of" />
                <field name="activity_type_id" string="Activity type" />
                <filter
                    name="filter_activities_my"
                    string="My Activities"
                    domain="[('activity_user_id', '=', uid)]"
                    invisible="1" />
                <separator invisible="1" />
                <filter
                    name="my_purchases_filter"
                    string="My Orders"
                    domain="[('user_id', '=', uid)]" />
                <filter
                    name="activities_overdue"
                    string="Late Activities"
                    help="Show all records whose next activity date is past"
                    domain="[('my_activity_date_deadline', '&lt;', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_today"
                    string="Today Activities"
                    domain="[('my_activity_date_deadline', '=', 'today')]"
                    invisible="1" />
                <filter
                    name="activities_upcoming_all"
                    string="Future Activities"
                    domain="[('my_activity_date_deadline', '&gt;', 'today')]"
                    invisible="1" />
                <group>
                    <filter
                        name="vendor"
                        string="Vendor"
                        context="{'group_by': 'partner_id'}" />
                    <filter
                        name="representative"
                        string="Buyer"
                        context="{'group_by': 'user_id'}" />
                    <filter
                        name="tags"
                        string="Tags"
                        context="{'group_by': 'tag_ids'}" />
                    <filter
                        name="order_month"
                        string="Order Date"
                        context="{'group_by': 'date_order'}" />
                </group>
            </search>
        </field>
    </record>

    <record id="view_purchase_order_search_quotation" model="ir.ui.view">
        <field name="name">purchase.order.search.inherit.rfq</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_search" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <filter name="my_purchases_filter" position="replace">
                <field name="origin" />
                <separator />
                <filter
                    name="my_purchases"
                    string="My Purchases"
                    domain="[('user_id', '=', uid)]" />
                <filter
                    name="starred"
                    string="Starred"
                    domain="[('priority', '=', '1')]" />
                <separator />
                <filter
                    name="draft_rfqs"
                    string="Quotations"
                    domain="[('state', '=', 'draft')]" />
                <filter
                    name="waiting_rfqs"
                    string="RFQ Sent"
                    domain="[('sent', '=', True), ('state', '=', 'draft')]" />
                <filter
                    name="purchases"
                    string="Purchase Orders"
                    domain="[('state', '=', 'done')]" />
                <separator />
                <filter
                    name="late"
                    string="Late"
                    domain="[('state', '=', 'draft'), ('date_order', '&lt;', 'now')]" />
                <filter
                    name="not_acknowledged"
                    string="Not Acknowledged"
                    domain="[('state', '=', 'done'), ('acknowledged', '=', False)]" />
                <filter
                    name="late_receipt"
                    string="Late Receipts"
                    domain="[('state', '=', 'done'), ('is_late', '=', True)]" />
                <separator />
                <filter
                    name="activities_exception"
                    string="Warnings"
                    domain="[('activity_exception_decoration', '!=', False)]" />
                <separator />
                <filter
                    name="filter_create_date"
                    string="Create Date"
                    date="create_date" />
            </filter>
        </field>
    </record>

    <record id="view_purchase_order_search_2" model="ir.ui.view">
        <field name="name">purchase.order.search.inherit.po</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_search" />
        <field name="mode">primary</field>
        <field name="arch" type="xml">
            <filter name="my_purchases_filter" position="after">
                <filter
                    name="purchase_orders"
                    string="Purchase Orders"
                    domain="[('state', '=', 'done')]"
                    invisible="1" />
                <separator />
                <filter
                    name="not_invoiced"
                    string="Waiting Bills"
                    help="Purchase orders that include lines not invoiced."
                    domain="[('invoice_state', 'in', ['to do', 'partial'])]" />
                <filter
                    name="invoiced"
                    string="Bills Received"
                    help="Purchase orders that have been invoiced."
                    domain="[('invoice_state', '=', 'done')]" />
                <separator />
                <filter
                    name="activities_exception"
                    string="Warnings"
                    domain="[('activity_exception_decoration', '!=', False)]" />
                <separator />
                <filter name="order_date" string="Order Date" date="date_order" />
            </filter>
        </field>
    </record>

    <record id="view_purchase_order_calendar" model="ir.ui.view">
        <field name="name">purchase.order.calendar</field>
        <field name="model">purchase.order</field>
        <field name="priority" eval="2" />
        <field name="arch" type="xml">
            <calendar
                string="Purchase Orders"
                mode="month"
                create="0"
                color="partner_id"
                date_start="date_calendar_start"
                event_limit="5"
                hide_time="true"
                quick_create="0">
                <field name="currency_id" invisible="1" />
                <field name="state" invisible="1" filters="1" />
                <field name="partner_ref" />
                <field
                    name="partner_id"
                    options="{'icon': 'fa-solid fa-users'}"
                    avatar_field="avatar_128" />
                <field name="amount_total" widget="monetary" />
                <field name="payment_term_id" />
            </calendar>
        </field>
    </record>

    <record id="view_purchase_order_pivot" model="ir.ui.view">
        <field name="name">purchase.order.pivot</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <pivot string="Purchase Orders" sample="1" display_quantity="1">
                <field name="date_order" type="row" />
                <field name="amount_total" type="measure" />
            </pivot>
        </field>
    </record>

    <record id="view_purchase_order_graph" model="ir.ui.view">
        <field name="name">purchase.order.graph</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <graph type="bar" string="Purchase Orders" sample="1" stacked="1">
                <field name="date_order" />
                <field name="state" />
                <field name="amount_untaxed" type="measure" />
                <field name="amount_total" type="measure" />
            </graph>
        </field>
    </record>

    <record id="view_purchase_order_kanban" model="ir.ui.view">
        <field name="name">purchase.order.kanban</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <kanban
                sample="1"
                class="o_kanban_mobile"
                js_class="purchase_dashboard_kanban"
                quick_create="false">
                <field name="currency_id" />
                <progressbar
                    field="activity_state"
                    colors="{&quot;planned&quot;: &quot;success&quot;, &quot;today&quot;: &quot;warning&quot;, &quot;overdue&quot;: &quot;danger&quot;}" />
                <templates>
                    <t t-name="card">
                        <div class="d-flex align-items-baseline mb-2">
                            <field
                                name="priority"
                                widget="priority"
                                class="mt-1 me-1" />
                            <field name="partner_id" class="fw-bolder fs-5 me-2" />
                            <field
                                name="amount_total"
                                widget="monetary"
                                class="fw-bolder ms-auto flex-shrink-0" />
                        </div>
                        <footer class="align-items-end">
                            <div class="d-flex flex-wrap gap-1 text-muted text-nowrap">
                                <field name="name" />
                                <field
                                    name="date_order"
                                    options="{'show_time': false}" />
                                <field name="activity_ids" widget="kanban_activity" />
                            </div>
                            <field
                                name="state"
                                widget="label_selection"
                                options="{'classes': {'draft': 'info', 'cancel': 'default', 'done': 'success'}}"
                                class="ms-auto" />
                        </footer>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="view_purchase_order_kanban_without_dashboard" model="ir.ui.view">
        <field name="name">purchase.order.view.kanban.without.dashboard</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_kanban" />
        <field name="mode">primary</field>
        <field name="priority">20</field>
        <field name="arch" type="xml">
            <xpath expr="//kanban" position="attributes">
                <attribute name="js_class" />
            </xpath>
        </field>
    </record>

    <record id="view_purchase_order_list" model="ir.ui.view">
        <field name="name">purchase.order.list</field>
        <field name="model">purchase.order</field>
        <field name="priority" eval="1" />
        <field name="arch" type="xml">
            <list
                string="Purchase Orders"
                sample="1"
                decoration-muted="state=='cancel'">

                <field name="priority" widget="priority" nolabel="1" optional="show" />
                <field
                    name="name"
                    string="Reference"
                    readonly="1"
                    decoration-info="state == 'draft'" />
                <field
                    name="date_order"
                    optional="show"
                    column_invisible="not context.get('quotation_only', False)"
                    readonly="state in ['done', 'cancel']" />
                <field
                    name="date_confirmed"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
                <field name="partner_id" readonly="1" />
                <field name="origin" optional="hide" />
                <field name="partner_ref" optional="hide" />
                <field
                    name="activity_ids"
                    string="Next Activity"
                    widget="list_activity" />
                <field
                    name="activity_exception_decoration"
                    widget="activity_exception" />
                <field name="user_id" optional="show" />
                <field
                    name="amount_untaxed"
                    string="Untaxed"
                    widget="monetary"
                    optional="hide"
                    sum="Total Untaxed amount" />
                <field
                    name="amount_total"
                    widget="monetary"
                    optional="show"
                    sum="Total amount" />
                <field
                    name="currency_id"
                    column_invisible="True"
                    readonly="state in ['done', 'cancel']" />
                <field
                    name="state"
                    widget="badge"
                    optional="show"
                    decoration-info="state == 'draft'"
                    decoration-success="state == 'done'" />
                <field
                    name="date_commitment"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
                <field
                    name="invoice_state"
                    widget="badge"
                    optional="hide"
                    column_invisible="context.get('quotation_only', False)"
                    decoration-danger="invoice_state == 'over done'"
                    decoration-info="invoice_state == 'partial'"
                    decoration-success="invoice_state == 'done'"
                    decoration-warning="invoice_state == 'to do'" />
                <field
                    name="tag_ids"
                    widget="many2many_tags"
                    optional="hide"
                    options="{'color_field': 'color'}" />
                <field
                    name="company_id"
                    optional="show"
                    options="{'no_create': True}"
                    groups="base.group_multi_company"
                    readonly="1" />
            </list>
        </field>
    </record>

    <record id="view_purchase_order_list_kpis" model="ir.ui.view">
        <field name="name">purchase.order.inherit.purchase.order.list</field>
        <field name="model">purchase.order</field>
        <field name="priority" eval="10" />
        <field name="arch" type="xml">
            <list
                string="Purchase Orders"
                multi_edit="1"
                sample="1"
                class="o_purchase_order"
                decoration-info="state == 'draft'"
                decoration-muted="state == 'cancel'"
                js_class="purchase_dashboard_list">
                <header>
                    <button
                        name="action_cancel"
                        type="object"
                        string="Cancel"
                        confirm="Are you sure you want to cancel the selected RFQs/Orders?" />
                </header>
                <field
                    name="currency_id"
                    column_invisible="True"
                    readonly="state in ['done', 'cancel']" />

                <field name="priority" widget="priority" nolabel="1" optional="show" />
                <field name="name" string="Reference" readonly="1" decoration-bf="1" />
                <field
                    name="date_order"
                    widget="remaining_days"
                    optional="show"
                    invisible="state == 'done' or state == 'cancel'"
                    column_invisible="not context.get('quotation_only', False)"
                    readonly="state in ['done', 'cancel']" />
                <field
                    name="date_confirmed"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
                <field
                    name="date_commitment"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
                <field name="partner_id" readonly="1" />
                <field name="origin" optional="hide" />
                <field name="partner_ref" optional="hide" />
                <field name="user_id" widget="many2one_avatar_user" optional="show" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field
                    name="amount_untaxed"
                    string="Untaxed"
                    widget="monetary"
                    optional="hide"
                    sum="Total Untaxed amount" />
                <field
                    name="amount_total"
                    widget="monetary"
                    optional="show"
                    sum="Total amount"
                    decoration-bf="state == 'done'" />
                <field
                    name="state"
                    widget="badge"
                    optional="show"
                    decoration-info="state == 'draft'"
                    decoration-success="state == 'done'" />
                <field name="invoice_state" optional="hide" />
                <field
                    name="company_id"
                    optional="show"
                    options="{'no_create': True}"
                    groups="base.group_multi_company"
                    readonly="1" />
                <field
                    name="company_id"
                    groups="!base.group_multi_company"
                    column_invisible="True"
                    readonly="state in ['done', 'cancel']" />
            </list>
        </field>
    </record>

    <record id="view_purchase_order_list_2" model="ir.ui.view">
        <field name="name">purchase.order.view.list</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <list
                string="Purchase Order"
                multi_edit="1"
                sample="1"
                class="o_purchase_order"
                decoration-info="invoice_state == 'to do'"
                decoration-muted="state == 'cancel'">
                <header>
                    <button name="create_invoice" type="object" string="Create Bills" />
                    <button
                        name="action_cancel"
                        type="object"
                        string="Cancel"
                        confirm="Are you sure you want to cancel the selected RFQs/Orders?" />
                </header>
                <field name="priority" widget="priority" nolabel="1" optional="show" />
                <field name="partner_ref" optional="hide" />
                <field
                    name="name"
                    string="Reference"
                    readonly="1"
                    decoration-bf="1"
                    decoration-info="state == 'draft'" />
                <field
                    name="date_confirmed"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
                <field name="partner_id" readonly="state in ['done', 'cancel']" />
                <field
                    name="company_id"
                    optional="show"
                    options="{'no_create': True}"
                    groups="base.group_multi_company"
                    readonly="state in ['done', 'cancel']" />
                <field
                    name="company_id"
                    groups="!base.group_multi_company"
                    column_invisible="True"
                    readonly="state in ['done', 'cancel']" />
                <field name="user_id" widget="many2one_avatar_user" optional="show" />
                <field
                    name="date_order"
                    optional="show"
                    column_invisible="not context.get('quotation_only', False)"
                    readonly="state in ['done', 'cancel']" />
                <field name="activity_ids" widget="list_activity" optional="show" />
                <field name="origin" optional="show" />
                <field
                    name="amount_untaxed"
                    string="Untaxed"
                    widget="monetary"
                    optional="hide"
                    sum="Total Untaxed amount" />
                <field
                    name="amount_total"
                    widget="monetary"
                    optional="show"
                    sum="Total amount"
                    decoration-bf="1" />
                <field
                    name="currency_id"
                    column_invisible="True"
                    readonly="state in ['done', 'cancel']" />
                <field name="state" column_invisible="True" />
                <field
                    name="invoice_state"
                    widget="badge"
                    optional="show"
                    decoration-info="invoice_state == 'to do'"
                    decoration-success="invoice_state == 'done'" />
                <field
                    name="date_commitment"
                    optional="show"
                    column_invisible="context.get('quotation_only', False)" />
            </list>
        </field>
    </record>

    <record id="view_purchase_order_form" model="ir.ui.view">
        <field name="name">purchase.order.form</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <form
                string="Purchase Order"
                context="{'res_partner_search_mode': 'supplier'}"
                class="o_purchase_order">
                <header>
                    <field name="locked" invisible="1" />
                    <button
                        name="action_confirm"
                        id="action_confirm"
                        type="object"
                        string="Confirm"
                        context="{'validate_analytic': True}"
                        groups="purchase.group_purchase_user"
                        invisible="state != 'draft' or not sent"
                        class="btn-primary"
                        data-hotkey="q" />
                    <button
                        name="action_confirm"
                        type="object"
                        string="Confirm"
                        context="{'validate_analytic': True}"
                        groups="purchase.group_purchase_user"
                        invisible="state != 'draft' or sent"
                        data-hotkey="q" />
                    <button
                        name="action_send_rfq"
                        id="send_primary"
                        type="object"
                        string="Send"
                        groups="purchase.group_purchase_user"
                        invisible="state != 'draft' or sent"
                        class="btn-primary"
                        data-hotkey="g" />
                    <separator name="workflow" />
                    <button
                        name="action_send_rfq"
                        id="send"
                        type="object"
                        string="Send"
                        groups="purchase.group_purchase_user"
                        invisible="state == 'cancel' or (state == 'draft' and not sent)"
                        data-hotkey="g" />
                    <separator name="documents" />
                    <button
                        name="action_acknowledge"
                        type="object"
                        string="Acknowledge"
                        groups="purchase.group_purchase_user"
                        invisible="acknowledged or state != 'done'" />
                    <widget name="purchase_file_uploader" invisible="state != 'done'" />
                    <separator name="operations" />
                    <button
                        name="action_lock"
                        type="object"
                        string="Lock"
                        help="If the purchase is locked, you can not modify it anymore. However, you will still be able to bill or receive."
                        groups="purchase.group_order_lock"
                        invisible="locked or state != 'done'"
                        data-hotkey="l" />
                    <button
                        name="action_unlock"
                        type="object"
                        string="Unlock"
                        groups="purchase.group_order_unlock"
                        invisible="not locked"
                        data-hotkey="l" />
                    <button
                        name="action_cancel"
                        type="object"
                        string="Cancel"
                        confirm="Are you sure you want to cancel this order?"
                        groups="purchase.group_purchase_user"
                        invisible="state not in ['draft', 'done'] or not id or locked"
                        data-hotkey="x" />
                    <button
                        name="action_draft"
                        type="object"
                        string="Set to Draft"
                        groups="purchase.group_purchase_user"
                        invisible="state != 'cancel'"
                        data-hotkey="w" />
                    <field
                        name="state"
                        widget="statusbar"
                        readonly="1"
                        statusbar_visible="draft,done" />
                </header>
                <div
                    class="alert alert-warning"
                    role="alert"
                    invisible="not purchase_warning_text">
                    <field name="purchase_warning_text" class="fw-bold" />
                </div>
                <div
                    class="alert alert-warning p-3 text-center"
                    role="alert"
                    invisible="state != 'draft' or not has_archived_products">
                    <span>Warning: This order contains archived product(s)</span>
                </div>
                <div
                    class="alert alert-warning w-100 d-flex align-items-center gap-1"
                    invisible="state != 'draft' or not duplicated_order_ids"
                    role="alert">
                    <span>Warning: this order might be a duplicate of</span>
                    <field
                        name="duplicated_order_ids"
                        string="Duplicated Documents"
                        widget="x2many_buttons" />
                </div>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_view_invoice"
                            type="object"
                            icon="fa-regular fa-pen-to-square"
                            invisible="invoice_count == 0 or state == 'draft'"
                            class="oe_stat_button">
                            <field
                                name="invoice_count"
                                string="Vendor Bills"
                                widget="statinfo" />
                        </button>
                        <button
                            name="action_bill_matching"
                            type="object"
                            icon="fa-bullseye"
                            groups="account.group_account_invoice"
                            invisible="not partner_id or state != 'done' or not partner_bill_count or invoice_state == 'done'"
                            class="oe_stat_button">
                            <div class="o_field_statinfo">
                                <span class="o_stat_text">Bill Matching</span>
                            </div>
                        </button>
                        <button
                            name="action_price_comparison"
                            type="object"
                            icon="fa-dollar"
                            invisible="not show_comparison"
                            class="oe_stat_button">
                            <div class="o_field_statinfo">
                                <span class="o_stat_text">Price Comparison</span>
                            </div>
                            <field name="show_comparison" invisible="1" />
                        </button>
                    </div>
                    <div
                        class="badge rounded-pill text-bg-secondary float-end fs-6 border-0"
                        invisible="not locked">
                        <i class="fa-solid fa-lock" /> Locked </div>
                    <div class="oe_title">
                        <span class="o_form_label" invisible="state != 'draft'">Request for Quotation</span>
                        <span class="o_form_label" invisible="state == 'draft'">Purchase Order</span>
                        <h1 class="d-flex">
                            <field name="priority" widget="priority" class="me-3" />
                            <field name="name" readonly="1" />
                        </h1>
                    </div>
                    <group name="order_header">
                        <group name="partner_details">
                            <field
                                name="partner_id"
                                placeholder="Type to find a vendor..."
                                widget="res_partner_many2one"
                                context="{'res_partner_search_mode': 'supplier', 'show_address': 1, 'show_vat': True}"
                                readonly="state in ['done', 'cancel']" />
                            <field name="partner_ref" />
                            <field
                                name="company_id"
                                invisible="1"
                                readonly="state in ['done', 'cancel']" />
                        </group>
                        <group name="order_details">
                            <field
                                name="date_order"
                                invisible="state  == 'done'"
                                readonly="state in ['done', 'cancel']" />
                            <label for="date_confirmed" invisible="state != 'done'" />
                            <div
                                name="date_confirmed"
                                invisible="state != 'done'"
                                class="o_row">
                                <field name="date_confirmed" />
                            </div>
                            <label for="date_commitment" />
                            <div name="date_commitment_div" class="o_row">
                                <field
                                    name="date_commitment"
                                    readonly="state not in ['draft', 'done']" />
                            </div>
                            <field
                                name="currency_id"
                                force_save="1"
                                groups="base.group_multi_currency"
                                readonly="state in ['done', 'cancel']" />
                            <field
                                name="currency_id"
                                groups="!base.group_multi_currency"
                                invisible="1"
                                readonly="state in ['done', 'cancel']" />
                            <field
                                name="payment_term_id"
                                placeholder="Immediate"
                                options="{'no_open': True, 'no_create': True}"
                                readonly="invoice_state == 'done' or locked" />
                            <label
                                for="receipt_reminder_email"
                                groups="purchase.group_send_reminder"
                                class="d-none" />
                            <div
                                name="reminder"
                                class="o_row"
                                title="Automatically send a confirmation email to the vendor X days before the expected receipt date, asking him to confirm the exact date."
                                groups="purchase.group_send_reminder">
                                <field name="receipt_reminder_email" />
                                <span>Ask confirmation</span>
                                <div
                                    class="o_row oe_inline"
                                    invisible="not receipt_reminder_email">
                                    <field name="reminder_date_before_receipt" class="oe_inline" style="max-width: 30px;" /> day(s) before <widget name="toaster_button" title="Preview the reminder email by sending it to yourself." invisible="not id" button_name="send_reminder_preview" />
                                </div>
                            </div>
                        </group>
                    </group>
                    <notebook>
                        <page name="order_lines" string="Order Lines">
                            <field
                                name="line_ids"
                                widget="product_label_section_and_note_field_o2m"
                                mode="list,kanban"
                                context="{'default_state': 'draft'}"
                                options="{'subsections': True}"
                                readonly="state == 'cancel' or locked">
                                <list
                                    string="Purchase Order Lines"
                                    limit="200"
                                    editable="bottom"
                                    decoration-warning="(state == 'draft' and product_is_archived) or purchase_line_warn_msg">
                                    <control>
                                        <create
                                            name="add_product_control"
                                            string="Add a product" />
                                        <create
                                            name="add_section_control"
                                            string="Add a section"
                                            context="{'default_display_type': 'line_section'}" />
                                        <create
                                            name="add_note_control"
                                            string="Add a note"
                                            context="{'default_display_type': 'line_note'}" />
                                        <button
                                            name="action_add_from_catalog"
                                            type="object"
                                            string="Catalog"
                                            context="{'order_id': parent.id}"
                                            groups="purchase.group_purchase_user"
                                            class="px-4 btn-link" />
                                    </control>

                                    <field
                                        name="display_type"
                                        column_invisible="True" />
                                    <field name="currency_id" column_invisible="True" />
                                    <field
                                        name="price_unit_auto"
                                        column_invisible="True" />
                                    <field
                                        name="qty_transferred_method"
                                        column_invisible="True" />

                                    <field name="sequence" widget="handle" />
                                    <field
                                        name="product_id"
                                        widget="product_label_section_and_note_field"
                                        optional="show"
                                        force_save="1"
                                        domain="[('purchase_ok', '=', True), '|', ('company_id', '=', False), ('company_id', 'parent_of', parent.company_id)]"
                                        context="{                                             'company_id': parent.company_id,                                             'partner_id': partner_id,                                             'quantity': product_qty,                                         }"
                                        options="{'show_label_warning': True}"
                                        readonly="state in ['done', 'cancel'] or is_downpayment"
                                        required="not display_type and not is_downpayment" />
                                    <field
                                        name="name"
                                        widget="section_and_note_text"
                                        optional="show" />
                                    <field
                                        name="analytic_distribution"
                                        widget="analytic_distribution"
                                        optional="hide"
                                        options="{                                             'product_field': 'product_id',                                             'business_domain': 'purchase_order',                                             'amount_field': 'price_subtotal',                                         }"
                                        groups="analytic.group_analytic_accounting" />
                                    <field
                                        name="product_qty"
                                        readonly="is_downpayment" />
                                    <field
                                        name="qty_transferred"
                                        string="Received"
                                        optional="show"
                                        column_invisible="parent.state != 'done'"
                                        readonly="qty_transferred_method != 'manual' or is_downpayment"
                                        decoration-danger="not display_type and qty_transferred &gt; product_qty"
                                        decoration-info="not display_type and qty_transferred == 0"
                                        decoration-success="not display_type and qty_transferred == product_qty"
                                        decoration-warning="not display_type and qty_transferred &gt; 0 and qty_transferred &lt; product_qty" />
                                    <field
                                        name="qty_invoiced"
                                        string="Billed"
                                        optional="show"
                                        column_invisible="parent.state != 'done'"
                                        decoration-danger="not display_type and invoice_state == 'over done'"
                                        decoration-success="not display_type and invoice_state == 'done'"
                                        decoration-warning="not display_type and invoice_state == 'to do'" />
                                    <field
                                        name="product_uom_id"
                                        widget="many2one_uom"
                                        optional="show"
                                        force_save="1"
                                        options="{'no_create': True, 'no_open': True, 'quantity_field': 'product_qty'}"
                                        groups="uom.group_uom"
                                        readonly="state in ['done', 'cancel'] or is_downpayment"
                                        required="not display_type and not is_downpayment" />
                                    <field
                                        name="price_unit"
                                        readonly="qty_invoiced != 0" />
                                    <widget
                                        name="price_history"
                                        options="{'action': 'purchase.action_purchase_line_price_history'}" />
                                    <field
                                        name="tax_ids"
                                        widget="many2many_tax_tags"
                                        optional="show"
                                        domain="[('type_tax_use', '=', 'purchase'), ('company_ids', 'parent_of', parent.company_id), ('country_id', '=', parent.tax_country_id), ('active', '=', True)]"
                                        context="{'default_type_tax_use': 'purchase', 'search_view_ref': 'account.account_tax_view_search'}"
                                        options="{'no_create': True}"
                                        readonly="is_downpayment" />
                                    <field
                                        name="discount"
                                        string="Disc.%"
                                        width="50px"
                                        optional="hide"
                                        readonly="qty_invoiced != 0" />
                                    <field name="price_subtotal" string="Amount" />
                                    <field
                                        name="date_commitment"
                                        optional="hide"
                                        force_save="1"
                                        readonly="is_downpayment"
                                        required="not display_type and not is_downpayment" />
                                </list>
                                <form string="Purchase Order Line">
                                    <field name="sequence" invisible="1" />
                                    <field name="display_type" invisible="1" />
                                    <group>
                                        <group invisible="display_type">
                                            <label for="product_id" />
                                            <div class="d-flex align-items-baseline">
                                                <span
                                                    class="fa-solid fa-exclamation-triangle text-warning me-1"
                                                    title="This product is archived"
                                                    invisible="state != 'draft' or not product_is_archived" />
                                                <field
                                                    name="product_id"
                                                    widget="many2one_barcode"
                                                    force_save="1"
                                                    domain="[('purchase_ok', '=', True), '|', ('company_id', '=', False), ('company_id', 'parent_of', parent.company_id)]"
                                                    context="{'partner_id': parent.partner_id}"
                                                    readonly="state in ['done', 'cancel'] or is_downpayment"
                                                    required="not display_type and not is_downpayment" />
                                            </div>
                                            <label for="product_qty" />
                                            <div class="o_row" name="ordered_qty">
                                                <field name="product_qty" />
                                                <field
                                                    name="product_uom_id"
                                                    widget="many2one_uom"
                                                    groups="uom.group_uom"
                                                    readonly="state in ['done', 'cancel'] or is_downpayment"
                                                    required="not display_type and not is_downpayment" />
                                            </div>
                                            <label
                                                for="qty_transferred"
                                                string="Received"
                                                invisible="parent.state != 'done'" />
                                            <div
                                                name="transferred_qty"
                                                invisible="parent.state != 'done'">
                                                <field
                                                    name="qty_transferred"
                                                    readonly="qty_transferred_method != 'manual'" />
                                            </div>
                                            <label
                                                for="qty_invoiced"
                                                string="Billed"
                                                invisible="parent.state != 'done'" />
                                            <div
                                                name="invoiced_qty"
                                                invisible="parent.state != 'done'">
                                                <field name="qty_invoiced" />
                                            </div>
                                            <field
                                                name="price_unit"
                                                readonly="qty_invoiced != 0" />
                                            <field
                                                name="tax_ids"
                                                widget="many2many_tax_tags"
                                                domain="[('type_tax_use', '=', 'purchase'), ('company_ids', 'parent_of', parent.company_id), ('country_id', '=', parent.tax_country_id), ('active', '=', True)]"
                                                context="{'default_type_tax_use': 'purchase', 'search_view_ref': 'account.account_tax_view_search'}"
                                                options="{'no_create': True}"
                                                readonly="is_downpayment" />
                                            <label for="discount" />
                                            <div name="discount">
                                                <field name="discount" readonly="qty_invoiced != 0" class="oe_inline" /> % </div>
                                        </group>
                                        <group invisible="display_type">
                                            <field
                                                name="date_commitment"
                                                readonly="is_downpayment"
                                                required="not display_type and not is_downpayment" />
                                            <field
                                                name="analytic_distribution"
                                                widget="analytic_distribution"
                                                options="{'product_field': 'product_id', 'business_domain': 'purchase_order'}"
                                                groups="analytic.group_analytic_accounting" />
                                        </group>
                                    </group>
                                    <label
                                        for="name"
                                        string="Description"
                                        invisible="display_type" />
                                    <label
                                        for="name"
                                        string="Section Name (eg. Products, Services)"
                                        invisible="display_type != 'line_section'" />
                                    <label
                                        for="name"
                                        string="Subsection Name"
                                        invisible="display_type != 'line_subsection'" />
                                    <label
                                        for="name"
                                        string="Note"
                                        invisible="display_type != 'line_note'" />
                                    <field name="name" />
                                    <div name="invoice_lines" invisible="display_type">
                                        <label for="invoice_line_ids" />
                                        <field name="invoice_line_ids" />
                                    </div>
                                </form>
                                <kanban class="o_kanban_mobile">
                                    <field name="display_type" />
                                    <field name="currency_id" />
                                    <control>
                                        <create
                                            name="add_product_control"
                                            string="Add product" />
                                        <create
                                            name="add_section_control"
                                            string="Add section"
                                            context="{'default_display_type': 'line_section'}" />
                                        <create
                                            name="add_note_control"
                                            string="Add note"
                                            context="{'default_display_type': 'line_note'}" />
                                        <button
                                            name="action_add_from_catalog"
                                            type="object"
                                            string="Catalog"
                                            context="{'order_id': parent.id}"
                                            groups="purchase.group_purchase_user"
                                            class="btn-secondary" />
                                    </control>
                                    <templates>
                                        <t t-name="card" class="row g-0 ps-0 pe-0">
                                            <t t-if="!record.display_type.raw_value">
                                                <aside class="col-2 p-1">
                                                    <span
                                                        t-att-title="record.product_id.value">
                                                        <field
                                                            name="product_id"
                                                            widget="image"
                                                            options="{'preview_image': 'image_128', 'img_class': 'object-fit-contain w-100'}" />
                                                    </span>
                                                </aside>
                                                <main class="col">
                                                    <div class="row">
                                                        <div class="col">
                                                            <span
                                                                class="fa-solid fa-exclamation-triangle text-warning me-1"
                                                                title="This product is archived"
                                                                invisible="state != 'draft' or not product_is_archived" />
                                                            <field
                                                                name="product_id"
                                                                class="fw-bold" />
                                                        </div>
                                                        <div class="col-auto">
                                                            <field
                                                                name="price_subtotal"
                                                                widget="monetary"
                                                                class="fw-bolder float-end pe-1" />
                                                        </div>
                                                    </div>
                                                    <div class="text-muted"> Quantity: <field name="product_qty" /> <field name="product_uom_id" />
                                                    </div>
                                                    <div class="text-muted"> Unit Price: <field name="price_unit" />
                                                    </div>
                                                    <t
                                                        t-if="record.discount?.raw_value">
                                                        <div class="text-muted"> Discount: <field name="discount" />% </div>
                                                    </t>
                                                </main>
                                            </t>
                                            <t
                                                t-if="['line_section', 'line_subsection', 'line_note'].includes(record.display_type.raw_value)">
                                                <div
                                                    t-att-class="{                                                         'fw-bolder': record.display_type.raw_value === 'line_section',                                                         'fw-bold': record.display_type.raw_value === 'line_subsection',                                                         'fst-italic': record.display_type.raw_value === 'line_note',                                                     }">
                                                    <field name="name" />
                                                </div>
                                            </t>
                                        </t>
                                    </templates>
                                </kanban>
                            </field>
                            <group name="note_group" col="6" class="mt-2 mt-md-0">
                                <group colspan="4" class="order-1 order-lg-0">
                                    <field
                                        name="notes"
                                        placeholder="Terms and conditions..."
                                        colspan="2"
                                        nolabel="1" />
                                </group>
                                <group
                                    name="order_total"
                                    colspan="2"
                                    class="oe_subtotal_footer d-flex order-0 order-lg-1 flex-column gap-0 gap-sm-3">
                                    <field
                                        name="tax_totals"
                                        widget="account-tax-totals-field"
                                        nolabel="1"
                                        readonly="1" />
                                </group>
                            </group>
                        </page>
                        <page name="other_information" string="Other Info">
                            <group>
                                <group name="responsible_info" string="Purchasing">
                                    <field
                                        name="user_id"
                                        widget="many2one_avatar_user" />
                                    <field
                                        name="company_id"
                                        options="{'no_create': True}"
                                        groups="base.group_multi_company"
                                        readonly="state in ['done', 'cancel']" />
                                    <field
                                        name="tag_ids"
                                        widget="many2many_tags"
                                        options="{'color_field': 'color'}" />
                                </group>
                                <group name="invoice_info" string="Invoicing">
                                    <field
                                        name="fiscal_position_id"
                                        options="{'no_create': True}"
                                        readonly="invoice_state == 'done' or locked" />
                                    <field
                                        name="journal_id"
                                        readonly="invoice_count != 0 and state == 'done'" />
                                    <field
                                        name="invoice_state"
                                        invisible="state != 'done'" />
                                </group>
                                <group name="tracking_info" string="Tracking">
                                    <field name="origin" />
                                </group>
                            </group>
                        </page>
                    </notebook>
                </sheet>
                <chatter />
            </form>
        </field>
    </record>

    <record id="view_purchase_order_activity" model="ir.ui.view">
        <field name="name">purchase.order.activity</field>
        <field name="model">purchase.order</field>
        <field name="arch" type="xml">
            <activity string="Purchase Orders">
                <field name="currency_id" />
                <templates>
                    <div t-name="activity-box" class="d-block">
                        <div class="d-flex justify-content-between">
                            <field
                                name="name"
                                display="full"
                                class="o_text_block o_text_bold" />
                            <div class="m-1" />
                            <field name="amount_total" widget="monetary" />
                        </div>
                        <div class="d-flex justify-content-between">
                            <field
                                name="partner_id"
                                display="full"
                                class="o_text_block"
                                muted="1" />
                            <div class="m-1" />
                            <field
                                name="state"
                                widget="badge"
                                decoration-info="state == 'draft'"
                                decoration-success="state == 'done'" />
                        </div>
                    </div>
                </templates>
            </activity>
        </field>
    </record>

    <record id="action_purchase_order" model="ir.actions.act_window">
        <field name="name">Requests for Quotation</field>
        <field name="res_model">purchase.order</field>
        <field name="path">purchases</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
        <field name="view_id" ref="view_purchase_order_list_kpis" />
        <field name="search_view_id" ref="view_purchase_order_search_quotation" />
        <field name="domain">[]</field>
        <field name="context">{'quotation_only': True}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No request for quotation found. Let's create one!
            </p>
            <p>
                Requests for quotation are documents that will be sent to your suppliers to request
                prices for different products you consider buying.
                Once an agreement has been found with the supplier, they will be confirmed and
                turned into purchase orders.
            </p>
        </field>
    </record>

    <record id="action_purchase_order_2" model="ir.actions.act_window">
        <field name="name">Purchase Orders</field>
        <field name="res_model">purchase.order</field>
        <field name="path">purchase-orders</field>
        <field name="view_mode">list,kanban,form,calendar,pivot,graph,activity</field>
        <field name="search_view_id" ref="view_purchase_order_search_2" />
        <field name="domain">[('state', '=', 'done')]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                No purchase order found. Let's create one!
            </p>
            <p>
                Once you ordered your products to your supplier, confirm your request for quotation
                and it will turn into a purchase order.
            </p>
        </field>
        <field
            name="view_ids"
            eval="[Command.clear(), Command.create({'view_mode': 'list', 'view_id': ref('purchase.view_purchase_order_list_2')}), Command.create({'view_mode': 'kanban', 'view_id': ref('purchase.view_purchase_order_kanban_without_dashboard')})]" />
    </record>

    <record id="action_purchase_order_3" model="ir.actions.act_window">
        <field name="name">Requests for Quotation</field>
        <field name="res_model">purchase.order</field>
        <field name="view_mode">form</field>
        <field name="view_id" ref="purchase.view_purchase_order_form" />
        <field name="search_view_id" ref="view_purchase_order_search_quotation" />
        <field name="target">main</field>
    </record>

    <record id="action_accrued_expense_entry" model="ir.actions.act_window">
        <field name="name">Accrued Expense Entry…</field>
        <field name="res_model">account.accrued.orders.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field
            name="group_ids"
            eval="[Command.link(ref('account.group_account_user'))]" />
    </record>

    <record id="action_mail_followers_edit" model="ir.actions.act_window">
        <field name="name">Add/Remove Followers…</field>
        <field name="res_model">mail.followers.edit</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
        <field
            name="context">{'default_res_model': 'purchase.order', 'default_res_ids': active_ids}</field>
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field name="binding_view_types">list,kanban</field>
        <field name="binding_icon">fa-solid fa-user-plus</field>
    </record>

    <record id="action_send_reminder" model="ir.actions.server">
        <field name="name">Send Reminder</field>
        <field name="model_id" ref="purchase.model_purchase_order" />
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field name="binding_view_types">form</field>
        <field
            name="group_ids"
            eval="[Command.link(ref('purchase.group_send_reminder'))]" />
        <field name="state">code</field>
        <field
            name="code">if records:
    action = records._send_reminder_mail(send_single=True)</field>
    </record>

    <record id="action_merger" model="ir.actions.server">
        <field name="name">Merge RFQs</field>
        <field name="model_id" ref="purchase.model_purchase_order" />
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field name="binding_view_types">list</field>
        <field name="binding_sequence">1</field>
        <field name="binding_icon">fa-solid fa-code-merge</field>
        <field
            name="group_ids"
            eval="[Command.link(ref('account.group_account_invoice'))]" />
        <field name="state">code</field>
        <field name="code">if records:
    action = records.action_merge()</field>
    </record>

    <record id="action_confirm_rfqs" model="ir.actions.server">
        <field name="name">Confirm RFQ</field>
        <field name="model_id" ref="purchase.model_purchase_order" />
        <field name="binding_model_id" ref="purchase.model_purchase_order" />
        <field name="binding_view_types">list,kanban</field>
        <field name="binding_icon">fa-solid fa-check</field>
        <field name="state">code</field>
        <field
            name="code">if records:
    res = records.action_confirm()
    if isinstance(res, dict):
        action = res</field>
    </record>

</odoo>
