<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-inherit="mail.Composer" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('o-mail-Composer-coreMain')]" position="before">
            <div t-if="env.displayRating" class="o-portal-Composer-starCard d-flex">
                <div class="o-portal-Composer-stars enabled" t-on-mouseleave="() => state.starValue = state.ratingValue">
                    <t t-set="index" t-value="0" />
                    <t t-foreach="Array(starValInteger)" t-as="num" t-key="num_index">
                        <i class="fa fa-star" role="img" aria-label="Full star" t-att-index="index" t-on-mousemove="onMoveStar" t-on-mouseleave="onLeaveStar" t-on-click="onClickStar"/>
                        <t t-set="index" t-value="index + 1" />
                    </t>
                    <t t-if="starValDecimal">
                        <i class="fa fa-star-half-o" role="img" aria-label="Half a star" t-att-index="index" t-on-mousemove="onMoveStar" t-on-mouseleave="onLeaveStar" t-on-click="onClickStar"/>
                        <t t-set="index" t-value="index + 1" />
                    </t>
                    <t t-foreach="Array(emptyStar)" t-as="num" t-key="num_index">
                        <i class="fa fa-star-o text-black-25" role="img" aria-label="Empty star" t-att-index="index" t-on-mousemove="onMoveStar" t-on-mouseleave="onLeaveStar" t-on-click="onClickStar"/>
                        <t t-set="index" t-value="index + 1" />
                    </t>
                </div>
                <div class="rounded badge text-bg-info" t-if="state.rateText" t-esc="state.rateText"/>
            </div>
        </xpath>
    </t>
</templates>