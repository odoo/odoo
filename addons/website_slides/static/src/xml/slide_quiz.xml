<?xml version="1.0" encoding="UTF-8"?>
<templates xml:space="preserve">
    <t t-name="slide.slide.quiz">
        <div class="o_wslides_js_quiz_container o_wslides_wrap h-100 w-100 overflow-auto pb-5">
            <div class="container">
                <div t-if="this.quiz.descriptionSafe" t-out="this.quiz.descriptionSafe" class="mt-3" />
                <div t-foreach="this.quiz.questions" t-as="question" t-key="this.question_index"
                     t-attf-class="o_wslides_js_lesson_quiz_question mt-5 mb-4 #{this.completed ? 'completed-disabled' : ''}"
                     t-att-data-question-id="question.id" t-att-data-title="question.question">
                    <div class="h4">
                        <small class="text-muted"><span t-out="this.question_index+1"/>. </small> <span t-out="this.question.question"/>
                    </div>
                    <div class="list-group">
                        <t t-foreach="this.question.answer_ids" t-as="answer" t-key="this.answer_index">
                            <a t-att-data-answer-id="answer.id" href="#"
                                t-att-data-text="answer.text_value"
                                t-attf-class="o_wslides_quiz_answer list-group-item d-flex align-items-center list-group-item-action #{this.slide.completed  &amp;&amp; answer.is_correct ? 'list-group-item-success' : '' }">

                                <label class="my-0 d-flex align-items-center justify-content-center me-2">
                                    <input type="radio"
                                        t-att-name="question.id"
                                        t-att-value="answer.id"
                                        class="d-none"/>
                                    <i t-att-class="'fa fa-circle text-400' + (!(this.slide.completed &amp;&amp; answer.is_correct) ? '' : ' d-none')"></i>
                                    <i class="fa fa-times-circle text-danger d-none"></i>
                                    <i t-att-class="'fa fa-check-circle text-success' + (this.slide.completed &amp;&amp; answer.is_correct ? '' :  ' d-none')"></i>
                                </label>
                                <span t-out="this.answer.text_value"/>
                            </a>
                            <t t-if="this.slide.completed and this.answer.is_correct" t-set="correct_answer_comment" t-value="this.answer.comment"/>
                        </t>
                        <div t-attf-class="o_wslides_quiz_answer_info list-group-item list-group-item-info #{correct_answer_comment ? '' : 'd-none'}">
                            <i class="fa fa-info-circle"/>
                            <span class="o_wslides_quiz_answer_comment ms-2">
                                <t t-if="this.correct_answer_comment" t-out="this.correct_answer_comment"/>
                            </span>
                        </div>
                    </div>
                </div>
                <div t-if="!this.slide.completed" class="o_wslides_js_lesson_quiz_validation pt-3"/>
                <div t-else="" class="row">
                    <div class="o_wslides_js_lesson_quiz_validation col py-2 bg-100 mb-2 border-bottom"/>
                </div>
            </div>
        </div>
    </t>

    <t t-name="slide.slide.quiz.validation">
        <div id="validation">
            <div t-if="!this.channel.isMember">
                <div class="o_wslides_join_course alert alert-info d-flex align-items-center justify-content-between">
                    <div t-if="this.channel.enroll == 'invite' and !this.channel.isMemberOrInvited">
                        <b>This course is private.
                            <span t-if="this.isPublicUser">
                                Please
                                <a t-att-href="'/web/login?redirect=' + this.redirectURL" class="fw-bold">
                                    sign in
                                </a>
                                to enroll.
                            </span>
                            <a t-else="" href="#" class="fw-bold o_wslides_js_channel_enroll"
                               t-att-data-channel-id="this.channel.id">
                                <span t-if="this.channel.requestedAccess" class="text-success">
                                    Responsible already contacted.
                                </span>
                                <span t-else="">
                                    Contact the responsible to enroll.
                                </span>
                            </a>
                        </b>
                        <span class="my-0 h4">
                            <span title="Succeed and gain karma" aria-label="Succeed and gain karma" class="badge bg-warning fw-bold ms-3">
                                + <t t-out="this.quiz.karmaGain"/> XP
                            </span>
                        </span>
                    </div>
                    <div t-else="" class="w-100">
                        <b class="h5 mb-0 o_wslides_quiz_join_course_message">
                            <span t-if="this.channel.enroll == 'public' or (this.channel.isMemberOrInvited and this.channel.enroll == 'invite')">
                                <t t-if="this.isPublicUser">
                                    Sign in and join the course to verify your answers!
                                </t>
                                <t t-else="">
                                    Join the course to take the quiz and verify your answers!
                                </t>
                            </span>
                        </b>
                        <span class="my-0 h4">
                            <span title="Succeed and gain karma" aria-label="Succeed and gain karma" class="badge bg-warning fw-bold ms-3">
                                + <t t-out="this.quiz.karmaGain"/> XP
                            </span>
                        </span>
                        <div class="o_wslides_join_course_widget float-end"/>
                    </div>
                </div>
                <span t-if="this.isPublicUser &amp;&amp; this.signupAllowed" class="d-block mt-2">
                    <span>Don't have an account?</span>
                    <a class="fw-bold" t-att-href="'/web/signup?redirect=' + this.redirectURL">Sign Up!</a>
                </span>
            </div>
            <div t-else="" class="d-md-flex align-items-center justify-content-between">
                <div t-att-class="'d-flex align-items-center' + (this.slide.completed ? ' alert alert-success my-0 py-1 px-3' : '')">
                    <button t-if="!this.slide.completed" title="Check answers"
                        class="btn btn-primary text-uppercase fw-bold o_wslides_js_lesson_quiz_submit">Check your answers</button>
                    <b t-else="" class="my-0 h5">Done!</b>
                    <span class="my-0 h5" style="line-height: 1">
                        <span role="button" title="Succeed and gain karma" class="badge bg-warning fw-bold ms-3">
                            + <t t-if="!this.slide.completed" t-out="this.quiz.karmaGain"/><t t-else="" t-out="this.quiz.karmaWon"/> XP
                        </span>
                    </span>
                    <div class="ms-3 d-none text-danger o_wslides_js_quiz_submit_error">
                        <i class="fa fa-close me-1"/>
                        <span class="o_wslides_js_quiz_submit_error_text"></span>
                    </div>
                </div>
                <div class="ms-auto mt-3 mt-md-0">
                    <button t-if="this.quiz.attemptsCount > 0 &amp;&amp; this.channel.canUpload" class="btn btn-light border o_wslides_js_lesson_quiz_reset me-1">
                        Reset
                    </button>
                    <button t-if="this.slide.completed &amp;&amp; this.slide.hasNext" class="btn btn-primary o_wslides_quiz_continue">
                        Continue <i class="oi oi-chevron-right ms-1"/>
                    </button>
                </div>
            </div>
            <div class="mt-4 o_wslides_js_lesson_quiz_resource_info d-none">
                <div t-if="this.quiz.slideResources.length > 0">
                    <span>Need help? Review related content:</span>
                    <ul>
                        <li t-foreach="this.quiz.slideResources" t-as="resource" t-key="this.resource_index">
                            <a t-att-href="resource.link or resource.download_url" t-out="this.resource.display_name"/>
                        </li>
                    </ul>
                </div>
            </div>
        </div>
    </t>

</templates>
