<?xml version="1.0" encoding="UTF-8"?>
<templates>
    <t t-name="slide.course.join">
        <div>
            <a role="button"
                class="btn btn-primary text-uppercase fw-bold o_wslides_js_course_join_link"
                title="Join the Course" aria-label="Join the Course"
                href="#">
                <t t-if="channelEnroll == 'public'" t-out="joinMessage"/>
                <t t-if="channelEnroll == 'invite' and isMemberOrInvited" t-out="joinMessage"/>
            </a>
        </div>
    </t>

    <t t-name="slide.course.join.popupContent">
        <div t-if="invitePreview">
            Please <a t-attf-href="/slides/#{channelId}/identify?invite_partner_id=#{invitePartnerId}&amp;invite_hash=#{inviteHash}">
            <t t-if="isPartnerWithoutUser">create an account</t><t t-else="">login</t>
            </a> to join this course
        </div>
        <div t-else="">
            <t t-if="errorSignupAllowed">
                Please <a t-attf-href="/web/login?redirect=#{courseUrl}">login</a> or <a t-attf-href="/web/signup?redirect=#{courseUrl}">create an account</a> to join this course
            </t>
            <t t-else="">
                Please <a t-attf-href="/web/login?redirect=#{courseUrl}">login</a> to join this course
            </t>
        </div>
    </t>
</templates>
