<?xml version="1.0"?>
<odoo>
    <record id="mail_activity_view_form" model="ir.ui.view">
        <field name="name">mail.activity.view.form.inherit.slides</field>
        <field name="model">mail.activity</field>
        <field name="inherit_id" ref="mail.mail_activity_view_form"/>
        <field name="arch" type="xml">
            <field name="summary" position="after">
                <field name="request_partner_id" invisible="res_model != 'slide.channel'"/>
            </field>
        </field>
    </record>
</odoo>
