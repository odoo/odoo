<?xml version="1.0" ?>
<odoo><data>

<!-- Channels home template -->
<template id='courses_home' name="Odoo Courses Homepage">
    <t t-set="body_classname" t-value="'o_wslides_body'"/>
    <t t-call="website.layout">
        <div id="wrap" class="wrap o_wslides_wrap">
            <div class="oe_structure oe_empty"/>
            <div class="container mt-3 mb-4">
                <div class="o_wslides_index_topbar_filters d-flex d-print-none align-items-center justify-content-end flex-wrap gap-2 w-100">
                    <h4 class="me-auto my-0 pe-sm-4">Courses</h4>
                    <button t-if="not is_public_user" class="btn d-block d-lg-none" type="button" data-bs-toggle="offcanvas" data-bs-target="#o_wslides_sidebar_offcanvas" aria-controls="o_wslides_sidebar_offcanvas"><i class="fa fa-bars"/></button>
                    <div class="d-flex flex-grow-1 flex-md-nowrap flex-wrap gap-2 justify-content-between justify-content-lg-end">
                        <div class="d-flex gap-2">
                            <a class="btn btn-light o_wslides_home_all_slides" href="/slides/all">All courses</a>
                        </div>
                        <div class="o_wslides_search d-flex w-100">
                            <t t-call="website.website_search_box_input">
                                <t t-set="_form_classes" t-valuef="o_wslides_nav_navbar_right order-lg-3 w-100 flex-grow-1"/>
                                <t t-set="search_type" t-valuef="slides"/>
                                <t t-set="action" t-valuef="/slides/all"/>
                                <t t-set="display_description" t-valuef="true"/>
                                <t t-set="display_detail" t-valuef="false"/>
                                <t t-set="placeholder">Search courses</t>
                                <input type="hidden" name="prevent_redirect" value="True"/>
                            </t>
                        </div>
                    </div>
                </div>
                <div class="o_wprofile_email_validation_container">
                    <t t-call="website_profile.email_validation_banner">
                        <t t-set="redirect_url" t-value="'/slides'"/>
                        <t t-set="send_alert_classes" t-value="'alert alert-danger alert-dismissable mt-4 mb-0'"/>
                        <t t-set="done_alert_classes" t-value="'alert alert-success alert-dismissable mt-4 mb-0'"/>
                        <t t-set="additional_validation_email_message"> and join this Community</t>
                        <t t-set="additional_validated_email_message"> You may now participate in our eLearning.</t>
                    </t>
                </div>
            </div>

            <div class="container o_wslides_home_main">
                <div class="row">
                    <t t-set="has_side_column" t-value="is_view_active('website_slides.toggle_leaderboard')"/>
                    <t t-if="is_public_user">
                        <div t-if="has_side_column" class="col-lg-3 order-3 order-lg-2">
                            <div class="row">
                                <div class="col-12 col-md-5 col-lg-12">
                                    <div class="ps-md-5 ps-lg-0">
                                        <t t-call="website_slides.slides_home_users_small"/>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </t>
                    <div t-else="" class="d-none d-lg-block col-lg-3 order-lg-2">
                        <t t-set="has_side_column" t-value="True"/>
                        <div class="o_wslides_home_aside_loggedin mb-0 mb-lg-4">
                            <div class="o_wslides_home_aside_title d-none d-lg-block mb-3">
                                <a t-att-href="'/profile/user/%s' % user.id" title="My profile" class="d-flex gap-2 text-decoration-none text-reset">
                                    <t t-call="website_slides.slides_misc_user_image">
                                        <t t-set="img_class" t-value="'rounded-circle'"/>
                                    </t>
                                    <div class="d-flex flex-column">
                                        <h6 class="mb-0" t-esc="user.name"/>
                                        <span> <t t-esc="user.karma"/> XP</span>
                                    </div>
                                </a>
                            </div>
                            <div id="o_wslides_home_aside_content" class="d-none d-lg-flex flex-lg-column gap-4">
                                <div t-call="website_slides.slides_home_user_profile_small"/>
                                <div t-call="website_slides.slides_home_user_achievements_small"/>
                                <div t-call="website_slides.slides_home_achievements_small"/>
                                <div t-call="website_slides.slides_home_users_small"/>
                            </div>
                        </div>
                    </div>
                    <div t-att-class="'col-lg-9 pe-lg-5 order-lg-1' if has_side_column else 'col-lg pr-lg'">
                        <div t-if="invite_error_msg" role="alert" class="o_not_editable alert alert-danger text-center" t-esc="invite_error_msg"/>
                        <div class="o_wslides_home_content_section mb-3"
                            t-if="not channels_popular">
                            <p class="h2">No Course created yet.</p>
                            <p groups="website_slides.group_website_slides_officer">Click on "New" in the top-right corner to write your first course.</p>
                        </div>
                        <div t-if="channels_my" class="container mb-5">
                            <div class="o_wslides_home_content_section">
                                <div t-foreach="channels_my" t-as="channel">
                                    <div class="d-flex">
                                        <div t-attf-class="align-items-md-center d-flex flex-grow-1 flex-column flex-md-row">
                                            <div class="flex-grow-1 mw-md-50">
                                                <a t-if="channel.is_member and not channel.completed" t-attf-href="/slides/#{slug(channel)}"
                                                t-out="channel.name"/>
                                                <span t-else="" t-field="channel.name"/>
                                                <span t-if="not channel.is_published" class="badge text-bg-danger">Unpublished</span>
                                            </div>
                                            <div t-attf-class="align-items-center d-flex flex-grow-1 #{'justify-content-sm-end' if channel.channel_type == 'documentation' else ''}">
                                                <t t-if="channel.is_member and channel.completed">
                                                    <div class="badge rounded-pill text-bg-success pull-right"><i class="fa fa-check"/> Completed</div>
                                                </t>
                                                <t t-elif="channel.is_member and channel.channel_type != 'documentation'">
                                                    <div class="d-none d-sm-block flex-grow-1">
                                                        <div class="progress me-3" style="height: 6px">
                                                            <div class="progress-bar" role="progressbar" t-att-aria-valuenow="channel.completion" aria-valuemin="0" aria-valuemax="100" t-attf-style="width:#{channel.completion}%;" aria-label="Progress bar"/>
                                                        </div>
                                                    </div>
                                                    <div>
                                                        <t t-out="channel.completion"/> %
                                                    </div>
                                                </t>
                                                <div t-else=""><t t-esc="channel.total_slides"/> steps</div>
                                                <div class="ms-4 text-muted">
                                                    <t t-out="'{:.1f}'.format(channel.total_time).rstrip('.0')"/> hours
                                                </div>
                                            </div>
                                        </div>
                                        <div class="col-2 d-flex flex-row-reverse">
                                            <a t-if="channel.is_member and not channel.completed" class="d-flex align-items-center" t-attf-href="/slides/#{slug(channel)}">
                                                <div class="d-none d-md-block me-2">Continue</div><i class="oi oi-arrow-right me-1"/>
                                            </a>
                                        </div>
                                    </div>
                                    <div t-if="not channel_last" class="row mx-1 mt-2">
                                        <hr/>
                                    </div>
                                </div>
                            </div>
                        </div>
                        <div class="o_wslides_home_content_section mb-5"
                            t-if="channels_popular">
                            <div class="d-flex justify-content-between o_wslides_home_content_section_title">
                                <h5 class="mb-3">Most popular courses</h5>
                                <a href="slides/all" class="float-end">View all</a>
                            </div>
                            <div class="row row-cols-1 row-cols-sm-2 row-cols-xl-3 g-4">
                                <t t-foreach="channels_popular[:3]" t-as="channel">
                                    <div class="col">
                                        <t t-call="website_slides.course_card"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                        <div class="o_wslides_home_content_section mb-5"
                            t-if="channels_newest">
                            <div class="o_wslides_home_content_section_title d-flex justify-content-between">
                                <h5 class="mb-3">Newest courses</h5>
                                <a href="slides/all" class="float-end">View all</a>
                            </div>
                            <div class="row row-cols-1 row-cols-sm-2 row-cols-xl-3 g-4">
                                <t t-foreach="channels_newest[:3]" t-as="channel">
                                    <div class="col">
                                        <t t-call="website_slides.course_card"/>
                                    </div>
                                </t>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="offcanvas offcanvas-start" tabindex="-1" id="o_wslides_sidebar_offcanvas" aria-labelledby="Sidebar offcanvas">
                    <div class="offcanvas-header">
                        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"></button>
                    </div>
                    <div class="offcanvas-body d-flex flex-column gap-4">
                        <div class="o_wslides_home_aside_title mb-3">
                            <a t-att-href="'/profile/user/%s' % user.id" title="My profile" class="d-flex gap-2 text-decoration-none text-reset">
                                <t t-call="website_slides.slides_misc_user_image">
                                    <t t-set="img_class" t-value="'rounded-circle'"/>
                                </t>
                                <div class="d-flex flex-column">
                                    <h6 class="mb-0" t-esc="user.name"/>
                                    <span> <t t-esc="user.karma"/> XP</span>
                                </div>
                            </a>
                        </div>
                        <div t-call="website_slides.slides_home_user_profile_small"/>
                        <div t-call="website_slides.slides_home_user_achievements_small"/>
                        <div t-call="website_slides.slides_home_achievements_small"/>
                        <div t-call="website_slides.slides_home_users_small"/>
                    </div>
                </div>
            </div>
            <t t-call="website_slides.courses_footer"></t>
        </div>
    </t>
</template>

<!-- Channel all/main template -->
<template id='courses_all' name="Odoo All Courses">
    <t t-set="body_classname" t-value="'o_wslides_body'"/>
    <t t-call="website.layout">
        <div id="wrap" class="wrap o_wslides_wrap">
            <!-- Repeat structure for every section to allow customization through website editor. !-->
            <div class="oe_structure oe_empty"/>
            <div class="container mt-3 mb-4">
                <!-- Navbar dynamically composed using displayed channel tag groups. -->
                <div class="o_wslides_index_topbar_filters d-flex d-print-none align-items-center justify-content-end flex-wrap gap-2 w-100">
                    <a class="nav-link nav-item pe-3" href="/slides"><i class="oi oi-chevron-left"/></a>
                    <h4 class="me-auto my-0 pe-sm-4">
                        <t t-if="search_my">
                            My Courses
                        </t>
                        <t t-elif="search_slide_category == 'certification'">
                            Certifications
                        </t>
                        <t t-else="">
                            All Courses
                        </t>
                    </h4>
                    <!-- Desktop filters -->
                    <t t-set="search_tag_groups" t-value="search_tags.mapped('group_id')"/>
                    <t t-foreach="tag_groups" t-as="tag_group">
                    <t t-set="group_frontend_tags" t-value="tag_group.tag_ids.filtered(lambda tag: tag.color)"/>
                        <a t-if="group_frontend_tags" t-att-class="'btn btn-light dropdown-toggle d-none d-lg-block %s' % ('active' if tag_group in search_tag_groups else '')"
                            href="/slides/all"
                            t-att-data-bs-target="'#navToogleTagGroup%s' % tag_group.id"
                            role="button" data-bs-toggle="dropdown"
                            aria-haspopup="true" aria-expanded="false"
                            t-esc="tag_group.name"/>
                        <div class="dropdown-menu" t-att-id="'navToogleTagGroup%s' % tag_group.id">
                            <t t-foreach="group_frontend_tags" t-as="tag">
                                <span t-att-class="'post_link cursor-pointer dropdown-item %s' % ('active' if tag in search_tags else '')"
                                    t-att-data-post="slide_query_url(tag=slugify_tags(search_tags.ids, toggle_tag_id=tag.id), my=search_my, search=search_term, slide_category=search_slide_category, prevent_redirect=True)"
                                    t-esc="tag.name"/>
                            </t>
                        </div>
                    </t>
                    <!-- Searchbar (desktop) -->
                    <t t-call="website.website_search_box_input">
                        <t t-set="_form_classes" t-valuef="o_wslides_nav_navbar_right d-none d-md-flex"/>
                        <t t-set="search_type" t-valuef="slides"/>
                        <!-- No action: remain on same URL -->
                        <t t-set="display_description" t-valuef="true"/>
                        <t t-set="display_detail" t-valuef="false"/>
                        <t t-set="placeholder">Search courses</t>
                        <t t-set="search" t-value="original_search or search_term"/>
                        <input t-if="search_my" type="hidden" name="my" t-att-value="1"/>
                        <input t-if="search_slide_category" type="hidden" name="slide_category" t-att-value="search_slide_category" />
                        <input type="hidden" name="prevent_redirect" value="True"/>
                    </t>
                    <!-- Searchbar (mobile)-->
                    <t t-call="website.website_search_box_input">
                    <t t-set="_form_classes" t-valuef="o_wslides_nav_navbar_right d-md-none flex-grow-1"/>
                    <t t-set="search_type" t-valuef="slides"/>
                    <!-- No action: remain on same URL -->
                    <t t-set="display_description" t-valuef="true"/>
                    <t t-set="display_detail" t-valuef="false"/>
                    <t t-set="placeholder">Search courses</t>
                    <t t-set="search" t-value="original_search or search_term"/>
                    <input t-if="search_my" type="hidden" name="my" t-att-value="1"/>
                    <input t-if="search_slide_category" type="hidden" name="slide_category" t-att-value="search_slide_category" />
                    <input type="hidden" name="prevent_redirect" value="True"/>
                    </t>
                    <button class="btn btn-light position-relative d-lg-none"
                        data-bs-toggle="offcanvas"
                        data-bs-target="#o_wslides_index_offcanvas">
                        <i class="fa fa-sliders"/>
                    </button>
                </div>
                <!-- Off canvas filters on mobile-->
                <div id="o_wslides_index_offcanvas" class="o_website_offcanvas offcanvas offcanvas-end d-lg-none p-0 overflow-visible mw-75">
                    <t t-set="search_tag_groups" t-value="search_tags.mapped('group_id')"/>
                    <div class="offcanvas-header">
                        <h5 class="offcanvas-title">Filters</h5>
                        <button type="button" class="btn-close" data-bs-dismiss="offcanvas" aria-label="Close"/>
                    </div>
                    <div class="offcanvas-body p-0">
                        <div class="accordion accordion-flush">
                            <t t-foreach="tag_groups" t-as="tag_group">
                                <t t-set="group_frontend_tags" t-value="tag_group.tag_ids.filtered(lambda tag: tag.color)"/>
                                <div class="accordion-item">
                                    <h2 class="accordion-header">
                                        <button class="accordion-button collapsed"
                                            type="button"
                                            data-bs-toggle="collapse"
                                            t-attf-aria-controls="o_wslides_offcanvas_cat_#{tag_group.name}"
                                            t-att-data-bs-target="'.o_wslides_offcanvas_cat_%s' % tag_group.name"
                                            aria-expanded="false">
                                            <t t-out="tag_group.name"/>
                                        </button>
                                    </h2>
                                    <div t-attf-id="o_wslides_offcanvas_cat_#{tag_group.name}" t-attf-class="o_wslides_offcanvas_cat_#{tag_group.name} accordion-collapse collapse">
                                        <div class="accordion-body pt-0">
                                            <ul class="list-group list-group-flush">
                                                <t t-if="tag_group.is_published and tag_group.tag_ids and any(tag.color for tag in tag_group.tag_ids)" t-foreach="tag_group.tag_ids" t-as="tag">
                                                    <li t-if="tag.color" class="list-group-item border-0 px-0">
                                                        <span t-att-data-post="'/slides/all?%s' % keep_query('*', tags=str((search_tags - tag).ids if tag in search_tags else (tag | search_tags).ids), prevent_redirect=True)"
                                                            class="post_link text-reset cursor-pointer" t-att-title="tag.name">
                                                            <div class="form-check">
                                                                <input class="form-check-input pe-none" type="checkbox" t-attf-name="#{tag.color}" t-att-checked="tag in search_tags"/>
                                                                <label class="form-check-label" t-attf-for="#{tag.color}" t-out="tag.name"/>
                                                            </div>
                                                        </span>
                                                    </li>
                                                </t>
                                            </ul>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </div>
                    </div>
                </div>
                <div class="o_wprofile_email_validation_container mb16 mt16">
                    <t t-call="website_profile.email_validation_banner">
                        <t t-set="redirect_url" t-value="'/slides'"/>
                        <t t-set="additional_validation_email_message"> and join this Community</t>
                        <t t-set="additional_validated_email_message"> You may now participate in our eLearning.</t>
                    </t>
                </div>
                <!-- Display tags -->
                <t t-if="search_my">
                      <span class="align-items-baseline border d-inline-flex ps-2 rounded mb-2">
                      <i class="fa fa-tag me-2 text-muted"/>
                      My Courses
                      <span t-att-data-post="slide_query_url(tag=slugify_tags(search_tags.ids), search=search_term, prevent_redirect=True)"
                         class="post_link cursor-pointer btn border-0 py-1">&#215;</span>
                    </span>
                </t>
                <t t-if="search_term">
                      <span class="align-items-baseline border d-inline-flex ps-2 rounded mb-2">
                      <i class="fa fa-tag me-2 text-muted"/>
                      <t t-esc="search_term"/>
                      <span t-att-data-post="slide_query_url(tag=slugify_tags(search_tags.ids), my=search_my, slide_category=search_slide_category, prevent_redirect=True)"
                         class="post_link cursor-pointer btn border-0 py-1">&#215;</span>
                    </span>
                </t>
                <t t-foreach="search_tags" t-as="tag">
                    <span t-attf-class="align-items-baseline d-inline-flex ps-2 rounded mb-2 o_filter_tag #{'o_color_%s' % tag.color}">
                        <i class="fa fa-tag me-2"/>
                        <t t-esc="tag.display_name"/>
                        <span t-att-data-post='slide_query_url(tag=slugify_tags(search_tags.ids, tag.id), my=search_my, search=search_term, slide_category=search_slide_category, prevent_redirect=True)'
                            class="post_link btn border-0 py-1">&#215;</span>
                    </span>
                </t>
            </div>
            <div class="container o_wslides_home_main pb-5">
                <div t-if="not channels and not search_term and not search_slide_category and not search_my and not search_tags">
                    <p class="h2">No Course created yet.</p>
                    <p groups="website_slides.group_website_slides_officer">Click on "New" in the top-right corner to write your first course.</p>
                </div>
                <div t-elif="search_term and not channels" class="alert alert-info mb-5">
                    No course was found matching your search <code><t t-esc="search_term"/></code>.
                </div>
                <div t-elif="not channels" class="alert alert-info mb-5">
                    No course was found matching your search.
                </div>
                <t t-else="">
                    <div t-if="original_search" class="alert alert-warning mb-5">
                        No results found for '<span t-esc="original_search"/>'. Showing results for '<span t-esc="search_term"/>'.
                    </div>
                    <div class="row row-cols-1 row-cols-sm-2 row-cols-lg-3 row-cols-xl-4 g-4">
                        <t t-foreach="channels" t-as="channel">
                            <div class="col">
                                <t t-call="website_slides.course_card"/>
                            </div>
                        </t>
                    </div>
                </t>
            </div>

            <t t-call="website_slides.courses_footer"></t>
        </div>
    </t>
</template>

<template id='courses_footer'>
    <section class="s_banner">
        <div class="oe_structure oe_empty" id="oe_structure_website_slides_course_footer_1"/>
    </section>
</template>

<template id='course_card' name="Course Card">
    <a t-attf-href="/slides/#{slug(channel)}" t-title="channel.name" class="text-decoration-none text-reset">
        <div t-attf-class="card o_wslides_course_card h-100 #{'o_wslides_course_unpublished' if not channel.is_published else ''}" t-cache="channel if is_public_user and not search_tags else None">
            <t t-set="channel_frontend_tags" t-value="channel.tag_ids.filtered(lambda tag: tag.color)"/>
            <div t-field="channel.image_1920" t-options="{'widget': 'image', 'preview_image': 'image_512'}" class="o_wslides_background_image ratio ratio-21x9">
                <t t-if="channel.partner_has_new_content" t-call="website_slides.course_card_information"/>
            </div>
            <div class="card-body">
                <h6 class="card-title mb-2 o_wslides_desc_truncate_2" t-attf-href="/slides/#{slug(channel)}" t-field="channel.name"/>
                <span t-if="not channel.is_published" class="badge text-bg-danger">Unpublished</span>
                <div class="card-text d-flex flex-column flex-grow-1 mt-1">
                    <div class="small" t-field="channel.description_short"/>
                    <div t-if="channel_frontend_tags" class="o_wslides_desc_truncate_2_badges d-flex flex-wrap gap-1 small">
                        <t t-foreach="channel_frontend_tags" t-as="tag">
                            <t t-if="search_tags">
                                <span t-att-data-post="slide_query_url(tag=slugify_tags(search_tags.ids, toggle_tag_id=tag.id), my=search_my, search=search_term, slide_category=search_slide_category, prevent_redirect=True)"
                                    t-attf-class="post_link cursor-pointer badge o_badge_clickable #{'o_color_'+str(tag.color)}" t-esc="tag.name"/>
                            </t>
                            <t t-else="">
                                <span t-att-data-post="slide_query_url(tag=slugify_tags(search_tags.ids, toggle_tag_id=tag.id), my=search_my, search=search_term, slide_category=search_slide_category, prevent_redirect=True)"
                                    t-attf-class="post_link cursor-pointer badge o_badge_clickable o_wslides_channel_tag #{'o_color_'+str(tag.color)}" t-esc="tag.name"/>
                            </t>
                        </t>
                    </div>
                </div>
            </div>
            <div class="card-footer">
                <div class="d-flex justify-content-between align-items-center">
                    <small t-if="channel.total_time" class="fw-light" t-esc="channel.total_time" t-options="{'widget': 'duration', 'unit': 'hour', 'round': 'minute'}"/>
                    <div class="d-flex flex-grow-1 justify-content-end">
                        <t t-if="channel.is_member and channel.completed">
                            <span class="badge text-bg-success pull-right"><i class="fa fa-check"/> Completed</span>
                        </t>
                        <div t-elif="channel.is_member and channel.channel_type != 'documentation'" class="progress w-50" style="height: 6px">
                            <div class="progress-bar" role="progressbar" t-att-aria-valuenow="channel.completion" aria-valuemin="0" aria-valuemax="100" t-attf-style="width:#{channel.completion}%;" aria-label="Progress bar"/>
                        </div>
                        <small t-else="" class="fw-light" t-esc="str(channel.total_slides) + ' steps'"/>
                    </div>
                </div>
            </div>
        </div>
    </a>
</template>

<template id="course_card_information" name='Course Information'>
    <t id="course_card_information_content">
    </t>
</template>

<template id="course_card_information_arrow" inherit_id="website_slides.course_card_information"
    active="True" name='New Content Ribbon'>
    <xpath expr="//t[@id='course_card_information_content']" position="inside">
        <span class="o_wslides_arrow">New Content</span>
    </xpath>
</template>

<template id='slides_home_achievements_small' name="Users">
    <t class="o_wslides_home_aside">
    </t>
</template>

<template id="toggle_latest_achievements" inherit_id="website_slides.slides_home_achievements_small" active="True" name='Display Achievements'>
    <xpath expr="//t[hasclass('o_wslides_home_aside')]" position="inside">
        <div t-if="achievements">
            <h6 class="mb-3">Latest achievements</h6>
            <div class="d-flex flex-column gap-3">
                <t t-foreach="achievements" t-as="achievement">
                    <t t-call="website_slides.achievement_card"/>
                </t>
            </div>
        </div>
    </xpath>
</template>

<template id='achievement_card' name="Achivement Card">
    <div class="d-flex">
        <t t-call="website_slides.slides_misc_user_image">
            <t t-set="user" t-value="achievement.user_id"/>
        </t>
        <div>
            <div class="lh-1" t-esc="achievement.user_id.name"/>
            <small class="text-muted" t-esc="achievement.badge_id.name"/>
        </div>
    </div>
</template>

<template id='slides_home_users_small' name="Users">
    <div class="o_wslides_home_aside">
    </div>
</template>

<template id="toggle_leaderboard" inherit_id="website_slides.slides_home_users_small" active="True" name='Display Leaderboard'>
    <xpath expr="//div[hasclass('o_wslides_home_aside')]" position="inside">
        <h6 class="mb-3">Leaderboard</h6>
        <t t-if="users">
            <div class="d-flex flex-column gap-3">
                <t t-foreach="users" t-as="user">
                    <t t-call="website_slides.user_quickkarma_card"/>
                </t>
            </div>
        </t>
        <t t-else=""><p>No leaderboard currently :(</p></t>
        <a t-if="users" href="/profile/users" class="d-block mt-2">View all<i class="oi oi-arrow-right ms-1"/></a>
    </xpath>
</template>

<template id='user_quickkarma_card' name="User QuickKarma Card">
    <div class="d-flex flex-wrap align-items-start justify-content-between">
        <div>
            <t t-call="website_slides.slides_misc_user_image"/>
            <div class="d-flex flex-column">
                <div class="lh-1" t-esc="user.name"/>
                <small class="text-muted" t-esc="user.rank_id.name"/>
            </div>
        </div>
        <span class="badge text-bg-success"><t t-esc="user.karma"/> xp</span>
    </div>
</template>

<template id='slides_home_user_profile_small' name="User Profile">
    <div class="o_wslides_home_aside d-flex flex-column gap-3">
        <div>
            <div t-if="user.rank_id" t-field="user.rank_id"/>
            <t t-set="next_rank_id" t-value="user._get_next_rank()"/>
            <div class="d-flex flex-wrap align-items-center gap-2">
                <div class="progress flex-grow-1" role="progressbar" aria-label="Your Experience" t-attf-aria-valuenow="#{int(100 * (user.karma - user.rank_id.karma_min) / (next_rank_id.karma_min - user.rank_id.karma_min))}%" aria-valuemin="0" aria-valuemax="100" style="height: 9px" t-attf-title="{{ '%s/%s xp' % (user.karma, next_rank_id.karma_min) if next_rank_id else ''}}">
                    <div class="progress-bar" t-attf-style="width: #{int(100 * (user.karma - user.rank_id.karma_min) / (next_rank_id.karma_min - user.rank_id.karma_min))}%;"/>
                </div>
                <span><t t-esc="int(100 * (user.karma - user.rank_id.karma_min) / (next_rank_id.karma_min - user.rank_id.karma_min))"/>%</span>
            </div>
        </div>
        <div>
            <div t-if="next_rank_id or user.rank_id" class="fw-bold">Next rank</div>
            <t t-set="rank_id" t-value="next_rank_id if next_rank_id else user.rank_id"/>
            <t t-set="img_max_width" t-value="'24px'"/>
            <div class="d-flex align-items-center gap-2 py-2">
                <div t-field="rank_id.image_1920"
                    t-attf-style="max-width: {{img_max_width or '50%'}};"
                    t-options="{'widget': 'image', 'preview_image': 'image_128'}"/>
                <a href="/profile/ranks_badges" t-field="next_rank_id.name"/>
            </div>
        </div>
        <div>
            <div t-if="next_rank_id" t-field="next_rank_id.description_motivational" class="border rounded-2 p-3"/>
            <div t-else="">Congratulations, you have reached the last rank!</div>
        </div>
    </div>
</template>

<template id='slides_home_user_achievements_small' name="User Achievements">
    <h6 class="mb-3">Your achievements</h6>
    <div class="d-flex flex-column gap-2">
        <t t-foreach="challenges" t-as="challenge">
            <t t-set="challenge_done" t-value="challenge in challenges_done if challenges_done else False"/>
            <div t-attf-class="d-flex gap-2 #{'opacity-50' if not challenge_done else ''}">
                <div t-if="challenge.reward_id.image_1920" t-field="challenge.reward_id.image_1920"
                    t-options="{'widget': 'image', 'preview_image': 'image_128', 'style': 'max-height: 44px'}"/>
                <img t-else="" t-attf-src="'/website_profile/static/src/img/badge_%s.svg' % (challenge.reward_id.level)" t-att-alt="challenge.reward_id.name" style="max-height: 44px"/>
                <div class="flex-grow-1">
                    <div class="small lh-1" t-esc="challenge.reward_id.name"/>
                    <small class="text-muted" t-esc="challenge.reward_id.description"/>
                </div>
                <i t-if="challenge_done" class="fa fa-check h5 text-success" aria-label="Done" title="Done" role="img"></i>
            </div>
        </t>
    </div>
    <a href="/profile/ranks_badges?badge_category=slides">View all<i class="oi oi-arrow-right ms-1"/></a>
</template>

<template id='slides_misc_user_image' name="User Avatar">
    <img t-attf-class="o_avatar {{img_class or 'rounded-circle float-start'}}"
        t-att-style="img_style if img_style else 'width: 32px; height: 32px;'"
        t-att-src="'/profile/avatar/%s?field=avatar_128' % user.id"
        t-att-alt="user.name"/>
</template>
</data></odoo>
