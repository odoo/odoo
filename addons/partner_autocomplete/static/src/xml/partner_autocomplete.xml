<?xml version="1.0" encoding="utf-8"?>
<templates>
    <t t-name="partner_autocomplete.PartnerAutoComplete" t-inherit="web.AutoComplete">
        <xpath expr="//t[@t-foreach='source.options']" position="after">
            <li
                class="o-autocomplete--dropdown-item ui-menu-item d-block"
                t-on-mouseenter="() => this.onOptionMouseEnter([source_index, option_index])"
                t-on-mouseleave="() => this.onOptionMouseLeave([source_index, option_index])"
                t-on-pointerdown="() => this.ignoreBlur = true"
                t-on-click="(ev) => this.searchWorldwide(ev)"
            >
                <a
                    t-attf-id="{{props.id or 'autocomplete'}}_{{source_index}}_{{option_index}}"
                    role="option"
                    href="#"
                    class="dropdown-item ui-menu-item-wrapper text-truncate"
                    t-att-class="{ 'ui-state-active': isActiveSourceOption([source_index, option_index]) }"
                    t-att-aria-selected="isActiveSourceOption([source_index, option_index]) ? 'true' : 'false'"
                >
                    Search Worldwide 🌎
                </a>
            </li>
        </xpath>
    </t>

    <t t-name="partner_autocomplete.PartnerAutoCompleteCharField" t-inherit="web.CharField">
        <xpath expr="//t[@t-else='']" position="before">
            <t t-elif="props.record.resModel !== 'res.partner' || props.record.data.company_type === 'company'">
                <PartnerAutoComplete
                    value="props.record.data[props.name] || ''"
                    sources="sources"
                    onSelect.bind="onSelect"
                    input="inputRef"
                    placeholder="placeholder || ''"
                />
            </t>
        </xpath>
    </t>

    <t t-name="partner_autocomplete.DropdownOption">
        <span class="o_partner_autocomplete_info d-flex align-items-center py-1 gap-5">
        <div class="flex-grow-1">
            <strong t-esc="option.name or '&#160;'"/>
            <div t-esc="option.description"/>
        </div>
        <div style="width: 32px; height: 32px">
            <img t-att-src="option.logoUrl" onerror="this.style.display='none'" alt="Placeholder" class="float-end img-fluid"/>
        </div>
        </span>
    </t>

    <t t-name="partner_autocomplete.CharFieldDropdownOption">
        <span class="o_partner_autocomplete_info d-flex align-items-center py-1 gap-5">
            <t t-call="partner_autocomplete.DropdownOption"/>
        </span>
    </t>

    <t t-name="partner_autocomplete.Many2oneDropdownOption">
        <span class="o_partner_autocomplete_info d-flex align-items-center gap-5">
            <t t-call="partner_autocomplete.DropdownOption"/>
        </span>
    </t>

    <t t-name="partner_autocomplete.InsufficientCreditNotification">
        <div class="o-hidden-ios">
            <a class="btn btn-link" t-att-href="credits_url"><i class="oi oi-arrow-right"/> Buy more credits</a>
        </div>
    </t>

    <t t-name="partner_autocomplete.AccountTokenMissingNotification">
        <div class="">
            <a class="btn btn-link" t-att-href="account_url" ><i class="oi oi-arrow-right"/> Set Your Account Token</a>
        </div>
    </t>
</templates>
