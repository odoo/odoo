<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="purchase_order_view_form_inherit" model="ir.ui.view">
        <field name="name">purchase.order.form.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_form" />
        <field name="arch" type="xml">
            <separator name="operations" position="before">
                <button
                    name="action_receipt_matching"
                    type="object"
                    string="Match Receipts"
                    groups="stock.group_stock_user"
                    invisible="state != 'done' or transfer_state in ('no', 'done')" />
            </separator>
            <button name="action_view_invoice" position="before">
                <button
                    name="action_view_picking"
                    type="object"
                    icon="fa-truck"
                    groups="stock.group_stock_user"
                    invisible="count_transfer_incoming == 0"
                    class="oe_stat_button">
                    <field
                        name="count_transfer_incoming"
                        string="Receipt"
                        help="Incoming Shipments"
                        widget="statinfo" />
                </button>
            </button>
            <xpath expr="//field[@name='currency_id']" position="after">
                <field name="is_shipped" invisible="1" />
            </xpath>
            <xpath expr="//div[@name='date_commitment_div']" position="inside">
                <div>
                    <button
                        name="%(action_purchase_vendor_delay_report)d"
                        type="action"
                        context="{'search_default_partner_id': partner_id}"
                        invisible="state == 'done' or not partner_id"
                        class="oe_link">
                        <span invisible="on_time_rate &lt; 0"><field name="on_time_rate" digits="[42, 0]" class="oe_inline" />% On-Time Delivery</span>
                        <span invisible="on_time_rate &gt;= 0">No data yet</span>
                    </button>
                </div>
            </xpath>
            <xpath expr="//label[@for='receipt_reminder_email']" position="attributes">
                <attribute name="invisible">date_effective</attribute>
            </xpath>
            <xpath expr="//div[@name='reminder']" position="attributes">
                <attribute name="invisible">date_effective</attribute>
            </xpath>
            <xpath expr="//div[@name='reminder']" position="after">
                <field name="date_effective" invisible="not date_effective" />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/form//field[@name='invoice_line_ids']"
                position="after">
                <field name="move_ids" />
            </xpath>
            <field name="invoice_state" position="before">
                <field name="transfer_state" invisible="state != 'done'" />
            </field>
            <xpath
                expr="//field[@name='line_ids']/form//field[@name='analytic_distribution']"
                position="before">
                <field name="propagate_cancel" groups="base.group_no_one" />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/list//field[@name='date_commitment']"
                position="after">
                <field name="move_dest_ids" column_invisible="True" />
            </xpath>
            <xpath
                expr="//page/field[@name='line_ids']/list/field[@name='product_qty']"
                position="after">
                <field name="forecasted_issue" column_invisible="True" />
                <button
                    name="action_product_forecast_report"
                    type="object"
                    title="Forecast Report"
                    icon="fa-area-chart"
                    invisible="not id or not forecasted_issue or not is_storable"
                    class="text-danger" />
                <button
                    name="action_product_forecast_report"
                    type="object"
                    title="Forecast Report"
                    icon="fa-area-chart"
                    invisible="not id or forecasted_issue or not is_storable" />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/list//field[@name='analytic_distribution']"
                position="before">
                <field
                    name="propagate_cancel"
                    optional="hide"
                    groups="base.group_no_one" />
            </xpath>
            <xpath
                expr="//field[@name='line_ids']/list//field[@name='qty_transferred']"
                position="attributes">
                <attribute name="column_invisible">parent.state != 'done'</attribute>
                <attribute name="readonly">product_type == 'consu'</attribute>
            </xpath>
            <xpath
                expr="//page[@name='other_information']/group/group"
                position="inside">
                <field name="default_location_dest_id_usage" invisible="1" />
                <field name="incoterm_id" readonly="locked" />
                <field name="incoterm_location" readonly="locked" />
            </xpath>
            <xpath expr="//div[@name='reminder']" position="after">
                <field
                    name="picking_type_id"
                    domain="[('code','=','incoming'), '|', ('warehouse_id', '=', False), ('warehouse_id.company_id', '=', company_id)]"
                    options="{'no_create': True}"
                    readonly="state in ['done', 'cancel']" />
                <field
                    name="dest_address_id"
                    invisible="default_location_dest_id_usage != 'customer'"
                    readonly="locked"
                    required="default_location_dest_id_usage == 'customer'" />
            </xpath>
        </field>
    </record>

    <record id="view_purchase_order_list_2_inherit" model="ir.ui.view">
        <field name="name">purchase.order.list.inherit</field>
        <field name="model">purchase.order</field>
        <field name="inherit_id" ref="purchase.view_purchase_order_list_2" />
        <field name="arch" type="xml">
            <field name="invoice_state" position="before">
                <field name="date_effective" column_invisible="True" />
                <field
                    name="transfer_state"
                    widget="badge"
                    optional="hide"
                    decoration-danger="date_commitment and (date_commitment &lt; date_effective or date_commitment &lt; datetime.datetime.combine(datetime.date.today(), datetime.time(0,0,0)).to_utc().strftime('%Y-%m-%d %H:%M:%S') and transfer_state!='done' and date_effective &lt;= date_commitment)"
                    decoration-success="transfer_state=='done'"
                    decoration-warning="date_commitment and date_commitment &gt;= datetime.datetime.combine(datetime.date.today(), datetime.time(0,0,0)).to_utc().strftime('%Y-%m-%d %H:%M:%S') and date_commitment &lt;= datetime.datetime.combine(datetime.date.today(), datetime.time(23,59,59)).to_utc().strftime('%Y-%m-%d %H:%M:%S') and transfer_state!='done'" />
            </field>
            <xpath expr="//field[@name='date_commitment']" position="attributes">
                <attribute name="decoration-danger">date_commitment and (date_commitment &lt;
                    date_effective or date_commitment &lt;
                    datetime.datetime.combine(datetime.date.today(),
                    datetime.time(0,0,0)).to_utc().strftime('%Y-%m-%d %H:%M:%S') and
                    transfer_state!='done' and date_effective &lt;= date_commitment)</attribute>
            </xpath>
        </field>
    </record>

</odoo>
