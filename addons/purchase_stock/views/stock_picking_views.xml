<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="stock.action_picking_tree_incoming" model="ir.actions.act_window">
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face o_view_nocontent_stock">
                    No receipt yet! Automate them with purchase orders.
                </p>
                <p class="text-muted">
                    Reduce stockouts with alerts, barcode app, replenishment propositions, etc.
                </p>
                <a class="btn btn-secondary" name="purchase.purchase_form_action" type="action">
                    Purchase Orders
                </a>
            </field>
    </record>

    <record model="ir.ui.view" id="view_picking_form">
        <field name="name">purchase.stock.view.picking.form</field>
        <field name="model">stock.picking</field>
        <field name="inherit_id" ref="stock.view_picking_form"></field>
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_return']" position="attributes">
                <attribute name="invisible">not show_return or not (state == 'done' or purchase_id)</attribute>
            </xpath>
        </field>
    </record>

</odoo>
