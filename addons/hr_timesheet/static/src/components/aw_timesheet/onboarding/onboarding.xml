<t t-name="hr_timesheet.OnboardingPage">
    <div class="container mt-4">
        <h4>ActivityWatch Onboarding</h4>

        <div class="mb-3">
            <div>Detected OS: <strong t-esc="state.os"/></div>
            <div>Detected browser: <strong t-esc="state.browser"/></div>
      </div>

      <!-- Browser extension -->
      <div class="form-check mb-3">
        <input class="form-check-input watcher" type="checkbox" value="aw-watcher-web" t-on-change="toggleWebWatcher" t-att-checked="state.installWeb"/>
        <label class="form-check-label">Install Browser Extension (recommended for better results)</label>
      </div>

        <!-- Developer -->
        <div class="form-check mb-2">
          <input class="form-check-input" type="checkbox" t-on-change="toggleDev"
                t-model="state.isDev" id="isDev"/>
          <label class="form-check-label" for="isDev">I am a developer</label>
        </div>

        <div t-if="state.isDev" id="ide-section" class="mb-3 ms-4">
          <div class="fw-bold">Which IDE do you use?</div>

          <div class="form-check">
            <input class="form-check-input ide-checkbox" type="checkbox" value="vscode"
                  t-on-change="toggleIDE"
                  t-att-checked="state.ideSelections.includes('vscode')"/>
            <label class="form-check-label">VSCode</label>
          </div>

          <div class="form-check">
            <input class="form-check-input ide-checkbox" type="checkbox" value="vim"
                  t-on-change="toggleIDE"
                  t-att-checked="state.ideSelections.includes('vim')"/>
            <label class="form-check-label">Vim</label>
          </div>

          <div class="form-check">
            <input class="form-check-input ide-checkbox" type="checkbox" value="jetbrains"
                  t-on-change="toggleIDE"
                  t-att-checked="state.ideSelections.includes('jetbrains')"/>
            <label class="form-check-label">JetBrains IDEs (IDEA, PyCharm, etc.)</label>
          </div>
        </div>

      <div class="mb-3">
        <button class="btn btn-secondary btn-sm text-nowrap" t-on-click="() => this.generateScript()">
          <i class="fa fa-download" aria-hidden="true" /> Download Configuration
        </button>
      </div>
    </div>
  </t>
