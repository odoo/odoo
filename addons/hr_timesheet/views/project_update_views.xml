<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="project_update_view_search_inherit" model="ir.ui.view">
            <field name="name">project.update.view.search.inherit</field>
            <field name="model">project.update</field>
            <field name="inherit_id" ref="project.project_update_view_search"/>
            <field name="arch" type="xml">
                <xpath expr="//filter[@name='my_updates']" position='after'>
                    <filter string="My Team's Updates" name="my_team_updates" domain="[('user_id.employee_id.parent_id.user_id', '=', uid)]"/>
                    <filter string="My Department's Updates" name="my_department_updates" domain="[('user_id.employee_id.member_of_department', '=', True)]"/>
                </xpath>
            </field>
        </record>
        <record id="project_update_view_kanban_inherit" model="ir.ui.view">
            <field name="name">project.update.view.kanban.inherit</field>
            <field name="model">project.update</field>
            <field name="inherit_id" ref="project.project_update_view_kanban"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='closed_task_percentage']" position="after">
                    <field name="display_timesheet_stats"/>
                    <field name="timesheet_percentage"/>
                </xpath>
                <div id="tasks_stats" position="after">
                    <div t-if="record.display_timesheet_stats.raw_value"
                         t-attf-class="{{ record.timesheet_percentage.raw_value > 100 ? 'text-danger' : (record.timesheet_percentage.raw_value >= 80 ? 'text-warning' : '') }}">
                        <span class="fa fa-fw fa-clock-o me-1" title="Time"/><field name="timesheet_time"/>
                        <t t-if="record.allocated_time.raw_value">
                            / <field name="allocated_time"/>
                        </t> <field name="uom_id"/>
                    </div>
                </div>
            </field>
        </record>
    </data>
</odoo>
