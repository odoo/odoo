<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_partner_bank_form" model="ir.ui.view">
            <field name="name">res.partner.bank.form</field>
            <field name="model">res.partner.bank</field>
            <field name="priority">15</field>
            <field name="arch" type="xml">
                <form string="Bank account" name="bank_account_form" create="0">
                    <sheet>
                        <widget name="web_ribbon" title="Archived" bg_color="text-bg-danger" invisible="active"/>
                        <group>
                            <group>
                                <field name="partner_id" invisible="1"/> <!-- Needed for default partner to be set for newly created records -->
                                <field name="account_number"/>
                                <div class="d-flex" colspan="1" invisible="not show_clearing_number">
                                    <field name="clearing_label_id" class="p-0" options="{'no_create_edit': True, 'no_open': True}"/>
                                    <label for="clearing_number" string=""/>
                                </div>
                                <div colspan="1" invisible="not show_clearing_number">
                                    <field name="clearing_number"/>
                                </div>
                                <field name="bank_bic"/>
                                <field name="holder_name"/>
                            </group>
                            <group>
                                <label for="allow_out_payment"/>
                                <div class="d-flex gap-2">
                                    <field name="allow_out_payment" widget="boolean_toggle" options="{'autosave': False}" class="w-25"/>
                                    <span class="d-flex gap-2 w-100">
                                        <span invisible="allow_out_payment" class="text-muted">Untrusted</span>
                                        <span invisible="not allow_out_payment" class="text-success">Trusted</span>
                                    </span>
                                </div>
                                <field name="company_id" placeholder="Visible to all" groups="base.group_multi_company" options="{'no_create': True}"/>
                            </group>
                        </group>
                        <notebook>
                            <page string="Note" name="internal_notes">
                                <field name="note" nolabel="1" placeholder="Internal notes..."/>
                            </page>
                            <page name="bank_information" string="Bank Information">
                                <group>
                                    <group name="bank_details">
                                        <field name="bank_name" string="Bank Name" placeholder="e.g. Bank of America"/>
                                        <label for="street" string="Bank Address"/>
                                        <div class="o_address_format">
                                            <field name="street" placeholder="Street..." class="o_address_street"/>
                                            <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                                            <field name="city" placeholder="City" class="o_address_city"/>
                                            <field name="state_id" placeholder="State" class="o_address_state"
                                                options='{"no_open": True}'
                                            />
                                            <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                                            <field name="country_id" placeholder="Country" class="o_address_country"
                                                options='{"no_open": True, "no_create": True}'
                                            />
                                        </div>
                                    </group>
                                </group>
                            </page>
                        </notebook>
                    </sheet>
                </form>
            </field>
        </record>

        <record id="view_partner_bank_tree" model="ir.ui.view">
            <field name="name">res.partner.bank.list</field>
            <field name="model">res.partner.bank</field>
            <field name="arch" type="xml">
                <list string="Bank Accounts" create="false" multi_edit="1" decoration-muted="(not active)">
                    <field name="sequence" widget="handle"/>
                    <field name="account_number"/>
                    <field name="partner_id" optional="hide"/>
                    <field name="holder_name" optional="hide"/>
                    <field name="bank_name" string="Bank" optional="hide"/>
                    <field name="bank_bic" optional="show"/>
                    <field name="clearing_number" optional="hide"/>
                    <field name="company_id" groups="base.group_multi_company" optional="hide"/>
                    <field name="allow_out_payment" widget="boolean_toggle" optional="show"/>
                    <field name="active" widget="boolean_toggle" optional="hide"/>
                </list>
            </field>
        </record>

        <record id="view_partner_bank_search" model="ir.ui.view">
            <field name="name">res.partner.bank.search</field>
            <field name="model">res.partner.bank</field>
            <field name="arch" type="xml">
                <search string="Bank Accounts">
                    <field name="bank_name" filter_domain="['|', ('bank_name','ilike',self), ('account_number','ilike',self)]" string="Bank Name"/>
                    <field name="company_id" invisible="context.get('company_hide', True)"/>
                    <field name="partner_id"/>
                    <separator/>
                    <filter name="inactive" string="Archived" domain="[('active','=',False)]" help="Show inactive bank account"/>
                </search>
            </field>
        </record>

        <record id="action_res_partner_bank_account_form" model="ir.actions.act_window">
            <field name="name">Bank Accounts</field>
            <field name="res_model">res.partner.bank</field>
            <field name="view_mode">list,form</field>
            <field name="help" type="html">
                <p class="o_view_nocontent_smiling_face">
                    Create a Bank Account
                </p>
                <p>
                    From here you can manage all bank accounts linked to you and your contacts.
                </p>
            </field>
        </record>

    </data>
</odoo>
