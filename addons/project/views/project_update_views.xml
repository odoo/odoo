<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="project_update_view_form" model="ir.ui.view">
        <field name="name">project.update.view.form</field>
        <field name="model">project.update</field>
        <field name="arch" type="xml">
            <form string="Project Update" create="0">
                <sheet>
                    <div class="oe_title">
                        <h1>
                            <field name="name" class="o_text_overflow" placeholder="Title of the Update"/>
                        </h1>
                    </div>
                    <group>
                        <group>
                            <field name="project_id" invisible="1"/>
                            <field name="color" invisible="1"/>
                            <field name="status_id" widget="status_with_color" domain="[('project_ids','=', project_id)]"
                                    context="{'default_project_id': project_id}" options="{'no_open': 1, 'no_create': 1, 'color_field': 'color'}"
                                    />
                            <field name="progress" widget="basic_progressbar"/>
                            <field name="user_id" readonly="1"/>
                            <field name="date"/>
                        </group>
                    </group>
                    <separator class="form-divider-h"/>
                    <field name="description"/>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" options="{'post_refresh':True}" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div>
            </form>
        </field>
    </record>

    <record id="project_update_view_kanban" model="ir.ui.view">
        <field name="name">project.update.view.kanban</field>
        <field name="model">project.update</field>
        <field name="arch" type="xml">
            <kanban class="o_pupdate_kanban" sample="1" js_class="project_update_kanban">
                <field name="color"/>
                <templates>
                    <t t-name="kanban-box">
                        <div t-attf-class="{{!selection_mode ? 'oe_kanban_color_' + kanban_getcolor(record.color.raw_value) : ''}} oe_kanban_global_click o_pupdate_kanban_card">
                            <!-- Project Update Kanban View is always ungrouped - see js_class -->
                            <div class="o_kanban_detail_ungrouped row mx-0">
                                <div class="col-lg-2 o_wvisitor_name mr-2">
                                    <h4><field name="name"/></h4>
                                </div>
                                <div class="col-lg-2 mx-2">
                                    <field name="color" invisible="1"/>
                                    <field name="status_id" widget="status_with_color" options="{'no_open': 1, 'color_field': 'color'}"/>
                                </div>
                                <div class="col-lg-2 mx-2">
                                    <field name="progress" widget="basic_progressbar"/>
                                    <div>Progress</div>
                                </div>
                                <div class="col-lg-2 mx-2">
                                    <b><field name="date"/></b>
                                    <div>Date</div>
                                </div>
                                <div class="col-lg-2 mx-2">
                                    <b><field name="user_id" widget="many2one_avatar_user"/></b>
                                    <b><t t-esc="record.user_id.value"/></b>
                                    <div>Author</div>
                                </div>
                                <separator class="form-divider-h"/>
                                <div class="col-lg-4 mr-2"> 
                                    <field name="description" widget="project_html_see_more"/>
                                </div>
                            </div>
                        </div>
                    </t>
                </templates>
            </kanban>
        </field>
    </record>

    <record id="project_update_status_view_form" model="ir.ui.view">
        <field name="name">project.update.status.view.form</field>
        <field name="model">project.update.status</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="color" widget="color_picker"/>
                            <field name="default"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="project_update_view_tree" model="ir.ui.view">
        <field name="name">project.update.view.tree</field>
        <field name="model">project.update</field>
        <field name="arch" type="xml">
            <tree create="0">
                <field name="name"/>
                <field name="status_id" widget="status_with_color"/>
                <field name="date"/>
            </tree>
        </field>
    </record>

    <record id="open_project_update_form" model="ir.actions.act_window">
        <field name="name">Project Update</field>
        <field name="res_model">project.update</field>
        <field name="view_mode">form</field>
    </record>

    <record id="project_update_posted_kanban" model="ir.actions.act_window">
        <field name="name">Project Updates</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">project.update</field>
        <field name="view_mode">kanban,form</field>
        <field name="domain">[('project_id', '=', active_id)]</field>
        <field name="help" type="html">
          <p class="o_view_nocontent_smiling_face">
            Wait for visitors to come to your website to see their history.
          </p>
        </field>
    </record>
</odoo>
