<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="portal_layout" name="Portal layout: project menu entry" inherit_id="portal.portal_layout" priority="40">
        <xpath expr="//t[@t-call='portal.portal_breadcrumbs']" position="replace">
            <t t-call="project.portal_breadcrumbs"/>
        </xpath>
    </template>

    <template id="portal_breadcrumbs" name="Portal layout: project menu entry" inherit_id="portal.portal_breadcrumbs" priority="40">
        <xpath expr="//ol[hasclass('o_portal_submenu')]" position="inside">
            <li t-if="page_name == 'project' or project or task" class="breadcrumb-item #{'active ' if not project and not task else ''}">
                <a t-if="project or task" t-attf-href="/my/projects">Projects</a>
                <t t-else="">Projects</t>
            </li>
            <li t-if="project" class="breadcrumb-item active">
                <t t-esc="project.name"/>
            </li>
            <li t-if="page_name == 'task' or task" class="breadcrumb-item #{'active ' if not task else ''}">
                <a t-if="task" t-attf-href="/my/project/{{ task.project_id.id }}?{{ keep_query() }}"
                   t-esc="task.project_id.name"/>
                <a t-else="">Tasks</a>
            </li>
            <li t-if="task" class="breadcrumb-item active">
                <span t-field="task.name"/>
            </li>
        </xpath>
    </template>

    <template id="portal_my_home" name="Portal My Home: project entries" inherit_id="portal.portal_my_home" priority="40">
        <xpath expr="//div[hasclass('o_portal_docs')]" position="inside">
            <t t-if="project_count" t-call="portal.portal_docs_entry">
                <t t-set="title">Projects</t>
                <t t-set="url" t-value="'/my/projects'"/>
                <t t-set="count" t-value="project_count"/>
            </t>
            <t t-if="task_count" t-call="portal.portal_docs_entry">
                <t t-set="title">Tasks</t>
                <t t-set="url" t-value="'/my/tasks'"/>
                <t t-set="count" t-value="task_count"/>
            </t>
        </xpath>
    </template>

    <template id="portal_my_projects" name="My Projects">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Projects</t>
            </t>
            <t t-if="not projects">
                <div class="alert alert-warning mt8" role="alert">
                    There are no projects.
                </div>
            </t>
            <t t-if="projects" t-call="portal.portal_table">
                <tbody>
                    <tr t-foreach="projects" t-as="project">
                        <td>
                            <a t-attf-href="/my/project/#{project.id}?{{ keep_query() }}"><span t-field="project.name"/></a>
                        </td>
                        <td class="text-right">
                            <a t-attf-href="/my/tasks?{{keep_query('debug', filterby=project.id)}}">
                                <t t-esc="project.task_count" />
                                <t t-esc="project.label_tasks" />
                            </a>
                        </td>
                    </tr>
                </tbody>
            </t>
        </t>
    </template>

    <template id="portal_my_project" name="My Project">
        <t t-call="project.portal_layout">
            <t t-set="title">Tasks</t>
            <iframe t-attf-src="/embed/project/{{ project.id }}?{{ keep_query() }}&amp;website_id={{ website_id }}"
                    class="o_project_portal_iframe"/>
        </t>
    </template>

    <template id="portal_my_tasks" name="My Tasks">
        <t t-call="portal.portal_layout">
            <t t-set="breadcrumbs_searchbar" t-value="True"/>

            <t t-call="portal.portal_searchbar">
                <t t-set="title">Tasks</t>
            </t>
            <t t-if="not grouped_tasks">
                <div class="alert alert-warning mt8" role="alert">
                    There are no tasks.
                </div>
            </t>
            <t t-if="grouped_tasks">
                <t t-call="portal.portal_table">
                    <t t-foreach="grouped_tasks" t-as="tasks">
                        <thead>
                            <tr t-attf-class="{{'thead-light' if not groupby == 'none' else ''}}">
                                <th t-if="groupby == 'none'">Name</th>
                                <th t-else="">
                                    <em class="font-weight-normal text-muted"><span t-field="tasks[0].sudo().project_id.label_tasks"/> for project:</em>
                                    <span t-field="tasks[0].sudo().project_id.name"/></th>
                                <th class="text-center">Stage</th>
                                <th class="text-left">Ref</th>
                            </tr>
                        </thead>
                        <tbody>
                            <t t-foreach="tasks" t-as="task">
                                <tr>
                                    <td>
                                        <a t-attf-href="/my/task/#{task.id}?{{ keep_query() }}"><span t-field="task.name"/></a>
                                    </td>
                                    <td class="text-center">
                                        <span class="badge badge-pill badge-info" title="Current stage of the task" t-esc="task.stage_id.name" />
                                    </td>
                                    <td class="text-left">
                                        #<span t-esc="task.id"/>
                                    </td>
                                </tr>
                            </t>
                        </tbody>
                    </t>
                </t>
            </t>
        </t>
    </template>

    <template id="portal_my_task" name="My Task">
        <t t-call="project.portal_layout">
            <iframe t-attf-src="/embed/project/{{ task.project_id.id }}/{{ task.id }}?{{ keep_query() }}&amp;website_id={{ website_id }}"
                    class="o_project_portal_iframe"/>
        </t>
    </template>

    <template id="portal_project">
        &lt;!DOCTYPE html&gt;
        <html>
            <head>
                <meta http-equiv="X-UA-Compatible" content="IE=edge,chrome=1"/>
                <meta http-equiv="content-type" content="text/html; charset=utf-8" />
                <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
                <base target="_parent" />

                <t t-call-assets="web.assets_common"/>
                <t t-call-assets="web.assets_backend"/>
                <t t-call-assets="web_editor.summernote"/>
                <t t-call-assets="web_editor.assets_editor"/>
                <script type="text/javascript" src="/project/static/src/js/portal_web_client.js"></script>
                <t t-call-assets="project.assets_public_project"/>
            </head>
            <body class="o_project_portal_iframe_content o_web_client">
                <portal-project t-att-data-params="json.dumps(params)" t-att-data-options="json.dumps({'search': true})"/>
            </body>
        </html>
    </template>
</odoo>
