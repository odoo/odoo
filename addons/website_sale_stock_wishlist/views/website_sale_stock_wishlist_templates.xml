<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="product_wishlist" inherit_id="website_sale_wishlist.product_wishlist">
        <xpath expr="//p[hasclass('o_wsale_wish_item_description')]" position="after">
            <small class="text-danger mb-2" t-if="wish.product_id._is_sold_out() and not wish.product_id.allow_out_of_stock_order">Temporarily out of stock</small>
        </xpath>
        <xpath expr="//div[hasclass('o_wsale_wish_grid_content')]/div" position="after">
            <small class="text-danger mb-2" t-if="wish.product_id._is_sold_out() and not wish.product_id.allow_out_of_stock_order">Temporarily out of stock</small>
        </xpath>
    </template>
    <template id="product_wishlist_cta" inherit_id="website_sale_wishlist.product_wishlist_cta">
        <xpath expr="//button[@id='add_to_cart_button']" position="replace">
            <div>
                <button
                        t-if="not combination_info['prevent_zero_price_sale']"
                        id="add_to_cart_button"
                        class="o_wish_add btn btn-outline-primary text-nowrap"
                        t-att-disabled="is_sold_out"
                >
                        Add to Cart
                </button>
                <t t-if="not wish.product_id.allow_out_of_stock_order and wish.product_id._is_sold_out()">
                    <t
                        t-set="has_stock_notification"
                        t-value="wish.product_id._has_stock_notification(wish.partner_id) or request and wish.product_id.id in request.session.get('product_with_stock_notification_enabled', set())"
                    />
                    <div id="stock_notification_div"
                    t-if="not wish.product_id.allow_out_of_stock_order"
                        class="small mt-2">
                        <div
                            t-if="not has_stock_notification"
                            class="btn btn-link px-0"
                            id="wishlist_stock_notification_message"
                        >
                            <small>
                                <i class="fa fa-envelope-o me-2"/>Get notified when back in stock
                            </small>
                        </div>
                        <div id="stock_notification_form" class="d-none">
                            <div class="input-group">
                                <input
                                    id="stock_notification_input"
                                    class="form-control"
                                    name="email"
                                    type="text"
                                    placeholder="youremail@gmail.com"
                                    t-att-value="request.env.user.partner_id.email or request.session.get('stock_notification_email', '')"
                                />
                                <div
                                    id="wishlist_stock_notification_form_submit_button"
                                    class="btn btn-primary"
                                >
                                    <i class="fa fa-paper-plane"/>
                                </div>
                            </div>
                            <div
                                id="stock_notification_input_incorrect"
                                class="d-none form-text text-danger"
                            >
                                Invalid email
                            </div>
                        </div>
                        <div id="stock_notification_success_message" class="text-muted"
                            t-att-class="'text-muted' if has_stock_notification else 'd-none'">
                            <i class="fa fa-bell"/>
                            We'll notify you once the product is back in stock.
                        </div>
                    </div>
                </t>
            </div>
        </xpath>
    </template>
</odoo>
