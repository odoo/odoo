<?xml version="1.0"?>
<odoo>

    <record id="view_order_form_inherit_sale_stock" model="ir.ui.view">
        <field name="name">sale.order.form.sale.stock</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form" />
        <field name="arch" type="xml">
            <button name="action_view_invoice" position="before">
                <button
                    name="action_view_delivery"
                    type="object"
                    class="oe_stat_button"
                    icon="fa-truck"
                    invisible="delivery_count == 0"
                    groups="stock.group_stock_user"
                >
                    <field name="delivery_count" widget="statinfo" string="Delivery" />
                </button>
            </button>
            <group name="sale_shipping" position="attributes">
                <attribute name="groups"></attribute><!-- Remove the res.group on the group and set
                it on the field directly-->
                <attribute name="string">Delivery</attribute>
            </group>
            <label for="date_commitment" position="before">
                <field name="warehouse_id" options="{'no_create': True}" force_save="1"
                    readonly="state == 'done'" />
                <field name="incoterm" options="{'no_open': True, 'no_create': True}" />
                <field name="incoterm_location" />
                <field name="picking_policy" required="True"
                    readonly="state not in ['draft', 'sent']" />
            </label>
            <span name="expected_date_span" position="attributes">
                <attribute name="invisible">date_effective and date_commitment</attribute>
            </span>
            <div name="date_commitment_div" position="replace">
                <div class="o_row">
                    <field name="date_commitment" />
                    <span class="text-muted"
                        invisible="not date_planned or (date_effective and date_commitment)">
                        Expected: <field name="date_planned" class="oe_inline" /></span>
                </div>
                <field name="date_effective" invisible="not date_effective" />
                <field name="transfer_state" invisible="state != 'done'" />
            </div>
            <xpath expr="//page[@name='other_information']//field[@name='date_planned']"
                position="after">
                <field string=" " name="json_popover" widget="stock_rescheduling_popover"
                    invisible="not show_json_popover" />
            </xpath>
            <xpath expr="//field[@name='line_ids']//form//field[@name='analytic_distribution']"
                position="before">
                <field name="route_ids" widget="many2many_tags" groups="stock.group_adv_location"
                    options="{'no_create': True}" />
            </xpath>
            <xpath expr="//field[@name='line_ids']//form//field[@name='product_uom_id']"
                position="after">
                <widget name="qty_at_date_widget" />
            </xpath>
            <xpath expr="//field[@name='line_ids']//list//field[@name='analytic_distribution']"
                position="after">
                <field
                    name="route_ids"
                    widget="many2many_tags"
                    groups="stock.group_adv_location"
                    options="{'no_create': True}"
                    optional="hide" />
            </xpath>
            <xpath expr="//field[@name='line_ids']//list//field[@name='qty_transferred']"
                position="after">
                <widget name="qty_at_date_widget" width="20px" />
            </xpath>
        </field>
    </record>

    <record id="sale_order_tree" model="ir.ui.view">
        <field name="name">sale.order.list.inherit.sale.stock</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_tree" />
        <field name="arch" type="xml">
            <field name="tag_ids" position="after">
                <field
                    name="warehouse_id"
                    options="{'no_create': True}"
                    readonly="state == 'done'"
                    groups="stock.group_stock_multi_warehouses"
                    optional="hide"
                />
            </field>
        </field>
    </record>

    <record id="view_order_tree" model="ir.ui.view">
        <field name="name">sale.order.list.inherit.sale.stock</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_tree" />
        <field name="arch" type="xml">
            <field name="date_commitment" position="attributes">
                <attribute name="decoration-danger">
                    (
                    (
                    date_commitment &lt; date_effective
                    or date_commitment &lt; datetime.datetime.combine(
                    datetime.date.today(),
                    datetime.time(0,0,0)
                    ).to_utc().strftime('%Y-%m-%d %H:%M:%S')
                    )
                    and transfer_state in ['to do', 'partial']
                    and date_effective &lt;= date_commitment
                    or transfer_state == 'partial'
                    )
                </attribute>
            </field>
            <field name="invoice_state" position="before">
                <field
                    name="transfer_state"
                    decoration-success="transfer_state == 'done'"
                    decoration-warning="transfer_state == 'partial'"
                    decoration-info="transfer_state in ['to do', 'partial']"
                    widget="badge"
                    optional="hide" />
            </field>
        </field>
    </record>

    <record id="sale_stock_sale_order_view_search_inherit" model="ir.ui.view">
        <field name="name">sale_stock.sale.order.search.inherit</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.sale_order_view_search_inherit_sale" />
        <field name="arch" type="xml">
            <filter name="order_date" position="after">
                <filter string="Late Availability" name="late_availability"
                    domain="[('late_availability', '=', True)]" />
            </filter>
        </field>
    </record>

</odoo>