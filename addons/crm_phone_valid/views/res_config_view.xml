<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- inherit view of Lead Form : adding the "convert to issue" button -->
    <record id="sale_config_settings_form_crm_phone_validate" model="ir.ui.view">
        <field name="name">sale.config.settings.inherit.phone.validate</field>
        <field name="model">sale.config.settings</field>
        <field name="inherit_id" ref="crm.sale_config_settings_view_form_inherit_crm" />
        <field name="arch" type="xml">
            <xpath expr="//div[@id='mod_crm_phone_valid']" position="replace">
                <div class="mt16" id="mod_crm_phone_valid" attrs="{'invisible': [('module_crm_phone_valid','=',False)]}">
                    <label for="crm_phone_valid_method"/>
                    <field name="crm_phone_valid_method"/>
                </div>
            </xpath>
        </field>
    </record>

</odoo>
