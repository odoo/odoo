<?xml version="1.0"?>
<odoo>

    <record id="sale_order_view_form" model="ir.ui.view">
        <field name="name">sale.order.form</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="sale.view_order_form"/>
        <field name="arch" type="xml">
            <field name="user_id" position="after">
                <field name="carrier_id" invisible="1"/> <!-- will be used by pickup_location_field widget-->
            </field>
            <field name='partner_shipping_id' position="attributes">
                <attribute name="invisible" add="is_pickup_required" separator="or"/>
            </field>
            <field name='partner_shipping_id' position="after">
                <field
                    name="partner_shipping_id"
                    string="Pickup Point"
                    invisible="not is_pickup_required"
                    widget="pickup_location_many2one"
                />
            </field>
        </field>
    </record>

    <record id="sale_order_list_view_ecommerce" model="ir.ui.view">
        <field name="name">website.sale.order.list.inherit.website.sale.stock</field>
        <field name="model">sale.order</field>
        <field name="inherit_id" ref="website_sale.sale_order_list_view_ecommerce"/>
        <field name="arch" type="xml">
            <field name="tag_ids" position="after">
                <field
                    name="warehouse_id"
                    options="{'no_create': True}"
                    readonly="state == 'sale'"
                    groups="stock.group_stock_multi_warehouses"
                    optional="hide"
                />
            </field>
        </field>
    </record>

</odoo>
