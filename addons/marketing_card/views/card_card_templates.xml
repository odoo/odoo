<?xml version="1.0"?>
<odoo>
<!--Notable pitfalls-->
<!--Unpatched wkhtmltoimage uses an old version of webkit, meaning things like standard flexbox
    are not supported and -webkit flags should be used.-->
<!--Your local fonts may diverge from production fonts, make sure the designs work as intended
    in their final production environment.-->
<template id="marketing_card.common_style">
    <t t-if="dark_mode">
        <t t-set="template_primary_text_color">#ffffff</t>
        <t t-set="template_secondary_text_color">#161315</t>
        <t t-set="template_primary_color">#161315</t>
        <t t-set="template_secondary_color">#dedede</t>
    </t>
    <t t-else="">
        <t t-set="template_primary_text_color">#000000</t>
        <t t-set="template_secondary_text_color">#ffffff</t>
        <t t-set="template_primary_color">#f9f9f9</t>
        <t t-set="template_secondary_color">#000000</t>
    </t>
    <style>
    body {
        @charset "UTF-8";
        padding: 80px;
        font-family: Roboto, Ubuntu, "Noto Sans", Arial, sans-serif, "Segoe UI Emoji", "Segoe UI Symbol", "Noto Color Emoji";
        font-size: 2em;
        background: <t t-out="template_primary_color"/>;
        color: <t t-out="template_primary_text_color"/>;
        background-size: contain;
        background-repeat: no-repeat;
        background-position: center center;
        background-attachment: fixed;
        display: -webkit-box;
        display: flex;
        -webkit-box-orient: vertical;
        flex-direction: column;
        -webkit-box-pack: justify;
        justify-content: space-around;
    }
    .button_background {
        background: <t t-out="template_secondary_color"/>; color: <t t-out="template_secondary_text_color"/>;
    }
    .header {
        -webkit-line-clamp: 2;
        max-height: 2.5em;
        line-height: 1.2em;
    }
    .header, .subheader, .cta_button h1 {
        display: -webkit-box;
        -webkit-box-orient: vertical;
        overflow: hidden;
    }
    .header, .subheader, .cta_button h1 {
        margin: 0.1em;
    }
    .subheader, .cta_button h1{
        -webkit-line-clamp: 1;
        max-height: 2em;
        line-height: 1.2em;
    }
    .cta_button {
        text-align: center;
        width: fit-content;
        min-width: 30%;
        max-width: 50%;
        padding: 0.2rem 1rem;
    }
    .rounded_button {
        border-radius: 100px;
    }
    .rounded_rectangle_button {
        border-radius: 16px;
    }
    .footer {
        width: 100%;
        display: -webkit-box;
        display: flex;
        -webkit-box-orient: horizontal;
        flex-direction: row;
        -webkit-box-pack: justify;
        justify-content: space-between;
    }
    .footer &gt; div {
        height: 100%;
    }
    .profile_section {
        margin-right: 0.5rem;
        font-size: 2.5rem;
        display: -webkit-box;
        display: flex;
        -webkit-box-orient: horizontal;
        flex-direction: row;
        -webkit-box-align: center;
        align-items: center;
    }
    .text_section {
        display: -webkit-box;
        display: flex;
        -webkit-box-orient: vertical;
        flex-direction: column;
        -webkit-box-align: start;
        align-items: flex-start;
        align-content: flex-start;
        -webkit-box-pack: center;
        justify-content: center;
    }
    .profile_section .text_section .text_subsections {
        font-size: 2rem;
        margin-left: 1rem;
    }
    </style>
</template>

<template id="marketing_card.template_1">
    <t t-set="role_values" t-value="_get_role_values(elements, object, default_values, preview_values)"/>
    <html>
        <head>
            <meta charset="utf-8"/>
            <t t-call="marketing_card.common_style">
                <t t-set="dark_mode" t-value="dark_mode"/>
            </t>
            <style>
                body {
                    -webkit-box-align: center;
                    align-items: center;
                }
                .center_column {
                    text-align: center;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: vertical;
                    flex-direction: column;
                    -webkit-box-align: center;
                    align-items: center;
                    -webkit-box-pack: justify;
                    justify-content: space-around;
                    line-height: 4rem;
                    height: 60%;
                    width: 100%;
                }
                .center_column .cta_button {
                    min-width: 50%;
                    max-width: 70%;
                }
                .footer {
                    -webkit-box-align: center;
                    align-items: center;
                    height: 30%;
                }
                .profile_section img {
                    object-fit: cover;
                    border-radius: 100px;
                }
                .profile_section .text_section span {
                    display: block;
                }
                .logo {
                    max-height: 80px;
                    width: auto;
                }
            </style>
        </head>
        <body t-attf-style="background-image: url('data:image/png;base64,{{role_values['background']}}');">
            <div class="center_column">
                <div class="title">
                    <h1 class="header" t-out="role_values['header']" t-att-style="'color: %s;' % elements['header'].text_color"></h1>
                    <h2 class="subheader" t-out="role_values['subheader']" t-att-style="'color: %s;' % elements['subheader'].text_color"></h2>
                </div>
                <div class="button_background rounded_button cta_button" t-if="role_values['button']">
                    <h1 t-out="role_values['button']" t-att-style="'color: %s;' % elements['button'].text_color">button text</h1>
                </div>
            </div>
            <div class="footer">
                <div class="profile_section">
                    <t t-if="role_values['image_1']">
                        <img
                        t-attf-src="data:image/png;base64,{{role_values['image_1']}}"
                        alt="Profile Picture"
                        width="128px"
                        height="128px"
                        t-if="role_values['image_1']"
                        />
                        <div t-else=""></div>
                        <div class="text_section">
                            <span t-out="role_values['section_1']" t-att-style="'color: %s;' % elements['section_1'].text_color"/>
                            <div class="text_subsections">
                                <span t-out="role_values['subsection_1']" t-att-style="'color: %s;' % elements['subsection_1'].text_color"/>
                                <span t-out="role_values['subsection_2']" t-att-style="'color: %s;' % elements['subsection_2'].text_color"/>
                            </div>
                        </div>
                    </t>
                </div>
                <img
                    t-attf-src="data:image/png;base64,{{role_values['image_2']}}"
                    alt="Profile Picture"
                    height="auto"
                    class="logo"
                    t-if="role_values['image_2']"
                />
            </div>
        </body>
    </html>
</template>

<template id="marketing_card.template_2">
    <t t-set="role_values" t-value="_get_role_values(elements, object, default_values, preview_values)"/>
    <html>
        <head>
            <meta charset="utf-8"/>
            <t t-call="marketing_card.common_style">
                <t t-set="dark_mode" t-value="dark_mode"/>
            </t>
            <style>
                body {
                    -webkit-box-align: center;
                    align-items: center;
                    justify-content: space-between;
                }
                .title {
                    line-height: 4rem;
                    width: 100%;
                }
                .footer {
                    -webkit-box-align: center;
                    align-items: center;
                    height: 30%;
                }
                .cta_button {
                    text-align: center;
                    width: fit-content;
                    max-height: 6rem;
                }
                .profile_section img {
                    object-fit: cover;
                    border-radius: 100px;
                }
                .profile_section .text_section {
                    margin-left: 0.5rem;
                }
                .profile_section .text_section span {
                    display: block;
                }
                .logo_line {
                    width: 100%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: horizontal;
                    flex-direction: row;
                    -webkit-box-align: center;
                    align-items: center;
                    align-content: center;
                    -webkit-box-pack: end;
                    justify-content: flex-end;
                }
                .logo {
                    margin: 0.1rem;
                    align-self: flex-end;
                    max-height: 80px;
                    width: auto;
                }
            </style>
        </head>
        <body t-attf-style="background-image: url('data:image/png;base64,{{role_values['background']}}');">
            <div class="logo_line">
                <img
                    t-attf-src="data:image/png;base64,{{role_values['image_2']}}"
                    alt="Profile Picture"
                    class="logo"
                    t-if="role_values['image_2']"
                />
            </div>
            <div class="title">
                <h1 class="header" t-out="role_values['header']" t-att-style="'color: %s;' % elements['header'].text_color"></h1>
                <h2 class="subheader" t-out="role_values['subheader']" t-att-style="'color: %s;' % elements['subheader'].text_color"></h2>
            </div>
            <div class="footer">
                <div class="profile_section">
                    <img
                        t-attf-src="data:image/png;base64,{{role_values['image_1']}}"
                        alt="Profile Picture"
                        width="128px"
                        height="128px"
                        t-if="role_values['image_1']"
                    />
                    <div t-else=""></div>
                    <div class="text_section">
                        <span t-out="role_values['section_1']" t-att-style="'color: %s;' % elements['section_1'].text_color"/>
                        <div class="text_subsections">
                            <span t-out="role_values['subsection_1']" t-att-style="'color: %s;' % elements['subsection_1'].text_color"/>
                            <span t-out="role_values['subsection_2']" t-att-style="'color: %s;' % elements['subsection_2'].text_color"/>
                        </div>
                    </div>
                </div>

                <div class="button_background rounded_button cta_button" t-if="role_values['button']">
                    <h1 t-out="role_values['button']" t-att-style="'color: %s;' % elements['button'].text_color">button text</h1>
                </div>
            </div>
        </body>
    </html>
</template>

<template id="marketing_card.template_3">
    <t t-set="role_values" t-value="_get_role_values(elements, object, default_values, preview_values)"/>
    <html>
        <head>
            <meta charset="utf-8"/>
            <t t-call="marketing_card.common_style">
                <t t-set="dark_mode" t-value="dark_mode"/>
            </t>
            <style>
                body {
                    -webkit-box-pack: justify;
                    justify-content: space-between;
                }
                .title {
                    line-height: 4rem;
                    width: 80%;
                }
                .footer {
                    -webkit-box-align: end;
                    align-items: flex-end;
                    height: 50%;
                }
                .cta_button {
                    text-align: center;
                    width: 100%;
                    max-width: 100%;
                    max-height: 6rem;
                }
                .profile_image {
                    object-fit: cover;
                    border-radius: 100px;
                }
                .cta_section {
                    max-width: 75%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: vertical;
                    flex-direction: column;
                    -webkit-box-align: center;
                    align-items: center;
                    -webkit-box-pack: justify;
                    justify-content: space-between;
                }
                .cta_section .text_section {
                    width: 100%;
                    font-size: 2.5rem;
                }
                .cta_section .text_subsections {
                    font-size: 1.7rem;
                }
                .logo {
                    position: absolute;
                    right: 80px;
                    top: 80px;
                    max-height: 80px;
                    width: auto;
                }
            </style>
        </head>
        <body t-attf-style="background-image: url('data:image/png;base64,{{role_values['background']}}');">
            <img
                t-attf-src="data:image/png;base64,{{role_values['image_2']}}"
                alt="Profile Picture"
                class="logo"
                t-if="role_values['image_2']"
            />
            <div class="title">
                <h2 class="subheader" t-out="role_values['subheader']" t-att-style="'color: %s;' % elements['subheader'].text_color"></h2>
                <h1 class="header" t-out="role_values['header']" t-att-style="'color: %s;' % elements['header'].text_color"></h1>
            </div>
            <div class="footer">
                <div class="cta_section">
                    <div class="text_section">
                        <span t-out="role_values['section_1']" t-att-style="'color: %s;' % elements['section_1'].text_color"/>
                        <div class="text_subsections">
                            <span t-out="role_values['subsection_1']" t-att-style="'color: %s;' % elements['subsection_1'].text_color"/>
                            <t t-if="role_values['subsection_1'] and role_values['subsection_2']">-</t>
                            <span t-out="role_values['subsection_2']" t-att-style="'color: %s;' % elements['subsection_2'].text_color"/>
                        </div>
                    </div>
                    <div class="button_background rounded_rectangle_button cta_button" t-if="role_values['button']">
                        <h1 t-out="role_values['button']" t-att-style="'color: %s;' % elements['button'].text_color">button text</h1>
                    </div>
                </div>
                <img
                    class="profile_image"
                    t-attf-src="data:image/png;base64,{{role_values['image_1']}}"
                    alt="Profile Picture"
                    width="212px"
                    height="212px"
                    t-if="role_values['image_1']"
                />
            </div>
        </body>
    </html>
</template>

<template id="marketing_card.template_4">
    <t t-set="role_values" t-value="_get_role_values(elements, object, default_values, preview_values)"/>
    <html>
        <head>
            <meta charset="utf-8"/>
            <t t-call="marketing_card.common_style">
                <t t-set="dark_mode" t-value="dark_mode"/>
            </t>
            <style>
                body {
                    justify-content: space-between;
                }
                .header {
                    -webkit-line-clamp: 3;
                    max-height: 4.5em;
                    line-height: 1.2em;
                }
                .title {
                    line-height: 4rem;
                    width: 80%;
                }
                .footer {
                    height: 50%;
                }
                .cta_button {
                    text-align: center;
                    max-height: 6rem;
                }
                .profile_image {
                    object-fit: cover;
                    border-radius: 100px;
                }
                .cta_section {
                    max-width: 75%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: vertical;
                    flex-direction: column;
                    -webkit-box-align: center;
                    align-items: center;
                }
                .text_section {
                    font-size: 2.5rem;
                }
                .text_subsections {
                    font-size: 1.7rem;
                }
                .cta_button {
                    width: fit-content;
                }
                .logo_line {
                    width: 100%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: horizontal;
                    flex-direction: row;
                    -webkit-box-align: center;
                    align-items: center;
                    align-content: center;
                    -webkit-box-pack: start;
                    justify-content: flex-start;
                }
                .logo {
                    max-height: 80px;
                    width: auto;
                }
                .profile_image {
                    position: absolute;
                    right: 80px;
                    top: 80px;
                }
            </style>
        </head>
        <body t-attf-style="background-image: url('data:image/png;base64,{{role_values['background']}}');">
            <div class="logo_line">
                <img
                    t-attf-src="data:image/png;base64,{{role_values['image_2']}}"
                    alt="Profile Picture"
                    class="logo"
                    t-if="role_values['image_2']"
                />
            </div>
            <img
                class="profile_image"
                t-attf-src="data:image/png;base64,{{role_values['image_1']}}"
                alt="Profile Picture"
                width="212px"
                height="212px"
                t-if="role_values['image_1']"
            />
            <div class="title">
                <h2 class="subheader" t-out="role_values['subheader']" t-att-style="'color: %s;' % elements['subheader'].text_color"></h2>
                <h1 class="header" t-out="role_values['header']" t-att-style="'color: %s;' % elements['header'].text_color"></h1>
            </div>
            <div class="footer">
                <div class="text_section">
                    <span t-out="role_values['section_1']" t-att-style="'color: %s;' % elements['section_1'].text_color"/>
                    <div class="text_subsections">
                        <span t-out="role_values['subsection_1']" t-att-style="'color: %s;' % elements['subsection_1'].text_color"/>
                        <t t-if="role_values['subsection_1'] and role_values['subsection_2']">-</t>
                        <span t-out="role_values['subsection_2']" t-att-style="'color: %s;' % elements['subsection_2'].text_color"/>
                    </div>
                </div>

                <div class="button_background rounded_button cta_button" t-if="role_values['button']">
                    <h1 t-out="role_values['button']" t-att-style="'color: %s;' % elements['button'].text_color">button text</h1>
                </div>
            </div>
        </body>
    </html>
</template>

<template id="marketing_card.template_5">
    <t t-set="role_values" t-value="_get_role_values(elements, object, default_values, preview_values)"/>
    <html>
        <head>
            <meta charset="utf-8"/>
            <t t-call="marketing_card.common_style">
                <t t-set="dark_mode" t-value="dark_mode"/>
            </t>
            <style>
                body {
                    height: 100%;
                }
                .title {
                    line-height: 4rem;
                    width: 80%;
                }
                .footer {
                    margin-top: 5%;
                    height: 40%;
                }
                .cta_button {
                    text-align: center;
                    max-height: 6rem;
                }
                .profile_image {
                    object-fit: cover;
                    border-radius: 100px;
                }
                .cta_section {
                    max-width: 75%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: vertical;
                    flex-direction: column;
                    -webkit-box-align: center;
                    align-items: center;
                }
                .text_section {
                    font-size: 2.5rem;
                }
                .text_subsections {
                    height: 100%;
                    font-size: 1.7rem;
                }
                .cta_button {
                    width: fit-content;
                    min-width: 30%;
                    max-width: 50%;
                }
                .logo_line {
                    width: 100%;
                    display: -webkit-box;
                    display: flex;
                    -webkit-box-orient: horizontal;
                    flex-direction: row;
                    -webkit-box-align: center;
                    align-items: center;
                    align-content: center;
                    -webkit-box-pack: justify;
                    justify-content: space-between;
                }
                .logo {
                    max-height: 80px;
                    width: auto;
                }
            </style>
        </head>
        <body t-attf-style="background-image: url('data:image/png;base64,{{role_values['background']}}');">
            <div class="logo_line">
                <img
                    class="profile_image"
                    t-attf-src="data:image/png;base64,{{role_values['image_1']}}"
                    alt="Profile Picture"
                    width="120px"
                    height="120px"
                    t-if="role_values['image_1']"
                />
                <div class="button_background rounded_button cta_button" t-if="role_values['button']">
                    <h1 t-out="role_values['button']" t-att-style="'color: %s;' % elements['button'].text_color">button text</h1>
                </div>
            </div>
            <div class="title">
                <h2 class="subheader" t-out="role_values['subheader']" t-att-style="'color: %s;' % elements['subheader'].text_color"></h2>
                <h1 class="header" t-out="role_values['header']" t-att-style="'color: %s;' % elements['header'].text_color"></h1>
            </div>
            <div class="footer">
                <div class="text_section">
                    <span t-out="role_values['section_1']" t-att-style="'color: %s;' % elements['section_1'].text_color"/>
                    <div class="text_subsections">
                        <span t-out="role_values['subsection_1']" t-att-style="'color: %s;' % elements['subsection_1'].text_color"/>
                        <t t-if="role_values['subsection_1'] and role_values['subsection_2']">-</t>
                        <span t-out="role_values['subsection_2']" t-att-style="'color: %s;' % elements['subsection_2'].text_color"/>
                    </div>
                </div>
                <img
                    t-attf-src="data:image/png;base64,{{role_values['image_2']}}"
                    alt="Profile Picture"
                    class="logo"
                    t-if="role_values['image_2']"
                />
            </div>
        </body>
    </html>
</template>
</odoo>
