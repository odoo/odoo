<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="view_res_groups_privilege_list" model="ir.ui.view">
        <field name="name">res.groups.privilege.list</field>
        <field name="model">res.groups.privilege</field>
        <field name="arch" type="xml">
            <list default_group_by="category_id">
                <field name="name" />
                <field
                    name="group_ids"
                    widget="many2many_tags"
                    context="{'short_display_name': True}" />
            </list>
        </field>
    </record>

    <record id="view_res_groups_privilege_form" model="ir.ui.view">
        <field name="name">res.groups.privilege.form</field>
        <field name="model">res.groups.privilege</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <h1>
                        <field name="name" placeholder="Privilege Name" />
                    </h1>
                    <group>
                        <group>
                            <field name="sequence" groups="base.group_no_one" />
                            <field
                                name="category_id"
                                invisible="'default_category_id' in context" />
                            <field name="placeholder" />
                        </group>
                    </group>
                    <notebook>
                        <page name="groups" string="Groups">
                            <field
                                name="group_ids"
                                widget="many2many"
                                domain="[('privilege_id', '=', False)]"
                                context="{'default_privilege_id': id}">
                                <list string="Groups">
                                    <field name="sequence" widget="handle" />
                                    <field name="name" />
                                </list>
                            </field>
                        </page>
                        <page name="notes" string="Notes">
                            <field name="description" />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_res_groups_privilege" model="ir.actions.act_window">
        <field name="name">Privileges</field>
        <field name="res_model">res.groups.privilege</field>
    </record>

    <record id="view_groups_search" model="ir.ui.view">
        <field name="name">res.groups.search</field>
        <field name="model">res.groups</field>
        <field name="arch" type="xml">
            <search string="Groups">
                <field name="full_name" string="Group" />
                <field
                    name="implied_by_ids"
                    string="Added from"
                    filter_domain="[('implied_by_ids.full_name','ilike',self)]" />
                <field
                    name="implied_ids"
                    string="Implied"
                    filter_domain="[('implied_ids.full_name','ilike',self)]" />
                <separator />
                <field name="share" />
                <filter
                    name="filter_no_share"
                    string="Internal Groups"
                    domain="[('share','=',False)]" />
            </search>
        </field>
    </record>

    <record id="view_groups_list" model="ir.ui.view">
        <field name="name">res.groups.list</field>
        <field name="model">res.groups</field>
        <field name="arch" type="xml">
            <list string="Implied groups" default_order="privilege_id,name">
                <field name="privilege_id" />
                <field name="name" />
                <field name="comment" optional="hide" />
                <field name="implied_ids" widget="many2many_tags" optional="hide" />
                <field name="implied_by_ids" widget="many2many_tags" optional="hide" />
            </list>
        </field>
    </record>

    <record id="view_groups_form" model="ir.ui.view">
        <field name="name">res.groups.form</field>
        <field name="model">res.groups</field>
        <field name="arch" type="xml">
            <form string="Groups">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button
                            name="action_show_all_users"
                            type="object"
                            help="Users having this group (implicitly or explicitly)"
                            icon="fa-users"
                            groups="base.group_no_one"
                            class="oe_stat_button">
                            <field
                                name="all_users_count"
                                string="Users"
                                widget="statinfo" />
                        </button>
                    </div>
                    <h1>
                        <field name="name" placeholder="Group Name" required="1" />
                    </h1>
                    <group>
                        <group>
                            <field
                                name="privilege_id"
                                invisible="'default_privilege_id' in context"
                                readonly="context.get('is_action_res_users')" />
                            <field
                                name="share"
                                invisible="'default_privilege_id' in context"
                                readonly="context.get('is_action_res_users')" />
                            <field name="api_key_duration" groups="base.group_no_one" />
                        </group>
                    </group>
                    <notebook>
                        <page name="user_ids" string="Users">
                            <field
                                name="user_ids"
                                context="{'search_default_filter_no_share':1}" />
                        </page>
                        <page name="inherit_groups" string="Inherited">
                            <group>
                                <field name="implied_ids" />
                                <field name="implied_by_ids" />
                                <field name="disjoint_ids" widget="many2many_tags" />
                            </group>
                        </page>
                        <page name="menus" string="Menus">
                            <field name="menu_access" />
                        </page>
                        <page name="views" string="Views">
                            <field name="view_access" groups="base.group_system" />
                        </page>
                        <page name="access_rights" string="Accesses">
                            <field
                                name="access_ids"
                                context="{'default_kind': 'permission'}">
                                <list string="Accesses" editable="top">
                                    <field name="name" />
                                    <field name="model_id" />
                                    <field
                                        name="kind"
                                        widget="badge"
                                        decoration-success="kind == 'permission'"
                                        decoration-warning="kind == 'guard'" />
                                    <field name="guard_scope" optional="hide" />
                                    <field name="domain" />
                                    <field name="for_read" />
                                    <field name="for_write" />
                                    <field name="for_create" />
                                    <field name="for_unlink" />
                                </list>
                            </field>
                        </page>
                        <page name="notes" string="Notes">
                            <field name="comment" />
                        </page>
                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_res_groups" model="ir.actions.act_window">
        <field name="name">Groups</field>
        <field name="res_model">res.groups</field>
        <field name="context">{'search_default_filter_no_share': 1}</field>
        <field
            name="help">A group is a set of functional areas that will be assigned to the user in
            order to give them access and rights to specific applications and tasks in the system.
            You can create custom groups or edit the ones existing by default in order to customize
            the view of the menu that users will be able to see. Whether they can have a read,
            write, create and delete access right can be managed from here.</field>
    </record>

    <record id="view_default_groups_form" model="ir.ui.view">
        <field name="name">res.groups default groups form</field>
        <field name="model">res.groups</field>
        <field name="priority">100</field>
        <field name="arch" type="xml">
            <form string="Groups">
                <sheet>
                    <group>
                        <field
                            name="implied_ids"
                            widget="res_user_group_ids"
                            colspan="2"
                            nolabel="1" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

</odoo>
