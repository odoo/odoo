# Local administration inside the container.
local   all             all                                     trust
local   replication     all                                     trust

# Password auth for application traffic and replica streaming.
host    all             all             0.0.0.0/0               md5
host    replication     all             0.0.0.0/0               md5
