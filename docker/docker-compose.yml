name: rkpos

# ── How to use ────────────────────────────────────────────────────────────────
# 1. Copy .env.example → .env  (or leave defaults — values are already set)
# 2. docker compose pull
# 3. docker compose up -d
#
# Cloudflare Tunnel handles SSL and external routing.
# Nginx listens on 127.0.0.1:80 — configure your tunnel to point there.
# ─────────────────────────────────────────────────────────────────────────────

services:

  # ── PostgreSQL 16 ──────────────────────────────────────────────────────────
  postgres:
    image: postgres:16-alpine
    restart: always
    environment:
      POSTGRES_USER:     ${DB_USER:-odoo}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-odoo}
      POSTGRES_DB:       ${DB_NAME:-odoo-db}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "127.0.0.1:5432:5432"   # reachable by DB tools via SSH tunnel
    networks:
      - backend
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-odoo}"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s

  # ── Odoo 19 (image pulled from Docker Hub) ────────────────────────────────
  odoo:
    image: rkpos/rkpos-odoo:latest
    restart: always
    depends_on:
      postgres:
        condition: service_healthy
    environment:
      DB_HOST:              postgres
      DB_PORT:              "5432"
      DB_USER:              ${DB_USER:-odoo}
      DB_PASSWORD:          ${DB_PASSWORD:-odoo}
      DB_NAME:              ${DB_NAME:-odoo-db}
      ODOO_MASTER_PASSWORD: ${ODOO_MASTER_PASSWORD:-rkpassword}
      ODOO_WORKERS:         ${ODOO_WORKERS:-9}
    volumes:
      - odoo_data:/var/lib/odoo
      - odoo_logs:/var/log/odoo
    networks:
      - backend
      - frontend
    ports:
      - "127.0.0.1:8069:8069"   # Odoo HTTP  — reachable via SSH tunnel
      - "127.0.0.1:8072:8072"   # Odoo WS/longpolling

  # ── Portainer CE ──────────────────────────────────────────────────────────
  portainer:
    image: portainer/portainer-ce:latest
    restart: always
    command: --http-enabled   # enables port 9000 so nginx can proxy without TLS internally
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data
    ports:
      - "127.0.0.1:9443:9443"   # direct HTTPS access via SSH tunnel
    networks:
      - frontend

  # ── Nginx (HTTP only — SSL handled by Cloudflare Tunnel) ──────────────────
  nginx:
    image: nginx:alpine
    restart: always
    ports:
      - "127.0.0.1:80:80"   # loopback only — Cloudflare Tunnel connects here
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    depends_on:
      - odoo
      - portainer
    networks:
      - frontend

# ── Persistent volumes ────────────────────────────────────────────────────────
volumes:
  postgres_data:
  odoo_data:
  odoo_logs:
  portainer_data:

# ── Networks ─────────────────────────────────────────────────────────────────
networks:
  backend:          # postgres ↔ odoo only
    driver: bridge
  frontend:         # nginx ↔ odoo ↔ portainer
    driver: bridge
