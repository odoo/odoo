# HG changeset patch
# Parent 08454bef90bec6050f009164318e43141fe332ef

diff --git a/openacademy/course.py b/openacademy/course.py
--- a/openacademy/course.py
+++ b/openacademy/course.py
@@ -1,5 +1,5 @@
 # -*- coding: utf-8 -*-
-from openerp import api, fields, models
+from openerp import api, fields, models, exceptions
 
 class Course(models.Model):
     _name = 'openacademy.course'
@@ -56,6 +56,13 @@ class Session(models.Model):
                 },
             }
 
+    @api.one
+    @api.constrains('instructor_id', 'attendee_ids')
+    def _check_instructor_not_in_attendees(self):
+        partners = [att.partner_id for att in self.attendee_ids]
+        if self.instructor_id and self.instructor_id in partners:
+            raise exceptions.ValidationError("A session's instructor can't be an attendee")
+
 class Attendee(models.Model):
     _name = 'openacademy.attendee'
     _rec_name = 'partner_id'
