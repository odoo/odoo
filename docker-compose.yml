services:
  web:
    image: odoo:19.0
    ports:
      - "8099:8069"
    volumes:
      - odoo-web-data:/var/lib/odoo
      - ./custom_addons:/mnt/extra-addons
    env_file:
      - .env
    restart: always

volumes:
  odoo-web-data:
