# -*- coding: utf-8 -*-
from openerp.osv import orm, fields

class {{ model }}(orm.Model):
    _name = "{{ module_name|snake }}.{{ model|snake }}"

    _columns = {
        'name': fields.char(),
    }
