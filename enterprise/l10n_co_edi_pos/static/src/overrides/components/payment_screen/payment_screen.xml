<?xml version="1.0" encoding="UTF-8" ?>
<templates id="template" xml:space="preserve">
    <t t-inherit="point_of_sale.PaymentScreenButtons" t-inherit-mode="extension">
        <xpath expr="//div[hasclass('payment-buttons')]//button[hasclass('js_invoice')]" position="replace">
            <!-- Remove the invoice button when dian is enabled -->
            <t t-if="!this.pos.company.l10n_co_edi_pos_dian_enabled">$0</t>
        </xpath>
    </t>
</templates>
