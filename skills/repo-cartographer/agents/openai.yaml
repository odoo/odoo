interface:
  display_name: "Repo Cartographer"
  short_description: "Map module boundaries before edits"
  default_prompt: "Use $repo-cartographer to map the relevant Kodoo modules, dependencies, and extension points before we implement changes."
